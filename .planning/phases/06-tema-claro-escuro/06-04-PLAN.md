---
phase: 06-tema-claro-escuro
plan: 04
type: execute
wave: 4
depends_on: ["06-01", "06-02", "06-03"]
files_modified:
  - TESTES.md
  - CLAUDE.md
  - .claude/CLAUDE.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [PLAT-04, PLAT-05, PLAT-15, PLAT-16]

must_haves:
  truths:
    - "TESTES.md ganha uma seção da Fase 6 no mesmo formato das seções anteriores, com preparação, roteiro manual por superfície, conferência isolada de não regressão da refrigeração e bloco de testes automatizados"
    - "O roteiro manual cobre os cinco critérios de sucesso da fase, incluindo o de legibilidade dos estados de alerta nos dois temas e o que só se prova fechando e reabrindo o navegador"
    - "O roteiro registra por escrito a limitação de D-01: em /mapa o chrome alterna e o mapa Leaflet continua escuro nos dois temas, e isso é esperado, não defeito"
    - "A não regressão da refrigeração é conferida de forma isolada e por comando, não por presunção: nenhum commit da fase tocou o diretório e o arquivo continua sem referenciar arquivo compartilhado, chave de tema ou atributo de tema (PLAT-15)"
    - "A não regressão funcional dos 6 módulos é conferida por gate estrutural: nenhuma view, id, modal, aba ou permissão mudou, e a suíte automatizada cresceu sem nenhum teste removido (PLAT-16)"
    - "A documentação do projeto passa a descrever shared/tema.js, a convenção data-theme com valores em português, a chave de armazenamento e o script de pré-desenho replicado nas 7 superfícies"
    - "REQUIREMENTS.md registra PLAT-04 e PLAT-05 com a evidência por comando que os sustenta, e reverifica PLAT-15 e PLAT-16 para a Fase 6 como foi feito em 05-07 para a Fase 5"
    - "As pendências que o ambiente autônomo não consegue fechar ficam registradas por escrito como pendência nomeada, e não desaparecem como se tivessem sido verificadas"
    - statement: "Nenhuma conferência visual pós-login pôde ser feita neste plano: o ambiente autônomo não tem credenciais do Supabase nem navegador controlável, então toda evidência aqui é estática, por comando"
      verification: backstop
    - statement: "A verificação de contraste registrada nos planos foi calculada pela fórmula de luminância relativa da WCAG 2.1 sobre os valores fechados; a conferência com ferramenta de inspeção de navegador permanece no roteiro manual"
      verification: backstop
  prohibitions:
    - statement: "Nenhum arquivo em refrigeracao/ é lido para modificação, escrito ou referenciado; a única interação permitida é conferência de leitura (D-04, PLAT-15)"
      status: unverified
      verification: judgment
    - statement: "Nenhuma correção de código é feita neste plano; se a auditoria encontrar falha, ela é reportada e corrigida no plano de origem, não aqui"
      status: unverified
      verification: judgment
    - statement: "Nenhum arquivo de código (shared/, tests/, os 7 index.html) é modificado — este plano só escreve documentação e registro de requisitos"
      status: unverified
      verification: judgment
    - statement: "Nenhum item do roteiro manual é marcado como concluído sem evidência; itens que dependem de navegador ou de credenciais ficam explicitamente em aberto"
      status: unverified
      verification: judgment
    - statement: "Nenhuma rota é acrescentada ou removida do arquivo de configuração do Vercel"
      status: unverified
      verification: judgment
  artifacts:
    - path: "TESTES.md"
      provides: "Roteiro manual da Fase 6 por superfície e conferência isolada de não regressão da refrigeração"
      contains: "Fase 6"
    - path: "CLAUDE.md"
      provides: "Arquitetura atualizada — shared/tema.js, convenção de tema e script de pré-desenho nas 7 superfícies"
      contains: "tema.js"
    - path: ".claude/CLAUDE.md"
      provides: "Contexto de projeto atualizado com o módulo de tema entre os arquivos compartilhados"
      contains: "tema.js"
    - path: ".planning/REQUIREMENTS.md"
      provides: "PLAT-04 e PLAT-05 fechados com evidência; PLAT-15 e PLAT-16 reverificados para a Fase 6"
      contains: "PLAT-04"
  key_links:
    - from: "TESTES.md"
      to: "as 7 superfícies e os 5 critérios de sucesso da fase"
      via: "seção nova listando o que abrir e o que confirmar em cada uma, no formato das seções anteriores do arquivo"
      pattern: "Fase 6"
    - from: "auditoria da fase"
      to: "refrigeracao/ e mapa/xmap.css"
      via: "diferença de nomes de arquivo entre o commit de referência da fase e a ponta atual, mais busca negativa dentro dos arquivos"
      pattern: "refrigeracao"
    - from: "REQUIREMENTS.md"
      to: "os comandos que sustentam cada requisito"
      via: "evidência citada junto do requisito, no mesmo formato que 05-07 usou para PLAT-15 e PLAT-16"
      pattern: "verificado em 06-04"
---

<!-- planner-discipline-allow: pmoc-tema -->
<!-- planner-discipline-allow: data-theme -->
<!-- planner-discipline-allow: xmap.css -->

<objective>
Fechar a fase provando os cinco critérios de sucesso em vez de presumi-los, e registrar por escrito o que
o ambiente autônomo **não** consegue provar.

É o mesmo papel que o plano 05-07 teve na Fase 5, e vale a mesma disciplina: a rota `/refrigeracao`
continuou ativa a fase inteira, então é fácil deixá-la aberta numa aba e supor que está tudo igual sem
checar. Aqui ela é conferida de forma isolada, por comando.

Nenhuma linha de código é escrita neste plano. Se a auditoria encontrar defeito, o defeito volta para o
plano de origem (06-01, 06-02 ou 06-03); ele não é remendado aqui.

Purpose: PLAT-15 e PLAT-16 são gates de não regressão que a REQUIREMENTS.md marca para reverificação em
toda fase de 5 a 10 — a Fase 6 é a segunda a passar por eles. E PLAT-04/PLAT-05 só se fecham com o
roteiro manual escrito, porque a prova final deles é visual e depende de fechar e reabrir o navegador.
Output: `TESTES.md` com a seção da Fase 6; `CLAUDE.md` e `.claude/CLAUDE.md` descrevendo o módulo de
tema; `REQUIREMENTS.md` com a evidência de cada requisito da fase.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-tema-claro-escuro/06-01-SUMMARY.md
@.planning/phases/06-tema-claro-escuro/06-02-SUMMARY.md
@.planning/phases/06-tema-claro-escuro/06-03-SUMMARY.md
@CLAUDE.md
</context>

## Decisões travadas que este plano registra

- **D-01** — `mapa/xmap.css` fica fora do escopo e permanece dark-only. O roteiro manual da Fase 6
  registra isso como **limitação conhecida e esperada**, com a frase explícita de que o mapa Leaflet
  continuar escuro no tema claro **não** é defeito a reportar. A auditoria confere por comando que o
  arquivo não foi tocado.
- **D-02** — o portal entra na fase; são **7 superfícies**. O roteiro manual percorre as 7, não 6.
- **D-03** — atributo `data-theme` com valores `claro`/`escuro` e chave `pmoc-tema`. A documentação do
  projeto passa a registrar essa convenção, para que uma fase futura não invente outra.
- **D-04** — `refrigeracao` congelada e intocada. Conferência isolada, por comando, neste plano.

## Suposições sinalizadas — sonda de bordas sem SPEC

A sonda determinística classificou os 4 requisitos como `unclassified` → `unresolved` (classificador
anglófono, requisitos em português). As 4 linhas ficam registradas, não promovidas e não descartadas — e
este plano é o lugar onde elas viram texto visível em `TESTES.md` e em `REQUIREMENTS.md`:

| Requisito | Status da sonda | Como este plano trata a suposição |
|---|---|---|
| PLAT-04 | unresolved | A evidência estática (botão emitido por `montarShell`, script nas 7 superfícies) fecha o que é automatizável; o clique real em cada uma das 7 superfícies fica escrito como item aberto do roteiro manual |
| PLAT-05 | unresolved | A evidência estática (chave única, lista fechada, consulta de preferência do sistema) fecha o mecanismo; fechar e reabrir o navegador, e navegar entre dois módulos, ficam escritos como itens abertos do roteiro |
| PLAT-15 | unresolved | Conferência estática por histórico de commits e por busca negativa fecha o que é automatizável; a conferência humana com inspetor de rede permanece pendente herdada da Fase 5 e é repetida aqui, não substituída |
| PLAT-16 | unresolved | Gate estrutural mais suíte automatizada crescente fecham o que é automatizável; a conferência visual pós-login continua pendente por falta de credenciais no ambiente autônomo |

Bordas reais desta fase, todas levadas para o roteiro manual como itens numerados: armazenamento local
bloqueado por política do navegador; valor salvo corrompido na chave de tema; navegador sem consulta de
preferência de esquema de cor; troca de tema com modal aberto; duas abas do mesmo navegador abertas em
módulos diferentes.

## Artefatos que este plano cria (Artifacts this phase produces)

Este plano não cria símbolo de código. Cria seções de documento:

**Markdown — `TESTES.md`**
- Seção nova: `## Fase 6 — Tema claro/escuro — auditoria de fechamento`
- Subseções: preparação, roteiro manual por superfície, bordas a exercitar, conferência isolada de não regressão da refrigeração, testes automatizados

**Markdown — `CLAUDE.md` e `.claude/CLAUDE.md`**
- Descrição de `shared/tema.js` entre os arquivos compartilhados, da convenção `data-theme` com valores em português, da chave `pmoc-tema` e do script clássico de pré-desenho replicado nas 7 superfícies

**Markdown — `.planning/REQUIREMENTS.md`**
- Evidência anexada a PLAT-04, PLAT-05, PLAT-15 e PLAT-16

<tasks>

<task type="auto">
  <name>Task 1: Escrever a seção da Fase 6 em TESTES.md</name>

  <read_first>
    - `TESTES.md` linhas 309-368 — a seção da Fase 5, que é o modelo exato de estrutura, de tom e de granularidade a seguir (parágrafo de abertura explicando o que a fase mudou, preparação, fluxo manual, conferência isolada da refrigeração, testes automatizados)
    - `TESTES.md` linhas 258-282 — a seção do módulo Mapa, para ver como o arquivo já registra limitação específica de módulo
    - `.planning/ROADMAP.md`, seção da Fase 6 — os cinco critérios de sucesso, que são o que o roteiro precisa exercitar um a um
    - `.planning/phases/06-tema-claro-escuro/06-01-SUMMARY.md`, `06-02-SUMMARY.md` e `06-03-SUMMARY.md` — o que a fase entregou de fato, incluindo a contagem final de testes automatizados
  </read_first>

  <files>TESTES.md</files>

  <action>
Acrescentar ao fim de `TESTES.md` uma seção nova, no mesmo formato e com o mesmo nível de detalhe da
seção da Fase 5, com o título de auditoria de fechamento da Fase 6 e a data de execução.

**Parágrafo de abertura.** Em português, explicando em prosa o que a fase mudou: que `shared/pmoc.css`
passou a ter um segundo tema em tokens; que `shared/tema.js` é a implementação única de detecção,
validação, aplicação, alternância e persistência; que o botão sai de `shared/shell.js` e por isso aparece
nos 6 módulos de uma vez; que um script clássico no cabeçalho das 7 superfícies aplica o tema antes do
primeiro desenho; e que o portal recebeu tokens próprios porque não carrega a folha comum, mas nenhuma
lógica própria. Fechar o parágrafo dizendo que a refrigeração ficou fora e que a conferência de que ela
não foi tocada é um passo isolado abaixo, não uma presunção.

**Preparação.** Rodar a suíte automatizada na raiz do repositório, sem argumento, antes de qualquer
conferência manual — é a primeira linha de defesa. Servir o repositório por HTTP a partir da raiz, porque
abrir por caminho de arquivo quebra a descoberta de credenciais e agora também o módulo de tema do
portal. Limpar o armazenamento local do domínio antes de começar, para que o primeiro item do roteiro
teste de fato uma primeira visita.

**Roteiro manual, por superfície.** Um bloco a repetir nas 7 superfícies (`/`, `/maquinas`,
`/transportes`, `/eletrica`, `/fonoclama`, `/predial`, `/mapa`), com itens de caixa de seleção cobrindo:
o botão de tema aparecer na barra superior (no portal, no cabeçalho); o clique alternar o tema sem
recarregar a página; o rótulo do botão mudar junto, oferecendo sempre o tema que ainda não está em vigor;
a página não piscar no tema errado ao ser recarregada com o tema claro em vigor; e a cor da barra do
navegador acompanhar (visível em navegador móvel).

**Roteiro dos cinco critérios de sucesso**, cada um como item próprio e nomeado, para que nenhum se perca:
alternância presente e sem recarga; ausência de CSS de tema por módulo (conferido lendo o bloco de estilo
de cada `index.html` e confirmando que só há `--accent`); persistência ao fechar e reabrir o navegador e
ao navegar de um módulo para outro; primeira visita sem preferência salva seguindo o sistema operacional
(testar trocando o tema do sistema com o armazenamento limpo); e legibilidade nos dois temas de todos os
estados de alerta — abrir uma view com aviso de vencido, uma com estoque baixo e uma com equipamento
inoperante, nos dois temas, e confirmar que o texto do aviso e a borda dele têm a mesma cor e são
legíveis sobre o fundo.

**Bordas a exercitar**, como bloco próprio de itens numerados, porque são o que quebra em produção e não
aparece em teste automatizado: abrir com o armazenamento local bloqueado por política do navegador e
confirmar que a página carrega e o tema ainda alterna dentro da sessão; gravar à mão um valor inválido na
chave de tema pelo console e recarregar, confirmando que o tema cai para a preferência do sistema em vez
de aplicar o valor inválido; abrir um modal e alternar o tema com ele aberto, confirmando que o modal não
fecha e o que estava digitado não se perde; abrir dois módulos em abas diferentes, alternar numa e
confirmar que a outra passa a exibir a nova preferência ao ser recarregada.

**Limitação conhecida de D-01**, como bloco próprio e com texto explícito: em `/mapa`, a barra superior,
o rodapé, o botão e os painéis que vêm da folha comum alternam; a barra lateral de camadas, as legendas e
os balões de marcador do Leaflet continuam escuros nos dois temas, porque `mapa/xmap.css` tem um sistema
de tokens próprio e ficou fora do escopo por decisão registrada no roadmap. **Isso é esperado e não deve
ser reportado como defeito.** Registrar como candidato a item de backlog para uma fase futura.

**Conferência isolada de não regressão da refrigeração (PLAT-15)**, no mesmo formato da Fase 5, com
parágrafo curto explicando por que o passo é isolado de propósito, e com quatro itens: verificação
estática por histórico (nenhum commit da fase tocou o diretório), verificação estática por busca (o
arquivo continua sem referenciar arquivo compartilhado, chave de tema ou atributo de tema), verificação
humana com o inspetor de rede aberto depois da fase concluída, e percurso do fluxo principal. Os dois
primeiros são marcáveis com a evidência da Task 2; os dois últimos ficam **em aberto**, com a mesma
observação da Fase 5 de que o ambiente autônomo não tem credenciais nem navegador controlável.

**Testes automatizados.** Bloco de comando com a suíte na raiz e o resultado esperado, citando o número
exato de testes ao fim da fase e o número no início (25), e nomeando os dois arquivos de teste novos.

Não marcar nenhum item como concluído sem evidência. Itens que dependem de navegador ou de credenciais
ficam com a caixa vazia e uma observação de que estão pendentes para o UAT.
  </action>

  <acceptance_criteria>
    - A seção existe: `grep -q "Fase 6" TESTES.md` e `grep -qi "tema claro" TESTES.md`
    - As 7 superfícies aparecem no roteiro: `for r in "/maquinas" "/transportes" "/eletrica" "/fonoclama" "/predial" "/mapa"; do grep -qF -- "$r" TESTES.md || exit 1; done`
    - Os cinco critérios têm item próprio: a seção da Fase 6 contém referência a alternância sem recarga, a ausência de CSS por módulo, a persistência entre sessões, a preferência do sistema na primeira visita e à legibilidade dos estados de alerta
    - As bordas estão registradas: a seção menciona armazenamento bloqueado, valor inválido na chave, modal aberto e duas abas
    - A limitação de D-01 está escrita: a seção menciona `xmap.css` ou o mapa Leaflet continuar escuro, com a marcação de que é esperado
    - A conferência isolada da refrigeração existe com quatro itens: a seção menciona `refrigeracao` e `PLAT-15`
    - O arquivo cresceu: `test $(wc -l < TESTES.md) -gt 368`
    - Nenhum arquivo de código foi tocado: `git diff --name-only -- shared/ tests/ '*.html'` vazio
  </acceptance_criteria>

  <verify>
    <automated>bash -c 'set -e; grep -q "Fase 6" TESTES.md; grep -qi "tema" TESTES.md; for r in "/maquinas" "/transportes" "/eletrica" "/fonoclama" "/predial" "/mapa"; do grep -qF -- "$r" TESTES.md || { echo "ROTA AUSENTE NO ROTEIRO: $r"; exit 1; }; done; SEC=$(sed -n "/## Fase 6/,\$p" TESTES.md); test $(echo "$SEC" | wc -l) -ge 50 || { echo "SECAO CURTA DEMAIS"; exit 1; }; for termo in "prefers-color-scheme\|sistema operacional" "pmoc-tema\|chave" "modal" "aba" "xmap\|Leaflet" "refrigeracao" "PLAT-15"; do echo "$SEC" | grep -qiE "$termo" || { echo "FALTA NO ROTEIRO: $termo"; exit 1; }; done; test $(wc -l < TESTES.md) -gt 368; test -z "$(git diff --name-only -- shared/ tests/)"; echo TASK1_TESTES_MD_OK'</automated>
  </verify>

  <done>`TESTES.md` tem a seção da Fase 6 no formato das anteriores, com preparação, roteiro pelas 7 superfícies, item nomeado para cada um dos cinco critérios de sucesso, bloco de bordas, limitação de D-01 escrita como esperada e conferência isolada da refrigeração com os itens humanos explicitamente em aberto.</done>
</task>

<task type="auto">
  <name>Task 2: Auditar a fase por comando e atualizar CLAUDE.md, .claude/CLAUDE.md e REQUIREMENTS.md</name>

  <read_first>
    - `CLAUDE.md` — seção "Architecture", em especial os parágrafos sobre `/shared/auth.js` e `/shared/shell.js`, que são o formato a seguir para descrever `shared/tema.js`
    - `.claude/CLAUDE.md` — a lista de camadas e de arquivos compartilhados, que precisa ganhar o módulo de tema
    - `.planning/REQUIREMENTS.md` linhas 21-22 (PLAT-04 e PLAT-05), linhas 47-48 (PLAT-15 e PLAT-16, com o formato de evidência que 05-07 estabeleceu) e a tabela de rastreabilidade nas linhas 125-137
    - `.planning/phases/06-tema-claro-escuro/06-01-SUMMARY.md`, `06-02-SUMMARY.md`, `06-03-SUMMARY.md` — a evidência real produzida por cada plano, que é o que a REQUIREMENTS.md vai citar
  </read_first>

  <files>CLAUDE.md, .claude/CLAUDE.md, .planning/REQUIREMENTS.md</files>

  <action>
**Parte A — auditoria por comando, antes de escrever qualquer coisa.**

Rodar e guardar a saída dos seguintes gates. O commit de referência da fase é `351b13c`, o último antes
da execução. Se **qualquer** um falhar, parar e reportar: a correção pertence ao plano de origem, não a
este.

1. Suíte automatizada na raiz, sem argumento: sai 0, zero falhas, e a contagem é maior que os 25 do
   início da fase. Anotar o número exato.
2. Nenhum teste desapareceu: a contagem de casos por arquivo em `tests/` é maior ou igual à do início da
   fase em cada arquivo pré-existente.
3. `refrigeracao/` intocada: a diferença de nomes de arquivo entre o commit de referência e a ponta
   atual, restrita a esse diretório, é vazia. Complementar com busca negativa dentro de
   `refrigeracao/index.html` por referência a arquivo compartilhado, por chave de tema e por atributo de
   tema — os três devem dar zero.
4. `mapa/xmap.css` intocado: a mesma diferença de nomes restrita a esse arquivo é vazia.
5. As 7 superfícies têm o script de pré-desenho e as 7 cópias são idênticas.
6. Nenhum módulo declara token de cor além da cor de destaque, e nenhum tem bloco de tema próprio.
7. Nenhuma dependência nova e nenhum passo de build: não existe `package.json`, e nenhum `index.html`
   passou a carregar origem externa que já não carregasse.
8. As 7 rotas de `vercel.json` continuam apontando para arquivos existentes.
9. Nenhum arquivo fora do escopo da fase foi modificado: a diferença de nomes entre o commit de
   referência e a ponta atual, excluindo `.planning/`, contém apenas os arquivos declarados nos campos de
   arquivos modificados dos planos 06-01, 06-02 e 06-03, mais os deste plano.

**Parte B — `CLAUDE.md`.** Na seção de arquitetura, acrescentar `shared/tema.js` à lista de arquivos
compartilhados, no mesmo formato conciso das entradas de `shared/auth.js` e `shared/shell.js`: dizer o
que ele faz (detecção, validação por lista fechada, aplicação, alternância e persistência de tema), quem
o consome (os 6 módulos por `aplicarShell`, o portal diretamente) e qual é a consequência de mudá-lo
(muda o tema das 7 superfícies de uma vez). Acrescentar, na mesma seção ou na de convenções, a convenção
travada: atributo `data-theme` no elemento raiz com os valores `claro` e `escuro`, chave de armazenamento
local `pmoc-tema`, tema escuro como padrão de `:root` em `shared/pmoc.css` e tema claro no bloco de
seletor de atributo. Registrar as duas exceções de escopo: o portal duplica os tokens porque não carrega
a folha comum, e `mapa/xmap.css` permanece dark-only por decisão.

Registrar também, com uma frase, a regra que mais facilmente se perde numa fase futura: o script que
aplica o tema no cabeçalho das 7 páginas é clássico de propósito, porque módulo ES é adiado e rodaria
depois do primeiro desenho — e as 7 cópias precisam continuar idênticas, o que a suíte automatizada
passa a exigir.

**Parte C — `.claude/CLAUDE.md`.** Acrescentar `shared/tema.js` onde o arquivo já lista os componentes e
as camadas compartilhadas, com uma linha no mesmo estilo das entradas existentes. Não reescrever o
arquivo: acrescentar nas listas certas e ajustar apenas as frases que passaram a estar erradas.

**Parte D — `.planning/REQUIREMENTS.md`.** Marcar PLAT-04 e PLAT-05 como atendidos, anexando a cada um a
evidência por comando produzida na Parte A, no mesmo formato que 05-07 usou para PLAT-15 e PLAT-16 (texto
corrido depois do requisito, com o comando e o resultado). Acrescentar a PLAT-15 e a PLAT-16 a
reverificação da Fase 6, sem apagar o registro da Fase 5 — os dois são gates recorrentes, e o histórico
por fase é o que dá valor a eles. Registrar em cada um dos quatro, de forma explícita, a **pendência
herdada**: nenhuma conferência visual pós-login nem conferência humana com inspetor de rede pôde ser
feita em ambiente autônomo, e ela fica registrada em `TESTES.md`, seção da Fase 6, como item para o UAT.
Atualizar a tabela de rastreabilidade, mudando o estado de PLAT-04 e PLAT-05 e anotando a Fase 6 nas
linhas de PLAT-15 e PLAT-16.

Nenhuma correção de código neste plano. Nenhum item marcado sem evidência.
  </action>

  <acceptance_criteria>
    - Suíte verde e crescida: `node --test` sai 0, zero falhas, contagem maior que 25
    - `refrigeracao/` intocada: `git diff --name-only 351b13c..HEAD -- refrigeracao/ | wc -l` é 0; e `grep -c 'shared/\|pmoc.css' refrigeracao/index.html`, `grep -c 'pmoc-tema' refrigeracao/index.html` e `grep -c 'data-theme' refrigeracao/index.html` são todos 0
    - `mapa/xmap.css` intocado: `git diff --name-only 351b13c..HEAD -- mapa/xmap.css | wc -l` é 0
    - As 7 cópias do script de pré-desenho continuam idênticas: as linhas extraídas das 7 superfícies reduzem a 1 valor distinto
    - Zero-build preservado: `test ! -f package.json`
    - As 7 rotas do Vercel resolvem: cada destino declarado em `vercel.json` corresponde a arquivo existente
    - `CLAUDE.md` descreve o módulo de tema e a convenção: `grep -qF "tema.js" CLAUDE.md` e `grep -qF "pmoc-tema" CLAUDE.md` e `grep -qF "data-theme" CLAUDE.md`
    - `.claude/CLAUDE.md` lista o módulo de tema: `grep -qF "tema.js" .claude/CLAUDE.md`
    - `REQUIREMENTS.md` traz a evidência da fase: `grep -qF "06-04" .planning/REQUIREMENTS.md` e as linhas de PLAT-04 e PLAT-05 estão marcadas como atendidas
    - Nenhum arquivo de código foi tocado neste plano: `git diff --name-only -- shared/ tests/ '*.html' */index.html` vazio
  </acceptance_criteria>

  <verify>
    <automated>bash -c 'set -e; SAIDA=$(node --test 2>&1); echo "$SAIDA" | tail -8; echo "$SAIDA" | grep -qE "fail 0"; N=$(echo "$SAIDA" | grep -oE "tests [0-9]+" | head -1 | grep -oE "[0-9]+"); test "$N" -gt 25; test $(git diff --name-only 351b13c..HEAD -- refrigeracao/ | wc -l) -eq 0; test $(grep -c "shared/\|pmoc.css" refrigeracao/index.html) -eq 0; test $(grep -c "pmoc-tema" refrigeracao/index.html) -eq 0; test $(grep -c "data-theme" refrigeracao/index.html) -eq 0; test $(git diff --name-only 351b13c..HEAD -- mapa/xmap.css | wc -l) -eq 0; for f in index.html maquinas/index.html transportes/index.html eletrica/index.html fonoclama/index.html predial/index.html mapa/index.html; do grep -h "pmoc-tema" $f | head -1; done | sort -u | wc -l | grep -qx 1; test ! -f package.json; for d in $(grep -oE "\"destination\": *\"[^\"]+\"" vercel.json | sed "s/.*: *\"//;s/\"//"); do test -f ".${d}" || test -f "${d#/}" || { echo "ROTA QUEBRADA: $d"; exit 1; }; done; grep -qF "tema.js" CLAUDE.md; grep -qF "pmoc-tema" CLAUDE.md; grep -qF "data-theme" CLAUDE.md; grep -qF "tema.js" .claude/CLAUDE.md; grep -qF "06-04" .planning/REQUIREMENTS.md; test -z "$(git diff --name-only -- shared/ tests/)"; echo TASK2_AUDITORIA_OK_TOTAL=$N'</automated>
  </verify>

  <done>Os nove gates da auditoria passaram e estão registrados; `CLAUDE.md` e `.claude/CLAUDE.md` descrevem `shared/tema.js`, a convenção `data-theme` com valores em português, a chave `pmoc-tema` e a regra do script clássico; `REQUIREMENTS.md` fecha PLAT-04 e PLAT-05 com evidência e reverifica PLAT-15 e PLAT-16 para a Fase 6, com as pendências humanas nomeadas em vez de omitidas.</done>
</task>

</tasks>

<threat_model>
Nível ASVS 1, bloqueio em severidade `high`. Este plano não escreve código: escreve documentação e
registro de requisitos. A superfície de risco é de **veracidade**, não de execução.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Evidência produzida pelos planos 06-01 a 06-03 → registro em `REQUIREMENTS.md` | Um requisito marcado como atendido sem evidência real vira dívida invisível para a próxima fase |
| Ambiente autônomo → conferência que exige navegador e credenciais | O que não pode ser provado aqui precisa ficar escrito como pendência, não desaparecer |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-12 | Repudiation | `REQUIREMENTS.md` marcando requisito como atendido sem o comando que o sustenta | medium | mitigate | Cada requisito recebe a evidência por comando junto do texto, no formato que 05-07 estabeleceu; gate automatizado exige a citação do plano no arquivo |
| T-06-13 | Information Disclosure | Pendência de conferência humana desaparecendo do registro e sendo lida numa fase futura como já verificada | medium | mitigate | As pendências herdadas são reescritas explicitamente nos quatro requisitos e no roteiro de `TESTES.md`, com caixa de seleção vazia |
| T-06-14 | Tampering | Auditoria "consertando" um defeito encontrado, escondendo a origem dele | low | mitigate | Proibição explícita registrada em `must_haves.prohibitions` e gate automatizado exigindo que nenhum arquivo de `shared/` ou `tests/` tenha diferença neste plano |
| T-06-SC | Tampering | Cadeia de suprimentos de pacotes | low | accept | Nenhum pacote é instalado; um dos gates da auditoria é justamente confirmar que o projeto continua sem `package.json` |

Nenhuma ameaça deste plano é `high`.
</threat_model>

<verification>
1. `node --test` na raiz sai 0, zero falhas, contagem maior que 25 e nenhum teste removido.
2. `refrigeracao/` intocada por histórico e por busca negativa (D-04, PLAT-15).
3. `mapa/xmap.css` intocado (D-01).
4. As 7 cópias do script de pré-desenho continuam idênticas.
5. Projeto continua zero-build, sem `package.json` e sem origem externa nova.
6. As 7 rotas de `vercel.json` resolvem para arquivos existentes.
7. `TESTES.md`, `CLAUDE.md`, `.claude/CLAUDE.md` e `REQUIREMENTS.md` refletem o que a fase entregou, com as pendências humanas nomeadas.
</verification>

<success_criteria>
- Os cinco critérios de sucesso da fase têm roteiro escrito e verificável em `TESTES.md`, incluindo os que só se provam com navegador.
- PLAT-04 e PLAT-05 estão fechados com evidência por comando; PLAT-15 e PLAT-16 estão reverificados para a Fase 6 sem apagar o registro da Fase 5.
- As limitações de escopo (D-01 no mapa, D-02 no portal) estão escritas como esperadas, não descobertas por quem for usar.
- A documentação do projeto descreve `shared/tema.js` e a convenção de tema, para que a Fase 7 não invente outra.
- Nenhuma linha de código foi escrita neste plano.
</success_criteria>

<output>
Criar `.planning/phases/06-tema-claro-escuro/06-04-SUMMARY.md` ao concluir.
</output>
