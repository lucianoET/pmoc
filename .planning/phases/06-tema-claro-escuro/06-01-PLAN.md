---
phase: 06-tema-claro-escuro
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - shared/pmoc.css
  - shared/auth.js
autonomous: true
requirements: [PLAT-04, PLAT-16]

must_haves:
  truths:
    - "shared/pmoc.css ganha um bloco [data-theme=\"claro\"] que redefine os 12 tokens de cor (7 neutros + 5 semânticos) e nada mais, deixando --accent e --ff herdados de :root (D-03)"
    - "O bloco :root continua com exatamente os mesmos 14 valores de hoje — o tema escuro é o padrão e não muda um pixel"
    - "Todos os 12 tokens de cor do tema claro atingem no mínimo 4,5:1 de contraste sobre os três fundos do tema claro (--bg, --surface, --surface2), medido pela fórmula de luminância relativa da WCAG 2.1"
    - "Existe o token derivado --accent-texto, igual a var(--accent) no tema escuro e igual a uma versão escurecida por color-mix no tema claro, usado em toda regra que pinta a cor de destaque como texto ou como borda de componente"
    - "Nenhuma regra de shared/pmoc.css depende de o fundo ser escuro: não sobra nenhum branco translúcido nem nenhum hex de cor semântica duplicado fora do bloco de tokens"
    - "As classes de alerta (.co-warn, .co-red, .co-ok, .co-blue), os selos (.b-ok, .b-warn, .b-red, .b-blue, .b-gold) e o botão destrutivo (.btn-d) derivam fundo e texto dos tokens, então acompanham a troca de tema automaticamente (critério de sucesso 5)"
    - "A tela de login compartilhada continua legível no tema claro: o botão Entrar deixa de fixar texto quase-preto sobre um fundo que passa a ser escuro no tema claro"
    - "Existe a classe .btn-tema em shared/pmoc.css, com nome que não contém a subsequência nav, pronta para o botão que o plano 06-02 injeta na barra superior"
    - "node --test na raiz continua verde com os 25 testes do baseline, sem nenhum teste removido ou enfraquecido (PLAT-16)"
    - statement: "Nenhum módulo consegue sobrescrever --accent-texto a partir do próprio <style>, porque nenhum módulo declara esse token — a derivação por color-mix é a única implementação (critério de sucesso 2)"
      verification: backstop
    - statement: "O scrim do overlay (preto a 60%) e a sombra do toast (preto a 45%) continuam pretos nos dois temas, por serem profundidade e não texto — decisão consciente, não hardcode esquecido"
      verification: backstop
  prohibitions:
    - statement: "Nenhum arquivo em refrigeracao/ é lido para modificação, escrito ou referenciado (D-04, PLAT-15)"
      status: unverified
      verification: judgment
    - statement: "Nenhum arquivo em mapa/ é tocado — inclusive e principalmente mapa/xmap.css, que permanece dark-only (D-01)"
      status: unverified
      verification: judgment
    - statement: "Nenhum valor do bloco :root de shared/pmoc.css é alterado ou removido — o tema escuro sai desta fase idêntico ao que entrou"
      status: unverified
      verification: judgment
    - statement: "Nenhuma regra de tema é criada em arquivo de módulo; todo o CSS de tema desta fase vive em shared/pmoc.css (critério de sucesso 2)"
      status: unverified
      verification: judgment
    - statement: "Nenhuma dependência nova, nenhum passo de build, nenhum pré-processador de CSS é introduzido — o projeto continua zero-build"
      status: unverified
      verification: judgment
    - statement: "Nenhuma assinatura pública de shared/auth.js muda; o ajuste é estritamente de cor no atributo style de dois botões"
      status: unverified
      verification: judgment
  artifacts:
    - path: "shared/pmoc.css"
      provides: "Bloco de tema claro, token derivado --accent-texto, classe .btn-tema e eliminação dos hardcodes que travavam o tema claro"
      contains: "data-theme"
    - path: "shared/auth.js"
      provides: "Botão Entrar com cor de texto derivada de token, legível nos dois temas"
      contains: "var(--bg,#1a1a18)"
  key_links:
    - from: "atributo data-theme no elemento html"
      to: "os 12 tokens de cor do tema claro"
      via: "seletor de atributo [data-theme=\"claro\"] em shared/pmoc.css, de mesma especificidade que :root e declarado depois dele"
      pattern: "data-theme"
    - from: "--accent do módulo (declarado no <style> de cada index.html)"
      to: "--accent-texto usado nas regras de texto e borda"
      via: "color-mix in srgb escurecendo o destaque do módulo só no tema claro"
      pattern: "accent-texto"
    - from: "classes de alerta e selo"
      to: "tokens semânticos --green/--yellow/--red/--blue/--orange"
      via: "color-mix para o fundo e var() para o texto, no lugar de rgba e hex literais"
      pattern: "color-mix"
---

<!-- planner-discipline-allow: rgba(255,255,255 -->
<!-- planner-discipline-allow: rgba(90,158,111 -->
<!-- planner-discipline-allow: rgba(201,168,76 -->
<!-- planner-discipline-allow: rgba(184,92,74 -->
<!-- planner-discipline-allow: rgba(74,127,160 -->
<!-- planner-discipline-allow: rgba(192,120,64 -->
<!-- planner-discipline-allow: color:#c9a84c -->
<!-- planner-discipline-allow: color:#b85c4a -->
<!-- planner-discipline-allow: color:#5a9e6f -->
<!-- planner-discipline-allow: color:#8eb9d1 -->
<!-- planner-discipline-allow: color:#1a1a18 -->
<!-- planner-discipline-allow: btn-tema -->
<!-- planner-discipline-allow: nav -->
<!-- planner-discipline-allow: color-mix -->
<!-- planner-discipline-allow: var(--accent-texto) -->

<objective>
Preparar a folha comum para existir em dois temas. Este plano não cria botão, não cria JavaScript e não
toca em nenhum módulo: ele só faz `shared/pmoc.css` deixar de assumir que o fundo é escuro.

São duas coisas, nesta ordem: (1) acrescentar o bloco `[data-theme="claro"]` com os 12 tokens de cor do
tema claro, todos com contraste verificado em WCAG AA, mais o token derivado `--accent-texto`; e
(2) eliminar os pontos onde uma cor está escrita à mão em vez de sair de um token — são exatamente os
pontos que fariam o critério de sucesso 5 (estados de alerta legíveis) falhar em silêncio, sem erro de
console e sem teste vermelho.

Purpose: sem esses dois passos, ligar `data-theme="claro"` produziria uma página clara com bordas de
tabela invisíveis, callouts de vencimento/estoque baixo/INOP com texto na cor antiga e nome do módulo
ilegível no logo. O botão do plano 06-02 só faz sentido depois disto.
Output: `shared/pmoc.css` com tema claro completo e sem hardcode de cor dependente de fundo escuro;
`shared/auth.js` com os dois botões `Entrar` derivando a cor do texto de token.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-tema-claro-escuro/06-RESEARCH.md
@.planning/phases/06-tema-claro-escuro/06-PATTERNS.md
@CLAUDE.md
</context>

## Decisões travadas que este plano honra

- **D-01** — `mapa/xmap.css` (skin do Leaflet, tokens `--xm-*`, 394 linhas) fica **fora do escopo** e
  permanece dark-only. Este plano não abre esse arquivo. **Suposição explícita registrada:** o critério
  de sucesso 5 vale para o *chrome* do módulo `mapa` (barra superior, rodapé, botões, painéis que usam
  `shared/pmoc.css`), **não** para o mapa Leaflet em si, que continuará escuro nos dois temas.
- **D-02** — o portal `/index.html` entra no escopo da fase, mas **não** neste plano: o portal não
  carrega `shared/pmoc.css` e duplica os tokens inline, então recebe o próprio bloco de tema claro no
  plano 06-03. Os valores numéricos fechados aqui são a fonte da qual o portal copia.
- **D-03** — convenção em português: atributo `data-theme="claro" | "escuro"` no `<html>` e chave de
  `localStorage` `pmoc-tema`. O seletor CSS deste plano é `[data-theme="claro"]`, não `[data-theme="light"]`
  como a pesquisa havia esboçado. `[data-theme="escuro"]` **não** precisa de bloco: o tema escuro já é o
  valor de `:root`.
- **D-04** — `refrigeracao/` permanece congelada e intocada. Este plano só altera arquivos em `shared/`.

## Suposições sinalizadas — sonda de bordas sem SPEC

A fase não tem SPEC.md. A sonda determinística classificou os 4 requisitos como `unclassified` →
`unresolved` (o classificador é anglófono e os requisitos estão em português). As 4 linhas ficam
registradas como suposição visível, **não** promovidas a verificadas e **não** descartadas:

| Requisito | Status da sonda | Suposição assumida neste plano |
|---|---|---|
| PLAT-04 | unresolved | Assume-se que "alternar tema em qualquer módulo" se resolve com um único bloco de tokens em `shared/pmoc.css`; a verificação real é visual e fica no roteiro manual do plano 06-04 |
| PLAT-05 | unresolved | Fora do escopo deste plano (persistência é 06-02/06-03); nenhuma borda de persistência é fechada aqui |
| PLAT-15 | unresolved | Assume-se que não abrir nenhum arquivo de `refrigeracao/` basta para a não regressão; a conferência por histórico de commits fica no plano 06-04 |
| PLAT-16 | unresolved | Assume-se que "nenhum módulo perde funcionalidade" se prova por `node --test` verde mais gate estrutural de que nenhum token de `:root` sumiu |

Bordas reais desta fase derivadas à mão (as de CSS ficam aqui; as de runtime ficam em 06-02/06-03):
`--accent` definido por módulo em `<style>` inline vence o `[data-theme="claro"]` de `shared/pmoc.css`
por ordem de cascata — por isso o tema claro **não pode** tentar redefinir `--accent` e precisa do token
derivado `--accent-texto`.

## Artefatos que este plano cria (Artifacts this phase produces)

Símbolos **criados por este plano** — não existem hoje no repositório, não são drift:

**CSS — `shared/pmoc.css`**
- Seletor novo: `[data-theme="claro"]`
- Token novo: `--accent-texto` (declarado em `:root` e redefinido em `[data-theme="claro"]`)
- Classes novas: `.btn-tema`, `.btn-tema:hover`

Nenhum símbolo novo em `shared/auth.js` — a mudança lá é de valor, não de API.

<tasks>

<task type="auto">
  <name>Task 1: Acrescentar o bloco de tema claro, o token --accent-texto e a classe .btn-tema em shared/pmoc.css</name>

  <read_first>
    - `shared/pmoc.css` — arquivo inteiro (130 linhas). Interessam em especial: `:root` (linhas 8-13), `.logo-accent` (21), `.logo-dot` (22), `.topbar-back:hover` (26), `.nav-btn.active` (31), `.b-accent` (64), `.btn-p`/`.btn-s`/`.btn-s:hover` (68-70), `input:focus` (95) e `.shell-footer a:hover` (127)
    - `.planning/phases/06-tema-claro-escuro/06-PATTERNS.md` — seção "`shared/pmoc.css` — bloco `[data-theme=\"light\"]` e correção de hardcodes", que traz o bloco `:root` atual literal e a nota de que a convenção travada é `claro`/`escuro`, não `light`/`dark`
    - `.planning/phases/06-tema-claro-escuro/06-RESEARCH.md` — "Pattern 1" (só os tokens que mudam entram no bloco novo) e "Anti-Patterns to Avoid" (não copiar as 14 propriedades inteiras)
    - `transportes/index.html` linha 11, `eletrica/index.html` linha 10, `fonoclama/index.html` linha 10, `predial/index.html` linha 10, `maquinas/index.html` linha 11 — os cinco valores de `--accent` que o token derivado precisa escurecer sem quebrar (`#4aa0a0`, `#c9a84c`, `#4a7fa0`, `#8a7f5c`)
  </read_first>

  <files>shared/pmoc.css</files>

  <action>
Três acréscimos em `shared/pmoc.css`. Nenhuma remoção, nenhuma alteração de valor dentro de `:root`.

**1. Token derivado no `:root` existente.** Acrescentar `--accent-texto:var(--accent)` ao bloco `:root`
(linhas 8-13), na mesma linha de `--accent`. No tema escuro esse token é literalmente o destaque do
módulo, então nada muda visualmente. Ele existe porque `--accent` é declarado por cada módulo no
`<style>` do próprio `index.html`, que vem **depois** do `<link>` de `shared/pmoc.css` na cascata e tem a
mesma especificidade — ou seja, a folha comum não consegue redefinir `--accent` por tema. `--accent-texto`
é o contrário: só a folha comum o declara, então só a folha comum o controla.

**2. Bloco novo `[data-theme="claro"]`,** posicionado logo depois do bloco `:root` e antes da regra
universal de `box-sizing`. Ele declara **exatamente 13 propriedades** — os 7 tokens neutros, os 5
semânticos e o `--accent-texto` — e mais nada. `--accent` e `--ff` ficam de fora de propósito: o primeiro
é identidade do módulo e o segundo não tem cor.

Valores fechados (já conferidos por contraste, ver bloco de justificativa abaixo):

- `--bg` recebe `#f4f2ec`
- `--surface` recebe `#ffffff`
- `--surface2` recebe `#e8e4d8`
- `--border` recebe `#cec7b4`
- `--text` recebe `#24231f`
- `--text2` recebe `#55503f`
- `--text3` recebe `#67604f`
- `--green` recebe `#2f6b45`
- `--yellow` recebe `#7a6010`
- `--red` recebe `#9c3826`
- `--blue` recebe `#2b5f80`
- `--orange` recebe `#8f4f0e`
- `--accent-texto` recebe `color-mix(in srgb,var(--accent) 55%,#000)`

Justificativa dos números, para que ninguém os "arredonde" numa fase futura: cada um dos 12 tokens de
cor foi medido pela fórmula de luminância relativa da WCAG 2.1 contra os três fundos do próprio tema
claro (`#f4f2ec`, `#ffffff`, `#e8e4d8`) e **todos os 36 pares passam em AA (mínimo 4,5:1)**; o pior par é
`--yellow` sobre `--surface2`, em 4,71:1. Os 55% do `color-mix` são o teto que faz os quatro `--accent`
usados hoje pelos módulos (`#4aa0a0`, `#c9a84c`, `#4a7fa0`, `#8a7f5c`) passarem em AA sobre os três
fundos claros — o pior caso é o amarelo de Máquinas/Elétrica, em 5,10:1 sobre `--surface2`. A 62% esse
mesmo par cai para 4,24:1 e reprova; por isso 55% e não um número mais alto.

Acrescentar acima do bloco um comentário de seção no padrão do arquivo (`/* ── tema claro ── */`)
explicando em português que o tema escuro é o padrão de `:root`, que o bloco só redefine o que muda, e
que `--accent` fica de fora por ser identidade de cada módulo.

**3. Classe `.btn-tema`,** acrescentada junto à família de botões já existente (depois de `.btn:disabled`,
linha 73). É o estilo do botão que o plano 06-02 vai injetar na barra superior: fundo `var(--surface2)`,
cor `var(--text2)`, borda de 1px `var(--border)`, `border-radius:6px`, `padding:4px 9px`,
`font-size:13px`, `line-height:1`, `cursor:pointer`, `font-family:var(--ff)` e `transition:.15s`; no
estado `:hover`, `border-color` e `color` recebem `var(--accent-texto)`.

**O nome da classe é `btn-tema` e isso é uma restrição, não uma preferência:** `tests/shell.test.js`
afirma hoje, por expressão regular sobre a string gerada, que um topo sem abas não contém `class="nav"`
e que um topo com duas abas tem exatamente dois botões cuja classe começa por `nav-btn`. Qualquer nome
que contenha a subsequência `nav` quebra esses dois testes sem ter relação nenhuma com tema.

**Não mexer** em `.overlay` (fundo preto a 60%) nem na sombra de `.toast` (preto a 45%): são profundidade,
não texto, e continuam pretos nos dois temas de propósito. **Não mexer** na cor de texto de `.btn-p`
(`#0f1214` sobre `var(--accent)`): como `--accent` não muda por tema, esse par se comporta igual nos dois
e já passa em AA para os quatro destaques em uso.
  </action>

  <acceptance_criteria>
    - Bloco de tema claro existe: `grep -q "data-theme=.claro." shared/pmoc.css`
    - Token derivado existe nos dois lugares: `test $(grep -c -- "--accent-texto" shared/pmoc.css) -ge 2`
    - Os 13 valores do tema claro estão presentes: `for p in -- "--bg:#f4f2ec" "--surface:#ffffff" "--surface2:#e8e4d8" "--border:#cec7b4" "--text:#24231f" "--text2:#55503f" "--text3:#67604f" "--green:#2f6b45" "--yellow:#7a6010" "--red:#9c3826" "--blue:#2b5f80" "--orange:#8f4f0e"; do grep -qF -- "$p" shared/pmoc.css || exit 1; done`
    - O bloco de tema claro **não** redefine `--accent` nem `--ff`: a região entre a linha do seletor `data-theme` e a primeira `}` seguinte não contém `--accent:` nem `--ff:`
    - O bloco `:root` continua intacto — os 14 valores originais seguem no arquivo: `for t in -- "--bg:#1a1a18" "--surface:#242420" "--surface2:#2e2e2a" "--border:#38382f" "--text:#e8e4d8" "--text2:#a09a88" "--text3:#6a6458" "--green:#5a9e6f" "--yellow:#c9a84c" "--red:#b85c4a" "--blue:#4a7fa0" "--accent:#4aa0a0" "--orange:#c07840"; do grep -qF -- "$t" shared/pmoc.css || exit 1; done`
    - Classe do botão existe e não colide com as regex do shell: `grep -q "\.btn-tema" shared/pmoc.css` e `test $(grep -c "btn-tema" shared/pmoc.css | grep -c nav) -eq 0` (o nome não contém `nav`)
    - Arquivo cresceu (mudança aditiva): `test $(wc -l < shared/pmoc.css) -gt 130`
    - `node --test` sai 0 e reporta 25 testes
  </acceptance_criteria>

  <verify>
    <automated>bash -c 'set -e; grep -q "data-theme=.claro." shared/pmoc.css; test $(grep -c -- "--accent-texto" shared/pmoc.css) -ge 2; for p in "--bg:#f4f2ec" "--surface:#ffffff" "--surface2:#e8e4d8" "--border:#cec7b4" "--text:#24231f" "--text2:#55503f" "--text3:#67604f" "--green:#2f6b45" "--yellow:#7a6010" "--red:#9c3826" "--blue:#2b5f80" "--orange:#8f4f0e"; do grep -qF -- "$p" shared/pmoc.css || { echo "FALTA NO TEMA CLARO: $p"; exit 1; }; done; BLOCO=$(sed -n "/data-theme=.claro./,/}/p" shared/pmoc.css); echo "$BLOCO" | grep -q -- "--accent-texto"; test $(echo "$BLOCO" | grep -c -- "--accent:") -eq 0; test $(echo "$BLOCO" | grep -c -- "--ff:") -eq 0; for t in "--bg:#1a1a18" "--surface:#242420" "--surface2:#2e2e2a" "--border:#38382f" "--text:#e8e4d8" "--text2:#a09a88" "--text3:#6a6458" "--green:#5a9e6f" "--yellow:#c9a84c" "--red:#b85c4a" "--blue:#4a7fa0" "--accent:#4aa0a0" "--orange:#c07840"; do grep -qF -- "$t" shared/pmoc.css || { echo "TOKEN ESCURO PERDIDO: $t"; exit 1; }; done; grep -q "\.btn-tema" shared/pmoc.css; test $(grep -o "btn-tema" shared/pmoc.css | grep -c "nav") -eq 0; test $(wc -l < shared/pmoc.css) -gt 130; node --test >/dev/null; echo TASK1_TEMA_CLARO_OK'</automated>
  </verify>

  <done>`shared/pmoc.css` tem o bloco `[data-theme="claro"]` com os 12 tokens de cor verificados em AA mais `--accent-texto`, tem `--accent-texto` também em `:root`, tem a classe `.btn-tema`, não perdeu nenhum valor de `:root` e a suíte continua verde.</done>
</task>

<task type="auto">
  <name>Task 2: Eliminar de shared/pmoc.css e shared/auth.js as cores que dependem de fundo escuro</name>

  <read_first>
    - `shared/pmoc.css` linhas 54, 60-65, 71 e 76-79 — os pontos exatos a corrigir, e a linha 64 (`.b-accent`), que é o precedente de `color-mix` já em produção no projeto desde o plano 05-01
    - `shared/pmoc.css` linhas 21, 26, 31, 70, 95, 127 — as regras que hoje pintam `var(--accent)` como texto ou como borda de componente e passam a usar `var(--accent-texto)` criado na Task 1
    - `shared/auth.js` linhas 118-124 e 146-152 — os dois botões `Entrar` (senha e e-mail), únicos pontos do arquivo com cor de texto fixa; o resto do arquivo já usa `var(--token, alternativa)` e acompanha o tema sem mudança
    - `.planning/phases/06-tema-claro-escuro/06-RESEARCH.md` — "Common Pitfalls" 1 e 2, que explicam por que cada um destes pontos falha em silêncio no tema claro
  </read_first>

  <files>shared/pmoc.css, shared/auth.js</files>

  <action>
**Em `shared/pmoc.css` — cinco correções. Todas produzem pixel idêntico no tema escuro.**

1. **Borda de linha de tabela (`.tbl td`, linha 54).** A borda inferior é hoje branco a 4% de opacidade,
   uma técnica que só funciona quando se sabe que o fundo é escuro; sobre um fundo claro ela some. Trocar
   por `color-mix(in srgb,var(--border) 45%,transparent)`. A 45% do token de borda o resultado composto
   sobre a superfície escura fica praticamente na mesma tonalidade de hoje, e sobre a superfície clara a
   separação entre linhas passa a existir. Não usar `var(--border)` puro: no escuro isso deixaria a
   grade nitidamente mais forte do que é hoje, e este plano não redesenha nada.

2. **Fundo dos cinco selos (`.b-ok`, `.b-warn`, `.b-red`, `.b-blue`, `.b-gold`, linhas 60-63 e 65).** Cada
   um tem hoje um fundo em `rgba` com os componentes numéricos do respectivo token copiados à mão a 15%
   de opacidade. Trocar cada fundo por `color-mix(in srgb,var(--TOKEN) 15%,transparent)`, usando
   `--green`, `--yellow`, `--red`, `--blue` e `--orange` respectivamente — exatamente o formato que
   `.b-accent` já usa na linha 64. A cor do texto de cada um já é `var(--TOKEN)` e não muda; só o fundo
   passa a acompanhar o token quando o tema troca. Em `.b-accent`, trocar apenas a cor do texto de
   `var(--accent)` para `var(--accent-texto)`, mantendo o fundo derivado de `var(--accent)`.

3. **Botão destrutivo (`.btn-d`, linha 71).** Fundo e borda repetem os componentes de `--red` à mão, a
   20% e a 30%. Trocar o fundo por `color-mix(in srgb,var(--red) 20%,transparent)` e a borda por
   `color-mix(in srgb,var(--red) 30%,transparent)`. A cor do texto já é `var(--red)`.

4. **Callouts de alerta (`.co-warn`, `.co-red`, `.co-ok`, `.co-blue`, linhas 76-79).** São exatamente os
   avisos de vencido, estoque baixo e INOP citados no critério de sucesso 5. Cada um tem dois defeitos no
   mesmo seletor: o fundo repete os componentes do token à mão a 8%, e a **cor do texto é um hex literal**
   que hoje coincide com o token só porque ninguém mudou o token ainda. Corrigir os dois: fundo vira
   `color-mix(in srgb,var(--TOKEN) 8%,transparent)` e a cor do texto vira `var(--TOKEN)`, com `--yellow`,
   `--red`, `--green` e `--blue` respectivamente. Atenção ao `.co-blue`: a cor de texto literal dele
   **não** é igual ao token `--blue` (é um azul mais claro, escolhido à mão) — a correção alinha os dois,
   e essa é uma mudança visual mínima e intencional no tema escuro, a única do plano.

5. **Destaque como texto e como borda de componente.** Nas seis regras listadas em `<read_first>`, trocar
   `var(--accent)` por `var(--accent-texto)`: a cor do nome do módulo no logo (`.logo-accent`), a cor do
   link do portal em foco do ponteiro (`.topbar-back:hover`), a borda inferior da aba ativa
   (`.nav-btn.active`), a borda e a cor do botão secundário em foco do ponteiro (`.btn-s:hover`), a borda
   do campo de formulário em foco (`input:focus`, que vale também para `select` e `textarea`) e a cor do
   link do rodapé em foco do ponteiro (`.shell-footer a:hover`). **Não** trocar em `.logo-dot`,
   `.kc-accent` e `.btn-p`: nesses três o destaque é preenchimento decorativo ou fundo de botão, não
   texto nem contorno de componente, e deve continuar exibindo a cor de identidade do módulo sem
   escurecimento.

**Em `shared/auth.js` — uma correção, repetida nos dois botões `Entrar` (linhas 120 e 148).**

Os dois botões têm fundo `var(--yellow,#c9a84c)` e cor de texto fixa em um quase-preto. Com o tema claro
o token `--yellow` passa a ser um oliva escuro, e texto quase-preto sobre oliva escuro fica ilegível.
Trocar a cor do texto dos dois botões para `var(--bg,#1a1a18)`. No tema escuro `--bg` é exatamente o
mesmo quase-preto de hoje, então a tela de login sai byte-equivalente em aparência; no tema claro o texto
passa a ser o quase-branco do fundo claro sobre o oliva escuro, com contraste medido de 5,35:1.

Nenhuma outra alteração em `shared/auth.js`: nenhuma assinatura, nenhum fluxo de senha, nenhum fluxo de
e-mail, nenhum `_carregarPerfil`. O resto do arquivo já usa `var(--token, alternativa)` em todo lugar e
acompanha o tema sem uma linha de código nova.
  </action>

  <acceptance_criteria>
    - Sumiu o branco translúcido: `test $(grep -c "rgba(255,255,255" shared/pmoc.css) -eq 0`
    - Sumiram os cinco conjuntos de componentes de token copiados à mão: `test $(grep -cE "rgba\((90,158,111|201,168,76|184,92,74|74,127,160|192,120,64)" shared/pmoc.css) -eq 0`
    - Sumiram os quatro hex literais de cor de texto dos callouts: `test $(grep -cE "color:#(c9a84c|b85c4a|5a9e6f|8eb9d1)" shared/pmoc.css) -eq 0`
    - As duas exceções conscientes continuam pretas: `test $(grep -c "rgba(0,0,0" shared/pmoc.css) -eq 2`
    - `color-mix` passou a ser o padrão de fundo tingido: `test $(grep -c "color-mix" shared/pmoc.css) -ge 12`
    - O destaque como texto passa pelo token derivado: `test $(grep -c "var(--accent-texto)" shared/pmoc.css) -ge 7`
    - O destaque decorativo **não** foi trocado: `grep -q "\.logo-dot{width:8px;height:8px;border-radius:50%;background:var(--accent)}" shared/pmoc.css` (ou equivalente: a regra `.logo-dot` continua com `var(--accent)`)
    - `shared/auth.js` não tem mais cor de texto fixa nos botões: `test $(grep -c "color:#1a1a18" shared/auth.js) -eq 0` e `test $(grep -c "color:var(--bg,#1a1a18)" shared/auth.js) -eq 2`
    - `shared/auth.js` continua sendo ESM válido: cópia para `.mjs` e `node --check` sai 0
    - API pública de `shared/auth.js` intacta: `export const CARGOS_PADRAO`, `export class Auth` e `export async function cadastrarCargo` seguem presentes
    - `node --test` sai 0 e reporta 25 testes
  </acceptance_criteria>

  <verify>
    <automated>bash -c 'set -e; test $(grep -c "rgba(255,255,255" shared/pmoc.css) -eq 0; test $(grep -cE "rgba\((90,158,111|201,168,76|184,92,74|74,127,160|192,120,64)" shared/pmoc.css) -eq 0; test $(grep -cE "color:#(c9a84c|b85c4a|5a9e6f|8eb9d1)" shared/pmoc.css) -eq 0; test $(grep -c "rgba(0,0,0" shared/pmoc.css) -eq 2; test $(grep -c "color-mix" shared/pmoc.css) -ge 12; test $(grep -c "var(--accent-texto)" shared/pmoc.css) -ge 7; grep -q "logo-dot" shared/pmoc.css; sed -n "/\.logo-dot/p" shared/pmoc.css | grep -q "var(--accent)"; test $(grep -c "color:#1a1a18" shared/auth.js) -eq 0; test $(grep -c "color:var(--bg,#1a1a18)" shared/auth.js) -eq 2; cp shared/auth.js "${TMPDIR:-/tmp}/auth-tema.mjs"; node --check "${TMPDIR:-/tmp}/auth-tema.mjs"; for s in "export const CARGOS_PADRAO" "export class Auth" "export async function cadastrarCargo"; do grep -qF "$s" shared/auth.js || { echo "API QUEBRADA: $s"; exit 1; }; done; SAIDA=$(node --test 2>&1); echo "$SAIDA" | grep -qE "fail 0"; echo "$SAIDA" | grep -qE "tests 25"; echo TASK2_HARDCODES_OK'</automated>
  </verify>

  <done>Nenhuma regra de `shared/pmoc.css` depende mais de o fundo ser escuro: bordas, fundos tingidos e cores de texto saem todos de token, com as duas exceções de profundidade (scrim e sombra) preservadas de propósito; os dois botões `Entrar` de `shared/auth.js` derivam a cor do texto de `--bg`; a suíte continua com 25 testes verdes.</done>
</task>

</tasks>

<threat_model>
Nível ASVS 1, bloqueio em severidade `high`. Esta fase é 100% frontend, sem backend novo, sem tabela
nova, sem dado sensível novo — e este plano em particular altera apenas folha de estilo e dois atributos
`style` de botão. O registro abaixo é o que existe de fato; não há ameaça inventada para preencher tabela.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Folha de estilo comum → as 6 superfícies de módulo que a carregam | Uma regra errada em `shared/pmoc.css` se propaga para os 6 módulos de uma vez; é a mesma superfície de impacto que a Fase 5 criou de propósito |
| Navegador → PostgREST (Supabase) | Inalterada nesta fase: a chave anônima é pública por projeto e a autorização real continua sendo RLS no Postgres |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-01 | Denial of Service | `shared/pmoc.css` — regressão visual que torne texto ilegível nos 6 módulos ao mesmo tempo | medium | mitigate | Os 12 tokens do tema claro têm contraste medido em AA contra os três fundos do próprio tema antes de entrarem no arquivo; gate automatizado exige que nenhum valor de `:root` tenha sumido, garantindo que o tema escuro sai idêntico |
| T-06-02 | Tampering | `shared/auth.js` — alteração na tela de login, superfície de autenticação | low | mitigate | A mudança é restrita à cor de texto de dois botões; gate automatizado confere que as três exportações públicas continuam presentes e que o arquivo segue analisável como módulo ES |
| T-06-03 | Information Disclosure | Contraste insuficiente escondendo estado crítico (vencido, INOP) do operador no tema claro | medium | mitigate | Callouts e selos passam a derivar fundo e texto de token, e os tokens semânticos do tema claro passam em AA sobre os três fundos; roteiro manual de conferência fica registrado no plano 06-04 |
| T-06-SC | Tampering | Cadeia de suprimentos de pacotes | low | accept | Este plano não instala pacote nenhum — projeto zero-build, sem `package.json`, sem npm/pip/cargo; ver `06-RESEARCH.md` §"Package Legitimacy Audit" |

Nenhuma ameaça deste plano é `high`. T-06-01 e T-06-03 são `medium` e têm gate automatizado.
</threat_model>

<verification>
1. `node --test` na raiz sai 0 e reporta 25 testes, nenhum removido ou enfraquecido.
2. `shared/pmoc.css` contém `[data-theme="claro"]` com os 12 tokens de cor e `--accent-texto`, e o bloco não redefine `--accent` nem `--ff`.
3. Os 14 valores originais de `:root` continuam no arquivo — o tema escuro não mudou.
4. `grep -c 'rgba(255,255,255' shared/pmoc.css` retorna 0 e `grep -c 'rgba(0,0,0' shared/pmoc.css` retorna 2 (scrim e sombra, preservados de propósito).
5. Nenhum arquivo fora de `shared/` foi modificado: `git diff --name-only 351b13c..HEAD -- . ':!.planning' | grep -v '^shared/' | wc -l` é 0.
6. `refrigeracao/` intocada: `git diff --name-only 351b13c..HEAD -- refrigeracao/ | wc -l` é 0 (D-04, PLAT-15).
7. `mapa/xmap.css` intocado: `git diff --name-only 351b13c..HEAD -- mapa/ | wc -l` é 0 (D-01).
</verification>

<success_criteria>
- Existe um tema claro completo em tokens na folha comum, com contraste AA verificado, sem uma linha de CSS de tema em nenhum módulo (base do critério de sucesso 2).
- Os estados de alerta do critério de sucesso 5 (vencido, estoque baixo, INOP) derivam cor de token e vão acompanhar a troca de tema quando ela existir.
- O tema escuro sai desta fase idêntico ao que entrou, exceto pela cor de texto de `.co-blue`, alinhada ao próprio token de forma intencional e documentada.
- A tela de login compartilhada está pronta para os dois temas sem nenhuma lógica nova.
- Nenhum módulo foi tocado — a adoção acontece nos planos 06-02 e 06-03.
</success_criteria>

<output>
Criar `.planning/phases/06-tema-claro-escuro/06-01-SUMMARY.md` ao concluir.
</output>
