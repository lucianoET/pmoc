---
phase: quick-260810-k0q
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mapa/xmap.js
  - mapa/xmap.css
  - mapa/xmap-layers-aguada.js
  - mapa/xmap-layers-grama.js
  - mapa/xmap-layers-eletrica.js
  - mapa/index.html
  - mapa/app.js
  - vercel.json
  - index.html
  - TESTES.md
autonomous: true
requirements:
  - QUICK-260810-k0q
must_haves:
  truths:
    - "Abrir /mapa exibe a tela de login por cargo; após entrar, o mapa Leaflet renderiza centrado no CMASM."
    - "Os botões Mapa/Satélite alternam o basemap sem recarregar a página."
    - "Ativar/desativar um módulo na barra lateral mostra/oculta as camadas correspondentes e atualiza o painel de filtros."
    - "O portal (/) lista o módulo Mapa e o link abre /mapa."
    - "Nenhum arquivo sob /home/luc/DEV_ERP foi criado, alterado ou removido."
    - "As 9 identidades e as 23 viagens do CSV continuam registradas em transp_ativos/transp_viagens (import já concluído; esta tarefa apenas confere)."
  artifacts:
    - mapa/index.html
    - mapa/app.js
    - mapa/xmap.js
    - mapa/xmap.css
    - mapa/xmap-layers-aguada.js
    - mapa/xmap-layers-grama.js
    - mapa/xmap-layers-eletrica.js
    - vercel.json
    - index.html
    - TESTES.md
  key_links:
    - "mapa/index.html carrega, em scripts clássicos e nesta ordem: Leaflet → xmap.js → arquivos de camadas; só depois o bootstrap <script type=\"module\"> importa ./app.js (módulos são adiados, então xMap já existe quando app.js roda)."
    - "mapa/app.js importa Auth de ../shared/auth.js e criarClienteSupabase de ../shared/supabase-config.js (mesmo contrato de transportes/app.js)."
    - "vercel.json: rewrite /mapa → /mapa/index.html, seguindo as entradas dos módulos existentes."
    - "shared/supabase-config.js descobre as credenciais lendo ../maquinas/app.js por fetch — o módulo precisa ser servido por HTTP (python -m http.server ou Vercel), não aberto via file://."
---

<objective>
Portar o app legado de mapa `cmms-mapa` (Leaflet + JS puro, em /home/luc/DEV_ERP — somente leitura) para um novo módulo `/mapa` do pmoc, no padrão dos módulos existentes: zero-build, Supabase SDK via CDN, login por cargo com `shared/auth.js`, rewrite no `vercel.json` e card no portal.

**Achado que reduz o escopo desta tarefa — leia antes de executar:** a metade "importar o CSV" do pedido **já está feita e conferida**. O arquivo `supabase/11_transportes_seed.sql` já contém os 9 ativos (VTR-001..006, EMB-001..003) e as 23 viagens derivadas de `Mapa de VTR e EMB ATU 20FEV26.csv`, todas com `chave_importacao` no formato `csv-2026-01-30-*` e `importado_de` apontando para o nome do CSV. A conferência linha a linha contra o CSV está registrada em `.planning/phases/01-*/01-CONFERENCIA-IMPORT.md` (Fase 01, Plano 04) e não encontrou divergências: 9/9 ativos e 23/23 viagens reconciliam, e o seed é idempotente (`on conflict do update` por `codigo` e por `chave_importacao`).

Portanto **não** crie migração nova (`24_*.sql`) para reimportar o CSV — seria dado duplicado sem ganho e contraria a regra aditiva. O que falta é apenas confirmar que o seed está aplicado no banco de produção; isso vira uma consulta documentada na Task 3, executada pelo usuário no SQL Editor.

Purpose: entregar o módulo de mapa no padrão pmoc sem quebrar os módulos em produção e sem retrabalho de importação.
Output: diretório `mapa/` com 7 arquivos, rota `/mapa` no Vercel, card no portal, checklist de teste e consulta de conferência do import em `TESTES.md`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md

# Padrão de módulo pmoc a copiar (shell, login, bootstrap)
@transportes/index.html
@shared/supabase-config.js
@shared/auth.js

# Rota e portal
@vercel.json
@index.html

# Fonte legada — SOMENTE LEITURA, nunca editar
# /home/luc/DEV_ERP/cmms-mapa/{xmap.js,xmap.css,demo.html,xmap-layers-*.js}

# Import do CSV já concluído — referência, não refazer
# supabase/11_transportes_seed.sql
# .planning/phases/01-*/01-CONFERENCIA-IMPORT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Portar o núcleo xMap para mapa/</name>
  <files>mapa/xmap.js, mapa/xmap.css, mapa/xmap-layers-aguada.js, mapa/xmap-layers-grama.js, mapa/xmap-layers-eletrica.js</files>
  <action>
Copie os cinco arquivos de `/home/luc/DEV_ERP/cmms-mapa/` para `mapa/` usando `cp` (a origem é estritamente somente leitura: nunca use Edit/Write em nada sob /home/luc/DEV_ERP, e não rode git, format ou qualquer comando com efeito colateral lá). Todas as adaptações abaixo são feitas **nas cópias** dentro de `mapa/`.

Adaptações obrigatórias nas cópias:

1. `mapa/xmap-layers-grama.js` — o arquivo legado busca dados de uma API xCore de desenvolvimento na rede local (porta 8010) com fallback para dados mock. Remova a constante do endpoint e todo o caminho de `fetch`, deixando o módulo servir apenas os dados mock já embutidos no arquivo. Motivo: a página será servida por HTTPS no Vercel, e uma chamada a endereço de rede local nunca resolveria — geraria erro de conteúdo misto e ruído no console. Ao remover, **não cite o endereço nem a porta removidos em comentários** (o gate de verificação procura esse número no diretório inteiro); descreva como "API legada do xCore, removida no port".

2. `mapa/xmap.css` — o CSS usa `var(--xm-sans)` e `var(--xm-mono)` sem defini-las (vinham de um `assets/fonts.css` que não existe no pmoc). Declare as duas variáveis no topo do arquivo, dentro de um bloco `:root`, com pilhas de fontes de sistema: sans → `'Inter', system-ui, sans-serif` (a mesma família usada nos módulos pmoc); mono → `ui-monospace, 'JetBrains Mono', monospace`. Nenhum `@import` de fonte externa.

3. Em cada um dos cinco arquivos, acrescente um comentário de cabeçalho de duas linhas, em português, no padrão do projeto: a origem (portado do app legado cmms-mapa) e o aviso de que os dados de camada são de demonstração, não vindos do Supabase.

Não reescreva a lógica do xMap: `init`, `setModules`, `toggleBasemap`, `registerLayer`, `updateElement` e `utils` permanecem com a mesma assinatura pública, porque `mapa/app.js` (Task 2) depende delas. Os arquivos de camada continuam sendo IIFEs que chamam `xMap.registerLayer(nomeDoModulo, layerDefs)` no fim.

<!-- planner-discipline-allow: 8010 -->
  </action>
  <verify>
    <automated>node --check mapa/xmap.js && node --check mapa/xmap-layers-aguada.js && node --check mapa/xmap-layers-grama.js && node --check mapa/xmap-layers-eletrica.js && test -s mapa/xmap.css && grep -q -- '--xm-sans' mapa/xmap.css && ! grep -rq 8010 mapa/ && test "$(find /home/luc/DEV_ERP/cmms-mapa -type f -newermt 2026-08-01 | wc -l)" = "0" && echo OK</automated>
  </verify>
  <done>Os cinco arquivos existem em `mapa/`, passam no parser do Node, `xmap.css` define `--xm-sans`/`--xm-mono`, nenhuma referência à porta da API legada sobrou em `mapa/`, e nada em `/home/luc/DEV_ERP/cmms-mapa` foi tocado (nenhum **arquivo** com mtime posterior a 2026-08-01 — os fontes legados são de 2026-05-26).</done>
</task>

<task type="auto">
  <name>Task 2: Criar o app /mapa no padrão pmoc (login por cargo + shell)</name>
  <files>mapa/index.html, mapa/app.js</files>
  <action>
Crie `mapa/index.html` seguindo a estrutura de `transportes/index.html` (leia-o antes; copie o esqueleto, não a lógica de negócio):

- `<head>`: charset utf-8, viewport, `theme-color` `#1a1a18`, `<title>PMOC Mapa</title>`; script UMD do Supabase pelo mesmo CDN e mesma URL já usada em transportes; CSS e JS do Leaflet **na versão fixa 1.9.4** (mesma do app legado) com `crossorigin="anonymous"`; depois `<link rel="stylesheet" href="xmap.css">`.
- `<style>` inline: reaproveite o bloco `:root` da paleta pmoc de `transportes/index.html` (mesmas variáveis `--bg`, `--surface`, `--border`, `--text`, `--text2`, `--cyan`, `--ff`) mais o reset e as regras de `.topbar`, `.logo`, `.user-chip`, `.btn`. Acrescente o layout do mapa: uma grade com barra lateral de módulos (~220px) e a área do mapa ocupando o restante da altura da janela; a `div` do mapa precisa de altura explícita, senão o Leaflet renderiza com 0px.
- `<body>`: `<div id="login-screen"></div>` e `<div id="app">` (oculto por `#app{display:none}` no CSS, como nos outros módulos). Dentro de `#app`: topbar com o logo "PMOC <span class="logo-accent">Mapa</span>", o `<span class="user-chip" id="user-chip">—</span>` e o botão `onclick="sair()"`; barra lateral com um botão por módulo (`aguada` — Aguada / monitoramento hídrico, `grama` — Grama / controle vegetal, `eletrica` — Elétrica / sistema elétrico), cada um chamando `alternarModulo('<mod>', this)`; e `<div id="mapa"></div>`.
- Cada botão de módulo exibe um selo textual "demo" indicando dados de demonstração — as camadas portadas são mock, não vêm do Supabase, e a UI não pode sugerir o contrário.
- Antes do fechamento do body, em scripts clássicos e **nesta ordem**: Leaflet → `xmap.js` → `xmap-layers-aguada.js` → `xmap-layers-grama.js` → `xmap-layers-eletrica.js`. Só depois, o bloco `<script type="module">` de bootstrap idêntico ao de `transportes/index.html` (o que monta o caminho de `./app.js` tratando o protocolo `file:`).

Crie `mapa/app.js` no padrão dos módulos (ES module, português, globais UPPER_CASE, funções camelCase, seções `// ── nome ──`):

- `import { Auth } from '../shared/auth.js'` e `import { criarClienteSupabase } from '../shared/supabase-config.js'`.
- Estado: `supa`, `auth`, `USUARIO`, e um `Set` `MODULOS_ATIVOS`.
- Na inicialização: crie o cliente Supabase, instancie `Auth`, monte em `'#login-screen'` e registre `onLogin` — nele, guarde o usuário, preencha `#user-chip` com nome e cargo, exiba `#app` e só então chame `xMap.init('mapa', { modules: [], basemap: 'map', center: [-22.8400, -43.1055], zoom: 15, minZoom: 13, maxZoom: 19 })` (mesmos valores do demo legado). Inicializar o mapa antes de `#app` ficar visível faz o Leaflet medir um container de altura zero.
- `alternarModulo(mod, btn)`: alterna o módulo no `Set`, alterna a classe ativa no botão e chama `xMap.setModules([...MODULOS_ATIVOS])`.
- `sair()`: chama `auth.sair()` conforme o contrato de `shared/auth.js`.
- Exponha `alternarModulo` e `sair` em `window`, porque o HTML usa handlers `onclick` inline (padrão do projeto).
- Trate erro de carga como nos outros módulos: mensagem ao usuário, sem quebrar a tela.

Nenhuma tabela nova, nenhuma consulta ao Supabase além do login: este port entrega o componente de mapa e a autenticação. Alimentar camadas com dados reais (por exemplo, posições de `transp_ativos`) fica fora do escopo desta tarefa.
  </action>
  <verify>
    <automated>node --input-type=module --check < mapa/app.js && grep -q 'id="login-screen"' mapa/index.html && grep -q 'id="app"' mapa/index.html && grep -q 'leaflet@1.9.4' mapa/index.html && grep -q 'xmap.js' mapa/index.html && grep -q 'script type="module"' mapa/index.html && grep -q "shared/auth.js" mapa/app.js && grep -q "shared/supabase-config.js" mapa/app.js && echo OK</automated>
  </verify>
  <done>`mapa/app.js` passa no parser de módulo do Node e importa Auth e criarClienteSupabase de `shared/`; `mapa/index.html` tem a tela de login, o container `#app`, o Leaflet 1.9.4 fixado, os scripts do xMap em ordem antes do bootstrap `type="module"`.</done>
</task>

<task type="auto">
  <name>Task 3: Rotear /mapa no Vercel e no portal + registrar a conferência do import</name>
  <files>vercel.json, index.html, TESTES.md</files>
  <action>
1. `vercel.json`: acrescente `{ "source": "/mapa", "destination": "/mapa/index.html" }` ao array `rewrites`, depois da entrada de `/predial`, mantendo o alinhamento das colunas usado no arquivo. Não altere `cleanUrls`, `outputDirectory` nem as rotas existentes.

2. `index.html` (portal): acrescente um card no grid dos módulos ativos, logo depois do card do Predial, com a mesma marcação dos demais (`<a class="card" href="/mapa">`, `<div class="ico">`, `<div class="nm">Mapa</div>`, `<div class="ds">` e `<div class="tags">`). Ícone de mapa, nome "Mapa", descrição em português citando Leaflet, basemaps mapa/satélite e camadas por módulo. Nas tags, use `v0.1` e uma tag deixando explícito que as camadas são dados de demonstração — o portal não pode dar a entender que já existe telemetria real.

3. `TESTES.md`: acrescente uma seção nova no fim, no formato de checklist já usado no arquivo, com dois blocos:

   a. **Módulo Mapa (/mapa)** — passos manuais: servir o repositório por HTTP (`python -m http.server` a partir da raiz — abrir via `file://` quebra a descoberta de credenciais do `shared/supabase-config.js`); abrir `/mapa`; entrar por cargo; confirmar que o mapa renderiza centrado no CMASM; alternar Mapa/Satélite; ativar e desativar cada um dos três módulos verificando o painel de filtros; confirmar no console do navegador que não há erro de rede nem de conteúdo misto; sair e confirmar que a tela de login volta.

   b. **Conferência do import do CSV de VTR/EMB** — deixe registrado que a importação já foi aplicada por `supabase/11_transportes_seed.sql` e conferida em `.planning/phases/01-*/01-CONFERENCIA-IMPORT.md`, e inclua a consulta a rodar no SQL Editor do Supabase para confirmar o estado em produção: contar `transp_ativos` (esperado 9) e contar `transp_viagens` filtrando por `importado_de` igual ao nome do arquivo CSV (esperado 23), mais uma contagem de `chave_importacao` distintas para provar que não houve duplicação. Anote que, se os números vierem menores, a correção é reexecutar `11_transportes_seed.sql`, que é idempotente — e que **não** se deve criar migração nova para isso.
  </action>
  <verify>
    <automated>python3 -c "import json,sys; d=json.load(open('vercel.json')); sys.exit(0 if any(r.get('source')=='/mapa' for r in d['rewrites']) else 1)" && grep -q 'href="/mapa"' index.html && grep -q '/mapa' TESTES.md && grep -q '11_transportes_seed.sql' TESTES.md && echo OK</automated>
    <human-check>Servir o repositório por HTTP a partir da raiz, abrir /mapa, entrar com um cargo e confirmar: o mapa aparece centrado no CMASM, os botões Mapa/Satélite trocam o basemap, cada módulo da barra lateral acende/apaga suas camadas e o painel de filtros acompanha, o console não acusa erro de rede, e o card Mapa no portal (/) leva a /mapa. Conferir também que /transportes, /maquinas, /refrigeracao, /eletrica, /fonoclama e /predial continuam abrindo normalmente.</human-check>
  </verify>
  <done>`vercel.json` tem o rewrite de `/mapa` com as rotas antigas intactas, o portal tem o card apontando para `/mapa`, e `TESTES.md` traz o checklist do módulo e a consulta de conferência do import do CSV referenciando `11_transportes_seed.sql`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navegador → CDN de terceiros | Leaflet 1.9.4 (CSS+JS) e o SDK do Supabase são carregados de CDN pública e executam com todos os privilégios da página, inclusive acesso à sessão autenticada. |
| navegador → Supabase | O login por cargo troca senha por sessão; o controle de acesso real é RLS no Postgres, qualquer checagem no cliente é só UX. |
| código legado → módulo novo | Código copiado de `/home/luc/DEV_ERP/cmms-mapa` entra em um app com sessão autenticada e é servido por HTTPS. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-k0q-01 | Tampering | Leaflet via CDN em `mapa/index.html` | medium | mitigate | Fixar a versão exata 1.9.4 (nunca faixa/`latest`) e usar `crossorigin="anonymous"`. Task 2 grava a versão fixa; o gate automatizado verifica `leaflet@1.9.4` no HTML. |
| T-k0q-02 | Information Disclosure | camada `grama` portada, que buscava API de rede local | medium | mitigate | Task 1 remove o caminho de `fetch` para o host de desenvolvimento; o gate falha se a porta legada aparecer em qualquer arquivo de `mapa/`. Elimina requisição a endereço não-HTTPS a partir de página autenticada. |
| T-k0q-03 | Tampering | Injeção de HTML em `popupHTML` do xMap | low | accept | `popupHTML` interpola valores sem escapar, mas neste port todos os dados de camada são constantes locais versionadas — não há entrada de usuário nem de banco. Aceito enquanto as camadas forem estáticas; ao ligar camadas a dados do Supabase, escapar antes de interpolar vira pré-requisito (registrado no comentário de cabeçalho dos arquivos de camada, Task 1). |
| T-k0q-04 | Elevation of Privilege | novo módulo `/mapa` | low | accept | Não cria tabela, política, coluna nem consulta de escrita; usa o mesmo `Auth` e a mesma anon key já públicos por design, com RLS inalterada. Superfície de privilégio idêntica à dos módulos existentes. |
| T-k0q-05 | Tampering | Fonte legada em `/home/luc/DEV_ERP` | medium | mitigate | Somente `cp` de leitura; nenhuma escrita, Git ou build na origem. Gate automatizado da Task 1 falha se qualquer arquivo de `cmms-mapa` tiver mtime posterior a 2026-08-01 (os fontes são de 2026-05-26). O gate usa `-type f` de propósito: a mtime do próprio diretório já é posterior a essa data por motivo alheio ao port, e só as mtimes dos arquivos provam ausência de escrita. |
| T-k0q-SC | Tampering | cadeia de suprimentos de pacotes | low | accept | Não aplicável a esta tarefa: o projeto é zero-build e não executa npm/pip/cargo. Nenhum pacote é instalado; toda dependência entra por CDN com versão fixada (coberto por T-k0q-01). |
</threat_model>

<verification>
1. `node --check` passa nos quatro arquivos JS clássicos de `mapa/` e `node --input-type=module --check` passa em `mapa/app.js`.
2. Nenhuma referência à API legada de rede local sobrou em `mapa/`.
3. `/home/luc/DEV_ERP/cmms-mapa` intocado (nenhum arquivo com mtime posterior a 2026-08-01).
4. `vercel.json` é JSON válido, tem o rewrite de `/mapa` e mantém as seis rotas anteriores.
5. Portal e `TESTES.md` referenciam `/mapa`; `TESTES.md` traz a consulta de conferência do import.
6. Verificação humana: mapa renderiza, basemaps alternam, camadas alternam, módulos em produção continuam abrindo.
</verification>

<success_criteria>
- `/mapa` existe como módulo pmoc: login por cargo, zero-build, dependências por CDN com versão fixada, rota no Vercel e card no portal.
- O núcleo xMap foi portado com a API pública preservada e sem chamada a host de desenvolvimento.
- Nenhum arquivo sob `/home/luc/DEV_ERP` foi alterado.
- Nenhuma migração nova foi criada: o import do CSV permanece o já aplicado em `supabase/11_transportes_seed.sql`, agora com consulta de conferência documentada em `TESTES.md`.
- Os seis módulos existentes continuam funcionando.
</success_criteria>

<output>
Criar `.planning/quick/260810-k0q-importar-csv-programa-o-vtr-emb-em-trans/260810-k0q-SUMMARY.md` ao concluir.
</output>
