---
phase: quick-260822-8rz
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - refrigeracao/index.html
  - tests/refrigeracao-desktop.test.js
  - tests/fixtures/refrigeracao-css-mobile.css
autonomous: true
requirements: [QUICK-260822-8rz]

must_haves:
  truths:
    - "Em tela >=1024px a navegação é uma coluna lateral fixa à esquerda, e a barra de 5 botões some do rodapé sem que a marcação mude"
    - "Em tela >=1024px Inventário, OS-Manutenção, Inst./Remoção, PMOC, Alertas e Contratações são <table> de verdade, com ordenação e filtro por coluna"
    - "Clicar (ou tabular até) a linha da tabela abre exatamente a mesma ficha/gaveta que o cartão abre hoje"
    - "Em tela >=1024px a gaveta entra pela direita como painel lateral, e o gesto de arrastar para fechar não responde mais"
    - "Abaixo de 1024px nada mudou: nenhuma regra de CSS fora da @media foi alterada e nenhuma marcação de tabela é gerada"
    - "Ordenar por cabeçalho e ordenar pelo seletor de sempre nunca ficam ativos ao mesmo tempo — um cede para o outro, e a tela diz qual está valendo"
  artifacts:
    - refrigeracao/index.html
    - tests/refrigeracao-desktop.test.js
    - tests/fixtures/refrigeracao-css-mobile.css
  key_links:
    - "modoDaTela(largura) é o único lugar que decide cartão x tabela; TELA_LARGA é a única variável que os renderizadores leem"
    - "tabDesenhar() é o único desenhista de tabela — seis adaptadores de coluna, uma implementação"
    - "filtrarInventario() continua sendo a porta única do recorte do inventário, agora com os filtros de coluna como quarto parâmetro, para tela/etiqueta/CSV nunca divergirem"
    - "TAB_ESTADO tem uma entrada por tabela — ordenar o inventário não pode reordenar a lista de OS"
---

<objective>
Dar a `/refrigeracao` uma versão de computador: em `>=1024px` a navegação vira coluna
lateral, o conteúdo ganha largura útil, a gaveta vira painel lateral e as seis listas
viram tabelas ordenáveis e filtráveis por coluna. Abaixo de 1024px o módulo continua
byte-a-byte o que é hoje.

Purpose: o módulo é mobile-only esticado — em 1920px ele mostra 5 cartões numa coluna e
1.400px de fundo cinza. Quem opera o PMOC no computador precisa varrer 171 equipamentos,
comparar prédio com vencimento e ordenar por criticidade sem rolar 40 telas.

Output: um bloco `<style>` + um bloco `<script>` novos no fim de `refrigeracao/index.html`,
todo o CSS novo dentro de um único `@media (min-width:1024px)`, mais um gate permanente
(`tests/refrigeracao-desktop.test.js`) e a impressão digital do CSS de celular
(`tests/fixtures/refrigeracao-css-mobile.css`).

**Nada de dado, fluxo ou regra de negócio muda. É camada de apresentação.**
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@refrigeracao/index.html
@shared/tabela.js
@maquinas/estoque-tabela.js
@tests/inventario-ordem-refrigeracao.test.js
@tests/refrigeracao-planilha.test.js
@tests/refrigeracao-qr-nfc.test.js
@tests/refrigeracao-gaveta-qr.test.js
</context>

---

## Decisões travadas

As cinco primeiras vieram do usuário ("ok" ao desenho apresentado) e **não se reabrem**.
As demais fecham as sete perguntas que o desenho deixou em aberto.

| ID | Decisão | Por quê |
|----|---------|---------|
| **D-8rz-01** | **Mesmo arquivo.** Todo o layout de computador vive dentro de `@media (min-width:1024px)` em `refrigeracao/index.html`. | Um `desktop.html` forkaria um app de 273 KB em duas cópias que divergem na primeira correção. |
| **D-8rz-02** | **`matchMedia` escolhe qual renderizador roda**, com re-render ao cruzar o limiar. | Cartão e tabela são marcações diferentes; renderizar as duas e esconder uma dobraria 171 linhas de DOM. |
| **D-8rz-03** | **Ordenação/filtro replicados DENTRO do arquivo**, como núcleo puro testável. | O projeto tem um núcleo genérico com três consumidores, mas D-04 proíbe este módulo de importar qualquer coisa da pasta compartilhada. Replica-se a **ideia**, nunca o import. |
| **D-8rz-04** | **Celular intocado por construção.** Nenhuma regra de CSS fora da media query muda; nenhum seletor novo é declarado fora dela. | É a prova de que o computador não foi pago com o celular — e o gate afirma isso por comparação byte-a-byte, não por promessa. |
| **D-8rz-05** | **A linha da tabela abre a mesma ficha que o cartão abre hoje.** Ficha, QR, impressão e fluxo de OS não mudam. | A tabela é uma forma de achar a linha, não um segundo aplicativo. |
| **D-8rz-06** | **A ordenação de cabeçalho ENVOLVE `cmpInv`, não o substitui.** Sem coluna escolhida, a tabela do inventário sai na ordem de `cmpInv`/`invOrdem` — o seletor de sempre continua sendo a ordem padrão. Escolher uma coluna sobrepõe; fechar o ciclo (`asc`→`desc`→nenhuma) devolve o comando ao seletor. | Duas verdades sobre a ordem da mesma lista é o defeito clássico. Aqui só existe uma verdade por vez, e ela é visível. |
| **D-8rz-07** | **Mexer no seletor limpa a ordenação de cabeçalho** (`setInvOrdem` zera `TAB_ESTADO.inv.ord`), e uma ordenação de cabeçalho ativa acende um botão "Limpar ordenação" na barra da tabela. | Sem isso o usuário escolhe "Ordenar: criticidade" e **nada acontece**, porque um cabeçalho ainda manda — falha silenciosa. Dois caminhos de volta, e o estado anunciado. |
| **D-8rz-08** | **`cmpInv`, `CRIT_ORDEM`, `proxManut` e `isVencido` não são tocados.** A ordenação por criticidade da tabela reusa `CRIT_ORDEM` como valor numérico; a de vencimento reusa `proxManut`. | Já têm gate próprio (`tests/inventario-ordem-refrigeracao.test.js`), que recorta de `var CRIT_ORDEM =` até `function setChip(btn, val){` e avalia o trecho num sandbox com só quatro globais. Reusar em vez de reescrever mantém uma regra, não duas. |
| **D-8rz-09** | **`filtrarInventario` ganha um quarto parâmetro opcional** (`filtrosColuna`) e continua sendo a porta única do recorte do inventário. Tela, folha de etiquetas e CSV passam pelos mesmos quatro argumentos. | Se o filtro de coluna valesse só na tela, a planilha exportaria um conjunto diferente do que está à vista — e a importação, que arquiva por ausência dentro do escopo derivado do conteúdo, arquivaria por causa de um filtro de tela. Tela e papel não podem divergir (é o que D-5hy-24 e D-s3h-08 já exigem). |
| **D-8rz-10** | **A ordem do CSV e da etiqueta continua `cmpInv`**, mesmo com cabeçalho ordenando a tela. | Ordem de linha de um CSV não é assunto da tela, e o gate da planilha fixa `.sort(cmpInv)` de propósito. Recorte é uma coisa, ordem é outra. |
| **D-8rz-11** | **Movimentação (Inst./Remoção) entra**, virando a sexta tabela. | É uma lista de OS como as outras, com o mesmo cartão e o mesmo clique. Deixá-la de fora criaria um terceiro segmento que se comporta diferente dos outros dois na mesma aba. |
| **D-8rz-12** | **`tabDesenhar()` é o único desenhista de tabela**, dirigido por definição de coluna; as seis listas são seis definições, não seis desenhistas. | Mesmo motivo de `xmap-marcadores.js` ter substituído três cópias no `/mapa`: uma correção numa das seis nunca chegaria às outras cinco. |
| **D-8rz-13** | **Célula mista ordena pelo dado principal**, nunca pelo texto concatenado (`Prédio · Área` ordena por prédio). Criticidade ordena pela ordem semântica (`CRIT_ORDEM`), nunca alfabética. Datas ordenam por instante e filtram pelo texto em `dd/mm/aaaa`. | Precedente do projeto (`/reparos`, Estoque de Máquinas), e alfabético em criticidade poria ALTA antes de CRÍTICA. |
| **D-8rz-14** | **Vazio sempre por último, nas duas direções**, em coluna numérica e de texto. Zero não é vazio. | Vazio não é "menor", é "não informado" — a mesma regra do núcleo genérico do projeto e de `cmpInv` ("sem histórico sempre no fim"). |
| **D-8rz-15** | **Cabeçalho e corpo da tabela são desenhados por funções separadas.** Digitar num filtro redesenha só o `<tbody>`. | Redesenhar o `<thead>` a cada tecla mata o foco e o cursor do campo — o projeto já pagou por isso, e foi por isso que `renderMateriais()` virou cabeçalho + linhas. |
| **D-8rz-16** | **`TAB_ESTADO` tem uma entrada por tabela** (`inv`, `os`, `movim`, `pmoc`, `alert`, `contrat`), cada uma com `ord`, `filtros` e `aberto`. | Um estado global faria ordenar o inventário reordenar a lista de OS. |
| **D-8rz-17** | **A gaveta vira painel lateral só por CSS** (`translateX` em vez de `translateY`, largura `min(560px,46vw)`, alça escondida). A marcação de `#drawer` não muda. **O gesto de arrastar não responde em tela larga**: `touchstart` desiste quando `TELA_LARGA`. Escape fecha a gaveta nas duas larguras. | Arrastar para baixo é a linguagem do bottom-sheet; num painel lateral de notebook com tela sensível ao toque seria um fechamento acidental. `gestoFechaGaveta` **não é alterada** — o gate dela (D-tyx-03..07) continua verde por não haver o que mudar nela. |
| **D-8rz-18** | **O teto de 200px do QR da ficha não muda.** | Foi calibrado ontem (D-tyx-01/02) e tem gate com faixa 120–260. Um QR de 200px é escaneável no painel de 560px; mexer nele seria trocar uma decisão fechada por conta de espaço sobrando. |
| **D-8rz-19** | **Nenhum seletor novo fora da media query, e nenhum seletor de elemento nu** (`table{}`, `th{}`, `tr{}`). Tudo escopado em `.lista-tabela`. | `refrigeracao/index.html` já emite `<table class="tbl">` dentro da gaveta (painel de erros da planilha) e 12 `<table>` nas folhas de impressão — um `table{}` solto mudaria todas elas, inclusive no celular. |
| **D-8rz-20** | **Na tela larga o cabeçalho da tabela gruda no topo e `.search-wrap` deixa de grudar.** | As duas grudariam em `top:0` e se sobreporiam. Numa tela larga com navegação lateral fixa, a barra de busca rolar para fora é aceitável — o filtro por coluna cobre a mesma necessidade de perto. |
| **D-8rz-21** | **Sem virtualização de linha, sem paginação.** 171 linhas desenham direto. | Zero-build, sem dependência nova, e 171 `<tr>` não é um problema de desempenho em navegador nenhum deste século. Linha zebrada resolve a legibilidade. |
| **D-8rz-22** | **A linha inteira é clicável PARA O MOUSE, e a primeira célula de dado é um `<button>` de verdade** (com `event.stopPropagation()`), que é o caminho por teclado. `aria-sort` no `<th>`, cabeçalho é botão. | `tabindex` no `<tr>` é fingir semântica que a tabela não tem; um botão real é tabulável, anunciado e acionado por Enter e Espaço sem código nenhum. |
| **D-8rz-23** | **Lista vazia continua caindo no bloco `.empty` de hoje**, em qualquer largura: a guarda da tabela é inserida **depois** do ramo de lista vazia em cada renderizador. | As mensagens de vazio deste módulo carregam informação real ("169 sem histórico", "N removidos casam com a busca") e não podem virar uma tabela de zero linhas. |
| **D-8rz-24** | **Ao cruzar o limiar, só a página ativa é redesenhada.** Chip, busca digitada, ordenação e gaveta aberta sobrevivem porque moram em variáveis de módulo e em marcação estática, não no HTML que os renderizadores reescrevem. | Redesenhar as cinco páginas seria trabalho invisível; perder o que o usuário digitou ao arrastar a janela seria defeito. |
| **D-8rz-25** | **Gate vermelho antes de verde**: `tests/refrigeracao-desktop.test.js` é escrito e visto falhando antes de o núcleo existir. | Mesmo procedimento de 260822-48m. Um gate escrito depois do código tende a afirmar o que o código faz, não o que ele deveria fazer. |

### O que NÃO vira tabela — escrito para a próxima fase não "consertar"

| Fica como está | Por quê |
|----------------|---------|
| **Dashboard** (KPI + seções de alerta/PMOC) | KPI é cartão por natureza — decisão do usuário. |
| **Saldo das Atas** (`ctRenderSaldo`) | A leitura é a barra de saldo por item, não o número. Uma tabela de números perderia exatamente o que a tela existe para mostrar. |
| **Interior da gaveta** (`openDetail`, `manAbrirOS`, `ctOpenOS`, régua de passos, itens de orçamento, grade de fotos) | É um registro, não uma lista. O painel lateral muda de forma, não de conteúdo. |
| **Tela de login** | Fora do escopo; não é lista. |
| **Folhas de impressão** (`imprimirDocumento`, etiquetas, ficha A4) | Já são layouts A4 com tabelas próprias. |
| **`equipamentos.fabricante` como coluna** | A busca do topo já procura por fabricante, e a ficha o mostra. Oito colunas no inventário já é o teto do que se lê junto. |

---

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Núcleo puro de tabela (modo de tela, ordenação, filtro, colunas) + gate vermelho antes de verde</name>

  <files>
refrigeracao/index.html,
tests/refrigeracao-desktop.test.js,
tests/fixtures/refrigeracao-css-mobile.css
  </files>

  <behavior>
    - `modoDaTela(1023)` devolve `'cartao'`; `modoDaTela(1024)` e `modoDaTela(1920)` devolvem `'tabela'`.
    - `tabProximaOrdem(null)`→`'asc'`, `('asc')`→`'desc'`, `('desc')`→`null`, `('lixo')`→`'asc'`.
    - `tabNormalizar('Óleo')` === `tabNormalizar('oleo')`; tolera `null`, `undefined` e número.
    - `tabComparar` numérica: 2 antes de 10 crescente, 10 antes de 2 decrescente; `null`, `undefined`, `''` e `NaN` no fim nas DUAS direções; `0` ordena como número, nunca como vazio.
    - `tabComparar` de texto: `localeCompare('pt-BR')` (`Ácido` antes de `Bravo`); vazio no fim nas duas direções.
    - `tabComparar` com coluna que não existe na definição devolve 0 — nem ordena, nem quebra.
    - `tabAplicar(lista, ord, filtros, colunas)` sem `ord.coluna` preserva a ordem de entrada; filtros múltiplos combinam em conjunção; filtro em coluna inexistente é ignorado (não zera a lista); `'>'` e `'<'` são texto, não operador.
    - `COLS_INV`: criticidade ordena CRÍTICA→ALTA→MÉDIA→BAIXA (semântica, não alfabética) e filtra pelo texto `'CRÍTICA'`; `Prédio · Área` ordena pelo prédio e não pelo texto concatenado; `Próx. manutenção` de equipamento sem histórico vai para o fim nas duas direções.
    - `TAB_ESTADO` isola as tabelas: gravar ordem em `inv` deixa `os` intacto.
    - `filtrarInventario(dados, busca, chip)` com TRÊS argumentos devolve exatamente o que devolvia antes (todos os chips do gate de hoje); com o quarto argumento, filtra também por coluna, em conjunção, sem acento e sem caixa.
    - `linhasAlertas(ag, agora)` devolve uma linha por par motivo×equipamento (um equipamento com dois motivos aparece duas vezes), na mesma ordem em que as seções de hoje aparecem.
  </behavior>

  <action>
Escreva PRIMEIRO `tests/refrigeracao-desktop.test.js` e veja-o falhar (D-8rz-25: vermelho antes de
verde, como 260822-48m fez). Só então escreva o núcleo.

**1a. Impressão digital do CSS de celular (antes de tocar em qualquer CSS).**
Gere `tests/fixtures/refrigeracao-css-mobile.css` a partir do arquivo COMO ELE ESTÁ AGORA, com a
mesma função de extração que o gate vai usar: concatene o conteúdo de todos os blocos
`&lt;style&gt;…&lt;/style&gt;` de `refrigeracao/index.html` separados por `\n`, e remova por
casamento de chaves todo bloco `@media` de nível superior (hoje não existe nenhum dentro de
`&lt;style&gt;` — o único do arquivo mora dentro de uma string de JavaScript da folha de
impressão, e por isso não entra). Grave o resultado como está, sem normalizar nada.
Rode a geração por linha de comando, com o mesmo código-fonte da função copiado do teste — não
invente uma segunda extração.

**1b. O gate.** `tests/refrigeracao-desktop.test.js`, no padrão `node:test` + `node:assert` +
`node:vm` dos 14 gates de refrigeração: recorte por marcador de texto, avalie em sandbox, teste
COMPORTAMENTO. Casos: os listados em `&lt;behavior&gt;`, mais três estruturais:
  - o conteúdo extraído fora de `@media` é **estritamente igual** ao fixture (`assert.strictEqual`,
    para o diff apontar a regra que mudou) — é a prova de D-8rz-04;
  - o fixture tem mais de 8.000 caracteres (um fixture vazio faria o caso acima passar sem
    provar nada — é a lição do gate que ficou cego em 260818-vxu);
  - os quatro `grep` do PLAT-15 continuam em 0 em `refrigeracao/index.html`.

**1c. O núcleo.** Acrescente ao FIM do `&lt;body&gt;`, **depois** do último `&lt;/script&gt;` e antes
de `&lt;/body&gt;`, um bloco `&lt;script&gt;` novo (o bloco `&lt;style&gt;` companheiro entra na Task 2).
Não toque em nenhum bloco existente nesta tarefa, com as duas exceções de 1d e 1e.
Estilo do arquivo: `var`, `function`, sem arrow function, português, seções `/* ── nome ── */`.
Globais em MAIÚSCULA, funções em camelCase com o prefixo `tab`.

Seção `/* ── tabelas de desktop: núcleo puro ── */`, sem nenhuma API de navegador:
  - `var BP_DESKTOP = 1024;`
  - `function modoDaTela(largura)` → `'tabela'` ou `'cartao'`.
  - `function tabProximaOrdem(dir)` — ciclo travado, qualquer valor fora de `asc`/`desc` entra
    como "sem ordem".
  - `function tabNormalizar(texto)` — minúsculas + NFD sem diacríticos, tolerando nulo e número.
  - `function tabVazio(valor)` — nulo, indefinido, texto vazio ou `NaN`. Zero **não** é vazio.
  - `function tabComparar(a, b, coluna, dir, colunas)` — numérica por `valor()`,
    `localeCompare('pt-BR')` por `texto()` no resto, vazio no fim nas duas direções, coluna
    ausente devolve 0.
  - `function tabAplicar(lista, ord, filtros, colunas)` — filtra por subcadeia normalizada sobre
    `texto()` de cada coluna com filtro preenchido (conjunção), depois ordena.

Seção `/* ── tabelas de desktop: colunas ── */`. Cada coluna é
`{id, rotulo, tipo:'texto'|'numero', valor(x), texto(x), celula(x)}`, onde `celula` é opcional e
devolve HTML já escapado (pílula, distintivo); sem `celula`, a tabela imprime `esc(texto(x))`.
`texto()` é o que o filtro casa — por isso ele tem de conter a palavra que o usuário digitaria.

| Const | Coluna (id) | tipo | valor | texto | celula |
|---|---|---|---|---|---|
| `COLS_INV` | `id` | numero | `e.id` | `'#'+e.id` | — |
| | `local` | texto | `e.local` | `e.local` | — |
| | `predio` | texto | `e.predio` | `(e.predio\|\|'')+' · '+(e.area\|\|'')` | — |
| | `tipo` | texto | `e.tipo` | `e.tipo` | — |
| | `btu` | numero | `e.btu` (ausente → `null`) | `'12K BTU'` / `'900 BTU'` / `''` | — |
| | `estado` | texto | `rotuloEstadoEquip(e.funciona)` | idem + rótulo de situação quando houver | `statusPill(e.funciona)+situacaoPill(e.situacao)` |
| | `criticidade` | numero | `CRIT_ORDEM[autoCrit(e)]`, ausente → `99` | `autoCrit(e)` | `critPill(autoCrit(e))` |
| | `prox` | numero | `proxManut(e)` → instante, senão `null` | `fmtDate(...)` ou `'Sem hist.'` | `dueBadgeHtml(proxManut(e))` |
| `COLS_OS` | `data` | numero | `Date.parse(o.entry.date)` | `fmtDate(o.entry.date)` | — |
| | `local` / `predio` | texto | `o.equip.local` / `o.equip.predio` | idem | — |
| | `tipo` | texto | `o.entry.tipo` | idem | — |
| | `estado` | texto | `manRotulo(o.entry.status)` | idem | `statusPillOS(o.entry.status)` |
| | `tecnico` | texto | `o.entry.tecnico` | idem | — |
| `COLS_MOVIM` | `data`, `local`, `predio`, `tipo`, `estado` | — | como em `COLS_OS` | | |
| | `destino` | texto | rótulo do caso (instalação) ou destino da remoção | idem | a mesma linha que `renderMovim` já monta |
| `COLS_PMOC` | `predio`, `local`, `tipo` | texto | campos do equipamento | idem | — |
| | `criticidade` | numero | como em `COLS_INV` | | `critPill` |
| | `inspecao` / `preventiva` | numero | `nextPmoc(e,'inspecao'\|'preventiva')` → instante ou `null` | `fmtDate` ou `'Sem hist.'` | `dueBadgeHtml` |
| | `registros` | numero | `getEquipLog(e.id).length` | idem | — |
| `COLS_ALERT` | `motivo` | texto | `'Inoperante'` / `'Com restrições'` / `'PMOC vencida'` / `'Sem histórico'` | idem | — |
| | `local`, `predio` | texto | do equipamento | idem | — |
| | `criticidade` | numero | como em `COLS_INV` | | `critPill` |
| | `detalhe` | texto | observação, `'Vencida há Nd'` ou `'Nenhum registro de manutenção'` | idem | — |
| `COLS_CONTRAT` | `numero` | texto | `o.numero` | idem | — |
| | `local`, `predio` | texto | de `ctEquip(o.equip_id)` | idem | — |
| | `empresa` | texto | `o.empresa` | idem | — |
| | `valor` | numero | `o.valor_orcado \|\| ctOrcTotal(o.id)`, zero/ausente → `null` | `fmtMoney(...)` | — |
| | `ne` | texto | `o.ne` | idem | — |
| | `estado` | texto | rótulo de `CT_STATUS[o.status]` | idem | `ctStatusPill(o.status)` |

Seção `/* ── tabelas de desktop: estado por tabela ── */`:
`var TAB_ESTADO = { inv:{ord:{coluna:null,dir:null},filtros:{},aberto:false}, os:{…}, movim:{…},
pmoc:{…}, alert:{…}, contrat:{…} };` — seis entradas idênticas, uma por tabela (D-8rz-16).

Seção `/* ── tabelas de desktop: linhas de alerta ── */`: `function linhasAlertas(ag, agora)`,
que achata o agregado de `alertasPmoc` numa lista de `{motivo, equip, detalhe}`, uma linha por
par motivo×equipamento, na ordem das seções de hoje (inoperante, restrição, vencida, sem
histórico) e, dentro de cada uma, na ordem de criticidade que `renderAlerts` já aplica.

**1d. `filtrarInventario` ganha o quarto parâmetro (D-8rz-09).** No bloco existente
`/* ── inventário: a seleção filtrada, com dois consumidores ── */`, mude a assinatura para
`function filtrarInventario(dados, busca, chip, filtrosColuna)` e, ao fim do `filter`, aplique os
filtros de coluna quando `filtrosColuna` vier preenchido, via `tabAplicar`… **não**: aplique-os por
`tabNormalizar(col.texto(e)).indexOf(tabNormalizar(v)) >= 0` para cada par preenchido, sobre
`COLS_INV`. Com o argumento ausente, o comportamento é idêntico ao de hoje — é isso que mantém
verde o caso de `tests/refrigeracao-qr-nfc.test.js` que chama a função com três argumentos.
Atualize os três chamadores para passarem `TAB_ESTADO.inv.filtros`: `renderInv`,
`etiquetasDoInventario` e `exportarInventarioCsv`. **Não** troque o `.sort(cmpInv)` de
`exportarInventarioCsv` (D-8rz-10) nem a marcação da `.sec-head` do inventário — o gate da
planilha recorta essa marcação com a indentação exata.

**1e. `setInvOrdem` cede para o cabeçalho e vice-versa (D-8rz-07).** Dentro do trecho que o gate
de ordenação recorta, `setInvOrdem` passa a zerar `TAB_ESTADO.inv.ord` antes de chamar
`renderInv()`. É uma atribuição dentro do corpo da função — o sandbox daquele gate nunca a
executa, então ele continua verde. **Não toque em `CRIT_ORDEM`, `proxManut`, `isVencido` nem
`cmpInv`** (D-8rz-08).

Confira ao fim: `refrigeracao/index.html` continua sem citar a pasta compartilhada, a folha comum,
a chave de tema ou o atributo de tema — quatro contagens em zero. **Nunca escreva o caminho da
pasta compartilhada num comentário deste arquivo**: o gate conta ocorrências no arquivo inteiro,
comentário incluído.
  </action>

  <verify>
    <automated>node --test tests/refrigeracao-desktop.test.js && node --test 2>&1 | tail -8</automated>
  </verify>

  <done>
`node --test` verde com 772 + os casos novos (nenhum dos 772 quebrado). O gate novo foi visto
FALHANDO antes do núcleo existir. `tests/fixtures/refrigeracao-css-mobile.css` existe, tem mais de
8.000 caracteres e o gate o compara com o extraído do HTML. `grep -c 'shared/' refrigeracao/index.html`,
`pmoc.css`, `pmoc-tema` e `data-theme` continuam em 0.
  </done>
</task>

<task type="auto">
  <name>Task 2: Layout de computador — navegação lateral, largura útil, gaveta como painel lateral, troca de modo</name>

  <files>refrigeracao/index.html, tests/refrigeracao-desktop.test.js</files>

  <action>
Acrescente **um** bloco `&lt;style&gt;` novo imediatamente antes do bloco `&lt;script&gt;` criado na
Task 1, contendo **exatamente um** `@media (min-width:1024px){ … }` e nada fora dele. Cabeçalho
`/* ── layout de computador (>=1024px) ── */`.

**Navegação lateral sem mexer na marcação.** `#app` vira grade de duas colunas e duas linhas com
áreas nomeadas: a barra do topo ocupa a linha inteira, `#bottom-nav` ocupa a coluna da esquerda
(220px) e `#content` o resto. `#bottom-nav` passa a empilhar (`flex-direction:column`,
`align-items:stretch`), perde a borda de cima e a sombra, ganha borda à direita. `.nav-btn` vira
linha (`flex-direction:row`, `justify-content:flex-start`, ícone de largura fixa à esquerda,
rótulo em 14px). O distintivo (`.nav-badge`) sai do posicionamento absoluto do celular e vai para
o fim da linha (`position:static; margin-left:auto; transform:none`).

**Estado ativo com quatro sinais**, como o `/mapa` fixou em 260819-4gq — barra de 4px à esquerda
desenhada em `::before` (para o texto não pular ao acender), fundo tingido, peso 700 e cor. Uma
borda de 1px trocando de cor não se lê de relance e some para quem confunde azul com cinza.

**Largura útil.** `.page` ganha `max-width:1400px; margin:0 auto; padding:20px 24px 32px`.
`#fab` e `#toast` sobem do rodapé (`bottom:24px`), porque a barra de 70px não está mais lá.
`.search-wrap` deixa de grudar (D-8rz-20).

**A tabela** (só classes sob `.lista-tabela`, nunca `table{}`/`th{}`/`tr{}` nus — D-8rz-19):
largura total, `border-collapse:collapse`, linha zebrada em `tbody tr:nth-child(even)`, realce em
`:hover`, `thead th` grudado no topo com fundo opaco, célula com 10px 12px e 13px, alinhamento à
direita nas colunas numéricas por classe `.num`, cabeçalho como botão (`.th-acao` para o `⇅`/`↑`/`↓`
e o `⌕`), linha de filtro com `input[type=search]` ocupando a célula, `:focus-visible` preservado.
Barra da tabela (`.tab-barra`) acima do `&lt;table&gt;` para a contagem "N de 171" e os botões
"Limpar ordenação"/"Limpar filtros".

**A gaveta vira painel lateral (D-8rz-17).** `#drawer-panel` passa a
`top:0; right:0; bottom:0; left:auto; width:min(560px,46vw); max-height:none; border-radius:0;
transform:translateX(100%)`, e `#drawer.open #drawer-panel` a `translateX(0)`. `#drawer-handle`
some. `#drawer-footer` perde o preenchimento de área segura do iPhone. **Não mexa no teto de 200px
do QR** (D-8rz-18).

No bloco `&lt;script&gt;` da Task 1, seção `/* ── tabelas de desktop: modo de tela ── */`:
  - `var TELA_LARGA = false;`
  - `function aplicarModoDeTela(largura)` — grava `TELA_LARGA = (modoDaTela(largura)==='tabela')`
    e devolve o modo. A decisão mora em `modoDaTela`, que é pura e testada; esta função só guarda.
  - `function renderPaginaAtiva()` — descobre a página ativa por `document.querySelector('.page.active')`
    e chama o renderizador dela (`renderDash`/`renderInv`/`renderOS`/`renderPmoc`/`renderAlerts`).
    Para a aba de OS chame `renderOS()` **e nada mais**: ela já foi reescrita mais acima no arquivo
    para despachar entre manutenção, contratações e movimentação conforme `ctMode`. Duplicar esse
    despacho aqui criaria a segunda cópia da regra.
  - `function iniciarModoDeTela()` — `matchMedia('(min-width:'+BP_DESKTOP+'px)')`, aplica o modo na
    carga e escuta a mudança (`addEventListener('change', …)` com queda para `addListener` quando o
    navegador não tiver o primeiro, que é a forma ES5 segura), redesenhando **só a página ativa**
    (D-8rz-24). Chame-a num `DOMContentLoaded` próprio deste bloco.

**Guarda do gesto (D-8rz-17).** No bloco `SWIPE DRAWER`, a primeira linha do ouvinte de
`touchstart` passa a desistir quando `TELA_LARGA`. **Não altere `gestoFechaGaveta`** — a função
pura e o gate dela ficam como estão; o sandbox daquele gate nunca executa o corpo do ouvinte, então
ele segue verde.

**Escape fecha a gaveta.** Um `keydown` no documento, no bloco novo: `Escape` com a gaveta aberta
chama `closeDrawer()`. Vale nas duas larguras — num celular não há teclado, e num painel lateral é
o caminho esperado.

Acrescente ao gate: (a) existe exatamente um `@media` dentro dos blocos `&lt;style&gt;` e ele é
`(min-width:1024px)`; (b) todo seletor novo introduzido por esta tarefa aparece **dentro** desse
bloco — prove comparando o extraído "fora de @media" com o fixture da Task 1, que agora tem de
continuar idêntico byte a byte; (c) nenhum seletor de elemento nu (`table{`, `th{`, `td{`, `tr{`)
foi acrescentado em lugar nenhum das folhas; (d) `#bottom-nav` continua com exatamente cinco
`.nav-btn` na marcação — a navegação lateral é a mesma barra, deitada por CSS, não uma segunda
navegação; (e) `aplicarModoDeTela(1023)`/`(1024)` gravam `TELA_LARGA` como falso/verdadeiro
(comportamento em `node:vm`, não regex).
  </action>

  <verify>
    <automated>node --test tests/refrigeracao-desktop.test.js && node --test 2>&1 | tail -8</automated>
  </verify>

  <done>
`node --test` verde (772 + casos novos). O extraído "fora de @media" continua estritamente igual ao
fixture — prova de D-8rz-04. Existe exatamente um `@media` nas folhas embutidas e ele é
`min-width:1024px`. `grep -cE '(^|\})\s*(table|th|td|tr)\s*\{' refrigeracao/index.html` não aumentou.
A barra inferior continua com 5 `.nav-btn` na marcação.
  </done>
</task>

<task type="auto">
  <name>Task 3: As seis listas viram tabelas — um desenhista, seis adaptadores</name>

  <files>refrigeracao/index.html, tests/refrigeracao-desktop.test.js</files>

  <action>
No bloco `&lt;script&gt;` novo, seção `/* ── tabelas de desktop: cabeçalho e linhas ── */`.

**Um desenhista só (D-8rz-12).**
  - `function tabDesenhar(tid, colunas, itens, aoAbrir)` — devolve o HTML completo: barra
    (`tabBarra`) + `&lt;table class="lista-tabela"&gt;` + `tabCabecalho` + `tabCorpo`. `tid` é a
    chave em `TAB_ESTADO`; `aoAbrir` é o nome da função que abre a ficha, mais o argumento por item.
  - `function tabCabecalho(tid, colunas)` — uma linha de rótulos (cada `&lt;th&gt;` com `aria-sort`
    em `none`/`ascending`/`descending`, o rótulo, um `&lt;button&gt;` de ordenação com `⇅`/`↑`/`↓` e
    um `&lt;button&gt;` de filtro `⌕` com estado ativo) e, quando `TAB_ESTADO[tid].aberto`, uma
    segunda linha com um `input[type=search]` por coluna, cada um com `aria-label` próprio e
    `value` **escapado**.
  - `function tabCorpo(tid, colunas, itens, aoAbrir)` — aplica `tabAplicar` sobre os itens e
    devolve os `&lt;tr&gt;`. Cada célula usa `celula(x)` quando existir, senão `esc(texto(x))`. A
    **primeira** célula de dado embrulha o conteúdo num `&lt;button class="cel-abrir"&gt;` que chama
    `aoAbrir` e faz `event.stopPropagation()`; o `&lt;tr&gt;` carrega o mesmo `onclick` para o mouse
    (D-8rz-22). Colunas `tipo:'numero'` levam `class="num"`.
  - `function tabBarra(tid, visiveis, total)` — mostra `N de M` só quando há ordem ou filtro ativo,
    com "Limpar ordenação" quando `ord.coluna` não é nulo e "Limpar filtros" quando há filtro
    preenchido.
  - `function tabOrdenarColuna(tid, coluna)` — outra coluna começa em `asc`; a mesma avança no
    ciclo. Redesenha cabeçalho **e** corpo.
  - `function tabAlternarFiltros(tid, coluna)` — abre/fecha a linha de filtro; fechar limpa os
    filtros (um filtro invisível escondendo linhas é pior que nenhum filtro); ao abrir, foca o campo
    da coluna clicada.
  - `function tabAplicarFiltro(tid, coluna, valor)` — grava e redesenha **só o corpo** (D-8rz-15).
  - `function tabLimparOrdem(tid)` / `function tabLimparFiltros(tid)`.
  - `function tabRedesenhar(tid)` — chama o renderizador dono daquela tabela, para que a contagem
    de `#inv-cnt`/`#os-cnt`/`#pmoc-cnt` e o rótulo continuem saindo de um lugar só.

Publique no escopo global tudo que aparece em `onclick`/`oninput` — o arquivo usa manipuladores
embutidos como idioma, e as funções deste bloco precisam ser alcançáveis por nome.

**Seis guardas, uma linha cada (D-8rz-23).** Em cada renderizador, a guarda entra **depois** do
ramo de lista vazia e **antes** do `.map()` que monta os cartões, para as mensagens de vazio de
hoje continuarem valendo nas duas larguras:
  - `renderInv` → `if(TELA_LARGA){ el('inv-list').innerHTML = tabDesenhar('inv', COLS_INV, list, 'openDetail'); return; }`
  - `renderOS` (o corpo original, aquele que a reescrita mais abaixo guarda em `_origRenderOS`) → mesma forma com `'os'`, `COLS_OS`, `manAbrirOS`/`openDetail` conforme a regra de abertura que a função já calcula.
  - `renderMovim` → `'movim'`, `COLS_MOVIM`, `manAbrirOS`.
  - `renderPmoc` → `'pmoc'`, `COLS_PMOC`, `openDetail`.
  - `ctRenderList` → `'contrat'`, `COLS_CONTRAT`, `ctOpenOS` (depois do desvio de `ctChipSel==='saldo'`, que continua em cartões — D-8rz-“o que não vira tabela”).
  - `renderAlerts` → a guarda entra depois de os quatro conjuntos estarem calculados; monta a linha
    de "Total sem repetição" seguida de `tabDesenhar('alert', COLS_ALERT, linhasAlertas(ag, now), 'openDetail')`,
    **e atualiza o distintivo pela mesma via do ramo de celular**: extraia as duas linhas do
    distintivo para `function atualizarBadgeAlertas(ag)` e chame-a nos dois ramos. Duplicar duas
    linhas de contagem é exatamente a classe de defeito que este módulo já corrigiu duas vezes.

Não mude mais nada dentro desses renderizadores: os recortes de
`tests/refrigeracao-movimentacao-os.test.js`, `tests/refrigeracao-contagens.test.js` e
`tests/refrigeracao-qr-nfc.test.js` afirmam, por expressão regular sobre o corpo, coisas como
`osEhMovimentacao(o.entry)`, `alertasPmoc(equipamentosOperacionais(DATA)`, `equipamentosOperacionais(DATA)`
e `filtrarInventario(`. Mantenha a guarda numa única linha, sem introduzir uma chave `}` na coluna
zero dentro de `renderInv` (o recorte daquele caso vai da assinatura até o primeiro `\n}`).

**Segurança da célula.** Todo texto que vem do banco atravessa `esc()` — inclusive o `value` do
campo de filtro, que é digitado pelo usuário e reimpresso no cabeçalho. Nenhuma célula concatena
campo de banco dentro de atributo sem escapar. As funções `celula` reusam as pílulas que já existem
(`statusPill`, `critPill`, `situacaoPill`, `statusPillOS`, `ctStatusPill`, `dueBadgeHtml`), que já
escapam por dentro.

Acrescente ao gate os casos de comportamento: `tabCorpo` de uma lista de três itens devolve três
`&lt;tr&gt;`; ordenar por uma coluna numérica muda a ordem dos identificadores na saída; um filtro
por coluna reduz a contagem de `&lt;tr&gt;`; um campo com `&lt;script&gt;` no valor sai escapado no
HTML gerado (nem `&lt;script` nem aspas cruas no `value` do filtro); `aria-sort` acompanha o ciclo
de ordenação; a primeira célula traz um `&lt;button` com `stopPropagation`. Estruturais: as seis
guardas existem, e nenhuma delas fica ANTES do ramo de lista vazia do seu renderizador.
  </action>

  <verify>
    <automated>node --test tests/refrigeracao-desktop.test.js && node --test 2>&1 | tail -8</automated>
  </verify>

  <done>
`node --test` verde (772 + casos novos, nenhum dos 14 gates de refrigeração quebrado). As seis
listas emitem `&lt;table class="lista-tabela"&gt;` quando `TELA_LARGA`, e cartões quando não.
O extraído "fora de @media" continua idêntico ao fixture. Os quatro `grep` do PLAT-15 seguem em 0.
Conferência manual anotada em `TESTES.md`: em 1440px, navegação à esquerda, tabela ordenável nas
seis listas, clique e Tab abrindo a mesma ficha, gaveta entrando pela direita, Escape fechando; e
em 375px a tela idêntica à de hoje.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Postgres → célula da tabela | Todo campo de `equipamentos`, `logs_manutencao` e `os_contratacao` passa a ser impresso em contexto HTML novo (célula, atributo `title`, `value` de campo) |
| Usuário → cabeçalho da tabela | O texto digitado no filtro de coluna é reimpresso no `value` do próprio campo a cada redesenho |
| Largura da janela → escolha de renderizador | Um valor externo (a media query) passa a decidir qual código gera o DOM |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-8rz-01 | Tampering | `tabCorpo` / funções `celula` | high | mitigate | Todo texto de banco sai por `esc()`; as pílulas reusadas já escapam por dentro; caso de gate com `<script>` no valor do campo |
| T-8rz-02 | Tampering | `tabCabecalho` (`value` do filtro) | high | mitigate | O `value` do `input` é escapado; caso de gate provando que aspas e `<` não escapam do atributo |
| T-8rz-03 | Information Disclosure | Tabela do inventário | low | accept | A tabela mostra os mesmos campos que o cartão e a ficha já mostram ao mesmo cargo; nenhum campo novo é exposto, e o modo observador continua governado por `somenteLeitura()` |
| T-8rz-04 | Denial of Service | 171 linhas sem virtualização | low | accept | Volume conhecido e fixo (171 equipamentos, dezenas de OS); zero-build proíbe biblioteca de virtualização e o custo real é irrelevante (D-8rz-21) |
| T-8rz-05 | Elevation of Privilege | Nenhum caminho de escrita novo | low | accept | A tarefa não cria nenhum `insert`/`update`; o único clique da tabela é abrir a ficha, que já tem suas próprias travas |
| T-8rz-SC | Tampering | instalação de pacote | high | mitigate | Não se aplica: zero dependência nova, zero-build, sem `npm`/`package.json` — se surgir necessidade de pacote, o trabalho para e volta para revisão |
</threat_model>

<verification>
1. `node --test` → 772 casos de hoje verdes + os casos novos, zero falha.
2. `grep -c 'shared/' refrigeracao/index.html` = 0 (idem `pmoc.css`, `pmoc-tema`, `data-theme`).
3. O conteúdo de CSS fora de `@media` extraído do HTML é estritamente igual a
   `tests/fixtures/refrigeracao-css-mobile.css` — D-8rz-04 provado, não prometido.
4. Existe exatamente um `@media` dentro dos blocos `<style>`, e ele é `(min-width:1024px)`.
5. Conferência manual (anotada em `TESTES.md`, sem navegador headless no repositório):
   em 1440px — navegação lateral com estado ativo legível, as seis listas em tabela, ordenar e
   filtrar por coluna sem perder o foco do campo, clique e `Tab`+`Enter` abrindo a mesma ficha,
   gaveta entrando pela direita, `Escape` fechando; arrastar a janela de 1440 para 900 e de volta
   sem perder chip, busca digitada nem gaveta aberta; em 375px — tela idêntica à de hoje.
</verification>

<success_criteria>
- [ ] `node --test` verde, com os 772 casos de hoje intactos
- [ ] Gate novo `tests/refrigeracao-desktop.test.js` visto FALHANDO antes de o núcleo existir
- [ ] CSS fora da media query byte-idêntico ao fixture (D-8rz-04)
- [ ] Um único `@media`, `min-width:1024px`, e nenhum seletor de elemento nu acrescentado
- [ ] Seis tabelas com ordenação e filtro por coluna, `aria-sort` e caminho por teclado
- [ ] `cmpInv`, `CRIT_ORDEM`, `proxManut`, `isVencido` e `gestoFechaGaveta` não alterados
- [ ] `filtrarInventario` é a porta única do recorte, com três argumentos se comportando como antes
- [ ] `refrigeracao/index.html` segue standalone: quatro `grep` do PLAT-15 em 0
- [ ] Nenhuma migração, nenhuma dependência nova, nenhum arquivo fora de `pmoc-overlay` tocado
</success_criteria>

<output>
Criar `.planning/quick/260822-8rz-refrigeracao-layout-de-desktop-com-tabel/SUMMARY.md` ao terminar,
citando as decisões D-8rz-01..D-8rz-25 por identificador.
</output>

<!-- planner-discipline-allow: shared/ -->
<!-- planner-discipline-allow: pmoc.css -->
<!-- planner-discipline-allow: pmoc-tema -->
<!-- planner-discipline-allow: data-theme -->
</content>
</invoke>
