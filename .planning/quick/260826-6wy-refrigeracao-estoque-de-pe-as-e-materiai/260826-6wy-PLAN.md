---
phase: quick-260826-6wy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/44_refrigeracao_estoque.sql
  - refrigeracao/index.html
  - tests/refrigeracao-estoque.test.js
  - CLAUDE.md
  - README.md
  - TESTES.md
autonomous: true
requirements:
  - D-6wy-01
  - D-6wy-02
  - D-6wy-03
  - D-6wy-04
  - D-6wy-05
  - D-6wy-06

must_haves:
  truths:
    - "Existe um catálogo de peças e materiais próprio de /refrigeracao (`materiais`), com código, tipo, aplicação, unidade, preço, estoque atual e mínimo (D-6wy-01)."
    - "Ao lançar um item de MATERIAL numa OS, quem delineia escolhe do catálogo e a descrição, a unidade e o valor unitário vêm preenchidos e congelados na linha — e texto livre continua aceito (D-6wy-02)."
    - "Quando a OS entra em EM_EXECUCAO, os materiais lançados nela saem do estoque UMA vez: repetir a transição não debita de novo (D-6wy-03)."
    - "Existe uma página Estoque, alcançável pela barra de navegação, onde se vê a lista, se corrige atual/mínimo/preço, se cadastra material e se registra entrada de recebimento (D-6wy-04)."
    - "A página de Alertas mostra o que está abaixo do mínimo, e cada linha leva à página Estoque (D-6wy-05)."
    - "Com a migração 44 ainda não aplicada, a tela publicada se comporta byte a byte como hoje: sem botão de Estoque na navegação, sem catálogo no formulário de item, sem baixa, sem alerta (D-6wy-06)."
  artifacts:
    - "supabase/44_refrigeracao_estoque.sql — aditiva, sem drop, escrita e conferida, aplicada pelo usuário DEPOIS do deploy do frontend"
    - "refrigeracao/index.html — sonda EST_OK, catálogo MATERIAIS, núcleo puro de estoque, baixa idempotente, formulário de item com catálogo, página Estoque, bloco de alerta"
    - "tests/refrigeracao-estoque.test.js — gate novo: migração conferida por regex, núcleo puro por comportamento em node:vm, idempotência com supa dublado"
  key_links:
    - "manAtualizarOS → estBaixarItensDaOS: o ÚNICO ponto onde o estoque desce, e só na transição para EM_EXECUCAO"
    - "estJaBaixadoDaOS lê estoque_movimentos: o registro do próprio estoque responde pela idempotência, nunca uma bandeira paralela"
    - "EST_OK declarado dentro do recorte `── fluxo da OS interna: porta de escrita ──` — os cinco gates que já carregam esse recorte recebem EST_OK falso de graça, e o fixture da gaveta continua byte a byte"
    - "tests/refrigeracao-desktop.test.js compara o CSS fora de @media byte a byte (D-8rz-04/D-cf8-20) — nenhuma regra de CSS pode nascer nesta task"
    - "tests/refrigeracao-topbar-parque.test.js e tests/refrigeracao-desktop.test.js exigem exatamente cinco .nav-btn na marcação — por isso o botão de Estoque é injetado em runtime"
---

<objective>
`/refrigeracao` é o único módulo da plataforma que faz manutenção sem saber o que tem na prateleira. Máquinas tem `maq_materiais` + movimentos + baixa na execução; Transportes tem a mesma forma desde a migração 23; a refrigeração lança item de OS como **texto livre com valor digitado** — não há catálogo, não há saldo, não há mínimo, e nenhuma peça consumida numa OS sai de lugar nenhum.

Esta task traz a mesma forma para cá: catálogo `materiais`, histórico `estoque_movimentos`, `os_itens.material_id`, baixa idempotente quando a OS entra em execução, página Estoque com mínimo e entrada, e alerta do que está abaixo do mínimo.

**Fora de escopo, por decisão do usuário (task B, próxima):** pedidos de material, listas de compra, recebimento de lista. Não criar — nem tabela, nem tela, nem função.

Purpose: uma OS de refrigeração hoje registra que trocou um capacitor sem que capacitor nenhum saia da prateleira; o almoxarifado do módulo é um campo de texto.
Output: migração 44 escrita (aplicada pelo usuário depois do deploy), `refrigeracao/index.html` alterado em seis pontos, um gate novo em `node:test`, e o fato registrado em CLAUDE.md/README.md/TESTES.md.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md

**Baseline conferido nesta máquina antes de planejar:** `node --test tests/*.test.js` → **958 testes, 0 falhas**. Use sempre o glob `tests/*.test.js`; a forma `node --test tests/` falha com MODULE_NOT_FOUND neste Node.

**Obrigatório antes de escrever o SQL:** carregue a skill `supabase-postgres-best-practices` (índice em toda coluna de chave estrangeira, `add column if not exists` para migração reexecutável, grant de sequence em `generated always as identity`, policy sem função de sessão não precisa de `(select …)`).

**Regras de contorno que esta task NÃO pode violar:**
- D-04: `/refrigeracao` é congelada e standalone — **nenhum `import` de `shared/`**. O que for compartilhado com outros módulos em ideia é replicado dentro do arquivo.
- D-8rz-04 / D-cf8-20: `tests/refrigeracao-desktop.test.js` compara todo o **CSS fora de `@media`** byte a byte com `tests/fixtures/refrigeracao-css-mobile.css`. **Nenhuma regra de CSS nova, em lugar nenhum** — nem dentro de um `@media` de fachada. A interface nova reusa classe que já existe (`.ds`, `.ds-title`, `.orc-item`, `.orc-tipo`, `.orc-desc`, `.orc-val`, `.orc-del`, `.sec-head`, `.sec-title`, `.sec-count`, `.chips`, `.chip`, `.kpi-row`, `.kpi-card`, `.kpi-label`, `.kpi-val`, `.kpi-sub`, `.alert-card`, `.alert-icon`, `.alert-body`, `.alert-title`, `.alert-sub`, `.pill`, `.pill-ok`, `.pill-nok`, `.pill-or`, `.fgl`, `.fgi`, `.fgs`, `.fgt`, `.fg2`, `.fg-full`, `.btn-primary`, `.btn-secondary`, `.btn-sm`, `.btn-success`, `.empty`, `.saldo-row`, `.saldo-hdr`, `.saldo-desc`) ou estilo inline no template, como o módulo já faz em dezenas de pontos.
- O nome das tabelas de estoque dos **outros** módulos (as com prefixo `maq_` e `transp_`) **não pode aparecer em lugar nenhum de `refrigeracao/index.html`** — nem em comentário explicativo. O estoque é por módulo (D-06 de Transportes) e o gate novo proíbe a menção. Ao comentar a origem da ideia, escreva "o mesmo desenho que os outros módulos já usam", sem nomear tabela alheia.
- O projeto **arquiva, nunca apaga**: nada de `drop` na migração; material fora de uso vira `ativo = false`.

**Pontos exatos em `refrigeracao/index.html`** (linhas do HEAD atual — confira pelo texto do marcador, nunca pelo número):
- `~653` `/* ── fluxo da OS interna: porta de escrita ── */` … `~794` `/* ── REALTIME ── */`: a seção onde moram `MAN_FLUXO_OK`, `manAtualizarOS`, `MOV_OK`, `UNI_OK`, `dbToItem`, `OS_ITENS`, `carregarItensOS`, `osRegistrarEvento`. **É aqui que todo o estoque de servidor entra** (ver D-6wy-08 abaixo).
- `~680` dentro de `manAtualizarOS`: o bloco `if (UNI_OK && patch && patch.status !== undefined)` que grava a trilha. O gancho da baixa entra logo depois dele.
- `~1041-1060` `<div id="bottom-nav">`: cinco `<button class="nav-btn">`. **Não acrescente um sexto na marcação.**
- `~990` `<div class="page" id="page-alert">`: a página de alertas. `page-estoque` nasce irmã dela.
- `~1358` `MAN_ACOES_CARGO`.
- `~1668` `function navTo(page, btn)`.
- `~2253` `function renderAlerts()`, com **dois ramos** (`if(TELA_LARGA){ … return; }` e o de cartões) — o bloco de alerta de estoque tem de entrar nos dois.
- `~2005` `osInjectChipsExecutor()`: a técnica de injeção dinâmica a copiar para a navegação.
- `~4348` `/* ── OS unificada: itens de serviço e material ── */`: `osItensHtml`, `osAddItem`, `osAddItemUI`, `osDelItem`.
- `~4740` `acessoLivre()` e `~4777` `initAppOnce()`: as duas sequências de arranque, com as sondas e as cargas fora do `Promise.all`.
- `~7300` `renderPaginaAtiva()`: despacho por `.page.active` quando a largura cruza o limiar.

**Decisões travadas pelo usuário (não revisitar):** D-6wy-01 migração 44; D-6wy-02 item de OS escolhido do catálogo; D-6wy-03 baixa idempotente ao entrar em EM_EXECUCAO; D-6wy-04 página Estoque; D-6wy-05 alerta abaixo do mínimo; D-6wy-06 sonda `EST_OK`.

**Decisões desta task (do planejador, registradas para o SUMMARY):**
- **D-6wy-07** — o botão de Estoque é **injetado em runtime** (`estInjectNav()`, a mesma técnica de `osInjectChipsExecutor`), nunca escrito na marcação. Dois motivos, ambos duros: `tests/refrigeracao-topbar-parque.test.js` e `tests/refrigeracao-desktop.test.js` afirmam **exatamente cinco** `.nav-btn` em `#bottom-nav` (e a lista de rótulos), e "sem a migração 44 o botão não existe" fica estruturalmente verdadeiro em vez de escondido por CSS — o que D-6wy-06 pede. A `<div class="page" id="page-estoque">` **é** marcação estática, inerte enquanto ninguém a ativa, como `page-ficha` e `page-os-detalhe` já são.
- **D-6wy-08** — `EST_OK`, `MATERIAIS` e todas as funções de escrita/leitura do estoque nascem **dentro do recorte `── fluxo da OS interna: porta de escrita ──`**, o mesmo que `tests/refrigeracao-os-pagina.test.js`, `refrigeracao-trilha-os.test.js`, `refrigeracao-fluxo-os-interna.test.js`, `refrigeracao-os-unificada.test.js` e `refrigeracao-encerramento-os.test.js` já carregam nos seus sandboxes. Consequência calculada: nesses cinco gates `EST_OK` chega **declarado e falso**, o gancho da baixa e o catálogo do formulário ficam inertes, e `tests/fixtures/refrigeracao-os-gaveta.json` continua **byte a byte** o de hoje — nenhum dos cinco precisa de uma linha mudada para continuar passando. Declarar em outro lugar produziria `ReferenceError` em sandbox, que é falha de infraestrutura fingindo de regressão.
- **D-6wy-09** — a página Estoque desenha **lista de cartões nas duas larguras**, não `tabDesenhar`. Precedente no próprio arquivo: `ctRenderSaldo()` (o relatório de Saldo Atas) retorna antes do ramo `TELA_LARGA` e é cartão em qualquer tela. Dois motivos: `.lista-tabela` só existe **dentro** do `@media (min-width:1024px)` (uma tabela no celular sairia sem estilo, e criar estilo é proibido), e o teclado da edição inline não sobrevive a uma tabela que se redesenha a cada tecla — foi exatamente o defeito que Máquinas pagou e documentou ao partir `renderMateriais()` em cabeçalho e corpo.
- **D-6wy-10** — `estItensParaBaixa` **consolida por material**: duas linhas de MATERIAL do mesmo material na mesma OS produzem **um** decremento e **uma** linha de saída. Sem isso o histórico teria duas linhas para o mesmo consumo e a aritmética do saldo dependeria da ordem em que as linhas foram lançadas.
- **D-6wy-11** — existe uma **ponte de campos** `CAMPOS_MATERIAL` (camelCase do app ↔ snake_case do Postgres), lida por `dbToMaterial`/`materialParaDb`, e o gate a compara **coluna a coluna** com a migração 44. É a lição já paga em `/calibracao`: um campo fora do mapa é gravado como null sem erro nenhum.
- **D-6wy-12** — `material_id` entra no payload de `osAddItem` **só quando um material foi escolhido**. Quando não foi, o payload é byte a byte o de hoje, e `tests/refrigeracao-os-unificada.test.js` continua passando sem uma linha mudada.
- **D-6wy-13** — **limite conhecido, registrado e não resolvido:** voltar de EM_EXECUCAO para trás **não estorna** o estoque. A saída já aconteceu no mundo (a peça saiu da prateleira quando a execução começou); um estorno automático inventaria uma entrada que ninguém recebeu. Correção manual: registrar uma **entrada** na página Estoque, que é o caminho que já existe. Fica em TESTES.md e no README.
- **D-6wy-14** — o índice de `estoque_movimentos` é composto `(os_id, tipo)`, porque a sonda de idempotência filtra pelos dois; ele também serve de índice da chave estrangeira `os_id` (coluna líder).
- **D-6wy-15** — `estoque_movimentos.os_id` é `on delete set null`, não referência nua. `delLogEntryAsync` existe e apaga linha de `logs_manutencao`; com referência nua o Postgres recusaria a exclusão e a função devolveria `false` sem dizer por quê. O movimento de estoque **não** pode ser apagado junto (a peça saiu de verdade), então o vínculo se solta e o histórico fica.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: migração 44, sonda EST_OK, catálogo em memória e núcleo puro — nenhuma interface ainda</name>
  <files>tests/refrigeracao-estoque.test.js, supabase/44_refrigeracao_estoque.sql, refrigeracao/index.html</files>
  <behavior>
    Escreva o gate PRIMEIRO e veja-o falhar (RED) antes de existir migração ou código.

    Sobre a migração (regex sobre o SQL, como os outros gates fazem com migrações):
    - declara `create table if not exists materiais` e `create table if not exists estoque_movimentos`
    - `materiais` traz as colunas id, codigo, nome, tipo, unidade, aplicacao, preco, estoque_atual, estoque_minimo, obs, ativo — nem mais, nem menos
    - `materiais.codigo` é unique; `tipo` tem lista fechada consumivel|peca; `estoque_atual` e `estoque_minimo` têm check de não-negativo; `preco` aceita nulo ou não-negativo; `ativo` nasce true
    - `estoque_movimentos` traz id uuid, material_id, os_id, tipo (lista fechada entrada|saida), quantidade com check maior que zero, motivo, registrado_em
    - `os_itens` ganha material_id por `add column if not exists`, anulável
    - existe índice para cada chave estrangeira nova, incluindo o composto (os_id, tipo)
    - o arquivo inteiro **não contém a palavra drop fora de `drop policy if exists`** — a migração é aditiva
    - RLS ligada nas duas tabelas, com as quatro policies por tabela nos nomes r_sel_/r_ins_/r_upd_/r_del_, select em `using (true)` e as três de escrita `to authenticated`
    - grants de sequence para a identity de `materiais`

    Sobre a ponte de campos (D-6wy-11), comparando o mapa com o SQL:
    - toda coluna de `materiais` na migração 44 aparece como valor em `CAMPOS_MATERIAL`
    - todo valor de `CAMPOS_MATERIAL` é uma coluna que a migração 44 declara

    Sobre o núcleo puro (comportamento em `node:vm`, sobre o código real do arquivo):
    - `estItensParaBaixa([])` → lista vazia
    - itens de tipo SERVICO nunca entram, mesmo com materialId
    - item de MATERIAL sem materialId (texto livre) nunca entra
    - item de MATERIAL com materialId e quantidade zero ou negativa nunca entra
    - dois itens de MATERIAL do mesmo materialId saem **consolidados numa entrada só**, com as quantidades somadas (D-6wy-10)
    - quantidade em string (`'2'`, como o Postgres devolve numeric) conta como número
    - `estAbaixoDoMinimo` devolve só o que tem atual estritamente menor que mínimo; mínimo zero nunca alerta; material com `ativo` falso nunca alerta
    - `estMotivoDaOS` produz o motivo com o número da OS de contrato quando ele existe, e com um identificador curto da OS quando não existe

    Sobre a sonda e a carga (com `supa` dublado):
    - `estSondarEsquema()` devolve true e grava EST_OK true quando a leitura de `materiais` passa
    - devolve false, sem lançar, quando a leitura falha
    - `carregarMateriais()` com EST_OK falso **não consulta nada** e deixa MATERIAIS vazio
    - com EST_OK true, ordena e povoa MATERIAIS pela ponte de campos

    Sobre a compatibilidade (D-6wy-06):
    - `refrigeracao/index.html` declara `var EST_OK = false;` — o padrão é desligado
    - `estSondarEsquema` é chamada nas DUAS sequências de arranque (`initAppOnce` e `acessoLivre`), e `carregarMateriais` também, **fora** do `Promise.all` principal
    - nenhuma menção às tabelas de estoque dos outros módulos em `refrigeracao/index.html`
  </behavior>
  <action>
**(a) O gate, primeiro.** Crie `tests/refrigeracao-estoque.test.js` no idioma dos outros gates de `/refrigeracao` — `node:test` + `node:assert/strict`, `fs.readFileSync` de `refrigeracao/index.html` e de `supabase/44_refrigeracao_estoque.sql`, helper `recorte(marcadorIni, marcadorFim)` com asserção de que os dois marcadores foram achados, `vm.createContext`/`vm.runInContext`, e o helper `eq` que compara por `JSON.stringify` (objeto criado dentro do sandbox tem prototype de outro realm — `deepStrictEqual` falha com conteúdo idêntico; a armadilha está documentada em três gates deste módulo). Sem framework, sem `package.json`, sem dependência nova.

O recorte do estoque de servidor é `'/* ── estoque: catálogo, movimentos e baixa ── */'` até `'/* ── REALTIME ── */'`. Escreva no cabeçalho do arquivo, em português: que este gate foi escrito e visto falhando antes do código; por que a idempotência é conferida contra a **tabela de movimentos** e não contra uma bandeira; e que `EST_OK` mora deliberadamente dentro do recorte da porta de escrita (D-6wy-08), para os cinco gates que já o carregam receberem a sonda desligada de graça.

Rode e confirme a falha (RED): recorte não encontrado e arquivo de migração inexistente.

**(b) A migração.** Crie `supabase/44_refrigeracao_estoque.sql` no formato das migrações 40-43: cabeçalho em caixa de comentário explicando o porquê antes do quê, seções numeradas, e um bloco de conferência comentado no fim.

O cabeçalho tem de dizer, em português: que `/refrigeracao` é o único módulo que faz manutenção sem catálogo, e que o item de OS hoje é texto livre com valor digitado; que as tabelas nascem **sem prefixo**, porque essa é a convenção do módulo (`equipamentos`, `os_itens`, `logs_manutencao`), ao contrário dos módulos que usam prefixo; que a migração é **aditiva, sem drop**; e a **ordem de publicação** — o frontend vai a produção **antes** deste SQL, e `EST_OK` mantém a tela publicada idêntica até ele rodar (a mesma regra de D-cf8-25/D-q57-06/D-uyz-24; na ordem inversa a tela publicada continuaria sem saber que o catálogo existe, sem erro nenhum que denunciasse).

Seção 1 — `create table if not exists materiais`: `id bigint generated always as identity primary key`; `codigo text unique`; `nome text not null`; `tipo text not null default 'consumivel'` com check da lista fechada consumivel|peca; `unidade text not null default 'un'`; `aplicacao text` (tipo de equipamento a que a peça serve — SPLIT, PISO-TETO, … — **texto livre**, porque uma peça legítima pode servir a um tipo que ninguém cadastrou ainda, a mesma régua que o campo equivalente de outro módulo já aplica); `preco numeric` com check de nulo ou não-negativo; `estoque_atual numeric not null default 0` com check não-negativo; `estoque_minimo numeric not null default 0` com check não-negativo; `obs text`; `ativo boolean not null default true`.

Seção 2 — `create table if not exists estoque_movimentos`: `id uuid primary key default gen_random_uuid()`; `material_id bigint references materiais(id)`; `os_id uuid references logs_manutencao(id) on delete set null` (D-6wy-15 — comente o porquê: o módulo apaga linha de OS, e o movimento não pode ir junto, porque a peça saiu de verdade); `tipo text not null` com check da lista fechada entrada|saida; `quantidade numeric not null` com check maior que zero; `motivo text`; `registrado_em timestamptz default now()`.

Seção 3 — `alter table os_itens add column if not exists material_id bigint references materiais(id)`. Comente que a coluna é **anulável de propósito**: texto livre continua valendo (item não catalogado, e OS de contrato em que a empresa fornece o material).

Seção 4 — índices: `materiais` não precisa de nenhum além da unique de código (o catálogo é pequeno e sempre lido inteiro); `estoque_movimentos (material_id)`; `estoque_movimentos (os_id, tipo)` composto (D-6wy-14 — a sonda de idempotência filtra pelos dois, e a coluna líder já serve de índice da chave estrangeira); `os_itens (material_id)`.

Seção 5 — grants, no formato da migração 23: select para anon e authenticated nas duas tabelas; insert/update/delete só para authenticated; `grant usage, select on sequence materiais_id_seq to anon, authenticated` (identity precisa disso).

Seção 6 — RLS, no formato exato da seção 9 da migração 43: `do $$ declare t text; begin foreach t in array array['materiais','estoque_movimentos'] loop … end loop; end $$;`, com `alter table … enable row level security` e as quatro policies por tabela, cada uma precedida do seu `drop policy if exists` (é o que torna o bloco reexecutável — `create policy` não aceita `if not exists`). Select em `using (true)`; insert/update/delete `to authenticated` com `true`. Nenhuma policy chama função de sessão, então não há o que embrulhar em subconsulta.

Seção 7 — `comment on table` e `comment on column` para as duas tabelas, para `os_itens.material_id`, e para `materiais.aplicacao` (dizendo que é texto livre e por quê).

Seção 8 — bloco de conferência **comentado**, no formato da seção 11 da migração 43: contagem de colunas de `materiais`, as constraints das duas tabelas, a existência de `os_itens.material_id`, a contagem de policies (esperado: 8 = 4 × 2), e a contagem de linhas das duas tabelas no dia da aplicação (esperado: 0).

**Não aplique a migração.** Este arquivo só escreve o script.

**(c) O código de servidor.** Em `refrigeracao/index.html`, **dentro** da seção `── fluxo da OS interna: porta de escrita ──` (entre `osAdicionarComentario` e `carregarPerfil`, antes de `/* ── REALTIME ── */`), abra a subseção com o comentário de marcador `/* ── estoque: catálogo, movimentos e baixa ── */` e escreva:

- Um comentário de bloco em português explicando: por que a sonda é **própria**, separada de `MAN_FLUXO_OK`/`MOV_OK`/`UNI_OK` (a lição de D-uyz-13 e D-cf8-13 — estender uma sonda existente desligaria um fluxo inteiro num banco que tem as migrações anteriores e não tem esta); e por que ela mora aqui e não noutro lugar (D-6wy-08).
- `var EST_OK = false;` e `async function estSondarEsquema()` — uma leitura só, `select('id').limit(1)` sobre `materiais`, gravando e devolvendo `EST_OK`. Uma leitura, uma pergunta: a coluna `os_itens.material_id` chega na mesma migração, e uma segunda leitura não responderia nada que a primeira já não responda.
- `CAMPOS_MATERIAL`: mapa camelCase → snake_case cobrindo as onze colunas de `materiais` (id, codigo, nome, tipo, unidade, aplicacao, preco, estoqueAtual, estoqueMinimo, obs, ativo). Comente que um campo fora deste mapa é gravado como null sem erro nenhum, e que por isso o gate o compara coluna a coluna com a migração 44 (D-6wy-11).
- `dbToMaterial(r)` e `materialParaDb(m)` derivados do mapa, no espírito de `dbToItem`/`logParaDb`.
- `var MATERIAIS = [];` e `async function carregarMateriais()` — guardada por `EST_OK` (sem ele, esvazia e volta sem consultar), `select('*')` ordenado por `codigo`, `try/catch` com `console.warn`, exatamente a forma de `carregarItensOS`/`carregarComposicaoArp`. **Fora do `Promise.all` principal** (D-cf8-27).
- Núcleo puro, sem API de navegador nenhuma (é o que o gate roda em Node):
  - `estItensParaBaixa(itens)` — recebe a lista de itens de uma OS (formato de `dbToItem`), devolve a lista consolidada por material: só tipo MATERIAL, só com `materialId`, só com quantidade numérica maior que zero, somando quantidades do mesmo material (D-6wy-10). Cada entrada devolve materialId e quantidade.
  - `estAbaixoDoMinimo(materiais)` — devolve os que estão ativos e com `estoqueAtual` estritamente menor que `estoqueMinimo`. Mínimo zero nunca alerta: quem não declarou mínimo não pediu vigilância.
  - `estMotivoDaOS(entry)` — devolve o motivo do movimento de saída no formato "OS {n} — material lançado na ordem", onde {n} é o `numero` da OS quando ela é de contrato e o tem, e um identificador curto derivado do id quando não.
  - `estRotuloMaterial(m)` — o texto de uma opção do catálogo (código, nome, unidade e preço), usado pelo formulário de item na Task 2 e pela página na Task 3; puro, para o gate conferir o formato uma vez só.

**(d) Arranque.** Em `initAppOnce()` e em `acessoLivre()`, acrescente `await estSondarEsquema();` na sequência de sondas (depois de `uniSondarEsquema()`) e `await carregarMateriais();` na sequência de cargas (depois de `carregarComposicaoArp()`). As duas sequências têm de ficar simétricas — o gate confere as duas.

Commits: `test(260826-6wy): …` para o gate, `feat(260826-6wy): …` para a migração e o código.
  </action>
  <verify>
    <automated>node --test tests/refrigeracao-estoque.test.js 2>&1 | tail -8 && node --test tests/*.test.js 2>&1 | tail -8</automated>
  </verify>
  <done>O gate novo passa. Os 958 testes existentes continuam passando, **sem uma linha alterada em nenhum deles** — em particular `tests/refrigeracao-os-pagina.test.js` (cinco cenários byte a byte contra o fixture) e `tests/refrigeracao-desktop.test.js` (CSS fora de @media byte a byte). `tests/fixtures/refrigeracao-os-gaveta.json` e `tests/fixtures/refrigeracao-css-mobile.css` não foram tocados (`git diff --numstat` não os lista). A migração 44 existe, é aditiva e **não foi aplicada**.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: baixa idempotente ao entrar em EM_EXECUCAO, e item de OS escolhido do catálogo</name>
  <files>tests/refrigeracao-estoque.test.js, refrigeracao/index.html</files>
  <behavior>
    Estenda o gate PRIMEIRO e veja os casos novos falharem (RED).

    Baixa (com `supa` dublado, contando chamadas):
    - com EST_OK falso, `manAtualizarOS` com status EM_EXECUCAO **não consulta nem escreve nada** de estoque
    - com EST_OK true e uma OS com dois itens de MATERIAL catalogados, a transição para EM_EXECUCAO grava **duas** saídas em `estoque_movimentos`, cada uma com o os_id, o material_id, a quantidade e o motivo produzido por `estMotivoDaOS`
    - o `estoque_atual` de cada material é atualizado para o valor anterior menos a quantidade, **nunca abaixo de zero**
    - repetir a mesma transição com uma saída já existente para aquele os_id **não grava nada e não altera saldo nenhum** — a idempotência vem da consulta a `estoque_movimentos` filtrada por os_id e tipo saida
    - transição para qualquer outro status (DELINEAMENTO, APROVACAO, CONCLUIDA) não baixa nada
    - patch sem `status` não baixa nada
    - OS só com itens de SERVICO, ou só com material de texto livre, não gera movimento nenhum e não consulta o saldo
    - falha do insert do movimento não derruba a transição: `manAtualizarOS` continua devolvendo true e o aviso de sucesso continua sendo dado uma vez (a mesma regra que a trilha já segue, D-jpd-05)

    Formulário de item da OS:
    - com EST_OK falso, o formulário de item é **byte a byte** o de hoje — nenhum seletor de catálogo, nenhum identificador novo
    - com EST_OK true, o formulário traz um seletor com a opção de texto livre em primeiro lugar e uma opção por material ativo, no formato de `estRotuloMaterial`; material arquivado não aparece
    - escolher um material preenche descrição e valor unitário a partir do catálogo
    - `osAddItem` **sem** material escolhido produz payload byte a byte o de hoje (sem a chave material_id) — D-6wy-12
    - `osAddItem` **com** material escolhido inclui material_id e a unidade do catálogo, e o valor unitário gravado é o do catálogo no momento do lançamento
    - a linha renderizada de um item vindo do catálogo mostra o código do material; a de texto livre continua idêntica à de hoje
  </behavior>
  <action>
**(a) Estenda o gate primeiro**, com os casos acima, e rode para vê-los falhar.

Para a baixa, duble `supa.from(tabela)` cobrindo `logs_manutencao` (update/select/single), `os_comentarios` (insert), `estoque_movimentos` (select com dois `eq` e `limit`, e insert) e `materiais` (update com `eq`), registrando as chamadas numa lista — é o mesmo desenho de `carregarTelaCompleta` em `tests/refrigeracao-os-unificada.test.js` e de `tests/refrigeracao-trilha-os.test.js`. Carregue os recortes da ponte de campos, da porta de escrita e do vocabulário, como aqueles gates fazem, e ligue `EST_OK` no contexto quando o caso pedir.

Para o formulário, prove a compatibilidade **por comportamento, não por regex**: rode `osItensHtml` com `EST_OK` falso e com `EST_OK` true no mesmo sandbox e compare — a saída com a sonda desligada tem de ser idêntica à que o arquivo produz hoje (capture a string uma vez e afirme que ela não contém nenhum dos identificadores novos).

**(b) A baixa.** Ainda na subseção `── estoque: catálogo, movimentos e baixa ──`:

- `async function estJaBaixadoDaOS(osId)` — consulta `estoque_movimentos` por `os_id` e `tipo` saida, `limit(1)`, devolve booleano. Comente que a resposta vem do **registro do próprio estoque**, não de uma bandeira paralela que poderia dessincronizar dele.
- `async function estBaixarItensDaOS(osId, entry)` — devolve cedo se `EST_OK` for falso; resolve a lista por `estItensParaBaixa(OS_ITENS[osId] || [])` e devolve cedo se ela estiver vazia (sem consultar o saldo: uma OS só de serviço não tem por que perguntar nada ao estoque); pergunta `estJaBaixadoDaOS` e devolve cedo se já baixou; e então, para cada peça, acha o material em `MATERIAIS`, calcula o novo saldo como o maior entre zero e o atual menos a quantidade, grava o `update` em `materiais`, grava o `insert` em `estoque_movimentos` com tipo saida, os_id, material_id, quantidade e `estMotivoDaOS(entry)`, e atualiza o saldo no `MATERIAIS` em memória. Erro de escrita vira `console.warn`, nunca exceção que suba — a transição de estado já aconteceu.
- Comente, em português, o **limite conhecido D-6wy-13**: voltar de EM_EXECUCAO para trás não estorna, porque a peça saiu de verdade; a correção é uma entrada registrada na página Estoque.

Em `manAtualizarOS`, **logo depois** do bloco que grava a trilha de sistema e **antes** do `if (msg) showToast(...)`, acrescente uma linha só: se `EST_OK` e o patch traz status igual a EM_EXECUCAO, aguarde `estBaixarItensDaOS(logId, novo)` — passando `novo`, a linha recém-gravada, para que `estMotivoDaOS` leia o número atualizado. Um ponto só, como a trilha: qualquer caminho futuro que leve a OS à execução passa por aqui.

**(c) O formulário de item.** Em `osItensHtml`, no bloco `if (podeEditar)`, quando **e somente quando** `EST_OK` for verdadeiro, insira **antes** do campo Descrição um rótulo e um `<select class="fgs" id="oi-un-mat">` cuja primeira opção tem valor vazio e diz que o item é de texto livre, seguida de uma opção por material de `MATERIAIS` com `ativo` verdadeiro, valor igual ao id e texto igual a `estRotuloMaterial(m)` já escapado. O `onchange` chama `estPreencherItemDaOS()`. Com `EST_OK` falso, nada disso é concatenado e a string sai idêntica à de hoje — é o que mantém o fixture da gaveta intacto.

`function estPreencherItemDaOS()` (pode ficar junto de `osAddItemUI`): lê `oi-un-mat`; se vazio, não mexe em nada (quem já digitou não perde o que digitou); se preenchido, acha o material e escreve `nome` em `oi-un-desc` e o preço em `oi-un-vu`, e força o tipo do item para MATERIAL em `oi-un-tipo` — escolher uma peça do catálogo e deixar o tipo em Serviço seria uma contradição silenciosa.

`osAddItemUI` passa adiante o material escolhido: leia `oi-un-mat` só quando `EST_OK`, resolva o material em `MATERIAIS` e inclua `materialId` e `unidade` no objeto entregue a `osAddItem`. A validação de descrição/quantidade/valor continua exatamente a de hoje.

`osAddItem` acrescenta `material_id` ao payload **só quando `item.materialId` existe** (D-6wy-12) — construa o payload como hoje e acrescente a chave num `if`, nunca com valor null incondicional, que mudaria o payload de todo item de texto livre.

`dbToItem` ganha `materialId: r.material_id` (campo a mais numa ponte que já existe, sem efeito quando a coluna não existe).

Na linha renderizada de `osItensHtml`, quando o item tem `materialId` e `EST_OK` é verdadeiro, acrescente o código do material ao subtexto que já mostra quantidade × valor, escapado, reusando as classes que já estão ali. Nada de classe nova.

Commits: `test(260826-6wy): …` e `feat(260826-6wy): …`.
  </action>
  <verify>
    <automated>node --test tests/refrigeracao-estoque.test.js 2>&1 | tail -8 && node --test tests/*.test.js 2>&1 | tail -8</automated>
  </verify>
  <done>Todos os casos novos passam. A suíte inteira continua verde e `tests/fixtures/refrigeracao-os-gaveta.json` continua sem ser tocado (`git diff --numstat` não o lista) — a prova de que o formulário de item com a sonda desligada é byte a byte o de hoje. Se algum gate existente exigir `EST_OK`/`MATERIAIS` no seu sandbox (é o caso previsto para `tests/refrigeracao-movimentacao-os.test.js`, que carrega o recorte da tela sem o da porta de escrita num dos sandboxes), ele aprende o fato novo com uma linha declarando a sonda desligada — **nenhum caso apagado** (D-cf8-28).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: página Estoque — navegação injetada, lista, edição inline, cadastro e entrada</name>
  <files>tests/refrigeracao-estoque.test.js, refrigeracao/index.html</files>
  <behavior>
    Estenda o gate PRIMEIRO (RED).

    Navegação e página:
    - `#bottom-nav` continua com **exatamente cinco** `.nav-btn` na marcação
    - existe `<div class="page" id="page-estoque">` na marcação, irmã das outras, sem CSS próprio
    - `estInjectNav()` com EST_OK falso **não cria botão nenhum**; com EST_OK true cria um `.nav-btn` que chama `navTo('estoque', …)`, uma única vez (chamar duas vezes não duplica)
    - `estInjectNav` é chamada nas duas sequências de arranque, depois da sonda
    - `navTo('estoque', …)` chama `renderEstoque()`; `renderPaginaAtiva()` despacha `page-estoque` para `renderEstoque()`

    Permissão:
    - `MAN_ACOES_CARGO` ganha a ação `estoque` com admin, gestor e tecnico; observador não a tem, e nenhum papel de contratação a tem (o laço que já existe em `tests/refrigeracao-fluxo-os-interna.test.js` continua valendo)
    - cadastro e edição de cadastro do material continuam em `podeEditarCadastro` (admin e gestor)

    Lista:
    - `renderEstoque` desenha uma linha por material ativo, com código, nome, tipo, aplicação, unidade, atual, mínimo e preço
    - material abaixo do mínimo recebe o aviso; material em dia não recebe
    - catálogo vazio mostra o estado vazio, nunca uma lista em branco
    - com cargo sem a ação `estoque`, nenhum controle de edição, de entrada nem de cadastro é desenhado
    - a lista é a mesma nas duas larguras (não consulta `TELA_LARGA`)

    Escrita (com `supa` dublado):
    - `estSalvarLinha` grava só atual, mínimo e preço, recusa valor negativo, recusa cargo sem a ação, e recarrega o catálogo depois
    - `estSalvarMaterial` insere quando não há id em edição e atualiza quando há, recusa sem `podeEditarCadastro`, recusa nome vazio, e passa pela ponte `materialParaDb`
    - `estRegistrarEntrada` recusa quantidade zero ou negativa, recusa cargo sem a ação, grava uma linha `entrada` em `estoque_movimentos` com o motivo digitado, **soma** ao `estoque_atual` do material, e não toca em os_id nenhum
  </behavior>
  <action>
**(a) Estenda o gate primeiro** e veja falhar. Para a lista e os formulários, prove por **comportamento**: rode `renderEstoque` num sandbox com `el(id)` dublado (capturando `innerHTML` por id, como `tests/refrigeracao-os-pagina.test.js` faz) e afirme sobre a string produzida; não afirme por regex sobre o arquivo, exceto nos pontos estruturais (contagem de `.nav-btn`, existência da `page-estoque`, chamada nas duas sequências de arranque).

**(b) Marcação.** Acrescente `<div class="page" id="page-estoque"></div>` logo **depois** de `page-alert`, com um comentário curto dizendo que ela nasce vazia e só é preenchida por `renderEstoque()`, e que **não recebe CSS próprio** (todas as classes usadas já existem). Não toque em `#bottom-nav`.

**(c) Navegação injetada (D-6wy-07).** Escreva `estInjectNav()` no espírito de `osInjectChipsExecutor()`: volta cedo se `EST_OK` for falso ou se o botão já existir; cria um `<button class="nav-btn" id="nav-estoque">` com o ícone `fas fa-boxes` (o mesmo que `osItensHtml` já usa para itens) e o rótulo Estoque, `onclick` chamando `navTo('estoque', esteBotão)`, e o acrescenta a `#bottom-nav`. Comente o porquê: sem a migração 44 o botão **não existe**, em vez de existir escondido — e o número de botões na marcação estática continua sendo o que dois gates afirmam.

Chame `estInjectNav()` nas duas sequências de arranque, logo depois de `estSondarEsquema()`.

Em `navTo`, acrescente o despacho `if(page==='estoque') renderEstoque();` junto dos outros cinco. Em `renderPaginaAtiva`, acrescente o ramo de `page-estoque`. **Não** mexa em `ABAS_NAV`: aquela lista existe para o botão de voltar das páginas de detalhe, e o estoque não tem página de detalhe apontando para ele; acrescentá-la deslocaria índices que `tests/refrigeracao-ficha-pagina.test.js` afirma. Comente isso no ponto.

**(d) Permissão.** Acrescente `estoque: ['admin','gestor','tecnico']` a `MAN_ACOES_CARGO`, com um comentário dizendo que é a mesma lista de quem executa — quem consome peça é quem corrige saldo — e que cadastrar material continua sendo `podeEditarCadastro`, admin e gestor, porque cadastro é ato de registro, não de rotina.

**(e) `renderEstoque()` (D-6wy-09).** Uma lista de cartões, nas duas larguras, sem consultar `TELA_LARGA`. Estrutura sugerida, toda em classe existente:
- uma faixa de indicadores no topo com `.kpi-row`/`.kpi-card`: total de materiais ativos, quantos abaixo do mínimo, e o valor total em prateleira (soma de atual × preço), no formato monetário que `fmtMoney` já produz;
- um `.sec-head` com `.sec-title` e `.sec-count`, e — quando o cargo tem a ação — os botões de `+ Material` e o de valor unitário, em `.btn-sm`;
- uma linha por material reusando `.orc-item` com `.orc-tipo` (a mesma pílula que já distingue SERVICO de MATERIAL, aqui distinguindo peça de consumível — as duas classes de cor já existem), `.orc-desc` com nome, código, aplicação e unidade, `.orc-val` com o saldo e o mínimo, o aviso quando abaixo do mínimo com `.pill.pill-nok`, e os botões de editar e de entrada;
- estado vazio com `.empty` quando não há material.

A edição inline troca a `.orc-desc`/`.orc-val` daquela linha por três `.fgi` (atual, mínimo, preço) mais salvar e cancelar, **redesenhando só aquela linha** — nunca a lista inteira, senão o foco e o cursor se perdem a cada tecla (a lição de Máquinas, citada em D-6wy-09). Guarde o id em edição numa variável de módulo, no espírito de `MAN_EDIT_ID`.

`+ Material` e `⚙ Editar cadastro` abrem a **gaveta** (`openDrawer`, `#drawer-body`, `#drawer-footer`, `#dh-id`), no molde exato de `openEquipForm`: código, nome, tipo, aplicação, unidade, preço, atual, mínimo e observação, em `.fg2`/`.fgl`/`.fgi`/`.fgs`/`.fgt`, com Cancelar e Salvar no rodapé.

Entrada abre a gaveta com quantidade e motivo, e o rodapé com Registrar entrada.

**(f) Escrita.** `estSalvarLinha(id)`, `estSalvarMaterial()`, `estRegistrarEntrada(id)` — todas com **guarda na ação, não só no botão** (D-l7n-03: a lista de cargos é conferida ao abrir e ao gravar), no idioma de erro do módulo (`showToast` com a mensagem, `return`), gravando pela ponte `materialParaDb`, e chamando `carregarMateriais()` seguido de `renderEstoque()` depois de gravar. `estRegistrarEntrada` grava a linha em `estoque_movimentos` com tipo entrada, **sem os_id**, com o motivo digitado, e soma a quantidade ao saldo do material — a entrada é recebimento, nunca vínculo com OS.

Nenhuma regra de CSS. Nenhum identificador de classe novo. Se algo parecer exigir estilo novo, use estilo inline no template, como o módulo já faz.

Commits: `test(260826-6wy): …` e `feat(260826-6wy): …`.
  </action>
  <verify>
    <automated>node --test tests/refrigeracao-estoque.test.js 2>&1 | tail -8 && node --test tests/*.test.js 2>&1 | tail -8</automated>
  </verify>
  <done>A página existe e responde; os casos novos passam; a suíte inteira continua verde. `tests/fixtures/refrigeracao-css-mobile.css` não foi tocado e `tests/refrigeracao-desktop.test.js` continua passando — a prova de que nenhuma regra de CSS nasceu. `tests/refrigeracao-topbar-parque.test.js` continua afirmando cinco botões e cinco rótulos, sem uma linha alterada.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: alerta abaixo do mínimo, documentação e suíte verde</name>
  <files>tests/refrigeracao-estoque.test.js, refrigeracao/index.html, CLAUDE.md, README.md, TESTES.md</files>
  <behavior>
    Estenda o gate PRIMEIRO (RED).

    Alerta:
    - com EST_OK falso, `renderAlerts` produz saída **byte a byte** a de hoje, nos dois ramos
    - com EST_OK true e nenhum material abaixo do mínimo, idem — a seção só aparece quando há o que dizer
    - com EST_OK true e materiais abaixo do mínimo, a seção "Estoque abaixo do mínimo" aparece nos **dois** ramos (largura de celular e largura de computador), com uma linha por material, e cada linha leva à página Estoque
    - a contagem do distintivo de alertas **não** muda por causa do estoque: o distintivo continua contando equipamento, não peça, e `atualizarBadgeAlertas(ag)` continua aparecendo exatamente duas vezes no corpo de `renderAlerts`
    - o ramo de "Nenhum alerta ativo!" não afirma vazio quando existe alerta de estoque
  </behavior>
  <action>
**(a) Estenda o gate primeiro** e veja falhar.

**(b) O alerta.** Em `renderAlerts()`, **depois** de `semHistCrit` (a guarda de `TELA_LARGA` tem de continuar vindo depois dos quatro conjuntos calculados — `tests/refrigeracao-desktop.test.js` afirma essa ordem) calcule o bloco numa função separada, `estAlertaHtml()`, que devolve string vazia quando `EST_OK` é falso ou quando `estAbaixoDoMinimo(MATERIAIS)` não devolve nada, e senão devolve um `.sec-head` com o título "Estoque abaixo do mínimo" e a contagem, mais uma linha por material reusando `.alert-card`/`.alert-icon`/`.alert-body`/`.alert-title`/`.alert-sub`, com `onclick` levando à página Estoque (chame `navTo('estoque', el('nav-estoque'))`, o botão que `estInjectNav` criou — passar o botão é o que mantém o estado ativo da barra coerente).

Encaixe a string nos **dois** ramos: no ramo de tabela, antes do `return`, concatenando ao que já é escrito em `#alert-list` (inclusive no caminho de "Nenhum alerta ativo!", que só pode dizer isso quando **também** não há alerta de estoque); no ramo de cartões, concatenando ao `html` antes da decisão de estado vazio. **Não** acrescente uma terceira chamada de `atualizarBadgeAlertas`.

**(c) Documentação.** Três arquivos, em português, no formato que cada um já usa:

`CLAUDE.md` — um parágrafo de arquitetura no molde dos que já existem (negrito no título com a data, o problema antes da solução, os números medidos, as decisões e o porquê de cada uma, e o gate no fim). Tem de dizer: que era o único módulo que fazia manutenção sem catálogo, e que item de OS era texto livre com valor digitado; que as tabelas nascem **sem prefixo** por convenção do módulo; que a baixa acontece **na entrada em execução, nunca na abertura nem no delineamento**, e é idempotente pelo registro do próprio estoque; o **limite conhecido D-6wy-13** (voltar atrás não estorna, e a correção é uma entrada); que `EST_OK` mantém a tela publicada idêntica até a migração 44 rodar, e que ela mora dentro do recorte da porta de escrita justamente para os cinco gates existentes a receberem desligada (D-6wy-08); que o botão de navegação é injetado em runtime porque dois gates afirmam cinco botões e porque "sem migração, sem botão" tem de ser estrutural (D-6wy-07); que a página é cartão nas duas larguras, com o precedente de `ctRenderSaldo` e o motivo (a folha de tabela só existe no `@media` de computador, e edição inline não sobrevive a tabela que se redesenha); e que pedidos e listas de compra ficaram **fora de escopo, para a próxima task**.

`README.md`, seção **Pendências** — uma entrada dizendo que `supabase/44_refrigeracao_estoque.sql` está **escrita e conferida, aguardando aplicação** pelo usuário, **depois do deploy do frontend** (a mesma ordem de D-cf8-25), e o que conferir depois de aplicada.

`TESTES.md` — uma seção nova no fim, no formato das outras, com a data, dividida em **antes da migração 44** (a tela se comporta como hoje: sem botão Estoque, sem catálogo no formulário de item, sem alerta) e **depois de aplicada**: cadastrar um material com mínimo maior que zero; lançar um item de MATERIAL numa OS escolhendo do catálogo e conferir que descrição, unidade e valor vieram preenchidos; mover a OS para Em execução e conferir que o saldo caiu **uma** vez; mover para trás e para a frente de novo e conferir que **não** caiu de novo; conferir o movimento de saída na tabela com o os_id e o motivo; registrar uma entrada e conferir que o saldo subiu; conferir a seção "Estoque abaixo do mínimo" na página de Alertas nas duas larguras; e o **limite conhecido**: voltar de Em execução não estorna, o estorno é uma entrada registrada à mão.

Commits: `test(260826-6wy): …`, `feat(260826-6wy): …`, `docs(260826-6wy): …`.
  </action>
  <verify>
    <automated>node --test tests/*.test.js 2>&1 | tail -8 && git status --short</automated>
  </verify>
  <done>A suíte inteira passa (958 + os casos novos, 0 falhas). Os três documentos estão atualizados. `git status --short` está limpo — tudo comitado, em português, com o prefixo da task. Nenhum fixture foi tocado.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navegador → PostgREST | a `anon key` está no HTML por decisão de projeto; toda escrita atravessa aqui e só a RLS decide |
| formulário → `materiais` / `estoque_movimentos` | quantidade, preço e saldo digitados por usuário autenticado |
| `os_itens.material_id` → `materiais.id` | vínculo entre a OS e o catálogo, usado pela baixa |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-6wy-01 | Elevation of Privilege | policies de `materiais`/`estoque_movimentos` | high | mitigate | escrita `to authenticated` nas três policies de mutação; leitura em `using (true)` para não quebrar o cargo Livre (observador sem senha), que é o padrão da plataforma. A trava de cargo na tela é UX; o Postgres é quem recusa o anônimo. |
| T-6wy-02 | Tampering | `estoque_atual` | high | mitigate | check de não-negativo no banco; a baixa nunca escreve abaixo de zero; a idempotência é conferida contra `estoque_movimentos`, o registro do próprio estoque, não contra uma bandeira do cliente que uma retentativa de rede dessincronizaria |
| T-6wy-03 | Repudiation | consumo de peça sem rastro | medium | mitigate | toda saída grava linha em `estoque_movimentos` com os_id, material_id, quantidade, motivo e data; `on delete set null` no os_id preserva o histórico quando a OS é apagada (D-6wy-15) |
| T-6wy-04 | Tampering | valor unitário do item de OS | medium | mitigate | o preço é **congelado na linha** de `os_itens` no momento do lançamento (a coluna `valor_unitario` já existe); reprecificar o catálogo em setembro não pode mudar o custo de uma OS fechada em agosto |
| T-6wy-05 | Denial of Service | leitura da tela sem a migração | medium | mitigate | `EST_OK` guarda todo consumo; sem a migração 44 nenhuma consulta ao catálogo é feita e a tela publicada se comporta como hoje |
| T-6wy-06 | Information Disclosure | catálogo e saldos legíveis por anônimo | low | accept | leitura aberta é o padrão da plataforma (o cargo Livre existe e é observador sem senha); saldo de almoxarifado de consumível não é dado sensível, e fechar a leitura quebraria o modo observador em todo o módulo |
| T-6wy-SC | Tampering | instalação de pacote | low | accept | nenhuma dependência nova: sem npm, sem pip, sem cargo, sem CDN novo — a task inteira é HTML/JS/SQL do próprio repositório |
</threat_model>

<verification>
1. `node --test tests/*.test.js` → 958 + os casos novos, **0 falhas**.
2. `git diff --numstat` não lista `tests/fixtures/refrigeracao-os-gaveta.json` nem `tests/fixtures/refrigeracao-css-mobile.css` — a gaveta da OS e o CSS fora de `@media` ficaram byte a byte.
3. `tests/refrigeracao-topbar-parque.test.js` e `tests/refrigeracao-desktop.test.js` continuam afirmando cinco `.nav-btn` na marcação, sem alteração.
4. `supabase/44_refrigeracao_estoque.sql` existe, é aditiva (nenhum `drop` fora de `drop policy if exists`) e **não foi aplicada**.
5. Servido por HTTP local (`python -m http.server`), `/refrigeracao` continua abrindo e operando exatamente como hoje contra o banco atual — a sonda desligada não muda nada na tela.
6. Nenhum `import` de `shared/` em `refrigeracao/index.html`; nenhuma menção às tabelas de estoque dos outros módulos.
</verification>

<success_criteria>
- Migração 44 escrita, conferida, aditiva, com bloco de conferência comentado — e **não aplicada**.
- `refrigeracao/index.html`: sonda `EST_OK`, catálogo `MATERIAIS`, núcleo puro, baixa idempotente na entrada em execução, catálogo no formulário de item, página Estoque com edição inline / cadastro / entrada, e alerta abaixo do mínimo nos dois ramos.
- `tests/refrigeracao-estoque.test.js` novo, escrito e visto falhando antes de cada implementação, conferindo migração por regex e comportamento em `node:vm`.
- Suíte inteira verde, nenhum caso de gate existente apagado, nenhum fixture tocado.
- CLAUDE.md, README.md e TESTES.md registram o fato, a ordem de publicação e o limite conhecido D-6wy-13.
- Pedidos de material e listas de compra **não** foram criados.
</success_criteria>

<output>
Criar `.planning/quick/260826-6wy-refrigeracao-estoque-de-pe-as-e-materiai/260826-6wy-SUMMARY.md` ao terminar.
</output>
