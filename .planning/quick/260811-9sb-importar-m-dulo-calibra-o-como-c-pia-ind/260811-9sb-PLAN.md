---
phase: quick-260811-9sb
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - calibracao/index.html
  - calibracao/assets/fonts.css
  - calibracao/assets/erp-module-shell.css
  - calibracao/assets/fonts/dm-sans-latin-400-normal.woff2
  - calibracao/assets/fonts/dm-sans-latin-500-normal.woff2
  - calibracao/assets/fonts/dm-sans-latin-600-normal.woff2
  - calibracao/assets/fonts/dm-sans-latin-700-normal.woff2
  - calibracao/assets/fonts/jetbrains-mono-latin-400-normal.woff2
  - calibracao/assets/fonts/jetbrains-mono-latin-500-normal.woff2
  - vercel.json
  - index.html
autonomous: true
requirements: [QUICK-260811-9SB]

must_haves:
  truths:
    - "Acessar /calibracao abre o app 'CMASM — Controle de Calibração' com layout e fontes corretos (CSS e woff2 servidos do próprio módulo, sem referência ao diretório-pai)"
    - "No portal, o card Calibração é um link clicável que leva a /calibracao, no mesmo padrão visual dos cards em produção"
    - "Nenhum módulo existente é alterado: shared/, supabase/, refrigeracao/, maquinas/, transportes/, eletrica/, fonoclama/, predial/ e mapa/ ficam intocados"
  artifacts:
    - "calibracao/index.html (cópia do app legado, ~862 KB, com os dois <link> de CSS apontando para assets/ local)"
    - "calibracao/assets/fonts.css e calibracao/assets/erp-module-shell.css"
    - "calibracao/assets/fonts/ com os 6 woff2 (dm-sans 400/500/600/700 + jetbrains-mono 400/500)"
    - "vercel.json com rewrite /calibracao -> /calibracao/index.html"
    - "index.html do portal com card Calibração ativo"
  key_links:
    - "calibracao/index.html <link> -> calibracao/assets/*.css (caminho relativo local, sem subir diretório)"
    - "calibracao/assets/fonts.css url() -> calibracao/assets/fonts/*.woff2"
    - "Portal card href=/calibracao -> rewrite do vercel.json -> calibracao/index.html"
---

<objective>
Importar o app legado de Calibração (`/home/luc/DEV_ERP/cmms-calibracao_2026JUL28/calibracao_erp.html`) como módulo estático `/calibracao` do pmoc, como **cópia independente** (decisão travada do usuário): sem unificar com `shared/auth.js` nem `shared/shell.js`, sem integrar a árvore de locais, sem Supabase — o app é 100% client-side com dados em localStorage. Padrão análogo ao refrigeracao (fora da base compartilhada). A única integração é o link no portal + rewrite no Vercel.

Purpose: colocar o controle de calibração (~500 instrumentos metrológicos) acessível pela plataforma sem esperar a integração completa ao backend.
Output: diretório `calibracao/` autossuficiente + card ativo no portal + rota no `vercel.json`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md
@index.html
@vercel.json

Fatos já verificados (não re-derivar):
- Fonte: `/home/luc/DEV_ERP/cmms-calibracao_2026JUL28/calibracao_erp.html` (~862 KB, single-file, pt-BR). Sem Supabase, sem fetch; dados em localStorage.
- Os dois CSS referenciados pelo HTML existem em `/home/luc/DEV_ERP/cmms-erp/assets/`: `fonts.css` e `erp-module-shell.css`.
- Os 6 woff2 existem em `/home/luc/DEV_ERP/cmms-erp/assets/fonts/`: `dm-sans-latin-{400,500,600,700}-normal.woff2` e `jetbrains-mono-latin-{400,500}-normal.woff2` (nomes conferidos por `ls`).
- `DEV_ERP` é estritamente read-only: apenas leitura/cópia a partir dele; todo efeito colateral fica em `pmoc-overlay`.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Copiar o app de Calibração e seus assets como módulo autossuficiente</name>
  <files>calibracao/index.html, calibracao/assets/fonts.css, calibracao/assets/erp-module-shell.css, calibracao/assets/fonts/*.woff2 (6 arquivos)</files>
  <action>
    1. Criar `calibracao/assets/fonts/` na raiz do repo (`mkdir -p /home/luc/Downloads/pmoc-overlay/calibracao/assets/fonts`).
    2. Copiar (somente leitura no DEV_ERP):
       - `/home/luc/DEV_ERP/cmms-calibracao_2026JUL28/calibracao_erp.html` → `calibracao/index.html`
       - `/home/luc/DEV_ERP/cmms-erp/assets/fonts.css` e `/home/luc/DEV_ERP/cmms-erp/assets/erp-module-shell.css` → `calibracao/assets/`
       - os 6 woff2 de `/home/luc/DEV_ERP/cmms-erp/assets/fonts/` → `calibracao/assets/fonts/`
    3. Em `calibracao/index.html`, ajustar os dois `<link>` de CSS que hoje sobem um nível de diretório (assets do monorepo legado) para os caminhos locais: `assets/fonts.css` e `assets/erp-module-shell.css`. Nenhuma outra alteração de conteúdo — cópia independente por decisão travada do usuário (não adotar shared/auth.js nem shared/shell.js).
    4. Varredura defensiva: procurar em `calibracao/index.html` qualquer outro atributo href/src que suba diretório e, se existir, resolver para caminho local (copiando o asset correspondente para dentro de `calibracao/assets/`, preservando a estrutura relativa). Fazer o mesmo com `url()` em `erp-module-shell.css` — se referenciar assets além das fontes, copiá-los também.
    5. Conferir que todo `url()` de `calibracao/assets/fonts.css` resolve para arquivo existente sob `calibracao/assets/` (as fontes ficam em `fonts/`, relativo ao próprio CSS, então a estrutura copiada já resolve).
  </action>
  <verify>
    <automated>cd /home/luc/Downloads/pmoc-overlay && test -f calibracao/index.html && test -f calibracao/assets/fonts.css && test -f calibracao/assets/erp-module-shell.css && [ "$(ls calibracao/assets/fonts/*.woff2 | wc -l)" -ge 6 ] && [ "$(grep -c 'href="assets/fonts.css"' calibracao/index.html)" -eq 1 ] && [ "$(grep -c 'href="assets/erp-module-shell.css"' calibracao/index.html)" -eq 1 ] && [ "$(grep -Ec '(href|src)="\.\./' calibracao/index.html)" -eq 0 ] && (cd calibracao/assets && grep -oE "url\((['\"]?)[^)'\"]+" fonts.css | sed -E "s/url\(['\"]?//" | while read -r f; do test -f "$f" || { echo "FALTA: $f"; exit 1; }; done)</automated>
  </verify>
  <done>`calibracao/` existe com index.html + 2 CSS + 6 woff2; os dois links de CSS apontam para `assets/` local; nenhum href/src do módulo sobe diretório; todo url() de fonts.css resolve para arquivo copiado. Nada foi escrito em DEV_ERP.</done>
</task>

<task type="auto">
  <name>Task 2: Rotear /calibracao no Vercel e ativar o card no portal</name>
  <files>vercel.json, index.html</files>
  <action>
    1. Em `vercel.json`, adicionar ao array `rewrites`, após a entrada de `/mapa` e seguindo o alinhamento das linhas existentes: `{ "source": "/calibracao", "destination": "/calibracao/index.html" }`.
    2. Em `index.html` (portal), transformar o card inativo da Calibração (linha ~99, ícone 🔬, hoje um `<div>` com a classe modificadora `off` na grade da seção de módulos planejados) em link ativo no padrão dos cards em produção (linhas 53-95): movê-lo para o fim da grade "Em produção", logo após o card `/mapa`, como `<a class="card" href="/calibracao">`, mantendo ícone 🔬, nome "Calibração" e a descrição "~500 instrumentos metrológicos. Vencimentos e laudos.". Trocar as tags para `<span class="t t-ok">standalone</span><span class="t t-wip">dados locais</span>` — o app não usa o Supabase compartilhado (localStorage), e a tag antiga "Banco pronto" deixa de fazer sentido.
    3. Remover a seção de módulos planejados (o `<div class="lbl">` com 📋 e sua `<div class="grid">`), que fica vazia após a mudança. A regra CSS `.card.off` no `<style>` pode permanecer (inofensiva).
    4. Não tocar em nenhum outro arquivo: shared/, supabase/ e os módulos existentes ficam fora desta tarefa.
  </action>
  <verify>
    <automated>cd /home/luc/Downloads/pmoc-overlay && node -e "JSON.parse(require('fs').readFileSync('vercel.json','utf8'))" && [ "$(grep -c '"/calibracao"' vercel.json)" -eq 1 ] && [ "$(grep -c 'href="/calibracao"' index.html)" -eq 1 ] && [ "$(grep -c 'card off' index.html)" -eq 0 ] && [ "$(grep -c 'class="lbl"' index.html)" -eq 1 ] && git diff --name-only | grep -v '^\.planning/' | sort | grep -vxE 'index\.html|vercel\.json' | grep -v '^calibracao/' | wc -l | grep -qx 0</automated>
  </verify>
  <done>`vercel.json` continua JSON válido com o rewrite de /calibracao; o portal tem o card Calibração como link ativo na grade "Em produção"; a seção de planejados vazia foi removida (resta um único rótulo de seção); o diff toca apenas index.html, vercel.json e calibracao/.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navegador → arquivos estáticos | app 100% client-side; sem backend próprio, sem entrada de rede |
| mesma origem (pmoc-orcin.vercel.app) | o JS legado copiado roda na mesma origem dos módulos que guardam sessão Supabase no storage do navegador |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-quick9sb-01 | Information Disclosure | calibracao/index.html (JS legado na mesma origem) | medium | accept | Código legado interno da própria equipe, sem fetch nem dependência externa (conferido na sondagem); importado como está por decisão do usuário. Risco residual: um defeito no JS legado teria acesso ao storage da origem compartilhada. Revisita quando o módulo for integrado ao Supabase. |
| T-quick9sb-02 | Tampering | assets copiados (CSS/woff2) | low | mitigate | Cópia byte a byte de fonte conhecida e read-only (DEV_ERP), sem CDN nem instalação de pacote; gates da Task 1 conferem existência e resolução de todos os url(). |
| T-quick9sb-03 | Denial of Service | dados do usuário em localStorage | low | accept | Dados de calibração ficam só no navegador (perda ao limpar storage) — limitação conhecida e aceita da cópia independente; documentada na tag "dados locais" do card. |
</threat_model>

<verification>
Checagens automatizadas já cobertas nos gates das tasks. Conferência humana recomendada (preview local, ex.: `python -m http.server` na raiz):

1. Abrir `http://localhost:8000/calibracao/` — o app "CMASM — Controle de Calibração" carrega com fonte DM Sans (não a fonte fallback do sistema) e shell visual correto; console do navegador sem 404 de CSS/fonte.
2. Abrir `http://localhost:8000/` — card Calibração aparece na grade "Em produção" e o clique navega para o módulo.
3. Abrir /refrigeracao e /maquinas no preview — inalterados (nenhum arquivo deles tocado; `git status` mostra apenas calibracao/, index.html e vercel.json).
</verification>

<success_criteria>
- `calibracao/` autossuficiente: index.html + assets próprios, zero referência a diretório-pai, zero dependência de shared/ ou Supabase.
- Rewrite `/calibracao` presente em `vercel.json` válido.
- Card ativo no portal apontando para `/calibracao`, seção de planejados vazia removida.
- Nenhuma escrita em `/home/luc/DEV_ERP`; nenhum módulo existente alterado.
- Commit em português no padrão do repo (ex.: `feat(calibracao): importa módulo de calibração como cópia independente com link no portal`).
</success_criteria>

<output>
Criar `.planning/quick/260811-9sb-importar-m-dulo-calibra-o-como-c-pia-ind/260811-9sb-SUMMARY.md` ao concluir.
</output>
