---
phase: 13-gest-o-e-qualidade
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - shared/kanban.js
  - shared/calendario.js
  - maquinas/operacoes.js
  - maquinas/app.js
  - maquinas/index.html
  - tests/kanban-calendario-compartilhados.test.js
autonomous: true
requirements: [GEQ-04, PLAT-08, PLAT-09, PLAT-16]

must_haves:
  truths:
    - 'Kanban: coluna sem cartão → <div class="empty">{rótulo por coluna}</div> (padrão de renderOperacoes)'
    - 'Kanban: quem chama desenha vazio() até os dados chegarem — o núcleo não tem estado de carregamento'
    - 'Item com status fora da lista de colunas cai na primeira coluna (regra já existente em agruparOperacoes)'
    - 'Colunas por definição [{id, rotulo}], contagem em .kanban-count, cartão com título e .op-meta'
    - 'Cartão sem metadado opcional (máquina/área) omite a linha, não mostra "undefined"'
    - 'Kanban: contagem por coluna sempre visível (0 incluído); 1 cartão não muda o layout'
    - 'Calendário: mês sem evento → grade normal sem mensagem (comportamento existente preservado)'
    - 'Calendário: quem chama desenha vazio() até os dados chegarem — o núcleo não tem estado de carregamento'
    - 'Evento com data inválida (não AAAA-MM-DD) é ignorado por agruparPorData(); nunca lança'
    - 'gradeMes(ano,mes) → 7 colunas, células vazias antes do dia 1, dia de hoje com .hoje; eventos com prefixo de origem'
    - 'Evento sem título → rótulo cai para o nome da origem ("OS", "Operação")'
    - 'Calendário: 0/1/N eventos por dia empilham em .calendar-event; sem limite artificial'
    - { statement: '375px: uma coluna visível + seg-toggle para pular de coluna — conferir rolagem só dentro de .kanban, não da página (tests/mobile-375)', verification: backstop }
    - { statement: 'Título de cartão longo no kanban — quebra natural (.op-name), sem truncar; conferir na tela', verification: backstop }
    - { statement: 'Grade do calendário rola dentro do cartão (.calendar-grid overflow:auto, min-width 86px/dia) — conferir em 375px', verification: backstop }
    - { statement: '"Máquinas · Troca de óleo" pode estourar 86px — herda quebra de .calendar-event, sem nowrap; conferir na tela', verification: backstop }
    - 'Assunção explícita (edge probe devolveu GEQ-04 como unclassified): os casos-limite de GEQ-04 são status fora da lista de colunas (cai na primeira), lista de colunas vazia, evento com data inválida, evento sem título, mês de fevereiro bissexto e mês que começa no domingo (zero células vazias antes do dia 1) — casos próprios em tests/kanban-calendario-compartilhados.test.js'
    - 'Compatibilidade CommonJS (decisão do plano, 13-PATTERNS.md "Restrição estrutural importante para o planner"): maquinas/operacoes.js vira módulo ES puro — sem invólucro UMD e sem escrita no objeto de exportação do CommonJS — e continua carregável por require() pelo suporte nativo do Node >= 22.12 a require(esm), conferido no Node 24.18 deste repositório; a condição que sustenta isso é o arquivo nunca ganhar espera de nível de topo, e o precedente já em produção é shared/tabela.js carregado por require em tests/tabela-compartilhada.test.js'
    - 'Assunção explícita (edge probe devolveu PLAT-08 como unclassified): a prova de que o kanban foi extraído sem perda é que tests/operacoes-maquinas.test.js continua verde SEM UMA LINHA MUDADA, incluindo o caso do status desconhecido caindo em "programada"'
    - 'Assunção explícita (edge probe devolveu PLAT-09 como unclassified): a prova de que o calendário foi extraído sem perda é que o caso "combina operações e OS no calendário mensal" de tests/operacoes-maquinas.test.js continua verde sem alteração, com a mesma ordem e a mesma forma de evento ({id, data, origem, titulo})'
    - 'Assunção explícita (edge probe devolveu PLAT-16 como unclassified): não regressão é medida por node --test inteiro (baseline 1095 casos, 0 falhas) mais a permanência dos ids operacoes-kanban, agenda-calendario, operacoes-kpis e tb-areas e das funções renderOperacoes/renderAgenda em maquinas/app.js'
  artifacts:
    - shared/kanban.js
    - shared/calendario.js
    - tests/kanban-calendario-compartilhados.test.js
  key_links:
    - 'maquinas/operacoes.js deixa de declarar a lógica e passa a ser fachada sobre shared/kanban.js e shared/calendario.js, continuando a expor OperacoesMaq no escopo global para maquinas/app.js'
    - 'maquinas/index.html linha 994 passa a carregar /maquinas/operacoes.js como módulo ES, mantida ANTES de /maquinas/app.js — é a ordem que tests/integracao-operacoes-maquinas.test.js exige'
    - 'maquinas/app.js#renderAgenda deixa de montar a grade à mão e passa a injetar o texto devolvido por htmlCalendario — mesmo corte núcleo-puro/aplicador-de-DOM de shared/tema.js'
    - 'O bloco @media(max-width:600px) de maquinas/index.html permanece no módulo — tests/mobile-375.test.js exige que a @media própria de Máquinas continue existindo'
    - 'As 20 regras-base de kanban e calendário saem de maquinas/index.html neste plano e entram em shared/pmoc.css no plano 13-04, da mesma onda — os dois planos são as duas metades de D-10 e a onda só está completa com as duas'
  prohibitions:
    - statement: 'A extração não muda o comportamento observável de Máquinas — nenhum id, nenhuma classe, nenhuma ordem de evento e nenhum texto de tela do kanban e da agenda mudam'
      status: active
      verification: 'tests/operacoes-maquinas.test.js, tests/integracao-operacoes-maquinas.test.js, tests/chrome-icones.test.js e tests/mobile-375.test.js rodam sem alteração e continuam verdes'
    - statement: 'Nenhum dos quatro gates existentes que tocam Máquinas é alterado, enfraquecido ou removido para fazer a extração passar'
      status: active
      verification: 'git diff --stat sobre os quatro arquivos de teste está vazio ao fim do plano'
    - statement: 'Maquinas não fica com cópia local da lógica de kanban nem de calendário — operacoes.js delega, não duplica (GEQ-04)'
      status: active
      verification: 'grep por Object.fromEntries e por padStart em maquinas/operacoes.js, com comentários removidos, devolve 0; grep pelos dois imports de ../shared/ devolve 2'
    - statement: 'O kanban e o calendário não inventam cor fora dos tons semânticos — a cor do cartão e do evento vem de classe, nunca de atributo de estilo escrito em JavaScript'
      status: active
      verification: 'grep por literal de cor e por style= com cor nos dois núcleos, com comentários removidos, devolve 0'
    - statement: 'refrigeracao/ e mapa/xmap.js não são editados por este plano (D-19, D-13-03)'
      status: active
      verification: 'git diff --name-only -- refrigeracao/ mapa/xmap.js está vazio'
---

<objective>
Extrair kanban e calendário de `maquinas/` para `shared/kanban.js` e `shared/calendario.js`, e fazer
Máquinas consumi-los sem cópia local — fechando PLAT-08 e PLAT-09, que a Phase 8 tinha sido criada
para resolver e esta fase absorve.

Propósito: hoje o kanban e o calendário existem em um módulo só, misturados com DOM. Quando o
`/gestao` precisar dos dois (ações 5W2H em kanban, calendário consolidado), copiaria — e uma correção
num lado nunca chegaria ao outro. A extração é **refatoração, não mudança de comportamento**: a prova
disso são os dois gates existentes de Máquinas, que continuam passando sem uma linha mudada (D-08).

Saída: dois núcleos puros em `shared/`, `maquinas/operacoes.js` virando fachada, `renderOperacoes`/
`renderAgenda` reduzidos a aplicadores de DOM, e um gate novo. O CSS migra no plano 13-04.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/fase-13-gestao-qualidade/13-CONTEXT.md
@docs/fase-13-gestao-qualidade/13-PATTERNS.md
@docs/fase-13-gestao-qualidade/13-UI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Tarefa 1: shared/kanban.js e shared/calendario.js — núcleos genéricos</name>
  <files>shared/kanban.js, shared/calendario.js</files>
  <read_first>
    - `maquinas/operacoes.js` — arquivo inteiro (51 linhas): `STATUS_KANBAN`, `agruparOperacoes`,
      `criarEventosCalendario`, `validarOperacao`, `projetarUsoTotal` e o invólucro UMD
    - `maquinas/app.js` linhas 788-810 (`renderOperacoes`), 1135-1148 (`renderCartaoOperacao`) e
      1157-1183 (`renderAgenda`/`navegarAgenda`) — a marcação exata a preservar
    - `tests/operacoes-maquinas.test.js` — o contrato a preservar byte a byte, incluindo a forma do
      evento `{id, data, origem, titulo}` e a ordem esperada
    - `shared/tema.js` — o corte núcleo-puro/aplicador-de-DOM a repetir
    - `shared/componentes.js` — escape de texto e lista de tons
  </read_first>
  <behavior>
    - `agruparKanban([], [{id:'a'},{id:'b'}])` devolve um objeto com as duas chaves, ambas com array vazio
    - item cujo status não está na lista de colunas cai na **primeira** coluna, preservando a ordem de entrada
    - `agruparKanban(itens, [])` devolve objeto vazio e não lança
    - `htmlKanban` desenha a contagem de cada coluna inclusive quando é zero, e o bloco de coluna vazia com o rótulo recebido
    - metadado ausente no cartão não gera a linha correspondente — o texto "undefined" nunca aparece no HTML devolvido
    - `gradeMes(2026, 1)` (fevereiro de 2026) devolve 28 dias; `gradeMes(2024, 1)` devolve 29
    - `gradeMes` de um mês que começa no domingo devolve zero células vazias antes do dia 1
    - `gradeMes` devolve, para cada dia, a data em texto ISO com mês e dia preenchidos com zero à esquerda
    - `agruparPorData` ignora evento com data nula, vazia ou fora do formato ISO, sem lançar
    - `eventosDoMes(eventos, 2026, 7)` devolve só os de agosto de 2026, ordenados por data crescente
    - evento sem título recebe o rótulo da origem em vez de string vazia
    - `htmlCalendario` marca com a classe `hoje` a célula do dia recebido em `opcoes.hoje`, e nenhuma quando o dia não pertence ao mês
    - nenhuma das funções toca API de navegador: importar os dois módulos em Node e chamar tudo não lança
  </behavior>
  <action>
    Criar `shared/kanban.js` (D-06) e `shared/calendario.js` (D-07) como núcleos puros, cada um com
    cabeçalho-ensaio de três parágrafos numerados dizendo por que existe, o que não faz e a regra
    central — e, neste caso, de onde veio (extraído de `maquinas/`, PLAT-08 e PLAT-09).

    `shared/kanban.js` exporta:
    - `agruparKanban(itens, colunas, opcoes)` — `colunas` é `[{id, rotulo}]` e `opcoes` aceita
      `{campoStatus}` com padrão `'status'`; status desconhecido cai na primeira coluna, que é a
      regra que `agruparOperacoes` já aplica hoje (lá a primeira coluna é `programada`)
    - `htmlKanban(grupos, colunas, opcoes)` — `opcoes` traz `{cartao, vazio}`, onde `cartao` é uma
      função que o chamador fornece para desenhar cada item e `vazio` é o texto da coluna sem item
      (Máquinas passa "Nenhuma operação"; o genérico cai para "Nenhum item")

    A marcação devolvida é a mesma que `renderOperacoes` produz hoje: `<section class="kanban-col">`,
    `<div class="kanban-title"><span>{rótulo}</span><span class="kanban-count">{n}</span></div>`, e o
    bloco de coluna vazia usando a classe `empty`. Nenhuma classe nova.

    `shared/calendario.js` exporta:
    - `DIAS_SEMANA` e `MESES` — as duas listas de rótulos que hoje estão inline em `renderAgenda`
    - `gradeMes(ano, mes)` — devolve `{primeiroDiaSemana, totalDias, dias: [{dia, data}]}` (`mes` é
      base zero, como o resto do código de Máquinas já usa)
    - `agruparPorData(eventos)` — o `reduce` que hoje está dentro de `renderAgenda`, agora
      descartando data inválida em vez de criar uma chave lixo
    - `eventosDoMes(eventos, ano, mes)` — o filtro por prefixo de mês mais a ordenação por data,
      exatamente a lógica que hoje está no fim de `criarEventosCalendario`
    - `htmlCalendario(ano, mes, eventos, opcoes)` — devolve o texto do cabeçalho de dias da semana
      mais as células, com `opcoes` trazendo `{hoje, rotuloEvento, classeEvento}`

    A marcação devolvida é a mesma de hoje: `calendar-weekday`, `calendar-day`, `calendar-day is-empty`,
    `calendar-date`, `calendar-event` mais a classe de origem. A única adição é a classe `hoje` na
    célula do dia corrente, pedida pelo UI-SPEC para o calendário consolidado — Máquinas passa a
    recebê-la também, e é a única diferença visível desta extração.

    Todo texto de evento passa pelo mesmo escape de `shared/componentes.js`.
  </action>
  <acceptance_criteria>
    - `shared/kanban.js` contém `export function agruparKanban(` e `export function htmlKanban(`
    - `shared/calendario.js` contém `export function gradeMes(`, `export function agruparPorData(`,
      `export function eventosDoMes(` e `export function htmlCalendario(`
    - `grep -vE '^\s*(//|\*|/\*)' shared/kanban.js shared/calendario.js | grep -cE '\b(document|window|localStorage)\b'` = 0
    - `grep -vE '^\s*(//|\*|/\*)' shared/kanban.js shared/calendario.js | grep -cE '#[0-9a-fA-F]{3,8}|rgba?\(|hsl\('` = 0
    - `node -e "Promise.all([import('./shared/kanban.js'),import('./shared/calendario.js')]).then(([k,c])=>{if(c.gradeMes(2024,1).totalDias!==29)process.exit(1)})"` sai 0
  </acceptance_criteria>
  <verify>
    <automated>node --test tests/kanban-calendario-compartilhados.test.js</automated>
  </verify>
  <done>Os dois núcleos existem, são genéricos por definição de colunas e por lista de eventos, e devolvem a mesma marcação que Máquinas produz hoje.</done>
</task>

<task type="auto">
  <name>Tarefa 2: Máquinas passa a consumir os núcleos — fachada em operacoes.js e aplicadores em app.js</name>
  <files>maquinas/operacoes.js, maquinas/index.html, maquinas/app.js</files>
  <read_first>
    - `maquinas/operacoes.js` — o invólucro UMD atual e as quatro funções exportadas
    - `maquinas/index.html` linhas 990-1000 — as duas etiquetas de script (`/maquinas/operacoes.js`
      clássica na 994, `/maquinas/app.js` como módulo na 995)
    - `maquinas/app.js` linhas 1-20 (os imports já existentes de `../shared/`), 788-810, 1135-1148 e
      1157-1183
    - `tests/integracao-operacoes-maquinas.test.js` — arquivo inteiro (53 linhas): ele exige que
      `/maquinas/operacoes.js` apareça no HTML **antes** de `/maquinas/app.js`, que os ids
      `view-agenda`, `operacoes-kanban`, `agenda-calendario`, `modal-operacao`, `modal-area` e
      `modal-concluir-operacao` continuem no HTML, que `operacoes-kanban` fique dentro do bloco de
      `view-os`, e que `function renderOperacoes(` e `function renderAgenda(` continuem em app.js
    - `tests/operacoes-maquinas.test.js` — ele carrega `maquinas/operacoes.js` por `require`
    - `tests/mobile-375.test.js` linhas 173-181 — exige que `maquinas/index.html` continue tendo uma
      `@media(max-width:NNNpx)` que mencione o kanban
  </read_first>
  <action>
    Caminho escolhido (a decisão que o PATTERNS.md pedia para ser resolvida explicitamente): **(b)**
    — `maquinas/operacoes.js` deixa de ser UMD e vira **módulo ES** que importa de `shared/kanban.js`
    e `shared/calendario.js`, continuando a publicar `OperacoesMaq` no escopo global; e
    `maquinas/index.html` linha 994 passa a carregar esse arquivo com `type="module"`.

    O caminho (a) — manter o invólucro UMD sem importar nada — foi descartado porque deixaria a cópia
    da lógica dentro de Máquinas, que é exatamente o que GEQ-04 proíbe. As duas etiquetas de script
    continuam sendo módulos adiados, executados na ordem do documento, então `OperacoesMaq` existe
    antes de qualquer função de `app.js` rodar.

    **Compatibilidade CommonJS — por que o gate continua verde.** O arquivo **não** mantém invólucro
    UMD e **não** escreve no objeto de exportação do CommonJS: fica só com `import`, `export` e a
    publicação em `globalThis`. O que mantém `tests/operacoes-maquinas.test.js` verde, com o seu
    `require(moduloPath)` e a desestruturação das quatro funções, é o suporte **nativo** do Node
    ≥ 22.12 a carregar módulo ES por `require` — a chamada devolve o espaço de nomes do módulo, de
    onde a desestruturação tira `validarOperacao`, `agruparOperacoes`, `criarEventosCalendario` e
    `STATUS_KANBAN`. Conferido no Node 24.18 deste repositório, e já em produção no projeto:
    `shared/tabela.js` é módulo ES puro (só `export`) e `tests/tabela-compartilhada.test.js` o carrega
    por `require('../shared/tabela.js')` desde a quick-260818-vtm.

    Duas condições sustentam isso, e as duas são obrigações deste plano:
    - `maquinas/operacoes.js` **não pode ganhar** espera de nível de topo (`await` fora de função). Um
      módulo com espera de topo não é carregável por `require`, e essa é a única forma de quebrar o
      gate sem tocar no gate. As quatro funções são síncronas e continuam síncronas.
    - o projeto não tem `package.json`, então um arquivo `.js` é classificado **pela sintaxe**: é a
      presença de `import`/`export` que faz o mesmo arquivo ser carregável tanto por
      `<script type="module">` no navegador quanto por `require()` em Node. Nada de `.mjs`, nada de
      `package.json` novo — as duas coisas mudariam o resto do repositório.

    Em `maquinas/operacoes.js`:
    - manter `STATUS_KANBAN` (é a definição concreta de Máquinas, e é aqui que ela deve ficar),
      acrescentando ao lado a lista de colunas com rótulo, na mesma ordem e com os mesmos rótulos
      que `renderOperacoes` usa hoje: "Programadas", "Em execução", "Concluídas", "Canceladas"
    - `agruparOperacoes(operacoes)` passa a ser uma linha delegando para `agruparKanban`
    - `criarEventosCalendario(operacoes, ordensServico, ano, mes)` passa a montar as duas listas de
      evento (a mesma forma de hoje: `{id, data, origem, titulo}`, origem `operacao` e `os`, título
      caindo para "Operação" e "OS") e delegar o filtro e a ordenação para `eventosDoMes`
    - `validarOperacao` e `projetarUsoTotal` ficam byte a byte como estão — não têm nada a ver com
      esta extração
    - exportar as quatro funções por `export`, e publicar o mesmo objeto em `globalThis.OperacoesMaq`
      para que `maquinas/app.js` continue chamando `OperacoesMaq.validarOperacao` e
      `OperacoesMaq.projetarUsoTotal` sem alteração

    Em `maquinas/index.html`, duas alterações e só duas:
    - a linha 994 ganha `type="module"`; a ordem em relação a `/maquinas/app.js` não muda
    - as regras-base de kanban e de calendário saem do `<style>` do módulo, porque passam a viver na
      folha comum (D-10; quem as escreve lá é o plano 13-04, nesta mesma onda). Saem exatamente
      estas, na faixa de linhas 73-92: `.kanban`, `.kanban-col`, `.kanban-title`, `.kanban-count`,
      `.op-card`, `.op-card:last-child`, `.op-name`, `.op-meta`, `.op-actions`, `.calendar-head`,
      `.calendar-title`, `.calendar-grid`, `.calendar-weekday,.calendar-day`, `.calendar-weekday`,
      `.calendar-day`, `.calendar-day.is-empty`, `.calendar-date`, `.calendar-event`,
      `.calendar-event.op`, `.calendar-event.os`

    **Ficam** em `maquinas/index.html`, intocadas: `.ops-actions`, `.section-spaced` (são layout de
    aba, não do componente) e o bloco `@media(max-width:600px)` inteiro, byte a byte — este último
    porque `tests/mobile-375.test.js` exige que a `@media` própria de Máquinas continue existindo, e
    porque ele é o ajuste responsivo daquele módulo, não do componente. O `<style>` do módulo é
    carregado depois de `../shared/pmoc.css`, então a `@media` continua vencendo as regras-base que
    migraram.

    Em `maquinas/app.js`:
    - `renderOperacoes()` mantém o nome, os ids que lê (`operacoes-kanban`, `tb-areas`,
      `operacoes-kpis`), o ramo de erro de migração e a chamada final a `renderAreas()`; a montagem
      das colunas passa a ser `htmlKanban(...)` com `renderCartaoOperacao` como função de cartão e
      "Nenhuma operação" como texto de coluna vazia
    - `renderAgenda()` mantém o nome e o id que lê (`agenda-calendario`, `agenda-titulo`); deixa de
      montar cabeçalho e células à mão e passa a injetar o texto devolvido por `htmlCalendario`,
      passando a data de hoje; o título do mês passa a usar a lista `MESES` importada
    - `navegarAgenda` fica como está
    - acrescentar os imports de `../shared/kanban.js` e `../shared/calendario.js` no bloco de imports
      do topo, junto dos que já existem
  </action>
  <acceptance_criteria>
    - `node --test tests/operacoes-maquinas.test.js tests/integracao-operacoes-maquinas.test.js` sai 0
      **e** `git diff --quiet HEAD -- tests/operacoes-maquinas.test.js tests/integracao-operacoes-maquinas.test.js` sai 0 — os dois gates passam intocados (D-08)
    - `node -e "const m=require('./maquinas/operacoes.js'); const {validarOperacao,agruparOperacoes,criarEventosCalendario,STATUS_KANBAN}=m; if(typeof validarOperacao!=='function'||typeof agruparOperacoes!=='function'||typeof criarEventosCalendario!=='function'||!Array.isArray(STATUS_KANBAN)) process.exit(1)"` sai 0 — a carga por `require` do módulo ES devolve as quatro
    - `grep -c "module.exports" maquinas/operacoes.js` = 0 — sem invólucro UMD remanescente
    - `grep -c "^await\|^  await" maquinas/operacoes.js` = 0 — sem espera de nível de topo, que tiraria o arquivo do alcance de `require`
    - `git diff --stat tests/operacoes-maquinas.test.js tests/integracao-operacoes-maquinas.test.js tests/mobile-375.test.js tests/chrome-icones.test.js` está vazio
    - `grep -c 'src="/maquinas/operacoes.js" type="module"\|type="module" src="/maquinas/operacoes.js"' maquinas/index.html` = 1
    - `grep -c '@media(max-width:600px)' maquinas/index.html` = 1
    - `grep -cE '^\.(kanban|op-card|op-name|op-meta|op-actions|calendar-)' maquinas/index.html` = 0
      (as regras-base migraram; as de dentro da `@media` são indentadas e não casam com o começo de linha)
    - `grep -c '.kanban{grid-template-columns:repeat(4,260px)}' maquinas/index.html` = 1 — o ajuste responsivo do módulo continua onde estava
    - `grep -cE '^\.(ops-actions|section-spaced)' maquinas/index.html` = 2
    - `grep -c "from '../shared/kanban.js'" maquinas/operacoes.js` = 1 e `grep -c "from '../shared/calendario.js'" maquinas/operacoes.js` = 1
    - `grep -c 'globalThis.OperacoesMaq' maquinas/operacoes.js` = 1
    - `grep -vE '^\s*(//|\*|/\*)' maquinas/operacoes.js | grep -cE '\bObject\.fromEntries\b|\bpadStart\b'` = 0
    - `grep -c 'function renderOperacoes(' maquinas/app.js` = 1 e `grep -c 'function renderAgenda(' maquinas/app.js` = 1
  </acceptance_criteria>
  <verify>
    <automated>node --test</automated>
  </verify>
  <done>Máquinas desenha kanban e agenda a partir de `shared/`, sem lógica duplicada, e os quatro gates existentes que o cercam passam sem terem sido tocados.</done>
</task>

<task type="auto">
  <name>Tarefa 3: gate dos dois núcleos e da fachada</name>
  <files>tests/kanban-calendario-compartilhados.test.js</files>
  <read_first>
    - `tests/tabela-compartilhada.test.js` — estrutura a copiar
    - `tests/operacoes-maquinas.test.js` — o contrato que este gate NÃO repete (não duplicar caso), mas
      cujo estilo de dado sintético serve de modelo
    - `shared/kanban.js` e `shared/calendario.js` (criados na tarefa 1)
    - `docs/fase-13-gestao-qualidade/13-UI-SPEC.md` — linhas de `kanban.js` e `calendario.js`
      na tabela `## UI Considerations`
  </read_first>
  <action>
    Criar `tests/kanban-calendario-compartilhados.test.js` cobrindo, por comportamento em Node, um
    caso por item declarado na tarefa 1 — com atenção aos que são fáceis de errar na extração:
    fevereiro bissexto, mês que começa no domingo, data inválida descartada em vez de virar chave,
    evento sem título caindo para o nome da origem, status desconhecido caindo na primeira coluna,
    lista de colunas vazia, e contagem zero continuando visível.

    O mesmo arquivo protege a fachada por estrutura, e só por estrutura (o comportamento já é
    protegido pelos dois gates existentes de Máquinas): que `maquinas/operacoes.js` importe os dois
    núcleos, que não tenha ficado com a lógica duplicada, que publique `OperacoesMaq` no escopo
    global, e que `maquinas/index.html` carregue `/maquinas/operacoes.js` como módulo, antes de
    `/maquinas/app.js`, e continue com a sua própria `@media`.

    Um caso final confere a lição de D-8yc-01 na direção estrutural possível em texto: nenhuma regra
    de `maquinas/index.html` remanescente aplica recorte de conteúdo ao contêiner do kanban ou da
    grade do calendário.

    Comentário de gate no topo, no estilo dos existentes, dizendo que este arquivo protege a extração
    de PLAT-08/PLAT-09 e que os dois gates antigos de Máquinas são a outra metade da prova.
  </action>
  <acceptance_criteria>
    - `node --test tests/kanban-calendario-compartilhados.test.js` sai 0 com pelo menos 12 casos
    - `node --test` reporta 0 falhas e total maior que o baseline de 1095
    - `git diff --name-only tests/` lista apenas `tests/kanban-calendario-compartilhados.test.js`
    - `git diff --name-only -- refrigeracao/ mapa/xmap.js` está vazio
  </acceptance_criteria>
  <verify>
    <automated>node --test</automated>
  </verify>
  <done>O gate novo passa, os 66 arquivos de teste existentes continuam verdes e nenhum deles foi tocado.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| banco → HTML do kanban | `tipo_servico`, `operador`, nome da área e código da máquina vêm de `maq_operacoes`/`maq_areas`/`maq_ativos` e entram no cartão |
| banco → HTML do calendário | título de operação e de OS entram na célula do dia |
| escopo global do navegador | `OperacoesMaq` continua publicado em `globalThis` para `maquinas/app.js` |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-09 | Tampering | `shared/kanban.js` / `shared/calendario.js` (HTML devolvido) | high | mitigate | todo texto vindo de fora passa pelo escape de `shared/componentes.js`; a função de cartão é fornecida pelo chamador e o gate confere que o núcleo escapa o que ele mesmo escreve (rótulo de coluna, título de evento) |
| T-13-10 | Tampering | `globalThis.OperacoesMaq` | medium | accept | a publicação global já existe hoje e não é ampliada; qualquer script capaz de sobrescrevê-la já teria execução na página, onde o dano não depende deste objeto — a autoridade real continua sendo a RLS do Postgres |
| T-13-11 | Elevation of Privilege | botões "Iniciar"/"Concluir" no cartão do kanban | medium | mitigate | a função de cartão continua sendo `renderCartaoOperacao` de Máquinas, que já condiciona os botões a `podeEditarOperacoes()`; o núcleo genérico não decide permissão e a policy de `maq_operacoes` continua sendo a autoridade |
| T-13-12 | Denial of Service | `gradeMes` com ano/mês fora de faixa | low | mitigate | entrada não numérica ou mês fora de 0..11 devolve grade vazia em vez de laço sem fim, com caso de gate |
| T-13-SC | Tampering | instalação de pacote (npm/pip/cargo) | high | accept | esta fase não instala pacote nenhum — projeto zero-build, sem `package.json`; não há dependência a auditar |
</threat_model>

## Decisões deste plano

**D-13-03-A — `maquinas/operacoes.js` vira módulo ES puro, sem invólucro UMD.** `13-PATTERNS.md`
(seção "Restrição estrutural importante para o planner") deixou a escolha entre (a) manter o UMD sem
importar de `shared/` e (b) converter para módulo ES com `type="module"` na etiqueta de script,
exigindo que fosse resolvida explicitamente porque decide se `tests/operacoes-maquinas.test.js`
continua funcionando. Escolhido **(b)**: (a) deixaria a lógica duplicada dentro de Máquinas, que é o
que GEQ-04 proíbe. A preocupação registrada no PATTERNS — o `require()` CommonJS do gate — está
resolvida pelo suporte nativo do Node ≥ 22.12 a `require(esm)`, conferido no Node 24.18 deste
repositório e já em produção no projeto (`shared/tabela.js`, módulo ES puro, é carregado por
`require` em `tests/tabela-compartilhada.test.js`). A condição de validade — o arquivo nunca ganhar
espera de nível de topo — é obrigação escrita da tarefa 2 e tem critério de aceite próprio.

## Artifacts this phase produces

**Arquivos novos**
- `shared/kanban.js`
- `shared/calendario.js`
- `tests/kanban-calendario-compartilhados.test.js`

**Arquivos modificados**
- `maquinas/operacoes.js` — de UMD com lógica própria para módulo ES fachada sobre `shared/`
- `maquinas/index.html` — linha 994 ganha `type="module"`; as 20 regras-base de kanban e calendário saem do `<style>` do módulo (a `@media(max-width:600px)`, `.ops-actions` e `.section-spaced` ficam)
- `maquinas/app.js` — `renderOperacoes` e `renderAgenda` viram aplicadores de DOM; dois imports novos

**Funções e constantes exportadas**
- `shared/kanban.js`: `agruparKanban`, `htmlKanban`
- `shared/calendario.js`: `DIAS_SEMANA`, `MESES`, `gradeMes`, `agruparPorData`, `eventosDoMes`, `htmlCalendario`
- `maquinas/operacoes.js` (superfície preservada): `validarOperacao`, `agruparOperacoes`, `criarEventosCalendario`, `projetarUsoTotal`, `STATUS_KANBAN`, mais `globalThis.OperacoesMaq`

**Classe CSS nova emitida** (o CSS é escrito no plano 13-04)
`hoje` (aplicada a `.calendar-day`) — todas as demais classes de kanban e calendário são as já existentes

**Sem tabela, coluna, id HTML ou rota nova neste plano.**

<verification>
1. `node --test` — 0 falhas, total maior que o baseline de 1095
2. `git diff --stat tests/` mostra apenas o arquivo novo — os quatro gates que cercam Máquinas (`operacoes-maquinas`, `integracao-operacoes-maquinas`, `mobile-375`, `chrome-icones`) não foram tocados (critério 2 do ROADMAP, D-08)
3. `grep -c '@media(max-width:600px)' maquinas/index.html` = 1 — a `@media` própria de Máquinas continua no módulo
4. `git diff --name-only -- refrigeracao/ mapa/xmap.js` vazio (D-19, D-13-03)
5. `shared/pmoc.css` não foi tocado por este plano
</verification>

<success_criteria>
- Máquinas consome kanban e calendário de `shared/` sem cópia local, e `tests/operacoes-maquinas.test.js` e `tests/integracao-operacoes-maquinas.test.js` seguem passando sem uma linha mudada (critério 2 do ROADMAP — fecha PLAT-08 e PLAT-09)
- `shared/kanban.js` e `shared/calendario.js` são núcleos puros genéricos, com gate próprio (critério 1 do ROADMAP; GEQ-04)
- Nenhum módulo perde funcionalidade: ids, classes, ordem de eventos e textos de tela idênticos (PLAT-16)
- `node --test` verde
</success_criteria>

<output>
Criar `docs/fase-13-gestao-qualidade/13-03-SUMMARY.md` ao terminar
</output>
