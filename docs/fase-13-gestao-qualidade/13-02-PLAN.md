---
phase: 13-gest-o-e-qualidade
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - shared/gantt.js
  - shared/abc.js
  - shared/gut.js
  - predial/dominio.js
  - tests/gantt-compartilhado.test.js
  - tests/abc-compartilhado.test.js
  - tests/gut-compartilhado.test.js
autonomous: true
requirements: [GEQ-03, GEQ-05, GEQ-07]

must_haves:
  truths:
    - 'Nenhum item no período → vazio("Nenhuma ação no período", "Ajuste o intervalo ou cadastre uma ação")'
    - 'Gantt: quem chama desenha vazio() até os dados chegarem — o núcleo não tem estado de carregamento'
    - 'linhasGantt() com item sem data de início → item omitido e contado no retorno ({ignorados:N}); nunca lança'
    - 'Uma linha por item, barra com inicioPct/larguraPct, marca "Hoje" em var(--accent) atravessando as linhas'
    - 'Item sem fim → barra até hoje com rótulo "em aberto" (D-13-04)'
    - 'Gantt: 0 → vazio(); 1 → uma linha; N → N linhas na mesma grade; sem cópia singular/plural (o rótulo é o nome da ação)'
    - 'G/U/T nulos → total vazio e faixa "Não avaliado" (null diferente de 0, mesmo padrão D-500-02)'
    - 'GUT: selects desenhados com o item; sem estado de carregamento próprio'
    - 'Valor fora de GUT_ESCALA é recusado por classificarGut()/gutTotal() (devolve null), nunca lança'
    - 'Uma dimensão preenchida e outras nulas → total não calculado, faixa "Não avaliado"'
    - 'GUT: rótulos fixos (G/U/T e faixas de uma palavra) — não há texto livre'
    - 'Lista vazia → vazio("Nenhum item para classificar", "Cadastre itens com valor para gerar a curva")'
    - 'ABC: quem chama desenha vazio() até os dados chegarem — o núcleo não tem estado de carregamento'
    - 'Item com valor não numérico entra como 0 e vai para C; nunca lança'
    - 'Itens ordenados por valor desc, acumulado %, classe A/B/C por cortes [0.8, 0.95], pílula por tom'
    - 'ABC: tabela em .tbl-wrap rola horizontalmente; barra de acumulado em % nunca excede 100'
    - 'ABC: 0 → vazio(); 1 → uma linha classe A; N → curva completa'
    - { statement: 'Área de barras rola dentro do contêiner (.tbl-wrap-like) com coluna de rótulos sticky — conferir que o contêiner pai não tem overflow:hidden (lição D-8yc-01)', verification: backstop }
    - { statement: 'Nome de ação longo na coluna sticky — quebra natural, sem ellipsis; conferir na tela', verification: backstop }
    - { statement: '1 item → tudo A (100% >= 95%) — comportamento matemático correto; caso-limite coberto em tests/abc-compartilhado.test.js', verification: backstop }
    - { statement: 'Nome de material longo na tabela — conferir truncagem com title (regra D-8yc-10)', verification: backstop }
    - 'Assunção explícita (edge probe devolveu GEQ-03 como unclassified): os casos-limite de GEQ-03 são item sem data de fim, item sem data de início, item inteiramente fora do intervalo pedido, item que começa antes do intervalo e termina dentro, e intervalo de um único dia (largura não pode ser zero nem infinita) — cada um com caso próprio em tests/gantt-compartilhado.test.js'
    - 'Assunção explícita (edge probe devolveu GEQ-05 como unclassified): os casos-limite de GEQ-05 são dimensão nula (que não é zero), dimensão fora de GUT_ESCALA, total nulo na classificação, e as quatro fronteiras já testadas do legado (100/101 e 400/401) que precisam continuar idênticas — casos próprios em tests/gut-compartilhado.test.js, sem alterar tests/predial-dominio.test.js'
  artifacts:
    - shared/gantt.js
    - shared/abc.js
    - shared/gut.js
    - tests/gantt-compartilhado.test.js
    - tests/abc-compartilhado.test.js
    - tests/gut-compartilhado.test.js
  key_links:
    - 'predial/dominio.js deixa de declarar GUT_ESCALA e classificarGut e passa a reexportá-los de shared/gut.js — mesmo precedente literal já presente no arquivo para montarArvore/linhasVisiveis'
    - 'tests/predial-dominio.test.js continua passando sem uma linha mudada: ele importa predial/dominio.js por import() dinâmico, e o reexport preserva a superfície'
    - 'shared/gantt.js lê o tom da etapa de quem chama (shared/fluxo.js#tomDaEtapa no consumidor), nunca declara uma cor nem um vocabulário de estado próprio'
    - 'As classes emitidas (.gantt-*, .abc-*) são resolvidas por shared/pmoc.css, escrito no plano 13-04'
  prohibitions:
    - statement: 'A extração do GUT não muda o comportamento numérico de classificarGut para nenhuma entrada numérica — 0, 100, 101, 400, 401 e 1000 devolvem exatamente o que devolviam antes'
      status: active
      verification: 'tests/predial-dominio.test.js roda sem alteração e continua verde; tests/gut-compartilhado.test.js repete as mesmas seis fronteiras contra shared/gut.js'
    - statement: 'predial/dominio.js não mantém cópia local de GUT_ESCALA nem de classificarGut — reexporta, não duplica (GEQ-05)'
      status: active
      verification: 'grep por "export const GUT_ESCALA" e "export function classificarGut" em predial/dominio.js devolve 0; grep pelo reexport de ../shared/gut.js devolve 1'
    - statement: 'O Gantt não usa canvas, não usa biblioteca de gráfico e não inventa cor — as barras são elementos posicionados em porcentagem por CSS grid (D-13-04)'
      status: active
      verification: 'grep por canvas e por literal de cor em shared/gantt.js, com comentários removidos, devolve 0'
    - statement: 'shared/abc.js não conhece nenhum campo de domínio — o acessor de valor é sempre parâmetro, nunca um nome de coluna escrito no núcleo'
      status: active
      verification: 'grep por preco, custo, valor_unit, material e equipamento em shared/abc.js, com comentários removidos, devolve 0'
---

<objective>
Criar mais três núcleos puros da Onda A — `shared/gantt.js` (linha do tempo em CSS grid),
`shared/abc.js` (curva ABC genérica) e `shared/gut.js` (a matriz GUT que hoje vive só no Predial) —
e fazer `predial/dominio.js` reexportar o GUT em vez de declará-lo.

Propósito: o Gantt e a curva ABC não existem em lugar nenhum do repositório e seriam escritos duas
vezes assim que dois módulos precisassem deles; o GUT já existe, mas dentro de um módulo, o que
obrigaria o `/gestao` a copiá-lo para priorizar as ações 5W2H. D-13-02 diz que ele sobe e o Predial
reexporta — o mesmo movimento que `shared/arvore.js` já fez neste mesmo arquivo.

Saída: três arquivos em `shared/`, um reexport de uma linha em `predial/dominio.js`, três gates novos.
`tests/predial-dominio.test.js` fica sem uma linha mudada e continua verde (critério 3 do ROADMAP).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/fase-13-gestao-qualidade/13-CONTEXT.md
@docs/fase-13-gestao-qualidade/13-PATTERNS.md
@docs/fase-13-gestao-qualidade/13-UI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Tarefa 1: shared/gut.js e o reexport em predial/dominio.js</name>
  <files>shared/gut.js, predial/dominio.js</files>
  <read_first>
    - `predial/dominio.js` — arquivo inteiro (21 linhas): `GUT_ESCALA`, `classificarGut` e a linha
      final que já reexporta `montarArvore`/`linhasVisiveis` de `../shared/arvore.js`, com o
      comentário explicando o porquê — é exatamente o formato a repetir
    - `tests/predial-dominio.test.js` — o contrato a preservar: seis fronteiras numéricas de
      `classificarGut` (0, 100, 101, 400, 401, 1000); este arquivo **não pode ser alterado**
    - `shared/arvore.js` — o precedente de núcleo extraído que o Predial já consome
    - `predial/app.js` linha 9 (o import) e o trecho que usa `GUT_ESCALA` (por volta da linha 554),
      para confirmar que a superfície importada continua a mesma
  </read_first>
  <behavior>
    - `classificarGut(0)`, `(100)` devolvem `'ok'`; `(101)`, `(400)` devolvem `'atencao'`; `(401)`, `(1000)` devolvem `'critico'` — idêntico ao legado
    - `classificarGut(null)` e `classificarGut(undefined)` devolvem `null` — não avaliado não é "ok"
    - `classificarGut('muito')` devolve `null`, não lança
    - `gutTotal(6, 8, 10)` devolve 480; `gutTotal(0, 0, 0)` devolve 0 (zero é uma avaliação, não uma ausência)
    - `gutTotal(6, null, 10)` devolve `null` — uma dimensão faltando não vira total parcial
    - `gutTotal(6, 5, 10)` devolve `null` — 5 não está em `GUT_ESCALA`
    - `rotuloGut(null)` devolve o texto "Não avaliado"; `rotuloGut(480)` devolve o rótulo da faixa crítica
    - `GUT_ESCALA` continua sendo exatamente `[0, 1, 3, 6, 8, 10]`, na mesma ordem
    - importar `predial/dominio.js` continua entregando `GUT_ESCALA`, `classificarGut`, `montarArvore` e `linhasVisiveis`
  </behavior>
  <action>
    Criar `shared/gut.js` com o cabeçalho-ensaio no estilo de `shared/tabela.js`/`shared/componentes.js`
    (por que existe, o que não faz, a regra central), movendo `GUT_ESCALA` e `classificarGut` de
    `predial/dominio.js` **sem alterar o comportamento numérico** (D-09, D-13-02), e acrescentando
    `gutTotal(g, u, t)` e `rotuloGut(total)`.

    `gutTotal` devolve `null` quando qualquer dimensão for nula, indefinida ou estiver fora de
    `GUT_ESCALA` — nunca lança, nunca calcula um total parcial. `null` é "não avaliado" e é diferente
    de `0`, exatamente como os atributos técnicos de `/refrigeracao` já tratam (D-500-02): um total
    parcial afirmaria uma priorização que ninguém fez.

    `classificarGut` mantém as três faixas do legado para qualquer número (`> 400` crítico,
    `> 100` atenção, resto ok) e passa a devolver `null` para entrada nula, indefinida ou não
    numérica — acréscimo que não toca nenhuma das seis fronteiras já testadas.

    `rotuloGut(total)` devolve o texto de tela por faixa, com "Não avaliado" para `null`; os rótulos
    das três faixas são de uma palavra, sem texto livre.

    Em seguida, editar `predial/dominio.js`: apagar a declaração de `GUT_ESCALA` e de
    `classificarGut` e pôr no lugar um reexport de `../shared/gut.js`, precedido de um comentário
    curto explicando o porquê — no mesmo estilo e no mesmo lugar da linha que já reexporta
    `montarArvore`/`linhasVisiveis`. O reexport precisa entregar `GUT_ESCALA`, `classificarGut` e
    também `gutTotal`, para que o Predial possa consumir o total quando quiser.

    Não alterar `predial/app.js` — a superfície importada por ele continua idêntica.
  </action>
  <acceptance_criteria>
    - `node --test tests/predial-dominio.test.js` sai 0 **e** `git diff --stat tests/predial-dominio.test.js` está vazio
    - `grep -c 'export const GUT_ESCALA' predial/dominio.js` = 0
    - `grep -c 'export function classificarGut' predial/dominio.js` = 0
    - `grep -c "from '../shared/gut.js'" predial/dominio.js` = 1
    - `grep -c 'export const GUT_ESCALA' shared/gut.js` = 1 e `grep -c 'export function gutTotal' shared/gut.js` = 1
    - `git diff --stat predial/app.js` está vazio
  </acceptance_criteria>
  <verify>
    <automated>node --test tests/predial-dominio.test.js tests/gut-compartilhado.test.js</automated>
  </verify>
  <done>O GUT vive em `shared/gut.js`, o Predial reexporta sem cópia local, e o gate do Predial passa sem ter sido tocado.</done>
</task>

<task type="auto" tdd="true">
  <name>Tarefa 2: shared/gantt.js e shared/abc.js</name>
  <files>shared/gantt.js, shared/abc.js</files>
  <read_first>
    - `shared/componentes.js` — `vazio()`, `pilula()`, a lista de tons e a função de escape; o Gantt
      vazio e a curva ABC vazia reusam `vazio()` com os textos exatos do UI-SPEC
    - `shared/tabela.js` — a disciplina de "genérico por definição, nunca por nome de campo";
      `classificarAbc` recebe o acessor de valor como parâmetro pela mesma razão
    - `maquinas/operacoes.js` — `criarEventosCalendario`: o estilo funcional puro (map/filter/sort com
      comparação de data em texto ISO) que o Gantt deve seguir
    - `docs/fase-13-gestao-qualidade/13-UI-SPEC.md` — seções `shared/gantt.js` e
      `shared/abc.js` do contrato detalhado, e a seção Color (classe A usa accent, B info, C neutro)
  </read_first>
  <behavior>
    - `linhasGantt([], {inicio:'2026-09-01', fim:'2026-09-30'})` devolve `{linhas: [], ignorados: 0, hojePct: <número ou null>}`
    - item com `inicio` nulo ou não-ISO é omitido e somado em `ignorados`; a função não lança
    - item com `fim` nulo recebe `aberto: true` e a barra termina na marca de hoje
    - item inteiramente fora do intervalo é omitido e contado em `ignorados`
    - item que começa antes de `inicio` é recortado: `inicioPct` é 0 e a largura cobre só a parte dentro do intervalo
    - toda barra tem `0 <= inicioPct` e `inicioPct + larguraPct <= 100`, e `larguraPct > 0` mesmo para item de um único dia
    - intervalo degenerado (`inicio` igual a `fim`) não produz divisão por zero: devolve largura 100 para o item daquele dia
    - `hojePct` é `null` quando hoje está fora do intervalo — a marca não é desenhada fora da linha do tempo
    - `htmlGantt` com lista vazia devolve o HTML de `vazio()` com os dois textos do contrato
    - `classificarAbc([], acessor)` devolve `{linhas: [], total: 0}`
    - `classificarAbc` com um único item devolve uma linha classe `'A'` com acumulado 100
    - item com valor não numérico entra como 0 e cai na classe `'C'`; a função não lança
    - as linhas saem ordenadas por valor decrescente e o acumulado da última é 100 (dentro de tolerância de ponto flutuante)
    - cortes personalizados são respeitados; `cortes` fora de ordem ou fora de 0..1 caem no padrão `[0.8, 0.95]`
    - total zero (todos os valores nulos) não divide por zero: todas as linhas saem classe `'C'` com acumulado 0
  </behavior>
  <action>
    Criar `shared/gantt.js` (D-03, D-13-04) e `shared/abc.js` (D-04) como núcleos puros, cada um com
    cabeçalho-ensaio de três parágrafos numerados no formato de `shared/componentes.js`.

    `shared/gantt.js` exporta:
    - `linhasGantt(itens, opcoes)` — `itens` é `[{id, rotulo, inicio, fim, tom}]` com datas em texto
      ISO `AAAA-MM-DD`; `opcoes` é `{inicio, fim, hoje}`; devolve
      `{linhas: [{id, rotulo, inicioPct, larguraPct, aberto, tom}], hojePct, ignorados}`
    - `htmlGantt(itens, opcoes)` — devolve HTML em CSS grid, sem canvas e sem biblioteca

    A posição é sempre porcentagem do intervalo total, calculada por diferença de dias entre datas
    ISO — nunca pixel fixo. O item sem fim usa hoje como fim e leva `aberto: true`, desenhado com o
    rótulo auxiliar "em aberto" ao lado da barra. A marca de hoje é uma linha vertical atravessando
    todas as linhas, com o rótulo "Hoje"; é o único elemento accent do gráfico. O tom da barra vem
    do item (`tom`), resolvido pelo chamador a partir de `shared/fluxo.js` — o Gantt não conhece
    nenhum vocabulário de estado.

    `shared/abc.js` exporta `classificarAbc(itens, campoValor, cortes = [0.8, 0.95])`, puro e **sem
    HTML** (a peça visual é do consumidor). `campoValor` é uma função acessora ou o nome de um campo;
    nunca um nome de coluna escrito dentro do núcleo. Devolve
    `{linhas: [{item, valor, participacao, acumulado, classe}], total}` com `classe` em `'A'`/`'B'`/`'C'`.

    Nomes de classe emitidos por `htmlGantt` (contrato com o plano 13-04): `gantt`, `gantt-rotulos`,
    `gantt-rotulo`, `gantt-linhas`, `gantt-linha`, `gantt-barra`, `gantt-aberto`, `gantt-hoje`,
    `gantt-hoje-rotulo`, e os tons `gantt-tom-neutro`, `gantt-tom-info`, `gantt-tom-ok`,
    `gantt-tom-warn`, `gantt-tom-erro`. Nomes reservados para a curva ABC, usados pelos consumidores
    e escritos no plano 13-04: `abc`, `abc-linha`, `abc-barra`, `abc-classe-a`, `abc-classe-b`,
    `abc-classe-c`.
  </action>
  <acceptance_criteria>
    - `shared/gantt.js` contém `export function linhasGantt(` e `export function htmlGantt(`
    - `shared/abc.js` contém `export function classificarAbc(`
    - `grep -vE '^\s*(//|\*|/\*)' shared/gantt.js | grep -cE '\b(document|window|localStorage|canvas)\b'` = 0
    - `grep -vE '^\s*(//|\*|/\*)' shared/gantt.js | grep -cE '#[0-9a-fA-F]{3,8}|rgba?\(|hsl\('` = 0
    - `grep -vE '^\s*(//|\*|/\*)' shared/abc.js | grep -ciE '\b(preco|custo|valor_unit|material|equipamento)\b'` = 0
    - `grep -c 'Nenhuma ação no período' shared/gantt.js` = 1 e `grep -c 'em aberto' shared/gantt.js` >= 1
  </acceptance_criteria>
  <verify>
    <automated>node --test tests/gantt-compartilhado.test.js tests/abc-compartilhado.test.js</automated>
  </verify>
  <done>Gantt e curva ABC existem como núcleos puros genéricos, com os casos degenerados (item sem fim, item fora do intervalo, intervalo de um dia, total zero, um item só) resolvidos e testados.</done>
</task>

<task type="auto">
  <name>Tarefa 3: gates de comportamento dos três núcleos</name>
  <files>tests/gantt-compartilhado.test.js, tests/abc-compartilhado.test.js, tests/gut-compartilhado.test.js</files>
  <read_first>
    - `tests/tabela-compartilhada.test.js` — estrutura a copiar (Node puro, dado sintético dentro do
      próprio teste, comentário de gate no topo dizendo o que protege)
    - `tests/predial-dominio.test.js` — as seis fronteiras de `classificarGut` a repetir contra
      `shared/gut.js`, para que a regra fique provada dos dois lados
    - `shared/gantt.js`, `shared/abc.js`, `shared/gut.js` (criados nas tarefas 1 e 2)
    - `docs/fase-13-gestao-qualidade/13-UI-SPEC.md` — linhas de `gantt.js`, `gut.js` e
      `abc.js` na tabela `## UI Considerations`
  </read_first>
  <action>
    Criar os três gates, testando comportamento por importação em Node, nunca expressão regular sobre
    o texto do arquivo.

    `tests/gut-compartilhado.test.js` repete as seis fronteiras numéricas do legado contra
    `shared/gut.js` e acrescenta os casos que não existiam: total nulo, total não numérico, dimensão
    nula, dimensão fora da escala, e a distinção entre `0` (avaliado como zero) e `null` (não
    avaliado). Um caso a mais confere que importar `predial/dominio.js` continua entregando
    `GUT_ESCALA` e `classificarGut` — é o reexport provado por comportamento, não por texto.

    `tests/gantt-compartilhado.test.js` cobre um caso por comportamento declarado na tarefa 2, com
    atenção especial aos três que quebram cálculo de porcentagem: intervalo de um único dia, item
    que começa antes do intervalo, e hoje fora do intervalo.

    `tests/abc-compartilhado.test.js` cobre a ordenação decrescente, o acumulado chegando a 100, a
    fronteira exata dos cortes, o item de valor não numérico caindo em C, o caso de um único item
    (tudo A) e o caso de total zero (sem divisão por zero).
  </action>
  <acceptance_criteria>
    - `node --test tests/gut-compartilhado.test.js` sai 0 com pelo menos 8 casos
    - `node --test tests/gantt-compartilhado.test.js` sai 0 com pelo menos 10 casos
    - `node --test tests/abc-compartilhado.test.js` sai 0 com pelo menos 8 casos
    - `node --test` reporta 0 falhas e total maior que o baseline de 1095
    - `git diff --name-only tests/` lista apenas os três arquivos novos — nenhum teste existente alterado
  </acceptance_criteria>
  <verify>
    <automated>node --test</automated>
  </verify>
  <done>Os três gates passam, `tests/predial-dominio.test.js` continua verde sem ter sido tocado e o total de `node --test` cresceu.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| banco → HTML do Gantt | `rotulo` do item vem de `ges_acoes`/`maq_os` e entra no HTML da coluna de rótulos |
| entrada do usuário → cálculo de porcentagem | `inicio`/`fim` chegam como texto e podem ser nulos, mal formados ou invertidos |
| item do consumidor → classificação ABC | `campoValor` é fornecido pelo chamador e pode devolver qualquer coisa |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-05 | Tampering | `shared/gantt.js` (`htmlGantt`, coluna de rótulos) | high | mitigate | o rótulo passa pela mesma função de escape de `shared/componentes.js` antes de entrar no HTML; caso de gate com rótulo contendo caractere de marcação |
| T-13-06 | Tampering | `shared/gut.js` (`gutTotal`) | medium | mitigate | dimensão só é aceita se pertencer à lista fechada `GUT_ESCALA`; qualquer outro valor devolve `null` em vez de um total inventado que priorizaria a fila de ação errada |
| T-13-07 | Denial of Service | `linhasGantt` com intervalo degenerado | medium | mitigate | divisão por zero é impossível: intervalo de um dia é tratado como caso próprio, com caso de gate |
| T-13-08 | Denial of Service | `classificarAbc` com total zero | low | mitigate | total zero é caso próprio, sem divisão; todas as linhas saem classe C com acumulado 0 |
| T-13-SC | Tampering | instalação de pacote (npm/pip/cargo) | high | accept | esta fase não instala pacote nenhum — projeto zero-build, sem `package.json`; não há dependência a auditar |
</threat_model>

## Artifacts this phase produces

**Arquivos novos**
- `shared/gantt.js`
- `shared/abc.js`
- `shared/gut.js`
- `tests/gantt-compartilhado.test.js`
- `tests/abc-compartilhado.test.js`
- `tests/gut-compartilhado.test.js`

**Arquivo modificado**
- `predial/dominio.js` — declarações de `GUT_ESCALA`/`classificarGut` substituídas por reexport de `../shared/gut.js`

**Funções e constantes exportadas**
- `shared/gantt.js`: `linhasGantt`, `htmlGantt`
- `shared/abc.js`: `classificarAbc`
- `shared/gut.js`: `GUT_ESCALA`, `classificarGut`, `gutTotal`, `rotuloGut`
- `predial/dominio.js` (reexport): `GUT_ESCALA`, `classificarGut`, `gutTotal`

**Classes CSS emitidas** (o CSS é escrito no plano 13-04)
`gantt`, `gantt-rotulos`, `gantt-rotulo`, `gantt-linhas`, `gantt-linha`, `gantt-barra`,
`gantt-aberto`, `gantt-hoje`, `gantt-hoje-rotulo`, `gantt-tom-neutro`, `gantt-tom-info`,
`gantt-tom-ok`, `gantt-tom-warn`, `gantt-tom-erro`, `abc`, `abc-linha`, `abc-barra`,
`abc-classe-a`, `abc-classe-b`, `abc-classe-c`

**Sem tabela, coluna, id HTML ou rota nova neste plano.**

<verification>
1. `node --test` — 0 falhas, total maior que o baseline de 1095
2. `git diff --stat tests/predial-dominio.test.js` vazio — o gate do Predial não foi tocado (critério 3 do ROADMAP)
3. `git diff --stat predial/app.js` vazio — a superfície importada pelo Predial não mudou
4. `git diff --name-only -- refrigeracao/ mapa/xmap.js` vazio (D-19, D-13-03)
5. `shared/pmoc.css` não foi tocado por este plano (o CSS é do plano 13-04)
</verification>

<success_criteria>
- `shared/gantt.js`, `shared/abc.js` e `shared/gut.js` são núcleos puros com gate próprio (critério 1 do ROADMAP; GEQ-03, GEQ-05, e a parte de GEQ-07 relativa à curva ABC)
- Predial consome GUT de `shared/gut.js` por reexport, sem cópia local, com `tests/predial-dominio.test.js` intocado e verde (critério 3 do ROADMAP)
- `node --test` verde
</success_criteria>

<output>
Criar `docs/fase-13-gestao-qualidade/13-02-SUMMARY.md` ao terminar
</output>
