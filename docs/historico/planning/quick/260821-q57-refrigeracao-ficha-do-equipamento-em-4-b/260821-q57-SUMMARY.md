---
phase: quick
plan: 260821-q57
subsystem: ui
tags: [refrigeracao, mapa, ficha-equipamento, estado, cadastro-editavel, medicoes, supabase]

requires: []
provides:
  - "equipamentos.funciona migra de OK/NOK (sem trava) para OP/INOP/OR (primeira check constraint) — migração 41 escrita, aditiva, NÃO aplicada"
  - "equipamentos ganha modelo/data_fabricacao/link/manual_url; logs_manutencao ganha capacitor_marcha/capacitor_partida/tensao_medida"
  - "EQUIP_ESTADOS/EQUIP_ORDEM/EQUIP_EQUIVALENCIA + normalizarEstadoEquip/equipEstado/equipOperante/equipComRestricao/equipInoperante: vocabulário único de estado, lendo OK/NOK e OP/INOP/OR ao mesmo tempo (D-q57-06)"
  - "CAMPOS_EQUIP (ponte de leitura, 32 colunas) / EQUIP_EDITAVEIS (lista fechada de escrita) / equipParaDb / linkSeguro / podeEditarCadastro — primeiro caminho de escrita de cadastro que o módulo já teve"
  - "openDetail reorganizada em quatro blocos: Local, Dados do equipamento, Estado e PMOC (com Medições acompanhadas), Histórico de OS/manutenções"
  - "imprimirDocumento: folha timbrada A4 extraída de ctPrintOS, com dois consumidores (ctPrintOS + printFicha)"
  - "serieMedicoes/tendenciaMedicao/sparklineSVG/renderMedicoesFicha: tendência das 8 medições (5 antigas + 3 novas) em SVG à mão, sem biblioteca"
  - "atualizarEstadoEquip: a conferência do gestor grava o estado do equipamento, preso à OS que o causou (D-q57-04)"
  - "mapa/mapa-geometria.js ESTADOS ganha restricao; ESTADO_POR_MODULO.climatizacao cobre os cinco valores (OK/NOK/OP/INOP/OR); mapa/xmap-marcadores.js GRAVIDADE ganha restricao entre manutencao e standby"
affects: [refrigeracao, mapa]

tech-stack:
  added: []
  patterns:
    - "Vocabulário de estado com equivalência de leitura (EQUIP_EQUIVALENCIA) para permitir frontend publicado antes do SQL — mesmo idioma de manSondarEsquema/MAN_FLUXO_OK (260821-l7n) e de /calibracao (migração 39)"
    - "Ponte de campos + lista fechada de escrita separada (CAMPOS_EQUIP / EQUIP_EDITAVEIS), igual a CAMPOS_LOG do fluxo de OS interna — evita campo gravado como null por estar fora do mapa"
    - "Folha timbrada A4 promovida a função compartilhada (imprimirDocumento) só quando aparece o segundo consumidor — mesma regra que promoveu reguaPassos (l7n) e shared/tabela.js"
    - "SVG de tendência escrito à mão (sparklineSVG), no idioma de dvDonut já existente no arquivo — zero biblioteca de gráfico"

key-files:
  created:
    - supabase/41_refrigeracao_ficha_estado.sql
    - tests/refrigeracao-estado-equipamento.test.js
    - tests/refrigeracao-ficha-equipamento.test.js
  modified:
    - refrigeracao/index.html
    - mapa/mapa-geometria.js
    - mapa/xmap-marcadores.js
    - tests/refrigeracao-contagens.test.js
    - tests/refrigeracao-fluxo-os-interna.test.js
    - tests/mapa-cobertura.test.js
    - tests/mapa-legibilidade.test.js
    - TESTES.md

key-decisions:
  - "D-q57-01: equipamentos.funciona migra de OK/NOK para OP/INOP/OR (136 OK→OP, 35 NOK→INOP), ganhando a primeira check constraint que a coluna já teve"
  - "D-q57-02: equipamentos.estado NÃO é reaproveitada — guarda a idade aparente do aparelho (NOVA/SEMI/VELHA), não o funcionamento. A migração só comenta a coluna (comment on column); a tela rotula o campo 'Idade aparente', nunca 'Estado', com gate permanente"
  - "D-q57-03: as 8 medições acompanhadas (5 antigas + capacitor_marcha/capacitor_partida/tensao_medida) são colunas numeric tipadas, todas opcionais — nunca jsonb"
  - "D-q57-04: o estado muda na própria conferência do gestor, por um seletor no formulário de conferência — preso à OS que causou a mudança, sem tela nova"
  - "D-q57-05: a edição do cadastro (admin/gestor) entra junto da migração, ou os quatro campos novos nasceriam vazios sem caminho nenhum de preenchimento"
  - "D-q57-06: a leitura aceita os dois vocabulários (EQUIP_EQUIVALENCIA); a escrita só produz o novo — o frontend foi ao ar antes do SQL, com segurança"
  - "D-q57-07: OR conta como cesta própria (restricao) em alertasPmoc, contada uma única vez em todos/total; o KPI 'Fora de Operação' conta só INOP, para não inflar o número que orienta compra/contratação"
  - "D-q57-08: CAMPOS_EQUIP é a ponte única de leitura, EQUIP_EDITAVEIS a lista fechada de escrita — lat/lon/localId (posição é trabalho do /mapa) e ultimaManutencao (derivada da conferência) ficam fora da escrita"
  - "D-q57-09: link e manual_url só aceitam http(s)://, conferido nos dois lados (check no Postgres + linkSeguro na tela) — valor que não passa vira texto simples, nunca âncora"
  - "D-q57-10: a impressão da ficha é o segundo consumidor da folha timbrada — imprimirDocumento sobe de ctPrintOS, que vira casca sobre ela"
  - "D-q57-11: o gráfico de tendência é SVG escrito à mão (sparklineSVG), zero biblioteca; série sem leitura não desenha gráfico, diz que não há leitura"
  - "D-q57-12: as três medições novas contam como evidência de execução (manTemEvidencia), igual às cinco de hoje"
  - "D-q57-13: a trava de cargo do cadastro (admin/gestor) é UX apenas — a policy de equipamentos segue 'to authenticated using(true)', sem distinção de cargo"
  - "D-q57-14: no /mapa, OR vira estado próprio (restricao), entre manutencao e standby na escala de gravidade — não é dobrado em manutenção nem em sobreaviso"
  - "D-q57-15: ESTADO_POR_MODULO.climatizacao guarda os cinco valores (OK/NOK/OP/INOP/OR) de propósito — as duas entradas antigas garantem que a ordem de publicação não apague a cor de 171 marcadores"

patterns-established:
  - "statusPill/EQUIP_ESTADOS como ponto único do vocabulário de estado visual na refrigeração — todo call site (autoCrit, alertasPmoc, renderDash, renderInv, renderAlerts, ctNovaOS, dvRenderCharts) delega nos helpers em vez de comparar literais"

requirements-completed: []

coverage:
  - id: D1
    description: "Migração 41 escrita (aditiva, sem drop), com trava de URL combinada, os dois updates idempotentes de funciona antes da trava de estado, e comentários explicando a armadilha de `estado`"
    verification:
      - kind: unit
        ref: "tests/refrigeracao-estado-equipamento.test.js#migração 41: único arquivo 41_*.sql, sem drop table/column nem delete from, updates idempotentes, e a trava de funciona vem depois deles"
        status: pass
      - kind: unit
        ref: "tests/refrigeracao-estado-equipamento.test.js#as chaves de EQUIP_ESTADOS batem com o check(funciona in (...)) da migração 41, nos dois sentidos"
        status: pass
    human_judgment: false
  - id: D2
    description: "Vocabulário OP/INOP/OR na tela e no mapa, lendo OK/NOK e OP/INOP/OR ao mesmo tempo (D-q57-06), com OR como cesta/estado próprios (D-q57-07/14/15)"
    verification:
      - kind: unit
        ref: "tests/refrigeracao-estado-equipamento.test.js#normalizarEstadoEquip devolve OP/OR/INOP para os três valores novos, traduz OK/NOK, e recusa qualquer outra coisa"
        status: pass
      - kind: unit
        ref: "tests/refrigeracao-contagens.test.js#alertasPmoc (260821-q57): um equipamento OR cai na cesta restricao e entra em todos uma única vez"
        status: pass
      - kind: unit
        ref: "tests/mapa-cobertura.test.js#normalizarEstado(climatizacao, …) entende os cinco valores — os dois do legado e os três da migração 41"
        status: pass
      - kind: unit
        ref: "tests/mapa-legibilidade.test.js#restricao pesa mais que operante e que standby, e manutencao pesa mais que restricao"
        status: pass
    human_judgment: false
  - id: D3
    description: "Ficha em quatro blocos (Local, Dados do equipamento, Estado e PMOC, Histórico) e cadastro editável (modelo/data de fabricação/link/manual), restrito a admin/gestor"
    verification:
      - kind: unit
        ref: "tests/refrigeracao-ficha-equipamento.test.js#fiação: openDetail traz os quatro blocos na ordem, salvarEquip chama podeEditarCadastro(), e o formulário rotula o campo de estado como Idade aparente"
        status: pass
      - kind: unit
        ref: "tests/refrigeracao-ficha-equipamento.test.js#podeEditarCadastro é verdadeiro para admin/gestor, falso para tecnico/observador/vazio/nulo, e falso no modo observador mesmo com cargo admin"
        status: pass
      - kind: unit
        ref: "tests/refrigeracao-ficha-equipamento.test.js#linkSeguro aceita http/https (inclusive maiúsculo) e recusa javascript:, data:, protocolo-relativo, caminho e espaço à frente"
        status: pass
      - kind: manual_procedural
        ref: "TESTES.md#Refrigeração — ficha do equipamento em quatro blocos, cadastro editável, estado OP/INOP/OR (21/08/2026)"
        status: unknown
    human_judgment: true
    rationale: "A prova visual dos quatro blocos, da edição de cadastro por um gestor real e da folha impressa exige sessão autenticada e navegador, fora do alcance deste executor autônomo. Roteiro documentado em TESTES.md."
  - id: D4
    description: "Impressão A4 com dois consumidores (imprimirDocumento), medições acompanhadas com tendência (SVG à mão) e estado gravado pela conferência do gestor"
    verification:
      - kind: unit
        ref: "tests/refrigeracao-ficha-equipamento.test.js#imprimirDocumento tem dois consumidores (ctPrintOS e printFicha) e nenhum dos dois declara window.open próprio nem repete o CSS de A4"
        status: pass
      - kind: unit
        ref: "tests/refrigeracao-ficha-equipamento.test.js#tendenciaMedicao compara a PRIMEIRA com a ÚLTIMA leitura, não o mínimo com o máximo, e devolve null com menos de dois pontos"
        status: pass
      - kind: unit
        ref: "tests/refrigeracao-fluxo-os-interna.test.js#conferência aprovada (D-q57-04) chama atualizarEstadoEquip com o valor do seletor, gravando funciona junto de ultima_manutencao"
        status: pass
      - kind: unit
        ref: "tests/refrigeracao-fluxo-os-interna.test.js#atualizarEstadoEquip não grava quando o estado escolhido é igual ao atual, quando é vazio, e quando está fora da lista fechada — três chamadas, nenhuma escrita"
        status: pass
      - kind: manual_procedural
        ref: "TESTES.md#Refrigeração — ficha do equipamento em quatro blocos, cadastro editável, estado OP/INOP/OR (21/08/2026)"
        status: unknown
    human_judgment: true
    rationale: "A prova de que a pílula da ficha, o chip do inventário e a cor do marcador do /mapa mudam JUNTOS ao conferir uma OS exige sessão real contra o Supabase e dois módulos abertos lado a lado — fora do alcance deste executor autônomo. Roteiro documentado em TESTES.md."

duration: ~50min
completed: 2026-08-21
status: complete
---

# Quick Task 260821-q57: Ficha do equipamento em quatro blocos, cadastro editável, estado OP/INOP/OR — Summary

**`/refrigeracao` ganha uma ficha do equipamento reorganizada em quatro blocos com o primeiro caminho de escrita de cadastro que o módulo já teve, um vocabulário de estado de três palavras (OP/INOP/OR) que a tela lê nos dois sentidos com o banco, tendência das 8 medições acompanhadas em SVG à mão, e o estado da máquina mudando pela própria conferência do gestor — tudo sobre uma migração (41) escrita, aditiva e ainda não aplicada.**

## Performance

- **Duration:** ~50min
- **Completed:** 2026-08-21
- **Tasks:** 3/3 completas
- **Files modified:** 11 (3 criados: `supabase/41_refrigeracao_ficha_estado.sql`, `tests/refrigeracao-estado-equipamento.test.js`, `tests/refrigeracao-ficha-equipamento.test.js`; 8 modificados)

## Accomplishments

- `supabase/41_refrigeracao_ficha_estado.sql`: 4 colunas novas em `equipamentos` (modelo, data_fabricacao, link, manual_url), 3 em `logs_manutencao` (capacitor_marcha, capacitor_partida, tensao_medida), trava de esquema de URL combinada, os dois `update` idempotentes (OK→OP, NOK→INOP) guardados pelo valor antigo, e só depois deles a primeira `check` que `funciona` já teve. Comentário sobre `estado` mata a armadilha de nome na origem. Escrita, **não aplicada**.
- Vocabulário de estado (`EQUIP_ESTADOS`/`EQUIP_ORDEM`/`EQUIP_EQUIVALENCIA`, `normalizarEstadoEquip`/`equipEstado`/`equipOperante`/`equipComRestricao`/`equipInoperante`, `statusPill` reescrita): ponto único, lendo OK/NOK **e** OP/INOP/OR ao mesmo tempo (D-q57-06) — o frontend publicado hoje já entende o banco de amanhã. Todos os call sites que comparavam `'NOK'`/`'OK'` literalmente (autoCrit, alertasPmoc, renderDash, renderInv, renderAlerts, ctNovaOS, dvRenderCharts) migraram para os helpers.
- `alertasPmoc` ganha a cesta `restricao` (D-q57-07): OR não conta em "Fora de Operação" (só INOP conta), mas aparece em seção própria de Alertas, chip próprio no inventário e segmento próprio (âmbar) no gráfico "Estado da Frota" de `dvRenderCharts`.
- `mapa/mapa-geometria.js` ganha o estado `restricao` (cor `#facc15`, entre `manutencao` e `inoperante`); `ESTADO_POR_MODULO.climatizacao` passa a ter os **cinco** valores (D-q57-15) — as duas entradas antigas (OK/NOK) ficam de propósito para a ordem de publicação não apagar a cor de 171 marcadores. `mapa/xmap-marcadores.js` `GRAVIDADE` ganha `restricao` entre `manutencao` e `standby` (D-q57-14).
- `CAMPOS_EQUIP` (32 colunas, a tabela `equipamentos` inteira) / `EQUIP_EDITAVEIS` (lista fechada de escrita) / `equipParaDb` (duas guardas, `''`→`null`) / `linkSeguro` (só `http(s)://`) / `podeEditarCadastro` (admin/gestor): primeiro caminho de escrita de cadastro do módulo. `dbToEquip` reescrita como laço sobre o mapa — nove chaves que hoje somiam (`refrigerante`, `areaM2`, `peDireito`, `nPessoas`, `dissipacaoW`, `fatorInsolacao`, `localId`, `lat`, `lon`) passam a ser lidas.
- `openDetail` reorganizada em quatro blocos (**1 · Local**, **2 · Dados do equipamento**, **3 · Estado e PMOC**, **4 · Histórico de OS/manutenções**) — a ficha de hoje reorganizada, não duplicada. `openEquipForm`/`salvarEquip`: formulário com um campo por `EQUIP_EDITAVEIS`, três `<select>` de lista fechada (Estado, Criticidade, **Idade aparente** — rotulado só assim, nunca "Estado", D-q57-02), guarda dupla na ação.
- `imprimirDocumento` (D-q57-10): folha timbrada A4 extraída de `ctPrintOS` — DOCTYPE, CSS, cabeçalho institucional, assinaturas, rodapé e `window.open` moram só nela agora. `ctPrintOS` vira casca; `printFicha` é o segundo consumidor (quatro blocos como tabelas + últimas 10 OS + assinaturas "Responsável técnico"/"Chefe da DME").
- `MEDICOES_FICHA`/`serieMedicoes`/`tendenciaMedicao`/`sparklineSVG`/`renderMedicoesFicha` (D-q57-11): as 8 medições acompanhadas (5 antigas + 3 novas) com sparkline SVG escrito à mão. Série sem leitura não desenha gráfico — diz que não há leitura (hoje é o caso de todos os 171 equipamentos); série com um ponto mostra o valor e diz que não há tendência ainda. `medicoesFichaLinhasImpressao` leva a mesma informação, sem SVG, para o papel.
- Evidência de execução ganha as três medições novas (capacitor de marcha, capacitor de partida, tensão medida), agrupadas sob subtítulo "Elétrico"; `manTemEvidencia` aceita cada uma sozinha como evidência (D-q57-12); campo vazio nunca entra no patch, nunca apaga leitura anterior.
- `atualizarEstadoEquip` (D-q57-04): a conferência do gestor grava o estado escolhido no seletor "Estado do equipamento após a manutenção", pré-selecionado pelo estado atual; não grava quando o valor é inválido nem quando já é o atual (não existe update que não muda nada). A devolução da OS não toca no estado.
- `node --test`: 596/596 (556 de antes + 40 novos/alterados). Os quatro `grep -c` do PLAT-15 continuam em 0.

## Task Commits

1. **Task 1: migração 41 e vocabulário OP/INOP/OR na refrigeração e no mapa** - `1d55918` (feat)
2. **Task 2: ficha em quatro blocos, cadastro editável e folha timbrada com dois consumidores** - `da6d0f0` (feat)
3. **Task 3: capacitor/tensão na evidência, tendência de medições e estado na conferência** - `be6df82` (feat)

## Files Created/Modified

- `supabase/41_refrigeracao_ficha_estado.sql` — migração aditiva nova, escrita e conferida por teste, **não aplicada**
- `refrigeracao/index.html` — vocabulário de estado, ponte/formulário/cadastro de equipamentos, ficha em quatro blocos, folha timbrada compartilhada, evidência com as três medições novas, tendência das medições, estado na conferência
- `mapa/mapa-geometria.js` — estado `restricao`, `ESTADO_POR_MODULO.climatizacao` com cinco valores
- `mapa/xmap-marcadores.js` — `GRAVIDADE` com `restricao`
- `tests/refrigeracao-estado-equipamento.test.js` — gate novo, 8 casos
- `tests/refrigeracao-ficha-equipamento.test.js` — gate novo, 18 casos (ponte de campos, formulário, impressão, medições)
- `tests/refrigeracao-contagens.test.js` — terceiro recorte (vocabulário do estado) + casos da cesta `restricao`
- `tests/refrigeracao-fluxo-os-interna.test.js` — colunas da migração 41, evidência com as três medições novas, casos do estado na conferência
- `tests/mapa-cobertura.test.js` — vocabulário `climatizacao` com cinco valores, `ESTADOS.restricao`
- `tests/mapa-legibilidade.test.js` — gravidade de `restricao`, cobertura estrutural de `ESTADOS`
- `TESTES.md` — roteiro manual completo deste task

## Decisions Made

Ver `key-decisions` no frontmatter — D-q57-01 a D-q57-15, todas travadas no PLAN.md e seguidas à risca. Nenhuma decisão nova tomada durante a execução.

## Deviations from Plan

None - plano executado conforme escrito. Dois ajustes de implementação, ambos dentro do espaço de liberdade do plano (nenhum deles muda comportamento, decisão ou escopo):

- **Ordem textual de `openDetail`:** o bloco 4 (Histórico) foi escrito inline no ponto certo do template de HTML (em vez de uma variável `logHtml` construída antes dos blocos 1-3), para que a ordem visual dos quatro blocos bata com a ordem em que aparecem no código-fonte — é o que o próprio gate de fiação confere (comparação de índices no corpo da função).
- **`equipParaDb`/`atualizarEstadoEquip` testados por comparação campo a campo, não `deepEqual`:** objetos criados dentro do sandbox `node:vm` têm `Object.prototype` de outro realm — mesma armadilha já documentada em `tests/refrigeracao-encerramento-os.test.js` (260821-l7n).

## Issues Encountered

- As cores do gráfico de tendência (`MEDICOES_FICHA`) precisaram ser hexadecimais **literais**, copiados de `DV_CAT`/`DV_SEQ` — essas variáveis são declaradas mais abaixo no arquivo (perto de `dvRenderCharts`), e o bloco de medições, posicionado antes de `atualizarUltimaManutencao` (posição fixada pelo plano), roda em ordem de execução top-level ANTES delas terem valor. Referenciá-las diretamente lançaria `TypeError` na carga da página. Resolvido copiando os hexadecimais já existentes no arquivo — nenhuma cor nova inventada, D-q57-11 preservada.

## User Setup Required

**Migração `supabase/41_refrigeracao_ficha_estado.sql` precisa ser rodada no SQL editor do Supabase.** Ordem de publicação (D-q57-06): o frontend das três tasks (merge + push) vai a produção **primeiro** — nada muda para o usuário, a tela continua lendo OK/NOK normalmente. **Depois**, aplicar a migração 41 e colar o resultado do bloco de conferência do próprio arquivo no roteiro de `TESTES.md`. Até lá, o app funciona exatamente como hoje (testado nos dois lados: `tests/refrigeracao-estado-equipamento.test.js` prova que `normalizarEstadoEquip` traduz OK/NOK, e o roteiro manual pede a checagem visual antes de aplicar).

## Next Phase Readiness

- `node --test`: 596/596, 0 falhas
- `git diff --stat` (branch inteira) toca `refrigeracao/index.html`, `supabase/41_refrigeracao_ficha_estado.sql`, `TESTES.md`, cinco arquivos de teste, e exatamente dois arquivos de `mapa/` (`mapa-geometria.js`, `xmap-marcadores.js`) — `mapa/xmap.js`, `mapa/xmap.css` e `mapa/index.html` intocados; nada em `shared/`, `maquinas/`, `reparos/`, `calibracao/` ou `predial/`
- Os quatro `grep -c` do PLAT-15 em `refrigeracao/index.html`: 0/0/0/0
- **Pendência de RLS (D-q57-13, sem eufemismo):** a policy de `equipamentos` continua `for update to authenticated using(true)`, sem distinção de cargo — um técnico autenticado poderia editar o cadastro (modelo, link, etc.) por uma chamada REST montada à mão. A tela não oferece o botão a técnico/observador, mas fechar de verdade exige uma migração de policy, fora deste escopo.
- **Pendências pré-existentes, registradas e NÃO consertadas (ponto 3 do briefing):**
  - `autoCrit(e)` continua sendo uma função morta — os três ramos (`if(e.criticidadeManual)`, `if(equipInoperante(e))`, `return`) devolvem `e.criticidade` de qualquer forma. Só o literal `'NOK'` do meio virou `equipInoperante(e)`, como o plano pediu; a função não foi ampliada nem simplificada.
  - `dbToEquip` continua lendo `criticidade_manual` de `r`, coluna que **não existe** em `equipamentos` (conferido nas migrações 04+19+25+41) — a linha fica fora de `CAMPOS_EQUIP`, com o gate afirmando a ausência da coluna para ninguém "consertar" sem querer.
- **Migração 41:** escrita e conferida por teste (colunas, travas, ordem update→trava), **ainda não aplicada** em produção — próximo passo é o usuário rodá-la e seguir o roteiro em `TESTES.md`.
- Fora de escopo por decisão do plano, para não ser "consertado" sem querer: QR e NFC ficaram de fora deste task (decisão do usuário, vão para o quick seguinte).

## Self-Check: PASSED

- Os 3 arquivos criados (`supabase/41_refrigeracao_ficha_estado.sql`, `tests/refrigeracao-estado-equipamento.test.js`, `tests/refrigeracao-ficha-equipamento.test.js`) confirmados em disco.
- Os 3 commits de task (`1d55918`, `da6d0f0`, `be6df82`) confirmados em `git log`.
- `node --test`: 596/596 verde.
- Os quatro `grep -c` do PLAT-15 em `refrigeracao/index.html`: 0/0/0/0.
- `supabase/41_refrigeracao_ficha_estado.sql`: sem `drop table`, `drop column` ou `delete from` (fora dos comentários que os citam para explicar a ausência).
