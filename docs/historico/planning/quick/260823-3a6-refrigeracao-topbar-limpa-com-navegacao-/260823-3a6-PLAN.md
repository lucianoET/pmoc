---
phase: quick-260823-3a6
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - refrigeracao/index.html
  - tests/refrigeracao-topbar-parque.test.js
  - tests/refrigeracao-desktop.test.js
autonomous: true
requirements: [QUICK-260823-3a6]

must_haves:
  truths:
    - "A inscrição institucional não aparece mais na barra do topo do app; continua nas duas telas de login e no cabeçalho do documento impresso"
    - "A aba antes chamada Dashboard se lê Painel, e a antes chamada Inventário se lê Parque — em qualquer largura"
    - "Em tela >=1024px os cinco botões de navegação ficam dentro da barra escura do topo, entre a marca (à esquerda) e Buscar/Sair (à direita); a coluna lateral de 220px deixa de existir e o conteúdo herda a largura"
    - "Abaixo de 1024px a barra inferior de cinco botões continua exatamente como está hoje"
    - "A tabela do parque tem exatamente doze colunas, nesta ordem: #, Área, Prédio, Local, Tipo, BTU, Marca, Modelo, Estado, Criticidade, Próx. manutenção, Última inspeção"
    - "Cada uma das colunas novas ordena e filtra pelo próprio dado; a coluna composta Prédio·Área, que ordenava só por prédio, deixou de existir"
    - "A coluna Última inspeção mostra a data do log mais recente de tipo INSPEÇÃO daquele equipamento — e o plano registra por escrito que essa data NÃO é a base de que nextPmoc parte"
    - "Quando as doze colunas não cabem, a rolagem horizontal é da tabela, nunca da página, e nenhuma coluna é cortada"
    - "Nenhuma regra de CSS fora da @media foi criada, alterada ou removida — o fixture de celular não precisa ser regenerado"
  artifacts:
    - refrigeracao/index.html
    - tests/refrigeracao-topbar-parque.test.js
  key_links:
    - "Todo o CSS novo entra DENTRO do @media (min-width:1024px) que já existe — nenhum bloco <style> novo (a lição de D-8rz: um 7º bloco desloca a impressão digital do CSS de celular)"
    - "COLS_INV continua sendo a única definição de coluna do parque: a tela (tabCabecalho/tabCorpo) e o filtro por coluna de filtrarInventario leem a mesma lista"
    - "A renomeação é de rótulo; nenhum id, nome de função, chave de estado, marca de planilha ou nome de arquivo CSV muda"
---

<objective>
Quatro ajustes de apresentação em `/refrigeracao`, todos pedidos textualmente pelo usuário:
tirar a inscrição institucional da barra do topo, trazer a navegação para dentro dessa barra
(no computador), renomear duas abas (`Dashboard`→`Painel`, `Inventário`→`Parque`) e separar a
localização da tabela do parque em três colunas próprias (Área, Prédio, Local), acrescentando
Marca, Modelo e a data da última inspeção — a lista fechada de doze colunas que ele mandou.

Purpose: a barra do topo carrega hoje uma inscrição que já aparece no login e no papel, e a
navegação de ontem come 220px da esquerda numa tela que precisa de largura para doze colunas.
E a coluna `Prédio · Área` mentia: o cabeçalho anunciava dois campos e a ordenação usava um.

Output: edições em `refrigeracao/index.html` (marcação da barra do topo, rótulos, regras
acrescentadas ao `@media (min-width:1024px)` que já existe, e `COLS_INV`), mais um gate
permanente `tests/refrigeracao-topbar-parque.test.js` e a atualização de um caso de
`tests/refrigeracao-desktop.test.js` cujo fato mudou.

**Nada de dado, fluxo, regra de negócio ou migração. É camada de apresentação.**
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.claude/CLAUDE.md
@refrigeracao/index.html
@.planning/quick/260822-8rz-refrigeracao-layout-de-desktop-com-tabel/260822-8rz-SUMMARY.md
@tests/refrigeracao-desktop.test.js
@tests/inventario-ordem-refrigeracao.test.js
@tests/refrigeracao-planilha.test.js
@tests/refrigeracao-qr-nfc.test.js
</context>

<!-- planner-discipline-allow: CMASM · DME · UASG 744030 -->

---

## Estado conferido antes de planejar

Medido no arquivo em `a1ab6f1`, ramo `refrigeracao-topbar-parque`, árvore limpa,
`node --test` em **812/812 verde**. O executor confirma o baseline antes de começar; não
assume 812 de cor.

| Fato | Onde |
|------|------|
| A inscrição literal aparece **3 vezes** (não 4): linha 706 e linha 4578 (as duas telas de login) e linha 767 (a barra do topo). O cabeçalho do documento impresso (linha 5085) usa uma redação **expandida e diferente** — "CMASM · Divisão de Manutenção Especializada · UASG 744030 · ARP…" — e não entra nessa contagem. | `refrigeracao/index.html` |
| A barra do topo é `#topbar`: `<img>`, `.logo` (com o `<span>` da inscrição), `#topbar-action` (buscar) e um `<button>` sem id (sair, último filho). | linhas 765–769 |
| `#topbar .logo span{display:block;font-weight:400;font-size:11px;opacity:.7}` é a única regra que depende do `<span>`. O alinhamento vertical vem de `#topbar{align-items:center}`, não do span. | linha 30 |
| Os rótulos de navegação estão em `#bottom-nav`, cinco `.nav-btn`, ordem `dash, inv, os, pmoc, alert`. `mostrarApp` usa `document.querySelectorAll('.nav-btn')[1]` — índice, não rótulo. | linhas 855–874, 3231 |
| O layout de computador de ontem: `#app` vira grade `"topo topo" / "nav conteudo"`, com `#bottom-nav` na coluna esquerda de 220px, estado ativo de quatro sinais (barra `::before` à esquerda, fundo, peso 700, cor) e `.nav-badge{position:static;margin-left:auto}`. | dentro do `@media (min-width:1024px)`, linhas 4676–4730 |
| Não existe `<style>` próprio para o computador: o `@media` foi **mesclado** no último bloco `<style>` do arquivo, de propósito (deviação documentada em 260822-8rz). Criar um bloco novo desloca a impressão digital do CSS de celular por uma quebra de linha. | linha 4676 |
| `COLS_INV` tem 8 colunas: `id`, `local`, `predio` (rótulo `Prédio · Área`, `valor`=`e.predio`, `texto`=`predio · area`), `tipo`, `btu`, `estado`, `criticidade`, `prox`. | linha 6043 |
| `filtrarInventario` resolve o filtro por coluna **lendo `COLS_INV`** e casando contra `col.texto(e)`. Separar as colunas dá filtro por área de graça e não muda o contrato de três argumentos. | linha 1436 |
| `EQUIP_ROTULOS` já nomeia esses três campos: `area:'Área'`, `predio:'Prédio'`, `local:'Local'`. O vocabulário existe; a tabela é que não o usava. | linha 2053 |
| Não existe helper de pílula para área (só `statusPill`, `situacaoPill`, `critPill`, `dueBadgeHtml`). | — |
| **Marca e Modelo são `e.fabricante` e `e.modelo`** — confirmado em `CAMPOS_EQUIP`: `fabricante:'fabricante'`, `modelo:'modelo'`. `dbToEquip` cai para `''` em `fabricante`, **mas não em `modelo`** — `e.modelo` chega `null`. | linhas 358–392 |
| **`nextPmoc(e,tipo)` NÃO distingue inspeção de preventiva na data-base**: as duas partem de `e.ultimaManutencao \|\| getLatestLogDate(e.id)`; o que muda por tipo é só o **intervalo** (`PMOC_INT`). `proxManut` devolve a menor das duas. | linhas 959–966, 1380 |
| `getLatestLogDate(id)` devolve a data do log mais recente de **qualquer** `tipo` — inclusive `INSTALAÇÃO`/`REMOÇÃO`, que gravam no mesmo `logs_manutencao` (`MOV_TIPOS`, D-uyz-21). E **ordena o array em `_logCache` no lugar** (`.sort()` sem cópia) — efeito colateral pré-existente, não consertado aqui. | linhas 564–568, 2417 |
| `ultima_manutencao` é escrita **só na conferência da OS** (`aplicarConferenciaOS`) e **nunca retrocede** (D-jpd-03). | linhas 2298–2301 |
| `dueBadgeHtml(null)` já emite `Sem hist.` — o vocabulário de ausência de histórico existe e é uma pílula. | linha 1110 |
| A tabela do parque é escrita em `el('inv-list').innerHTML` e é a **única** coisa dentro desse contêiner. `.page` tem `overflow-x:hidden` — hoje, o que não couber é **cortado**, não rolado. | linhas 1508, 84 |
| O link profundo da ficha é `?equip=` (`PARAM_FICHA='equip'`) e usa o `id` — o mesmo número da coluna `#`. | linha 3287 |
| Nenhum gate afirma o texto da barra do topo, os rótulos das abas, o título da gaveta da planilha ou as frases com a palavra "inventário". O único caso que este trabalho quebra é o de `COLS_INV.predio`, em `tests/refrigeracao-desktop.test.js`. | `grep` em `tests/` |
| `tests/mobile-375.test.js` não lê `refrigeracao/index.html` (opera sobre `shared/pmoc.css` e os outros módulos) — não é risco aqui. | — |

---

## Decisões travadas

As duas primeiras vieram do usuário e **não se reabrem**. As demais fecham o que o pedido
deixou em aberto.

| ID | Decisão | Por quê |
|----|---------|---------|
| **D-3a6-01** | **A lista de colunas é do usuário, fechada e nesta ordem:** `#`, **Área**, **Prédio**, **Local**, **Tipo**, **BTU**, **Marca**, **Modelo**, **Estado**, **Criticidade**, **Próx. manutenção**, **Última inspeção** — doze. Ele nomeou onze; o `#` é a décima segunda por D-3a6-20. | Lista fechada, mandada por escrito. Deixa de ser leitura do planejador e vira requisito — por isso vira trava de gate (D-3a6-24), não só comportamento de ordenação. |
| **D-3a6-02** | **A navegação entra na barra do topo SÓ no computador.** No celular a barra inferior de cinco botões fica exatamente como está. | Numa tela de 375px a barra do topo já carrega logo, marca e duas ações; cinco abas não cabem, e barra inferior é o padrão certo no telefone. |
| **D-3a6-03** | **A inscrição sai de UM lugar só:** o `<span>` dentro de `#topbar .logo`. As duas ocorrências das telas de login e o cabeçalho expandido do documento impresso **ficam**. | No login a identificação diz de quem é o sistema para quem ainda não entrou; no papel ela é obrigatória num documento oficial. Dentro do app, com o usuário já autenticado, é ruído repetido em toda tela. |
| **D-3a6-04** | **Só o rótulo muda; nenhum identificador.** Continuam como estão: `#page-inv`, `#page-dash`, `navTo('inv')`, `navTo('dash')`, `renderInv`, `renderDash`, `#inv-list`, `#inv-chips`, `#inv-cnt`, `#inv-label`, `#inv-ordem`, `invChip`, `invOrdem`, `filtrarInventario`, `etiquetasDoInventario`, `exportarInventarioCsv`, `COLS_INV`, `TAB_ESTADO.inv`, `PLANILHA_MARCA` e o prefixo do arquivo CSV. | Trocar id quebra os 16 gates de refrigeração, o recorte por marcador que todos usam e a planilha já exportada em campo. O usuário pediu para mudar o que ele lê. |
| **D-3a6-05** | **"Inventário" vira "parque" onde nomeia a TELA; fica onde nomeia o CADASTRO.** Muda: o rótulo da aba, o `title` **e** o `aria-label` de `#btn-planilha`, e o título da gaveta (`'Planilha do inventário'`). Ficam: as 5 ocorrências de "Guardada — volta ao inventário", as mensagens de conferência da planilha ("id não existe no inventário", "o inventário mudou desde a exportação", "confira o inventário"), a nota do gráfico de idade aparente e o nome do arquivo CSV. | `title`/`aria-label` são texto que o usuário lê e ouve — entram na renomeação. Já "volta ao inventário" é vocabulário de destino de OS, aparece em impresso e em três telas diferentes: renomeá-lo é mexer no fluxo de movimentação, que ninguém pediu. Uma mensagem de erro de importação fala do cadastro, não da aba. |
| **D-3a6-06** | **`Dashboard` → `Painel`** só no rótulo do `.nav-btn`. O comentário `PAGE: DASHBOARD` na marcação fica. | Mesmo motivo de D-3a6-04. Comentário não é tela. |
| **D-3a6-07** | **Forma da navegação no computador: `#topbar` vira `display:contents` dentro da `@media`,** e a linha 1 da grade de `#app` passa a ter **cinco itens reais** — ícone, marca, `#bottom-nav`, buscar, sair. Sem sobreposição, sem deslocamento mágico (`left:220px`, `margin-right:90px`…), sem segunda cópia da navegação na marcação. | É a única forma CSS-only de pôr a navegação **entre** a marca e as ações sem mover `#bottom-nav` na marcação (o que exigiria `position:fixed` no celular, ou seja, mexer no CSS fora da `@media` e mudar o telefone). Duplicar a marcação daria duas fontes de verdade para estado ativo e distintivos, e o gate de ontem exige cinco `.nav-btn` num `#bottom-nav` só. |
| **D-3a6-08** | **Consequência aceita de D-3a6-07: o box de `#topbar` deixa de existir no computador.** A faixa escura passa a ser o fundo do próprio `#app`, e `#content` ganha explicitamente o fundo claro que hoje herda do `body`. | `display:contents` não gera caixa — sem essas duas regras o topo sai claro (texto branco invisível) e a área de conteúdo sai escura. As duas regras vivem dentro da `@media`; no celular nada disso existe. |
| **D-3a6-09** | **O estado ativo mantém os quatro sinais de 260819-4gq, girados de eixo:** a barra do `::before` deixa de ser lateral e vira sublinhado de 3px na base; fundo tingido, peso 700 e cor passam para a paleta do escuro (ativo `#fff`, inativo `rgba(255,255,255,.72)`), inclusive o ícone. | O contraste de ontem (`#1351B4` sobre branco) some sobre `#071D41`. Quatro sinais é o mínimo que sobrevive a daltonismo — nenhum deles sozinho carrega o significado. |
| **D-3a6-10** | **Os três distintivos (`#alert-badge`, `#os-badge`, `#pmoc-badge`) viram inline, logo depois do rótulo.** No celular são absolutos sobre o ícone; na coluna lateral de ontem eram `margin-left:auto`; numa aba horizontal isso os jogaria para longe do rótulo. | Mudou o eixo, muda a âncora. `position:static; transform:none; margin-left:6px` — o número fica colado no nome da aba que ele conta. |
| **D-3a6-11** | **O espaço da coluna lateral volta para o conteúdo.** `.page` continua com teto de 1400px centralizado; a tabela ganha ~220px de largura útil. | Em 1024px a área útil sai de 756px para **976px** — três colunas a mais de graça, e é o que faz as doze caberem a partir de ~1180px em vez de ~1400px. O que ainda falta abaixo disso é resolvido por D-3a6-23. |
| **D-3a6-12** | **A coluna composta morre.** `predio` passa a ter `texto()` = só o prédio; nasce `area` com `valor`/`texto` = `e.area`. Ambas `tipo:'texto'`. | Conserta a incoerência que o gate de ontem chegou a documentar: o cabeçalho dizia "Prédio · Área" e a ordenação usava só o prédio. Duas colunas, dois dados, duas ordenações honestas. |
| **D-3a6-13** | **Nenhuma regra nova de ordenação ou filtro.** As colunas novas são só definição: `tabComparar`/`tabAplicar` de ontem já dão `localeCompare('pt-BR')`, vazio por último nos dois sentidos e filtro por subcadeia sem acento e sem caixa. `filtrarInventario` ganha filtro por área **sem mudar uma linha**, porque resolve a coluna lendo `COLS_INV`. | O núcleo é de ontem e tem gate. Reusar mantém uma regra, não duas. |
| **D-3a6-14** | **Sem largura fixa e sem `table-layout:fixed`;** o que resolve o excesso é rolagem, não largura declarada (D-3a6-23). | Largura fixa em doze colunas quebraria Local, que é o único campo longo e variável ("Sala de Controle 2"). Layout automático dá a Local o que sobra e aos outros onze o que eles pedem. |
| **D-3a6-15** | **Área, Marca e Modelo não ganham pílula colorida.** Texto escapado, como Tipo e Local. | Não existe helper de área nem de fabricante no arquivo; criar um é trabalho de apresentação que ninguém pediu, e "VERMELHA"/"AZUL" já se leem sozinhas. |
| **D-3a6-16** | **O fixture `tests/fixtures/refrigeracao-css-mobile.css` NÃO é regenerado.** Nenhuma regra fora da `@media` é criada, alterada ou removida — **inclusive a regra `#topbar .logo span`, que fica no lugar mesmo sem `<span>` para casar**. Nenhum bloco `<style>` novo. | Remover a regra órfã custaria a impressão digital congelada e não devolve nada: sem `<span>` na marcação ela é inerte. E regenerar em silêncio esvazia o gate — é o único jeito de o caso D-8rz-04 continuar valendo alguma coisa. |
| **D-3a6-17** | **Se, ao implementar, o executor concluir que precisa mexer em CSS fora da `@media`: PARA, registra a regra e o porquê, regenera o fixture com a MESMA função de extração do gate, e põe o diff justificado no SUMMARY.** Nunca regenerar de passagem para "fazer passar". | D-8rz-04 existe para provar que o computador não foi pago com o celular. Um fixture atualizado sem justificativa transforma a prova em carimbo. |
| **D-3a6-18** | **Um caso do gate de ontem muda porque o fato mudou**, não é apagado: `COLS_INV.predio ordena pelo prédio, não pelo texto concatenado "Prédio · Área"` afirma `texto()==='F21 · ZZZ'`. Reescrever para provar a mesma intenção sem o composto, e o novo gate cobre o par `area`. | Precedente do projeto: 260819-5jf atualizou três gates pelo mesmo motivo. Gate é contrato do comportamento de hoje, não histórico. |
| **D-3a6-19** | **Gate vermelho antes de verde**, nas duas tarefas: escrever o caso, vê-lo falhar, só então implementar. Um commit atômico por tarefa. | Procedimento de 260822-48m e 260822-8rz. |
| **D-3a6-20** | **A coluna `#` fica, como primeira** — mesmo fora da lista de onze. **Reversível numa palavra:** se ele disser "tira o #", é apagar uma entrada de `COLS_INV`. | O `id` é a identidade que o QR, a etiqueta, o link profundo `?equip=` e o título da ficha usam: é o número que a pessoa lê na tabela e digita/confere no equipamento. Custa ~52px em 976 disponíveis e já existe. Tirá-la é uma mudança que ele também não pediu — e sem ela a tabela deixa de casar com a etiqueta colada na máquina. |
| **D-3a6-21** | **Marca = `e.fabricante`; Modelo = `e.modelo`.** Cabeçalhos `Marca` e `Modelo` — a palavra dele. **`modelo` está preenchido em 0 de 171 e `fabricante` na maioria: isso não é defeito, é o estado do cadastro.** Registrado aqui para ninguém "consertar" removendo a coluna vazia. | A coluna vazia é justamente o que dá endereço ao preenchimento: `modelo` nasceu na migração 41 e a importação de planilha de 260822-5hy existe para preencher exatamente esses campos em massa. Uma coluna que ninguém vê é uma coluna que ninguém preenche. Nota: a folha de cadastro, a busca e o CSV continuam dizendo "Fabricante" — o importador casa a coluna pelo texto literal do cabeçalho (`EQUIP_ROTULOS`), então mudar aquela palavra mudaria o contrato do arquivo, o que está fora deste pedido. |
| **D-3a6-22** | **A coluna é `Última inspeção`, com a semântica literal: a data do log mais recente com `tipo === 'INSPEÇÃO'` daquele equipamento.** `Sem hist.` quando não houver nenhuma. O usuário nomeou a coluna; a palavra é dele. | "Quando esta unidade foi inspecionada pela última vez" é uma pergunta que um PMOC responde o tempo todo, e a coluna vale sozinha — **não precisa fechar aritmética com a vizinha para ter valor**. Dos 14 logs de hoje, 7 são INSPEÇÃO. |
| **D-3a6-22b** | **Fato registrado, não renomeação (achado desta revisão): `nextPmoc` NÃO parte dessa data.** A base do ciclo é `e.ultimaManutencao \|\| getLatestLogDate(e.id)` — o último log de **qualquer** tipo (corretiva e movimentação inclusive) — e o que muda por tipo é só o **intervalo** de `PMOC_INT`. **Logo: `Próx. manutenção` menos `Última inspeção` NÃO dá o intervalo de `PMOC_INT`, e as duas datas podem discordar na mesma linha.** Escrever isso num comentário `/* ── … ── */` ao lado da coluna, no `CLAUDE.md` não, e **obrigatoriamente no SUMMARY**. | Duas datas lado a lado convidam à subtração. Sem esse registro, quem abrir a tabela em seis meses subtrai uma da outra e conclui coisa errada sobre o ciclo — e ninguém terá deixado escrito que a discordância é esperada, não defeito. Registrar é mais barato que a renomeação que este achado quase provocou, e não decide por ele uma questão de produto. |
| **D-3a6-23** | **Doze colunas não cabem em 1024px, e quem rola é a TABELA, nunca a página.** `#inv-list` ganha `overflow:auto` **e** teto de altura dentro da `@media`; `.page{overflow-x:hidden}` continua como cinto de segurança. Nenhuma coluna cede. | Conta com a área útil real: 1024 − 48 de `padding` de `.page` = **976px** (≈961 com barra de rolagem). Custo medido pelo conteúdo com `padding:10px 12px` e fonte 13px: `#` 52, Área 80, Prédio 64, Tipo 119, BTU 74, Marca 79, Modelo 76, Estado 110, Criticidade 94, Próx. 118, Última 122 = **988px**, mais Local (o campo longo) com no mínimo ~140 → **~1128px**. Ou seja: **cabe a partir de ~1180px** (1280 e 1366 folgam; em 1440 sobram ~264px para Local) e **falta na faixa 1024–1180**. Hoje, nessa faixa, `.page{overflow-x:hidden}` **cortaria** colunas sem dar como alcançá-las — pior que qualquer alternativa. Rolagem só na caixa da tabela mantém as doze acessíveis sem empurrar a página. O teto de altura vem junto **porque é o que salva o cabeçalho grudento**: com `overflow` no `#inv-list`, o `position:sticky` do `<thead>` passa a se prender a esse contêiner, e sem altura limitada ele nunca gruda (D-8rz-20 morreria em silêncio). Com teto, as 171 linhas rolam sob o cabeçalho congelado — melhor que hoje. Efeito colateral aceito: a barra de "N de M / Limpar filtros" rola junto. |
| **D-3a6-24** | **O gate afirma o CONJUNTO e a ORDEM de `COLS_INV`** — os doze `id` e os doze rótulos, em lista comparada de uma vez — além do comportamento de ordenação e filtro. | A lista deixou de ser leitura do planejador e virou requisito escrito do usuário. Requisito explícito merece trava: sem ela, inserir uma coluna no lugar errado passa verde, porque cada caso de ordenação testa uma coluna isolada e nenhum vê a fila. |

### O que este trabalho NÃO faz — escrito para ninguém "consertar" depois

| Fica como está | Por quê |
|----------------|---------|
| A barra inferior no celular | D-3a6-02, decisão do usuário. |
| Os chips "Área Vermelha"/"Área Azul" | Continuam sendo o único filtro por área no celular, onde não existe filtro por coluna. Redundância no computador é barata; ausência no telefone não. |
| `cmpInv`, `invOrdem` e o seletor "Ordenar:" | A ordem padrão da lista não muda. O seletor não ganha opção "área" — quem quer isso no computador ordena pela coluna, e D-8rz-06/07 já governam quem manda. |
| O `<span>` na marcação das duas telas de login | D-3a6-03. |
| A ordem das cinco abas | `mostrarApp` usa `querySelectorAll('.nav-btn')[1]` — reordenar mudaria a aba de destino sem que nada na tela dissesse por quê. |
| O CSV, a folha de etiquetas e as 12 folhas de impressão | Nenhuma delas lê `COLS_INV`; a ordem e o conteúdo continuam vindo de `cmpInv` e das colunas da planilha (D-8rz-10). O rótulo `Fabricante` no CSV **não** vira `Marca`: o importador casa a coluna pelo texto literal do cabeçalho. |
| `nextPmoc`, `proxManut`, `PMOC_INT` e a data-base do ciclo | A coluna nova não lê nem redefine a data-base do ciclo: ela varre o log por tipo, e só. Que o ciclo parta da última manutenção de qualquer tipo é simplificação anterior a este trabalho — corrigi-la é mexer no vencimento de 171 equipamentos, com gate próprio, e ninguém pediu. Fica registrada em D-3a6-22b. |
| **Um helper de data-base compartilhado com `nextPmoc`** | Chegou a ser planejado, e **sai**: existia só para sustentar a renomeação que D-3a6-22 descartou. Sem ela, é código a mais sem consumidor. |
| O `.sort()` em cima do array de `_logCache` dentro de `getLatestLogDate` | Efeito colateral real e pré-existente (ordena o cache no lugar). Consertar aqui misturaria uma correção de dado numa tarefa de apresentação; fica anotado no SUMMARY como pendência conhecida. |

---

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Barra do topo limpa, navegação dentro dela no computador, Painel e Parque</name>

  <files>
refrigeracao/index.html,
tests/refrigeracao-topbar-parque.test.js
  </files>

  <behavior>
    - A inscrição institucional literal aparece exatamente **2 vezes** no arquivo (as duas telas de login), e **nenhuma** dentro da região `<div id="topbar">…</div>`.
    - A região `#topbar` não contém mais nenhum `<span>`.
    - `#bottom-nav` continua com exatamente **cinco** `.nav-btn`, na ordem `dash, inv, os, pmoc, alert`, com os rótulos `Painel`, `Parque`, `OS`, `PMOC`, `Alertas`.
    - Os identificadores de D-3a6-04 continuam todos presentes no arquivo.
    - `#btn-planilha` tem `title` e `aria-label` falando em "parque", e nenhum dos dois fala em "inventário".
    - O título da gaveta da planilha é `'Planilha do parque'`.
    - As 5 ocorrências de "Guardada — volta ao inventário" continuam no arquivo (prova de que a renomeação não vazou para o vocabulário de OS).
    - Dentro do bloco `@media (min-width:1024px)`: `#topbar` recebe `display:contents`; `#bottom-nav` é colocado na linha 1 da grade de `#app`; `#app` recebe o fundo escuro e `#content` o fundo claro; `.nav-btn.active` carrega os quatro sinais (sublinhado, fundo, `font-weight:700`, cor clara).
    - Continua existindo **exatamente um** `@media` novo nos blocos `<style>`, e ele é `(min-width:1024px)`.
    - `grid-template-areas` com a área de coluna lateral não existe mais no arquivo.
  </behavior>

  <action>
Escreva PRIMEIRO os casos em `tests/refrigeracao-topbar-parque.test.js` e veja-os falhar (D-3a6-19).
Padrão dos 16 gates de refrigeração: `node:test` + `node:assert/strict`, leitura do HTML,
recorte por marcador de texto para o que é estrutural.

**1a. Marcação da barra do topo.** Em `#topbar`, apague apenas o elemento `span` de dentro de
`.logo`, deixando o texto `PMOC.Refrigeração` como único conteúdo da marca. Não toque no `<img>`,
nem em `#topbar-action`, nem no botão de sair, nem nas duas telas de login, nem no cabeçalho do
documento impresso (D-3a6-03). **Não mexa na regra `#topbar .logo span` da folha de estilo**
(D-3a6-16): ela fica órfã de propósito, e mexer nela custa o fixture congelado.

**1b. Rótulos.** No primeiro `.nav-btn`, troque o rótulo por `Painel`; no `#nav-inv`, por `Parque`.
Só o texto visível — `navTo('dash',…)`, `navTo('inv',…)`, o `id` e os ícones não mudam
(D-3a6-04/06). Em `#btn-planilha`, reescreva `title` e `aria-label` para falarem da planilha do
parque. Em `abrirPainelPlanilha`, o texto que vai para `el('dh-id')` passa a nomear o parque.
Não toque em nenhuma outra frase que contenha a palavra do cadastro (D-3a6-05) — em particular
o vocabulário de destino de OS e as mensagens de conferência da importação.

**1c. Navegação dentro da barra, no computador.** Tudo **dentro do `@media (min-width:1024px)`
que já existe** (mesclado no último bloco `<style>` do arquivo, logo antes de `qrcode.js`).
Nenhum bloco `<style>` novo, nenhuma regra fora da `@media` (D-3a6-16 e a deviação de 260822-8rz).

Substitua o trecho de navegação lateral (de `#app{display:grid…}` até a regra de `.nav-badge`,
inclusive) por uma linha de topo com cinco itens reais:

  - `#app`: grade de duas linhas (`56px 1fr`) e cinco colunas — `auto auto 1fr auto auto`, para
    ícone, marca, navegação, buscar e sair; `column-gap:10px` repõe o `gap` que o `#topbar`
    dava; fundo `#071D41` (D-3a6-08).
  - `#topbar{display:contents}` — o box some, os quatro filhos viram itens da grade (D-3a6-07).
  - Cada filho recebe `grid-row:1`, sua `grid-column` explícita (1 para o ícone, 2 para a marca,
    4 para `#topbar-action`, 5 para o último `button` de `#topbar`) e `align-self:center`;
    o ícone repõe o recuo esquerdo de 16px e o botão de sair o direito, que eram o
    `padding` do `#topbar`. **Não confie em posicionamento automático** — com um item externo
    no meio, a ordem só é determinística se estiver escrita.
  - `#bottom-nav`: `grid-row:1; grid-column:3; min-width:0;` (sem `min-width:0` a coluna `1fr`
    não encolhe abaixo do conteúdo e a grade estoura a janela), fundo transparente, sem borda,
    sem sombra, `flex-direction:row`, `justify-content:flex-start`, `overflow-x:auto`
    (numa janela estreita as abas rolam; nunca se amassam), `padding-bottom:0`.
  - `#content`: `grid-row:2; grid-column:1/-1;` e fundo `#F0F2F5` — hoje ele é transparente e
    herda do `body`; com `#app` escuro, sem essa regra a área de conteúdo sai escura (D-3a6-08).
  - `.nav-btn`: `flex:0 0 auto` (não estica mais para preencher a coluna), linha em vez de
    coluna, `padding:0 14px`, `font-size:13px`, cor `rgba(255,255,255,.72)`, altura cheia.
  - `.nav-btn::before` vira sublinhado: preso à base, altura 3px, largura cheia (D-3a6-09).
  - `.nav-btn.active`: sublinhado `#fff`, fundo `rgba(255,255,255,.10)`, `font-weight:700`,
    cor `#fff`; e uma regra irmã para o ícone ativo, senão ele fica no `#1351B4` da folha de
    celular, que sobre o azul-marinho quase não se lê.
  - `.nav-badge`: `position:static; transform:none; margin-left:6px` (D-3a6-10).
  - O resto do bloco (`.page`, `#fab`, `.search-wrap`, `.lista-tabela`, `.tab-barra`,
    `#drawer-panel`) fica intocado.

Deixe um comentário `/* ── … ── */` de uma linha registrando por que `#topbar` é
`display:contents` e por que o fundo escuro migrou para `#app` — sem isso a próxima pessoa
"conserta" devolvendo o fundo ao `#topbar` e o topo fica claro.

**1d. Gate.** `tests/refrigeracao-topbar-parque.test.js` com os casos de `&lt;behavior&gt;`.
Para a inscrição, afirme a **contagem exata de 2 no arquivo inteiro** e a ausência dentro do
recorte `<div id="topbar">` → primeiro `</div>` — contagem, não presença, porque um gate que só
procura no topo não notaria a inscrição sumindo do login. Para a estrutura do CSS, recorte o
bloco `@media (min-width:1024px)` por casamento de chaves e afirme sobre o recorte, nunca sobre
o arquivo todo. **Não duplique** o caso `D-8rz-04` (CSS fora de `@media` igual ao fixture): ele
é permanente, roda no mesmo `node --test` e continuar verde é o critério desta tarefa.
  </action>

  <verify>
    <automated>node --test tests/refrigeracao-topbar-parque.test.js tests/refrigeracao-desktop.test.js tests/inventario-ordem-refrigeracao.test.js</automated>
    <automated>test "$(grep -c 'CMASM · DME · UASG 744030' refrigeracao/index.html)" = "2"</automated>
    <automated>test -z "$(git diff --name-only -- tests/fixtures/refrigeracao-css-mobile.css)"</automated>
    <automated>test "$(grep -c 'shared/' refrigeracao/index.html)$(grep -c 'pmoc.css' refrigeracao/index.html)$(grep -c 'pmoc-tema' refrigeracao/index.html)$(grep -c 'data-theme' refrigeracao/index.html)" = "0000"</automated>
    <automated>node --test</automated>
  </verify>

  <done>
A inscrição institucional saiu da barra do topo e continua nas duas telas de login e no papel
(contagem 2, zero no recorte do `#topbar`). As abas se leem `Painel` e `Parque`, com todos os
identificadores intactos. No computador os cinco botões ficam dentro da barra escura, entre a
marca e Buscar/Sair, com estado ativo de quatro sinais e distintivos colados aos rótulos; a
coluna lateral de 220px não existe mais. O fixture de CSS de celular **não** aparece no
`git diff`. `node --test` verde, com os casos novos somados ao baseline de 812.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: As doze colunas do parque — localização separada, Marca, Modelo e Última inspeção</name>

  <files>
refrigeracao/index.html,
tests/refrigeracao-topbar-parque.test.js,
tests/refrigeracao-desktop.test.js
  </files>

  <behavior>
    - `COLS_INV` tem **doze** colunas, nesta ordem exata de `id`: `id`, `area`, `predio`, `local`, `tipo`, `btu`, `fabricante`, `modelo`, `estado`, `criticidade`, `prox`, `ultimaInspecao`.
    - Os doze rótulos, na mesma ordem: `#`, `Área`, `Prédio`, `Local`, `Tipo`, `BTU`, `Marca`, `Modelo`, `Estado`, `Criticidade`, `Próx. manutenção`, `Última inspeção`.
    - `area`: `valor`/`texto` devolvem `e.area`; AZUL antes de VERMELHA em crescente, o inverso em decrescente; área vazia por último nas **duas** direções.
    - `predio`: `texto()` devolve só o prédio — nenhuma forma composta sobrou. Dois equipamentos com o mesmo prédio e áreas diferentes **empatam** em `predio` (a área não vaza para a comparação do prédio) e desempatam em `area`.
    - `fabricante`: ordena por `e.fabricante`, texto vazio por último nas duas direções.
    - `modelo`: **tolera `e.modelo` nulo** (`dbToEquip` não o preenche) — não lança em `valor`, `texto`, na ordenação nem no filtro, e nulo vai para o fim nas duas direções.
    - `ultimaInspecao`: `tipo:'numero'`; `valor` é o **instante** da data do log mais recente com `tipo === 'INSPEÇÃO'`, `texto` é a mesma data em `dd/mm/aaaa`, e `Sem hist.` quando não houver nenhuma inspeção — em qualquer das duas direções o sem-histórico fica no fim.
    - **Equipamento com inspeções E corretivas devolve a data da INSPEÇÃO mais recente, não a do log mais recente** (uma corretiva posterior não vira "última inspeção").
    - **Equipamento só com corretivas devolve `Sem hist.`** — e um equipamento sem log nenhum, também.
    - A varredura **não altera o array devolvido por `getEquipLog`** (nada de `.sort()` no lugar): dois `valor()` seguidos no mesmo equipamento devolvem o mesmo instante e deixam o log na mesma ordem em que chegou.
    - `filtrarInventario(dados,'','todos',{area:'vermelha'})` devolve só os da área vermelha; `{area:'vermelha', fabricante:'springer'}` combina em conjunção; sem acento e sem caixa; filtrar por `ultimaInspecao` casa o texto `dd/mm/aaaa`.
    - `filtrarInventario` com TRÊS argumentos continua devolvendo exatamente o de antes.
    - `tabCabecalho('inv', COLS_INV)` emite doze `<th>` na ordem acima; `tabCorpo` emite doze `<td>` por linha.
    - Um `e.fabricante` ou `e.modelo` contendo `<script>` sai escapado na célula.
  </behavior>

  <action>
Escreva PRIMEIRO os casos e veja-os falhar (D-3a6-19).

**2a. `COLS_INV` — as doze entradas (D-3a6-01).** Ordem final: `id`, `area`, `predio`, `local`,
`tipo`, `btu`, `fabricante`, `modelo`, `estado`, `criticidade`, `prox`, `ultima`.
  - `area` (nova): rótulo `Área`, `tipo:'texto'`, `valor`/`texto` = `e.area`, sem `celula()`.
  - `predio`: perde o rótulo composto e o `texto()` concatenado — rótulo `Prédio`,
    `valor`/`texto` = `e.predio`.
  - `local`: inalterada, só muda de posição (segunda → quarta).
  - `fabricante` (nova): rótulo **`Marca`**, `tipo:'texto'`, `valor`/`texto` = `e.fabricante`
    (D-3a6-21 — o `id` da coluna é o nome do campo, o rótulo é a palavra do usuário).
  - `modelo` (nova): rótulo `Modelo`, `tipo:'texto'`, `valor`/`texto` = `e.modelo` **com queda
    para texto vazio** — `dbToEquip` não preenche esse campo e ele chega `null` nas 171 linhas;
    sem a queda, `texto()` devolve `null` e o filtro/`esc()` teriam de adivinhar.
  - `tipo`, `btu`, `estado`, `criticidade`, `prox`: inalteradas.
  - `ultimaInspecao` (nova), **D-3a6-22**: rótulo `Última inspeção`, `tipo:'numero'`.
    `valor` varre `getEquipLog(e.id)` pelo maior `date` entre as entradas com
    `tipo === 'INSPEÇÃO'` e devolve o instante, ou `null` se não houver nenhuma. `texto` é
    `fmtDate(...)` dessa data, ou `Sem hist.` — o mesmo vocabulário que `dueBadgeHtml` já usa
    para ausência. Sem `celula()`: a pílula de `Próx. manutenção` diz urgência, e urgência é
    sobre o futuro; uma data passada é só uma data.
    **Varra sem ordenar no lugar** — um `reduce`/laço pelo maior valor, nunca `.sort()` sobre o
    array devolvido por `getEquipLog`, que é o próprio `_logCache[id]`. `getLatestLogDate` faz
    exatamente isso hoje e é o efeito colateral que este plano registrou como pendência; não
    ganhe um segundo.
    **Escreva o comentário de D-3a6-22b junto da coluna:** que `nextPmoc` **não** parte desta
    data (parte de `e.ultimaManutencao || getLatestLogDate(e.id)`, o último log de qualquer
    tipo) e que, portanto, `Próx. manutenção` menos `Última inspeção` **não** é o intervalo de
    `PMOC_INT`. Sem essa linha, a próxima pessoa "conserta" uma discordância que é esperada.
    **Não** extraia helper de data-base compartilhado com `nextPmoc`, e não toque em `nextPmoc`.

Nada mais precisa mudar: `tabCabecalho`/`tabCorpo` percorrem a lista na ordem, e
`filtrarInventario` resolve a coluna do quarto argumento lendo `COLS_INV` — filtro por área,
marca, modelo e data nascem prontos (D-3a6-13). Não mexa em `cmpInv`, `invOrdem`,
`setInvOrdem`, `PMOC_INT`, `proxManut`, `isVencido`, nos chips, no CSV nem na folha de
etiquetas.

**2b. Largura e rolagem (D-3a6-23).** Dentro do `@media (min-width:1024px)` que já existe,
acrescente ao final do trecho de tabela: `#inv-list` com `overflow:auto`, um teto de altura
(`max-height:calc(100vh - 240px)`) e um piso (`min-height:320px`), mais o `-webkit-overflow-scrolling`
que o resto do arquivo já usa. **Só `#inv-list`** — as outras cinco tabelas têm no máximo seis
colunas e não precisam. Não declare largura de coluna nem `table-layout` (D-3a6-14).
Deixe um comentário de uma linha registrando que o teto de altura existe **para o
`position:sticky` do `<thead>` continuar funcionando** — com `overflow` no contêiner, o
cabeçalho passa a se prender a ele, e sem altura limitada ele nunca gruda. Sem essa linha, a
próxima pessoa remove o `max-height` por achá-lo arbitrário e mata D-8rz-20 em silêncio.

**2c. O caso de ontem que muda (D-3a6-18).** Em `tests/refrigeracao-desktop.test.js`, reescreva
`COLS_INV.predio ordena pelo prédio, não pelo texto concatenado "Prédio · Área"`: a intenção
continua sendo "prédio ordena por prédio", mas a prova passa a ser que a área **não participa**
da comparação de prédio e que `texto()` do prédio é só o prédio. Uma linha de comentário
dizendo que a forma composta foi separada em 260823-3a6. Não apague o caso; não relaxe para
`assert.ok(true)`. Rode também `tests/refrigeracao-qr-nfc.test.js` e
`tests/refrigeracao-planilha.test.js`: o sandbox do primeiro monta `filtrarInventario` à mão e
pode precisar de mais um global (foi o que aconteceu em 260822-8rz, Rule 3).

**2d. Gate.** Acrescente ao `tests/refrigeracao-topbar-parque.test.js` os casos de
`&lt;behavior&gt;`, avaliando o núcleo real no sandbox `node:vm` (mesmo recorte
`/* ── tabelas de desktop: núcleo puro ── */` → `</script>` do gate de ontem), nunca por regex
sobre a marcação. **D-3a6-24: a ordem e o conjunto das doze colunas são afirmados de uma vez**
— uma comparação da lista de `id` e uma da lista de `rotulo`, não doze asserções soltas: é o
que faz uma coluna inserida no lugar errado falhar. Para `ultimaInspecao`, três casos de
comportamento, com `getEquipLog` mockado no sandbox: **(1)** equipamento com inspeções **e**
corretivas — inclusive uma corretiva mais recente que a última inspeção — devolve a data da
**inspeção** mais recente; **(2)** equipamento só com corretivas devolve `Sem hist.` e vai para
o fim nas duas direções; **(3)** dois `valor()` seguidos devolvem o mesmo instante e o array do
log continua na ordem em que chegou (prova de que a varredura não ordenou no lugar).
O sandbox de `tests/refrigeracao-desktop.test.js` já mocka `getEquipLog` devolvendo `[]` — os
casos de lá continuam verdes sem edição, e é neste gate novo que os logs entram.
  </action>

  <verify>
    <automated>node --test tests/refrigeracao-topbar-parque.test.js tests/refrigeracao-desktop.test.js tests/refrigeracao-qr-nfc.test.js tests/refrigeracao-planilha.test.js tests/inventario-ordem-refrigeracao.test.js</automated>
    <automated>test -z "$(git diff --name-only -- tests/fixtures/refrigeracao-css-mobile.css)"</automated>
    <automated>node --test</automated>
  </verify>

  <done>
A tabela do parque mostra as doze colunas na ordem que o usuário mandou, com `Área`, `Prédio` e
`Local` separadas, `Marca`/`Modelo` lendo `fabricante`/`modelo` e `Última inspeção` mostrando o
log mais recente de tipo INSPEÇÃO — com o comentário registrando que essa data não é a base de
`nextPmoc` e que a subtração contra `Próx. manutenção` não fecha. Cada coluna ordena e
filtra pelo próprio dado, com vazio por último nos dois sentidos e sem uma linha nova de
ordenação. `modelo` nulo não lança em lugar nenhum. Quando as doze não cabem, quem rola é a
caixa da tabela, com o cabeçalho ainda grudado. O gate trava conjunto e ordem; o caso de ontem
sobre a coluna composta foi reescrito, não apagado. `node --test` verde.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dado do Supabase → marcação da tabela | `e.area`, `e.predio`, `e.fabricante` e `e.modelo` são texto livre vindo do cadastro e vão para dentro de `<td>`; `e.modelo` chega `null` nas 171 linhas |
| valor digitado no filtro de coluna → marcação do cabeçalho | o valor volta para o `value=` do `<input type=search>` a cada tecla |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-3a6-01 | Tampering | `COLS_INV.area`/`fabricante`/`modelo` → `tabCorpo` | medium | mitigate | nenhuma das três declara `celula()`, então `tabCorpo` aplica `esc(col.texto(item))` — o mesmo caminho já provado para Tipo e Local; caso de gate em 2d com valor contendo marcação |
| T-3a6-04 | Denial of service | `COLS_INV.modelo` com `e.modelo` nulo | low | mitigate | `valor`/`texto` caem para texto vazio; sem a queda, `texto()` devolveria `null` e a tela do parque quebraria em 171 de 171 linhas — o caso mais provável de todos, porque é o estado atual do banco |
| T-3a6-02 | Tampering | filtro de coluna → `tabCabecalho` | medium | accept | o caminho não muda nesta tarefa e já tem caso de gate próprio em `tests/refrigeracao-desktop.test.js` (valor com `<script>` sai escapado) |
| T-3a6-03 | Information disclosure | inscrição institucional | low | accept | sai da barra do topo por decisão de apresentação; continua nas duas telas de login e no documento impresso, que é onde ela identifica o órgão |
| T-3a6-SC | Tampering | instalação de pacote | low | accept | nenhuma: zero-build, sem `npm`, sem dependência nova, sem CDN novo |
</threat_model>

<verification>
1. `node --test` — 812 do baseline + os casos novos, **zero falhas**.
2. `git diff --name-only` toca exatamente: `refrigeracao/index.html`,
   `tests/refrigeracao-topbar-parque.test.js`, `tests/refrigeracao-desktop.test.js`.
   **`tests/fixtures/refrigeracao-css-mobile.css` NÃO pode aparecer** (D-3a6-16); se aparecer,
   D-3a6-17 se aplica e o SUMMARY explica a regra que mudou.
3. Nada em `shared/`, `supabase/`, nos outros módulos, nem em `/home/luc/DEV_ERP`.
4. Os quatro `grep -c` do PLAT-15 em `refrigeracao/index.html` continuam em 0 (D-04).
5. Conferência manual (sem navegador neste ambiente — mesma pendência de PLAT-15/16 e de
   260822-8rz): registrar o roteiro em `TESTES.md`.
   - **1440px:** a barra do topo mostra ícone, `PMOC.Refrigeração`, as cinco abas e, à direita,
     Buscar e Sair; a aba ativa se distingue por sublinhado + fundo + peso + cor; os
     distintivos de OS/PMOC/Alertas ficam colados aos rótulos; não há coluna lateral; a área de
     conteúdo é clara e a faixa do topo escura; a tabela do parque mostra as doze colunas na
     ordem pedida; ordenar e filtrar por Área e por Marca funciona; a coluna Modelo aparece
     vazia (0 de 171 preenchidos — é o esperado, D-3a6-21); a gaveta ainda entra pela direita.
   - **1024px exatos (a faixa apertada):** a tabela rola na horizontal **dentro da própria
     caixa**; a página **não** rola na horizontal e nenhuma coluna fica cortada; rolando as 171
     linhas, o cabeçalho continua grudado no topo da tabela. As cinco abas cabem entre a marca
     e as ações; se não couberem, rolam na horizontal — nunca se amassam nem empurram Sair
     para fora.
   - **`Última inspeção`, em qualquer largura:** num equipamento com inspeção registrada, a
     data é a da inspeção mais recente — mesmo havendo corretiva posterior; num equipamento sem
     inspeção nenhuma, `Sem hist.`, ainda que `Próx. manutenção` traga data (é a discordância
     esperada de D-3a6-22b, e é ela que o comentário no código explica). **Não** conferir
     subtração entre as duas colunas: elas não fecham, por construção.
   - **375px:** idêntico a hoje, com a barra inferior de cinco botões; a única diferença visível
     é a marca sem a segunda linha e os dois rótulos renomeados.
</verification>

<success_criteria>
- [ ] A inscrição institucional aparece 2 vezes no arquivo e nenhuma dentro de `#topbar`
- [ ] As abas se leem `Painel` e `Parque`; nenhum id, função, chave de estado ou nome de arquivo mudou
- [ ] `title`/`aria-label` de `#btn-planilha` e o título da gaveta falam do parque; o vocabulário de OS e as mensagens da importação não foram tocados
- [ ] Em `>=1024px` a navegação está dentro da barra do topo, com os quatro sinais de estado ativo e os distintivos inline
- [ ] Abaixo de 1024px nada mudou além dos rótulos e da marca sem segunda linha
- [ ] Nenhuma regra de CSS fora da `@media` foi criada, alterada ou removida; nenhum `<style>` novo; o fixture não foi regenerado
- [ ] Continua existindo exatamente um `@media` novo, e ele é `(min-width:1024px)`
- [ ] `COLS_INV` tem as doze colunas na ordem mandada pelo usuário (`#`, Área, Prédio, Local, Tipo, BTU, Marca, Modelo, Estado, Criticidade, Próx. manutenção, Última inspeção), com conjunto e ordem travados no gate
- [ ] Cada coluna nova ordena e filtra pelo próprio dado, com vazio por último nos dois sentidos; `e.modelo` nulo não lança em lugar nenhum
- [ ] `Última inspeção` mostra o log mais recente de tipo INSPEÇÃO (não o log mais recente), diz `Sem hist.` quando não há nenhuma, e não ordena o `_logCache` no lugar
- [ ] O achado de D-3a6-22b está escrito no código, ao lado da coluna, **e** no SUMMARY: `nextPmoc` parte de outra data, e a subtração entre as duas colunas de data não dá o intervalo de `PMOC_INT`
- [ ] Com as doze colunas apertadas, quem rola é `#inv-list`, não a página, e o cabeçalho continua grudado
- [ ] O caso do gate de ontem sobre a coluna composta foi reescrito, com o motivo registrado
- [ ] `node --test` verde
</success_criteria>

<output>
Criar `.planning/quick/260823-3a6-refrigeracao-topbar-limpa-com-navegacao-/260823-3a6-SUMMARY.md`
ao terminar, citando as decisões `D-3a6-01`..`D-3a6-24` por id.

**Obrigatório no SUMMARY, em parágrafo próprio (D-3a6-22b):** `nextPmoc` não parte da data que a
coluna `Última inspeção` mostra — a base do ciclo é `e.ultimaManutencao || getLatestLogDate(e.id)`,
o último log de qualquer tipo, e só o intervalo muda por tipo (`PMOC_INT`). Logo, `Próx. manutenção`
menos `Última inspeção` **não** dá o intervalo da criticidade, e as duas colunas podem discordar na
mesma linha: um equipamento sem nenhuma inspeção mostra `Sem hist.` ao lado de uma previsão com
data. Isso é esperado, não defeito.

Commits: um por tarefa, em português, prefixados `feat(260823-3a6):`.
</output>
