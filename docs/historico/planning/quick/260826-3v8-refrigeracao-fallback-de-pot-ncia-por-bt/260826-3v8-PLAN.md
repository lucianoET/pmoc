---
phase: quick-260826-3v8
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - refrigeracao/index.html
  - tests/refrigeracao-energia-fotos.test.js
  - tests/fixtures/refrigeracao-os-gaveta.json
  - CLAUDE.md
  - README.md
  - TESTES.md
autonomous: true
requirements:
  - QT-3v8-ENERGIA
  - QT-3v8-FOTOS

must_haves:
  truths:
    - "O card 'Energia Elétrica — Estimativa' deixa de mostrar 0 kW / 0 kWh / R$ 0,00 com o parque real (corrente_nominal nula nas 175 linhas, btu > 0 em 173)."
    - "Onde existe corrente nominal de placa, a conta continua sendo tensão × corrente (×√3 em 380 V) — dado de placa vence estimativa, byte a byte como hoje."
    - "Onde não existe corrente nem capacidade, a potência continua 0 — a estimativa não inventa número."
    - "A nota do card diz, na tela, que onde falta corrente de placa a potência é estimada pela capacidade (BTU/h, COP 3)."
    - "No celular, tocar em Fotos na execução da OS oferece câmera, fototeca e arquivos no mesmo seletor."
  artifacts:
    - "refrigeracao/index.html — dvPotenciaW com fallback por BTU, nota do card atualizada, input de fotos sem o atributo que força a câmera"
    - "tests/refrigeracao-energia-fotos.test.js — gate novo, comportamental (node:vm sobre o código real)"
    - "tests/fixtures/refrigeracao-os-gaveta.json — impressão digital da gaveta atualizada no único ponto que mudou"
  key_links:
    - "dvPotenciaW → potW/kwhMes/custoMes/kwhArea em dvRenderCharts (quatro chamadas, uma função)"
    - "tests/refrigeracao-os-pagina.test.js compara a gaveta byte a byte com o fixture — mexer no markup do input sem atualizar o fixture quebra 5 cenários"
    - "tests/refrigeracao-desktop.test.js compara o CSS fora de @media byte a byte (D-cf8-20) — nenhuma regra de CSS pode mudar aqui"
---

<objective>
Duas correções pequenas e independentes em `/refrigeracao`, o módulo congelado (D-04):

1. **Energia zerada.** `dvPotenciaW` só sabe multiplicar tensão × corrente, e `equipamentos.corrente_nominal` é NULL nas 175 linhas do banco (a migração 05 nunca importou o campo). Resultado: potência 0 → kWh/mês 0 → custo 0 no card "Energia Elétrica — Estimativa", com 8.890.000 BTU/h em operação do lado do dado que existe. Ganha um **fallback pela capacidade** quando não há corrente de placa: `P_W = btu ÷ 3412 ÷ COP`, COP 3 (~1,17 kW por 12.000 BTU/h).
2. **Fotos só pela câmera no celular.** O input de evidência da execução força a câmera; removê-lo faz o navegador oferecer câmera, fototeca e arquivos no mesmo seletor.

Purpose: o card de energia hoje é um zero que parece dado, e a evidência fotográfica só pode ser tirada na hora — quem fotografou antes, no equipamento, não consegue anexar.
Output: `refrigeracao/index.html` alterado em três pontos, um gate novo em `node:test`, o fixture da gaveta atualizado no único byte que mudou, e o fato registrado.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md

Diagnóstico já confirmado contra o banco de produção pelo orquestrador — **não refazer**, não consultar o Supabase de novo:
- `equipamentos.corrente_nominal` é NULL nas 175 linhas; `dbToEquip` faz `parseFloat(o.correnteNominal) || 0`.
- `horas_dia`/`dias_semana`/`tensao` preenchidos em 171 linhas; `btu > 0` em 173 (8.890.000 BTU/h em operação).
- Dado real de corrente entra depois pela planilha exportar/importar já existente (quick-260822-5hy) — **fora do escopo desta task**.

Pontos exatos em `refrigeracao/index.html` (linhas do HEAD atual, confira o texto, não o número):
- ~5161-5166: `dvFmt` e `dvPotenciaW`, logo após `DV_CAT`/`DV_SEQ`, dentro do `<script>` que começa em ~5156.
- ~5230-5257: bloco `/* ── energia elétrica ── */` de `dvRenderCharts`, terminando na `<div class="chart-note">`.
- ~3868: o input de fotos da execução (`id="man-ex-fotos"`), dentro do montador da seção "3 · Execução".

Baseline conferido nesta máquina: `node --test tests/*.test.js` → **945 testes, 0 falhas**. A forma `node --test tests/` **falha com MODULE_NOT_FOUND** neste Node (v24.18.0) — use sempre o glob.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: gate novo, escrito e visto FALHANDO antes da mudança</name>
  <files>tests/refrigeracao-energia-fotos.test.js</files>
  <behavior>
    Potência (comportamental, `dvPotenciaW` rodando de verdade num `node:vm`):
    - `{tensao:220, correnteNominal:5.5}` → `1210` (com corrente, é tensão × corrente, como hoje)
    - `{tensao:220, correnteNominal:'5.5'}` → `1210` (string do banco não muda o resultado)
    - `{tensao:380, correnteNominal:5.5}` → `3620` (trifásico ×1.732, como hoje)
    - `{tensao:220, correnteNominal:5.5, btu:12000}` → `1210` — dado de placa **vence** a estimativa, nunca soma com ela
    - `{tensao:220, correnteNominal:null, btu:12000}` → `1172` (fallback: 12000 ÷ 3412 ÷ 3 × 1000, arredondado)
    - `{tensao:220, correnteNominal:0, btu:9000}` → `879`
    - `{tensao:380, correnteNominal:null, btu:12000}` → `1172` — o mesmo valor do caso 220 V: √3 é da conta de corrente, a capacidade não muda com a tensão
    - `{tensao:220, correnteNominal:null, btu:0}` → `0`
    - `{}` → `0` (sem corrente e sem capacidade não se inventa potência)
    - `{correnteNominal:5.5}` → `1210` (tensão ausente continua caindo no padrão 220)
    Marcação (asserções estruturais por recorte, mesmo precedente de D-5hy-02/08):
    - o input de fotos da execução continua com `accept="image/*"` e `multiple`
    - o mesmo input **não** traz mais o atributo que restringe a origem do arquivo à câmera, e esse atributo não aparece em nenhum outro ponto de `refrigeracao/index.html`
    - a `chart-note` do card de energia menciona `BTU` e `COP` — a tela diz de onde veio o número estimado
  </behavior>
  <action>
Crie `tests/refrigeracao-energia-fotos.test.js` seguindo o idioma dos outros gates de `/refrigeracao` (`tests/refrigeracao-planilha.test.js` é o modelo mais próximo): `node:test` + `node:assert/strict`, `fs.readFileSync` de `refrigeracao/index.html`, helper `recorte(marcadorIni, marcadorFim)` com `assert.ok(ini > 0 && fim > ini, ...)`, e `vm.runInContext` num contexto criado por `vm.createContext`. Sem framework, sem `package.json`, sem dependência nova.

O recorte da potência vai de `'function dvPotenciaW('` até `'function dvDonut('` — **antes** de tocar em `refrigeracao/index.html` esse recorte já existe e roda, então o teste falha pelo valor errado (0 no lugar de 1172), que é a falha certa; se falhasse por recorte não encontrado, o gate não estaria provando nada. Depois da Task 2 o mesmo recorte precisa carregar as constantes junto com a função — mova o marcador inicial para `'/* ── potência estimada'`, o comentário que a Task 2 escreve logo antes das constantes, **e deixe assim já agora** (o recorte falha nesta task e passa na próxima; escreva a nota explicando isso no cabeçalho do arquivo, como os outros gates fazem).

O cabeçalho do arquivo, em português, deve registrar: (a) que este gate foi escrito e visto falhando antes da mudança, o procedimento dos gates de `/refrigeracao`; (b) por que o fallback existe — `corrente_nominal` é NULL nas 175 linhas do banco, então a conta de placa produzia zero para o parque inteiro; (c) que dado de placa vence estimativa, e que por isso o caso com corrente **e** capacidade é testado explicitamente.

Para a marcação, recorte a linha do input de fotos por `'id="man-ex-fotos"'` até o `'/>'` seguinte e afirme sobre esse pedaço; a ausência do atributo que força a câmera afirme sobre o arquivo inteiro, com `assert.ok(!HTML.includes(...))` montando o literal por concatenação a partir da palavra do atributo, não escrevendo-o inteiro no fonte do teste — assim o próprio gate não é a ocorrência que ele proíbe.
<!-- planner-discipline-allow: capture= -->
<!-- planner-discipline-allow: capture="environment" -->

Para a `chart-note`, recorte de `'/* ── energia elétrica ── */'` até `'/* ── BTU por prédio'` e afirme que o trecho contém `'chart-note'`, `'BTU'` e `'COP'`.
  </action>
  <verify>
    <automated>node --test tests/refrigeracao-energia-fotos.test.js 2>&1 | tail -8   # DEVE falhar nesta task (RED): valores de potência e asserções de marcação ainda contradizem o arquivo</automated>
  </verify>
  <done>O arquivo existe, roda, e falha por asserção (valores/marcação) — não por erro de sintaxe nem por recorte não encontrado no bloco de energia. Os 945 testes existentes continuam passando: `node --test tests/*.test.js` mostra as falhas apenas do arquivo novo.</done>
</task>

<task type="auto">
  <name>Task 2: fallback por BTU, nota do card, seletor de fotos livre — e o fixture da gaveta acompanhando o único byte que mudou</name>
  <files>refrigeracao/index.html, tests/fixtures/refrigeracao-os-gaveta.json</files>
  <action>
**(a) `dvPotenciaW` com fallback.** Substitua o corpo atual por, exatamente nesta forma (as duas constantes vêm antes da função, no mesmo `<script>`, imediatamente acima dela):

Um comentário de bloco iniciado por `/* ── potência estimada` explicando em português: sem corrente de placa (o campo é nulo nas 175 linhas — a migração 05 nunca o importou), a potência é estimada pela capacidade, BTU/h → kW térmicos (÷ 3412) → kW elétricos (÷ COP); COP 3 dá ~1,17 kW por 12.000 BTU/h; com corrente cadastrada, tensão × corrente continua sendo a conta, porque dado de placa vence estimativa.

Em seguida `var DV_BTU_POR_KW = 3412;` e `var DV_COP = 3;` (números soltos no meio da conta não se conferem contra nada), e a função:

```
function dvPotenciaW(e){
  var v=e.tensao||220, i=parseFloat(e.correnteNominal)||0;
  if (i) return v===380 ? Math.round(1.732*v*i) : Math.round(v*i);
  var btu = e.btu||0;
  return btu>0 ? Math.round(btu/DV_BTU_POR_KW/DV_COP*1000) : 0;
}
```

O `parseFloat` é deliberado e é a única mudança de semântica no ramo antigo: normaliza string, null e `'0'` a zero, de modo que **todo** caso sem corrente utilizável cai no fallback; para os valores que hoje produzem número, o resultado é idêntico ao de antes.

**(b) Nota do card.** No bloco `/* ── energia elétrica ── */`, a `<div class="chart-note">` passa a dizer que onde há corrente nominal de placa a potência é corrente × tensão (380 V trifásico ×√3), e onde falta — hoje, a maior parte do parque — é estimada pela capacidade: BTU/h ÷ 3412 ÷ COP 3, ~1,17 kW por 12.000 BTU/h. Preserve o resto da frase existente (horas de uso semanais cadastradas, fator de ciclo do compressor de 70%, apenas máquinas operacionais (OK), o consumo real varia com carga térmica e regulagem). É uma string JS entre aspas simples: não use apóstrofo dentro dela.

**(c) Input de fotos.** No montador da seção "3 · Execução" (`id="man-ex-fotos"`), remova o atributo que restringe a origem do arquivo à câmera traseira, junto com o espaço que o precede. `accept="image/*"` e `multiple` ficam.
<!-- planner-discipline-allow: capture="environment" -->

**(d) Fixture da gaveta.** `tests/fixtures/refrigeracao-os-gaveta.json` é a impressão digital byte a byte do que `manMontarOS` escreve (D-jar-17), e guarda esse mesmo input em dois cenários (`b` e `c`). O fato mudou de propósito, então o fixture aprende o fato novo — padrão do projeto (D-cf8-28), nenhum cenário apagado. A mudança é **exatamente** a remoção das duas ocorrências do mesmo trecho na sua forma escapada em JSON, e nada mais; regenerar pelo procedimento longo documentado no cabeçalho de `tests/refrigeracao-os-pagina.test.js` produziria arquivo idêntico, porque a única diferença na saída de `manMontarOS` é esse literal. Faça a substituição por texto, com Node, sobre o literal escapado (espaço inicial incluído), e **prove** que só isso mudou: o arquivo tem de ficar **exatamente 48 bytes menor** (2 × 24), e `git diff --numstat` tem de acusar 1 arquivo. Se o delta não for 48, pare e investigue — algo além do pretendido foi tocado.

Nada de CSS: D-cf8-20 compara o CSS fora de `@media` byte a byte com `tests/fixtures/refrigeracao-css-mobile.css`. Nada de `import` de `shared/` (D-04). Nada de consulta nova ao Supabase.

Commits em português: `feat(260826-3v8): …` para (a)+(b), `feat(260826-3v8): …` para (c)+(d), ou um commit por assunto — o gate da Task 1 entra com `test(260826-3v8): …`.
  </action>
  <verify>
    <automated>node --test tests/*.test.js 2>&1 | tail -8   # 945 + os novos, fail 0 — inclui refrigeracao-os-pagina (gaveta byte a byte) e refrigeracao-desktop (CSS byte a byte)</automated>
  </verify>
  <done>`node --test tests/*.test.js` fecha com `fail 0` e um total maior que 945. `git diff --stat tests/fixtures/refrigeracao-os-gaveta.json` mostra o arquivo 48 bytes menor. `grep -c 'DV_COP' refrigeracao/index.html` retorna 2 (a constante e o uso). Nenhuma regra de CSS alterada.</done>
</task>

<task type="auto">
  <name>Task 3: registrar o fato — CLAUDE.md, README (Pendências) e TESTES.md</name>
  <files>CLAUDE.md, README.md, TESTES.md</files>
  <action>
**CLAUDE.md** — uma nota curta (2-4 frases, no idioma e no tom das notas de arquitetura já existentes), colocada junto às demais notas de `/refrigeracao`: a estimativa de energia mostrava zero para o parque inteiro porque `corrente_nominal` é nula nas 175 linhas e `dvPotenciaW` só sabia tensão × corrente; agora, sem corrente de placa, a potência vem da capacidade (BTU/h ÷ 3412 ÷ COP 3), com o dado de placa vencendo a estimativa quando existe, e a nota do card dizendo isso na tela; e o input de evidência deixou de forçar a câmera, de modo que o técnico anexa foto tirada antes. Cite o gate `tests/refrigeracao-energia-fotos.test.js` e o fixture da gaveta atualizado nas duas ocorrências.

**README.md**, seção `## Pendências` — uma linha: a corrente nominal de placa continua ausente no banco (0 de 175); enquanto estiver, a potência de toda a frota é estimada pela capacidade, não medida, e o número real entra pela planilha exportar/importar já existente (quick-260822-5hy), equipamento por equipamento. Isso é a pendência honesta: o card deixou de mentir zero, mas ainda não diz a verdade medida.

**TESTES.md** — dois itens de verificação manual, do que o Node não prova:
- No celular, abrir uma OS em execução, tocar em **Fotos** e confirmar que o seletor oferece **câmera, fototeca e arquivos** (antes abria a câmera direto). Anexar uma foto da galeria e confirmar que o upload conclui.
- Na visão geral, conferir que o card **Energia Elétrica — Estimativa** deixou de mostrar 0 kW / 0 kWh / R$ 0,00, que as duas barras por área têm valor, e que a nota explica de onde veio o número.

Não mexa em mais nada nesses três arquivos.
  </action>
  <verify>
    <automated>node --test tests/*.test.js 2>&1 | tail -6 && git diff --stat CLAUDE.md README.md TESTES.md</automated>
  </verify>
  <done>Os três arquivos têm a nota, cada um em uma adição contida (nenhuma seção reescrita), e a suíte continua com `fail 0`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dispositivo do técnico → Supabase Storage (`os-fotos`) | arquivo escolhido pelo usuário atravessa aqui; a mudança amplia a **origem** do arquivo (câmera → também fototeca/arquivos) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-3v8-01 | Tampering | upload de evidência em `manRegistrarEvidencia` | low | accept | `accept` nunca foi controle de segurança (é dica de UI, contornável no seletor). A defesa real já existe e não muda: `f.type.indexOf('image/')!==0` descarta o arquivo, `ctCompressFoto` reencoda por canvas para JPEG 1280px — qualquer payload não-imagem não sobrevive ao reencode. `LIMITE_FOTOS_EVIDENCIA` segue limitando a quantidade. |
| T-3v8-02 | Information disclosure | metadados da foto escolhida da galeria | low | accept | O reencode por canvas descarta EXIF, inclusive GPS — uma foto da fototeca chega ao Storage sem localização, o que era verdade para a foto da câmera também. |
| T-3v8-03 | Repudiation | número de energia estimado confundido com medido | medium | mitigate | A `chart-note` passa a dizer, na própria tela, que onde falta corrente de placa a potência é **estimada** pela capacidade (BTU/h, COP 3) — o card já se chama "Estimativa"; a pendência fica registrada no README para que o dado real entre pela planilha. |

Sem `npm`/`pip`/`cargo`: nenhum pacote instalado, nenhuma dependência nova — o gate de legitimidade de pacotes não se aplica.
</threat_model>

<verification>
- `node --test tests/*.test.js` → `fail 0`, total > 945 (baseline 945 conferido antes da task).
- `tests/refrigeracao-os-pagina.test.js` verde nos cinco cenários — a gaveta continua provada byte a byte contra o fixture, agora com o fato novo.
- `tests/refrigeracao-desktop.test.js` verde — CSS fora de `@media` idêntico ao fixture (D-cf8-20).
- `git diff --stat` toca apenas: `refrigeracao/index.html`, `tests/refrigeracao-energia-fotos.test.js` (novo), `tests/fixtures/refrigeracao-os-gaveta.json` (−48 bytes), `CLAUDE.md`, `README.md`, `TESTES.md`.
- Nenhum `import` de `shared/` em `refrigeracao/index.html` (D-04) — `tests/tema-superficies.test.js` já prova.
</verification>

<success_criteria>
- Com o parque real (corrente nula, BTU preenchido), `dvPotenciaW` devolve potência > 0 e o card de energia mostra kW, kWh/mês e custo/mês diferentes de zero.
- Com corrente de placa cadastrada, o resultado é idêntico ao de hoje, inclusive no ramo trifásico de 380 V.
- Sem corrente e sem capacidade, o resultado continua 0.
- O input de fotos da execução aceita imagem de qualquer origem do dispositivo, mantendo `accept="image/*"` e `multiple`.
- Suíte inteira verde, fixture da gaveta atualizado só onde o fato mudou, fato registrado em CLAUDE.md/README/TESTES.md.
</success_criteria>

<output>
Create `.planning/quick/260826-3v8-refrigeracao-fallback-de-pot-ncia-por-bt/260826-3v8-SUMMARY.md` when done
</output>
