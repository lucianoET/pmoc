---
phase: quick
plan: 260818-r65
subsystem: maquinas
tags: [supabase, migration, rls, hora-homem, estoque, necessidades, lista-de-compra]
provides:
  - migração 34 (sistema/aplicação do material + maq_compras_listas/maq_compras_itens), escrita mas não aplicada
  - hora-homem movida da aba Estoque para modal aberto do cabeçalho da aba OS
  - material com campos sistema (texto livre) e aplicação (select), exibidos no estoque
  - aba Necessidades (ex-"Lista de compras") com cálculo por material, geração de lista de compra e recebimento item a item
affects: [maquinas]
tech-stack:
  added: []
  patterns:
    - "carga tolerante fora do Promise.all principal (carregarCompras), mesmo molde de carregarCustosOS/carregarComentarios"
    - "sonda única (MIGRACAO_34) cobrindo duas tabelas + duas colunas nascidas na mesma migração"
    - "preço/valor congelado na linha no momento da criação (preco_unit em maq_compras_itens, mesmo padrão de maq_os_materiais)"
key-files:
  created:
    - supabase/34_maquinas_compras_necessidades.sql
    - tests/necessidades-compras-maquinas.test.js
  modified:
    - maquinas/index.html
    - maquinas/app.js
    - tests/os-custos-maquinas.test.js
key-decisions:
  - "D1: hora-homem sai do painel da aba Estoque e passa a viver em modal-hora-homem, aberto por botão no cabeçalho da aba OS — é lá que o custo de mão de obra é consumido"
  - "D2: sistema/aplicação do material são texto livre (não FK) — uma peça serve modelos que nem sempre estão cadastrados"
  - "D3: 'Lista de compras' vira 'Necessidades' (id necessidades); exportarComprasCSV mantém o nome por D8, só troca o que lê"
  - "D4: fórmula da necessidade implementada literalmente — reposição de mínimo é PISO (Math.max), não parcela somada; corr sai de pecasParaBaixa() nas OS não executadas, prev usa o corte de 70% de calcVencimentos()"
  - "D5/D6: preço unitário congelado na criação da lista; recebimento nunca ultrapassa o pendente e fecha a lista sozinha quando o último item chega, sem flag paralela"
  - "D7: migração 34 tolerante — MIGRACAO_34 fica false sem as tabelas/colunas, e nenhuma tela quebra nem tenta gravar coluna inexistente"
duration: ~15min
completed: 2026-08-18
status: complete
---

# Quick Task 260818-r65: Estoque, Necessidades e hora-homem sai da aba Máquinas Summary

**Hora-homem sai da aba Estoque para um modal na aba OS; material ganha sistema/aplicação; "Lista de compras" vira "Necessidades" com cálculo por material, geração de lista de compra e recebimento item a item — tudo atrás da migração 34 (escrita, não aplicada).**

## Performance
- **Duration:** ~15min de execução (3 commits, 19:43–19:54)
- **Tasks:** 3/3 completas
- **Files modified:** 5 (2 novos, 3 modificados)

## Accomplishments
- `supabase/34_maquinas_compras_necessidades.sql` escrita — aditiva, com `maq_materiais.sistema`/`aplicacao`, `maq_compras_listas` e `maq_compras_itens` (preço congelado, `qtd_recebida` nunca negativa, unique por lista+material) — **não aplicada no banco**.
- Hora-homem (`cfg-valor-hora`/`btn-valor-hora`/`cfg-hora-ajuda`) saiu do painel da aba Estoque e passou a viver em `modal-hora-homem`, aberto pelo botão `🕒 Hora-homem` no cabeçalho da aba OS; o aviso de mão de obra sem valor aponta para o botão novo.
- Modal de material ganhou `mat-sistema` (texto livre com datalist de 8 sugestões) e `mat-aplicacao` (select alimentado por `opcoesAplicacao()`, unindo `ATIVOS.tipo_modelo` e `MODELOS` de `rep_modelos`); a tabela do estoque ganhou as colunas Sistema e Aplicação (7→9 colunas). Os dois campos só são gravados quando `MIGRACAO_34` está disponível.
- Aba renomeada de "Lista de compras" para "Necessidades" (`id="necessidades"`, ícone mantido). `necessidadePorMaterial()` calcula, por material, `{material, prev, corr, min_rep, bruto, aquisicao, a_comprar}` — reposição de mínimo é piso (não somada), aquisição desconta o que já está pedido e não chegou.
- Seleção na tabela gera uma `maq_compras_listas` com preço unitário congelado por linha; `receberItem()` dá entrada no estoque, insere movimento de `'entrada'`, recusa quantidade acima do pendente e fecha a lista sozinha quando o último item chega. Export CSV por lista, além do export da aba (nome de função mantido: `exportarComprasCSV`).
- `node --test`: 253/253 verdes (227 de base + 26 novos entre os três commits), nenhum teste apagado.

## Task Commits
1. **Task 1: Migração 34 — colunas do material e tabelas de lista de compra** - `4591abc`
2. **Task 2: Hora-homem sai do Estoque e material ganha sistema/aplicação** - `14c1b30`
3. **Task 3: Aba Necessidades — cálculo, listas de compra e recebimento** - `e260f80`

## Files Created/Modified
- `supabase/34_maquinas_compras_necessidades.sql` - migração aditiva (não aplicada): sistema/aplicação do material + maq_compras_listas/maq_compras_itens
- `tests/necessidades-compras-maquinas.test.js` - gate novo, crescido nos três commits (migração → material → necessidades)
- `maquinas/index.html` - modal-hora-homem, mat-campos-34 no modal de material, colunas Sistema/Aplicação no estoque, view-necessidades + modal-lista-compra
- `maquinas/app.js` - abrirModalHoraHomem, opcoesAplicacao, carregarCompras, necessidadePorMaterial, renderNecessidades, abrirModalListaCompra, salvarListaCompra, receberItem, exportarListaCompraCSV
- `tests/os-custos-maquinas.test.js` - gate atualizado: hora-homem fora de view-materiais, dentro de modal-hora-homem, botão no cabeçalho da OS, aviso sem menção à aba de estoque

## Decisions & Deviations

None - plano executado exatamente como escrito, incluindo a fórmula de D4 implementada literalmente (a lista `OS_NAO_EXECUTADA = ['aberta','delineamento','espera','pendente']` inclui o literal `'aberta'`, que nunca casa com um valor real de `maq_os.status` — os valores reais não-executados são `pendente`/`delineamento`/`espera`; o quarto item do array é inofensivo, mas nunca filtra nada sozinho. Mantido como está porque é exatamente o array que o texto do plano especificou, sem otimizar nem reinterpretar).

Duas correções pontuais fora do escopo estrito das tasks, feitas por estarem na mesma região de código tocada (Rule 1/2 do executor):
- Comentário HTML `<!-- COMPRAS -->` acima de `view-consumo` (leftover de antes desta task, já mal rotulado) corrigido para `<!-- CONSUMO -->`.
- Comentário `<!-- NECESSIDADES (migração 34) -->` acrescentado acima de `view-necessidades`, no mesmo padrão dos outros comentários de seção do arquivo.

## Known Stubs

Nenhum. A tela de Necessidades degrada de forma limpa sem a migração 34 (coluna Aquisição mostra travessão, botão de gerar lista some) — isso é comportamento intencional de D7, não um stub.

## Threat Flags

| Flag | File | Description |
|------|------|-------------|
| threat_flag: novo-write-path | supabase/34_maquinas_compras_necessidades.sql | Duas tabelas novas (`maq_compras_listas`, `maq_compras_itens`) com RLS `for all to authenticated` — mesmo padrão de política já usado no módulo (leitura pública, escrita autenticada), mas é uma nova superfície de escrita que nunca foi exercida em produção porque a migração ainda não foi aplicada. |

## Next Phase Readiness

- Migração 34 pronta para aplicação pelo orquestrador — depois de aplicada, `carregarCompras()` passa a preencher `COMPRAS_LISTAS`/`COMPRAS_ITENS` e `MIGRACAO_34` vira `true` automaticamente, sem deploy de código novo.
- Write path da aba Necessidades (gerar lista, receber item) segue **não exercido em produção** até a migração ser aplicada e testada manualmente — mesmo padrão de pendência já registrado para `/mapa` (Fase 10) e Reparos (18/08/2026): checklist a acrescentar em `TESTES.md` pelo orquestrador/usuário.
- `podeEscreverNoModulo()` (`admin`/`gestor`/`tecnico`) segue sendo o único conjunto de cargos usado — nenhuma lista nova foi introduzida, confirmado por gate.

## Self-Check: PASSED

- Todos os arquivos criados/modificados listados acima existem no disco.
- Os três hashes de commit (`4591abc`, `14c1b30`, `e260f80`) existem em `git log`.
- `node --test` na raiz: 253/253 verdes, 0 falhas — acima dos 227 de base.
