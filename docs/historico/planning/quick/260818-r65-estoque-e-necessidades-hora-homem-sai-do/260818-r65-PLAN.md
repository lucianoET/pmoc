---
phase: quick
plan: 260818-r65
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/34_maquinas_compras_necessidades.sql
  - maquinas/index.html
  - maquinas/app.js
  - tests/necessidades-compras-maquinas.test.js
  - tests/os-custos-maquinas.test.js
autonomous: true
requirements: []
must_haves:
  truths:
    - "O valor da hora-homem deixa de ocupar a aba Estoque e passa a ser editado num modal aberto do cabeçalho da aba OS, onde o custo de mão de obra é consumido (D1)."
    - "Um material pode declarar o sistema a que pertence e em que modelos é aplicado, e esses dois dados aparecem na tabela do estoque (D2)."
    - "A aba deixa de se chamar Lista de compras e passa a ser Necessidades, com id 'necessidades' (D3)."
    - "Cada material com necessidade mostra preventiva, corretiva, aquisição em curso e quanto falta comprar, pela fórmula travada (D4)."
    - "É possível gerar uma lista de compra a partir das linhas selecionadas, com preço unitário congelado na linha (D5)."
    - "Cada item de uma lista pode ser recebido isoladamente: o estoque sobe, o movimento de entrada fica registrado, e a lista fecha sozinha quando o último item chega (D6)."
    - "Num banco sem a migração 34 o módulo funciona exatamente como antes — sem coluna de aquisição, sem listas, sem erro (D7)."
    - "Cada lista de compra exporta seu próprio CSV, e o export da aba Necessidades continua existindo (D8)."
    - "node --test continua 100% verde (227 testes hoje, mais os novos)."
  artifacts:
    - "supabase/34_maquinas_compras_necessidades.sql — aditiva, sem DROP, não aplicada no banco"
    - "maquinas/index.html com modal-hora-homem, view-necessidades, modal-lista-compra e os dois campos novos no modal-material"
    - "maquinas/app.js com carregarCompras(), necessidadePorMaterial(), renderNecessidades(), salvarListaCompra(), receberItem()"
    - "tests/necessidades-compras-maquinas.test.js — gate novo, crescido em três commits"
  key_links:
    - "carregarCompras() → MIGRACAO_34 → colunas/botões que só existem com a migração aplicada (D7)"
    - "necessidadePorMaterial() → calcVencimentos() (prev) + pecasParaBaixa() (corr) + COMPRAS_ITENS (aquisicao)"
    - "receberItem() → maq_estoque_movimentos ('entrada') + maq_materiais.estoque_atual + fechamento automático da lista"
    - "abrirModalHoraHomem() → cfg-valor-hora/btn-valor-hora/cfg-hora-ajuda movidos de view-materiais para modal-hora-homem"
    - "osd-aviso-hora → aponta para o novo lugar do campo, não mais para a aba Estoque"
---

<objective>
Três frentes no módulo Máquinas, cada uma commitável sozinha: (1) a migração aditiva 34 que dá as colunas `sistema`/`aplicacao` ao material e cria as duas tabelas de lista de compra; (2) tirar a hora-homem da aba Estoque e levá-la para um modal na aba OS, além de expor os dois campos novos do material; (3) transformar a aba "Lista de compras" em "Necessidades", com cálculo por material, geração de lista de compra (BOM) e recebimento item a item.

Purpose: hoje a aba de compras é uma lista de leitura que ninguém consegue transformar em processo — não dá para dizer "isto já foi pedido", não dá para dar entrada no que chegou, e a necessidade some assim que a página recarrega. E a hora-homem está guardada na aba errada: quem precisa dela está lançando serviço numa OS, não conferindo estoque.
Output: `supabase/34_*.sql` escrito (não aplicado), módulo Máquinas com a aba Necessidades operante, gates novos e atualizados, `node --test` verde.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@.claude/CLAUDE.md
@maquinas/index.html
@maquinas/app.js
@supabase/32_maquinas_ficha.sql
@supabase/33_maquinas_material_servico.sql
@tests/os-custos-maquinas.test.js

Antes de escrever qualquer SQL, ler `.agents/skills/supabase-postgres-best-practices/SKILL.md`.

Fatos já apurados nesta investigação (não precisam ser reconfirmados — não releia estes arquivos só para conferi-los):

**Estado atual do código**
- `renderCompras()` vive em `maquinas/app.js:952-1022`, escreve em `#compras-content`, e guarda `window._comprasData` para o `exportarComprasCSV()` (app.js:1024-1034). Hoje ela mistura duas origens numa lista de texto livre (`motivo`), sem material_id, sem persistência.
- `view-compras` é `maquinas/index.html:310-314`; o CSS `.compra-grid/.compra-row/.compra-nome/.compra-det/.compra-qtd` está em `index.html:40-46`.
- `navItems` está em `boot()`, `maquinas/app.js:2456-2465`; a última entrada é a de compras. A faixa é montada em runtime por `shared/shell.js` — **não mexer em shared/shell.js**, ele serve os 6 módulos.
- O painel da hora-homem é `maquinas/index.html:246-259` (`panel-card` dentro de `view-materiais`), com `cfg-valor-hora`, `btn-valor-hora` e `cfg-hora-ajuda`. `renderMateriais()` (app.js:788-792) preenche o campo e chama `aplicarPermissoesConfig()`; `salvarValorHora()` está em app.js:877-899.
- O aviso da OS sem valor de hora está em `renderCustosOS`, app.js ~1663-1673 (`#osd-aviso-hora`), e a frase manda o usuário para a aba de estoque — é essa frase que muda.
- `modal-material` é `maquinas/index.html:614-648`; `abrirModalMaterial()`/`salvarMaterial()` estão em app.js:2213-2268. O bloco `mat-servico-wrap` já é o precedente de campo que **some** quando o dado de trás não existe (`wrap.style.display = SERVICOS.length ? '' : 'none'`).
- A tabela do estoque (`#tb-materiais`) tem 7 colunas hoje: Código, Nome, Tipo, Estoque atual, Mínimo, Preço un., Status (`index.html:294-299`, `renderMateriais` em app.js:794-855).
- `podeEscreverNoModulo()` (app.js:867) já devolve exatamente `admin`/`gestor`/`tecnico` — é o conjunto de D9, não criar lista de cargos nova.
- `carregarTudo()` (app.js:106-136) tem o `Promise.all` principal e depois quatro cargas tolerantes fora dele: `carregarCatalogoReparos()`, `carregarItensOS()`, `carregarCustosOS()`, `carregarComentarios()`. É esse o padrão de D7.
- `pecasParaBaixa(os)` (app.js:1435-1444) já resolve "lista da própria OS, com fallback para a previsão do plano/diagnóstico" — é ela que dá o `corr` de D4, não reimplementar.
- `calcVencimentos()` (app.js:445-466) devolve `{ativo, plano, proxUso, falta, pct}`; `PLANO_MATS` tem `{plano_id, material_id, quantidade}`.
- `baixarPecasDaOS()` (app.js:1461+) é o espelho do recebimento: atualiza `maq_materiais.estoque_atual` e **depois** insere em `maq_estoque_movimentos`. O recebimento segue a mesma ordem, com `tipo:'entrada'`.
- Idioma de escrita sob RLS já estabelecido no módulo: `.select()` na escrita e checagem de `data.length`, porque update/insert que não alcança linha permitida volta **sem** erro (ver `salvarValorHora`, `salvarMaterial`).

**Banco**
- `maq_materiais.id` é `integer` (serial) — por isso `maq_compras_itens.material_id` é `integer`, não bigint.
- `maq_estoque_movimentos` (01_maquinas_schema.sql:64-70) aceita `tipo in ('entrada','saida')`, `os_id` nullable — um movimento de recebimento entra sem `os_id`, só com `motivo`.
- Migração 32 é o modelo de tabela nova neste módulo: `create table if not exists`, índice, `alter table ... enable row level security`, `drop policy if exists` + `create policy` (leitura `using (true)`, escrita `for all to authenticated`), `comment on`. **Sem bloco de grants** — 32 não tem e funciona em produção.
- `rep_modelos` (26_reparos_schema.sql:19-34) tem `fabricante` e `modelo` (unique juntos) — a etiqueta de um modelo é `fabricante + ' ' + modelo`.
- `rep_modelos` **não** é carregado hoje por `maquinas/app.js` (só `rep_reparos`, `rep_reparo_materiais`, `rep_reparo_servicos`, `rep_servicos`). O select de aplicação precisa dessa carga nova, dentro de `carregarCatalogoReparos()`.
- A migração 28 existe porque `create table if not exists` é no-op numa tabela já existente. Escrever a 34 sabendo disso: se as tabelas já existirem com outro formato, a 34 não as corrige.

**Testes**
- `node --test` na raiz: 227 testes, 0 falhas. `node --test tests/` **não** funciona (erro) — o comando é `node --test` na raiz.
- O único gate que hoje codifica o arranjo antigo é `tests/os-custos-maquinas.test.js` (linha 110-117: exige `id="cfg-valor-hora"` no HTML — segue verdadeiro depois da mudança, porque o campo sobrevive no modal novo).
- Nenhum gate cita `view-compras`, `compras-content` nem `id: 'compras'` — a renomeação de D3 não quebra teste existente, mas ganha gate próprio agora.
- `tests/integracao-operacoes-maquinas.test.js:36` proíbe `id: 'operacoes'` e `id: 'vencimentos'` em navItems — o item novo `id: 'necessidades'` não conflita.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migração 34 — colunas do material e tabelas de lista de compra</name>
  <files>supabase/34_maquinas_compras_necessidades.sql, tests/necessidades-compras-maquinas.test.js</files>
  <action>
Ler `.agents/skills/supabase-postgres-best-practices/SKILL.md` antes de escrever o SQL.

Criar `supabase/34_maquinas_compras_necessidades.sql` seguindo o formato de `32_maquinas_ficha.sql`: cabeçalho em caixa de `═` explicando por que o arquivo existe (a aba de compras era leitura sem processo: não dava para dizer o que já foi pedido nem dar entrada no que chegou), e o SQL logo abaixo. Migração **aditiva**: nenhum `drop table`, nenhum `drop column`, nenhum `alter ... drop`. NÃO aplicar no banco — só escrever o arquivo.

Conteúdo, em português, na ordem:

1. As duas colunas de D2 em `maq_materiais`, com `add column if not exists`: `sistema text` e `aplicacao text`. Comentário em cada uma: `sistema` é o subsistema da máquina a que a peça pertence (texto livre, com sugestões na tela); `aplicacao` é em que modelos a peça serve, com o valor "Vários" para peça de mais de um modelo. Registrar no comentário por que é texto e não FK: uma peça serve modelos que nem sempre estão cadastrados, e a lista fixa recusaria cadastro legítimo.

2. `maq_compras_listas` conforme D5: `id uuid primary key default gen_random_uuid()`, `titulo text not null`, `descricao text`, `data date default current_date`, `status text not null default 'aberta' check (status in ('aberta','enviada','recebida','cancelada'))`, `criado_por text`, `criado_em timestamptz default now()`.

3. `maq_compras_itens` conforme D5: `id uuid primary key default gen_random_uuid()`, `lista_id uuid references maq_compras_listas(id) on delete cascade`, `material_id integer references maq_materiais(id)`, `quantidade numeric not null check (quantidade > 0)`, `preco_unit numeric`, `origem text check (origem in ('preventiva','corretiva','minimo','manual'))`, `qtd_recebida numeric not null default 0 check (qtd_recebida >= 0)`, `recebido_em timestamptz`, `unique (lista_id, material_id)`.

4. Comentário em `preco_unit` dizendo que o valor é congelado na linha no momento da criação da lista, pelo mesmo motivo já registrado em `30_maquinas_os_custos.sql`: reprecificar o catálogo não pode reescrever lista fechada.

5. Comentário registrando que a quantidade em aquisição **não** vira coluna em `maq_materiais`: ela é derivada de `quantidade - qtd_recebida` somada sobre os itens pendentes, porque coluna denormalizada dessincroniza do que foi realmente recebido.

6. Índices `if not exists`: `maq_compras_itens(lista_id)` e `maq_compras_itens(material_id)`.

7. RLS ligada nas duas tabelas, com o par de políticas de 32: leitura `for select using (true)` e escrita `for all to authenticated using (true) with check (true)`, cada uma precedida de `drop policy if exists`. Sem bloco de grants.

8. `comment on table` nas duas tabelas.

9. No cabeçalho, uma linha registrando que a 34 **ainda não foi aplicada em produção** na data em que o arquivo subiu, e a lição da 28: `create table if not exists` é no-op sobre tabela existente — conferir as colunas depois de aplicar, não confiar na ausência de erro.

Criar `tests/necessidades-compras-maquinas.test.js` no formato dos gates existentes (`node:test` + `node:assert/strict`, CommonJS, comentário de topo explicando o que o gate protege), com a **seção de migração** apenas:
- o arquivo `supabase/34_maquinas_compras_necessidades.sql` existe;
- cria as duas tabelas e acrescenta as duas colunas ao material;
- é aditiva: sem `drop table`, sem `drop column`;
- `preco_unit` existe em `maq_compras_itens` e o arquivo justifica o congelamento;
- `unique (lista_id, material_id)` presente — a mesma peça não entra duas vezes na mesma lista;
- `qtd_recebida` tem default 0 e check de não-negativo;
- os quatro status da lista estão no check;
- RLS ligada nas duas tabelas;
- os dois índices existem;
- nenhuma coluna de aquisição foi acrescentada a `maq_materiais` (asserção negativa sobre o SQL, protegendo a decisão de derivar em vez de guardar).
  </action>
  <verify>
    <automated>cd /home/luc/Downloads/pmoc-overlay && node --test 2>&1 | tail -6</automated>
  </verify>
  <done>`supabase/34_maquinas_compras_necessidades.sql` escrito (não aplicado no banco), `tests/necessidades-compras-maquinas.test.js` criado com a seção de migração, `node --test` verde com contagem acima de 227. Commit próprio.</done>
</task>

<task type="auto">
  <name>Task 2: Hora-homem sai do Estoque e material ganha sistema/aplicação</name>
  <files>maquinas/index.html, maquinas/app.js, tests/necessidades-compras-maquinas.test.js, tests/os-custos-maquinas.test.js</files>
  <action>
**A) Hora-homem muda de lugar (D1).**

Em `maquinas/index.html`: remover o `panel-card` da hora-homem de dentro de `view-materiais` (o bloco todo, incluindo o comentário HTML que justificava morar ali) e recriar seu conteúdo dentro de um overlay novo `modal-hora-homem`, colocado junto dos demais modais no fim do arquivo, no formato dos outros (`overlay > modal > modal-hd/modal-body/modal-ft`, `max-width:420px`). **Preservar os três ids** `cfg-valor-hora`, `btn-valor-hora` e `cfg-hora-ajuda` — `aplicarPermissoesConfig()` e `salvarValorHora()` dependem deles e não devem ser reescritos. O rodapé do modal tem Cancelar (`fecharModal('modal-hora-homem')`) e o botão de salvar já existente.

No cabeçalho de `view-os`, ao lado do link do catálogo de reparos e do botão `+ OS`, acrescentar um botão discreto `btn-s btn-sm` com id `btn-hora-homem`, rótulo curto com relógio, `title` explicando que ali se define o valor da hora usado no custo de mão de obra, chamando `abrirModalHoraHomem()`.

Em `maquinas/app.js`:
- criar `abrirModalHoraHomem()` na seção da hora-homem: preenche `cfg-valor-hora` com `valorHoraPadrao() ?? ''`, chama `aplicarPermissoesConfig()` e abre o overlay com `classList.add('open')`;
- remover de `renderMateriais()` as linhas que preenchiam o campo e chamavam `aplicarPermissoesConfig()` — a função volta a cuidar só da tabela;
- em `salvarValorHora()`, fechar o modal (`fecharModal('modal-hora-homem')`) antes do `await carregarTudo()`;
- reescrever a frase do aviso `#osd-aviso-hora` (em `renderCustosOS`) que hoje manda o usuário procurar o campo na aba de estoque: ela passa a apontar para o botão novo no topo da aba OS. Manter intacto o trecho inicial da frase que diz que nenhum valor está cadastrado — há gate sobre ele;
- exportar `abrirModalHoraHomem` no `Object.assign(window, {...})`, em ordem alfabética.

**B) Material ganha sistema e aplicação (D2).**

Em `maquinas/index.html`, no `modal-material`, acrescentar depois da linha de tipo/unidade/mínimo um bloco `fgrid` com dois campos, envolto num wrapper `id="mat-campos-34"`:
- `mat-sistema`: `input type="text"` com `list="lista-sistemas"` e um `<datalist id="lista-sistemas">` com as oito sugestões de D2 (Motor, Transmissão, Hidráulico, Elétrico, Corte, Chassi, Filtragem, Lubrificação). Texto livre — o datalist sugere, não restringe;
- `mat-aplicacao`: `<select>` vazio, preenchido em runtime.
Acrescentar `help` curto explicando que aplicação é o modelo em que a peça serve e que "Vários" é para peça compartilhada.

Na tabela do estoque (`#tb-materiais`), acrescentar duas colunas `<th>` — Sistema e Aplicação — logo depois de Tipo, levando o cabeçalho de 7 para 9 colunas. Ajustar os dois `colspan="7"` da linha de "nenhum material" (um no HTML, um em `renderMateriais`) para 9.

Em `maquinas/app.js`:
- em `renderMateriais()`, acrescentar as duas células nas **duas** ramificações do map (linha em edição e linha normal), exibindo `esc(m.sistema || '—')` e `esc(m.aplicacao || '—')`. Sem a migração 34 os campos vêm `undefined` e a célula mostra o travessão — nada quebra (D7);
- em `carregarCatalogoReparos()`, acrescentar a leitura de `rep_modelos` (`select('*').eq('ativo', true)`) ao `Promise.all` e uma global `MODELOS` que segue a mesma regra de indisponibilidade das outras quatro listas;
- criar `opcoesAplicacao()`: devolve a lista de etiquetas de modelo distintas, ordenada, unindo os `tipo_modelo` não-vazios de `ATIVOS` com `fabricante + ' ' + modelo` de `MODELOS`, deduplicada, com `'Vários'` fixo no topo. Comentar por que "Vários" é opção e não ausência de valor: peça compartilhada precisa dizer que é compartilhada, e campo vazio significa "ninguém classificou ainda";
- em `abrirModalMaterial(id)`, preencher `mat-sistema` com `material?.sistema || ''` e montar `mat-aplicacao` com uma opção vazia (`— não classificado —`) mais `opcoesAplicacao()`, marcando a atual. Esconder o wrapper `mat-campos-34` quando `!MIGRACAO_34`, no mesmo idioma de `mat-servico-wrap` — sem a migração não há onde gravar;
- em `salvarMaterial()`, incluir `sistema` e `aplicacao` no objeto `campos` **somente quando `MIGRACAO_34` for verdadeiro** (D7): num banco sem a migração, mandar coluna inexistente derrubaria todo cadastro de material. Valores vazios viram `null`, não string vazia.

A global `MIGRACAO_34` e a carga que a define são criadas na Task 3; nesta task, declarar `let MIGRACAO_34 = false` junto das demais globais e deixar `carregarCompras()` para a Task 3. Enquanto isso o wrapper fica escondido e os dois campos não são gravados — comportamento correto e commitável.

**C) Gates.**

Em `tests/os-custos-maquinas.test.js`, atualizar o teste que verifica que o valor da hora é editável: o campo continua existindo, mas agora dentro do overlay novo. Acrescentar asserções de que o overlay `modal-hora-homem` existe, de que o campo está dentro do bloco desse overlay e **não** dentro do bloco de `view-materiais` (recortar o HTML entre os marcadores de início/fim de cada bloco e comparar), de que existe o botão de abertura no cabeçalho de OS, e de que o texto do aviso não manda mais o usuário para a aba de estoque (asserção negativa sobre `maquinas/app.js`). Comentar no gate por que a hora-homem mudou de lugar: ela é preço de mão de obra, consumida na OS, não item de prateleira.

Em `tests/necessidades-compras-maquinas.test.js`, acrescentar a **seção do material**: os dois campos existem no modal, o datalist de sugestões existe com as oito opções, a tabela do estoque tem as duas colunas novas, `opcoesAplicacao()` põe "Vários" no topo e junta as duas origens de modelo, e `salvarMaterial()` só envia as duas colunas quando a migração está disponível (asserção sobre o trecho da função, protegendo D7).
  </action>
  <verify>
    <automated>cd /home/luc/Downloads/pmoc-overlay && node --test 2>&1 | tail -6</automated>
  </verify>
  <done>O painel da hora-homem não existe mais na aba Estoque; o modal abre pelo botão no cabeçalho da aba OS e salva; o modal de material edita sistema e aplicação e a tabela do estoque mostra as duas colunas; `node --test` verde. Commit próprio.</done>
</task>

<task type="auto">
  <name>Task 3: Aba Necessidades — cálculo, listas de compra e recebimento</name>
  <files>maquinas/index.html, maquinas/app.js, tests/necessidades-compras-maquinas.test.js</files>
  <action>
**A) Renomeação (D3).**

`maquinas/index.html`: `view-compras` → `view-necessidades`; `compras-content` → `necessidades-content`; título da view passa a Necessidades e o subtítulo explica que a linha soma demanda preventiva, corretiva e reposição de mínimo, descontando o que já está em aquisição. Manter os nomes de classe CSS `.compra-*` — descrevem uma linha de compra e continuam corretos; renomear só ids, views e funções.

`maquinas/app.js`: `renderCompras()` → `renderNecessidades()` (atualizar a chamada em `carregarTudo()`); em `navItems`, a última entrada passa a `{ id: 'necessidades', icone: '🛒', label: 'Necessidades' }`. `exportarComprasCSV()` **mantém o nome** (D8), passa a ler o novo estado e o arquivo baixado passa a se chamar `necessidades-maquinas-<data>.csv`.

**B) Carga tolerante (D7).**

Declarar as globais `COMPRAS_LISTAS`, `COMPRAS_ITENS` (a `MIGRACAO_34` já foi declarada na Task 2). Criar `carregarCompras()` **fora** do `Promise.all` principal, chamada em `carregarTudo()` logo depois de `carregarComentarios()`, no molde exato de `carregarCustosOS()`: lê `maq_compras_listas` (ordenada por data desc) e `maq_compras_itens` (com join em `maq_materiais(nome,codigo,unidade)`), e um erro em qualquer uma zera as duas listas e deixa `MIGRACAO_34 = false`. Comentar que as duas tabelas nascem juntas no mesmo arquivo, por isso uma sonda só responde pelas duas — e pelas duas colunas do material.

**C) Cálculo (D4).**

Criar `necessidadePorMaterial()`, imediatamente antes de `renderNecessidades()`, devolvendo uma linha por material com `{material, prev, corr, min_rep, bruto, aquisicao, a_comprar}`. Implementar a fórmula **literalmente como travada em D4**, sem otimizar nem reinterpretar:
- um `Map` por `material_id` e um helper que cria a linha zerada na primeira menção;
- `prev`: para cada item de `calcVencimentos()` com `pct >= 70`, somar a `quantidade` de cada `PLANO_MATS` daquele plano;
- `corr`: para cada OS cujo status esteja em `['aberta','delineamento','espera','pendente']`, somar a `quantidade` de cada peça devolvida por `pecasParaBaixa(os)` — reaproveitar a função, não reimplementar a resolução lista-da-OS/previsão;
- semear uma linha para todo material com `estoque_atual < estoque_minimo`, para que a reposição de mínimo apareça mesmo sem plano nem OS;
- `aquisicao`: somar `max(0, quantidade - qtd_recebida)` dos `COMPRAS_ITENS` cujas listas estejam em `aberta` ou `enviada`;
- fechar cada linha com `min_rep = max(0, estoque_minimo - estoque_atual)`, `bruto = max(prev + corr, min_rep)` e `a_comprar = max(0, bruto - estoque_atual - aquisicao)`;
- devolver só as linhas com `a_comprar > 0` ou `aquisicao > 0`, ordenadas por nome do material.
Comentar no código por que repor o mínimo é **piso** e não parcela somada: somar contaria a mesma peça duas vezes, uma pela demanda e outra pelo mínimo.

**D) Tela das necessidades.**

`renderNecessidades()` escreve em `#necessidades-content`:
- sem linhas: manter o callout verde de hoje, com o texto ajustado ao nome novo da aba;
- com linhas: um callout de resumo (quantidade de itens e total estimado, somando `a_comprar × preco`), uma barra de ações com o export CSV existente e — só quando `MIGRACAO_34 && podeEscreverNoModulo()` — o botão de gerar lista de compra; e uma tabela `tbl` com as colunas de D4 nesta ordem: Material (código destacado em `hi` + nome), Sistema, Aplicação, Estoque, Preventiva, Corretiva, Aquisição, A comprar, Sel., Qtd.
- a coluna Aquisição mostra travessão quando `!MIGRACAO_34` — não há de onde tirar o número;
- a seleção é um `input type="checkbox"` com id `nec-sel-<material_id>` e a quantidade um `input type="number"` com id `nec-qtd-<material_id>`, pré-preenchido com `a_comprar`, ambos só renderizados quando o botão de gerar lista está disponível. A leitura da seleção é feita no momento de gerar, direto do DOM — o mesmo idioma das demais telas do módulo;
- guardar as linhas em `window._necessidadesData` para o export CSV, com as colunas novas (sistema, aplicação, preventiva, corretiva, aquisição, a comprar) no lugar do campo de motivo em texto livre de hoje.

**E) Gerar lista de compra (D5, D9).**

Modal novo `modal-lista-compra` em `maquinas/index.html` (junto dos demais, `max-width:420px`) com `lc-titulo` (obrigatório, placeholder sugerindo referência do processo), `lc-desc` e um resumo `lc-resumo` de quantos itens vão entrar.

Em `app.js`:
- `abrirModalListaCompra()`: lê seleção e quantidades do DOM, guarda em `LISTA_NOVA_ITENS` (global nova) o par material/quantidade/origem, recusa com `alert` se nada estiver selecionado ou se alguma quantidade selecionada não for maior que zero, sugere um título com a data e preenche `lc-resumo`;
- `salvarListaCompra()`: valida o título; insere em `maq_compras_listas` com `criado_por: USUARIO?.nome || USUARIO?.role || null` e `.select()`, checando `data.length` no idioma de RLS já usado no módulo; insere os itens em `maq_compras_itens` com `preco_unit` **copiado do material no momento da criação** (congelado, D5) e `origem` derivada da linha — `preventiva` quando `prev > 0`, senão `corretiva` quando `corr > 0`, senão `minimo` quando `min_rep > 0`, senão `manual`; fecha o modal e chama `carregarTudo()`. Tratar erro com o idioma padrão `if(error){ alert('Erro: '+error.message); return }`.

**F) Listas e recebimento item a item (D6, D8, D9).**

Abaixo da tabela, `renderNecessidades()` desenha as listas de compra existentes (quando `MIGRACAO_34`), da mais recente para a mais antiga: por lista, um cartão `panel-card` com título, data, badge de status, descrição, botão de export CSV próprio e uma tabela dos itens — Material, Quantidade, Recebido, Pendente, Preço un., Total e ação. O botão de receber aparece por item, só quando `podeEscreverNoModulo()` e o pendente for maior que zero. Lista sem itens ou sem migração exibe o aviso correspondente.

`receberItem(itemId)`:
- calcula o pendente (`quantidade - qtd_recebida`), pergunta a quantidade com `prompt` já preenchido com o pendente;
- recusa quantidade não numérica, não positiva, ou **maior que o pendente** (D6: `qtd_recebida` nunca ultrapassa `quantidade`);
- atualiza o item somando em `qtd_recebida` e gravando `recebido_em`, com `.select()` e checagem de `data.length`;
- atualiza `maq_materiais.estoque_atual` somando a quantidade recebida e depois insere em `maq_estoque_movimentos` um `tipo:'entrada'` com `motivo` citando o título da lista — mesma ordem de `baixarPecasDaOS()`, que é o espelho desta operação;
- depois disso, se todo item da lista estiver com `qtd_recebida >= quantidade`, atualiza a lista para `recebida`;
- termina com `await carregarTudo()`.
Comentar que o item recebido some da coluna de aquisição sozinho, porque aquisição é derivada de `quantidade - qtd_recebida` — não há flag para sincronizar.

`exportarListaCompraCSV(listaId)`: CSV da lista, cabeçalho `Codigo,Nome,Quantidade,Unidade,PrecoUnit,Total,Origem,QtdRecebida`, arquivo `lista-compra-<titulo-normalizado>-<data>.csv`. É o insumo do processo licitatório (D8).

Exportar no `Object.assign(window, {...})`, em ordem alfabética: `abrirModalListaCompra`, `exportarListaCompraCSV`, `receberItem`, `salvarListaCompra`.

**G) Gate.**

Acrescentar a `tests/necessidades-compras-maquinas.test.js` a seção das necessidades:
- a aba se chama Necessidades e tem id `necessidades`; a view e o container têm os ids novos; nenhum id antigo de compras sobrou no HTML nem no app;
- `necessidadePorMaterial()` implementa a fórmula travada: reposição de mínimo entra por `Math.max` (piso) e não somada, `a_comprar` desconta estoque e aquisição, e a linha aparece quando há o que comprar ou algo em aquisição;
- `corr` sai de `pecasParaBaixa()` e considera exatamente os quatro status não executados;
- `prev` usa o mesmo corte de 70% de hoje;
- `carregarCompras()` está fora do `Promise.all` principal e zera as listas em caso de erro (D7);
- `preco_unit` do item é copiado do material na criação da lista (asserção sobre o trecho de `salvarListaCompra`);
- `receberItem()` recusa quantidade acima do pendente, insere movimento de entrada, soma no estoque do material e fecha a lista quando todos os itens chegaram;
- criar lista e receber item são oferecidos por `podeEscreverNoModulo()`, não por uma lista de cargos nova (asserção de que nenhum novo array de cargos foi introduzido no módulo);
- existe export CSV por lista, além do export da aba.
  </action>
  <verify>
    <automated>cd /home/luc/Downloads/pmoc-overlay && node --test 2>&1 | tail -6</automated>
  </verify>
  <done>A aba Necessidades calcula e mostra a necessidade por material com as colunas de D4, gera lista de compra a partir da seleção, recebe item a item dando entrada no estoque, fecha a lista sozinha e exporta CSV por lista; num banco sem a migração 34 nada quebra; `node --test` verde. Commit próprio.</done>
</task>

</tasks>

<verification>
1. `node --test` na raiz: 0 falhas, contagem acima dos 227 de hoje.
2. `git log --oneline -3` mostra três commits, um por task.
3. `grep -rn "view-compras\|compras-content\|id: 'compras'" maquinas/` não devolve nada.
4. `grep -c "drop table\|drop column" supabase/34_maquinas_compras_necessidades.sql` devolve 0 — migração aditiva.
5. A migração 34 **não** foi aplicada no banco: nenhuma chamada de `apply_migration` nem execução via `supabase/aplicar.py` nesta execução.
6. Abrindo `maquinas/index.html` no navegador contra a produção (sem a migração 34): a aba Necessidades carrega, mostra travessão na coluna de aquisição, não oferece o botão de gerar lista, e o cadastro de material continua salvando normalmente.
</verification>

<success_criteria>
- Hora-homem editável só pelo modal aberto no cabeçalho da aba OS; o aviso da OS aponta para lá.
- Material com sistema e aplicação no cadastro e na tabela do estoque.
- Aba Necessidades no lugar de Lista de compras, com as oito colunas de D4 mais seleção e quantidade.
- Lista de compra gerada com preço congelado, recebida item a item, fechando sozinha ao final.
- Degradação limpa sem a migração 34.
- `node --test` verde, nenhum teste removido.
</success_criteria>

<output>
Criar `.planning/quick/260818-r65-estoque-e-necessidades-hora-homem-sai-do/260818-r65-SUMMARY.md` ao final.
</output>
