---
phase: 05-base-unificada
plan: 07
type: execute
wave: 4
depends_on: ["05-01", "05-02", "05-03", "05-04", "05-05", "05-06"]
files_modified:
  - TESTES.md
  - CLAUDE.md
  - .claude/CLAUDE.md
  - README.md
autonomous: true
requirements: [PLAT-15, PLAT-16]

must_haves:
  truths:
    - "TESTES.md ganha uma seção da Fase 5 no mesmo formato das seções anteriores, com o roteiro manual por módulo e a conferência explícita de não regressão da refrigeração"
    - "Os seis módulos no escopo carregam a folha comum e nenhum deles declara token de cor, fonte ou espaçamento além da cor de destaque"
    - "Os seis módulos autenticam pelo login compartilhado e nenhum tem fluxo de login escrito inline"
    - "Os seis módulos montam cabeçalho e rodapé pelo shell comum, e os cinco que navegam por abas montam também a faixa de abas"
    - "A refrigeração não foi tocada em nenhum commit da fase e continua sem referenciar a folha comum, o shell ou qualquer arquivo compartilhado"
    - "As sete rotas do arquivo de configuração do Vercel continuam apontando para arquivos existentes"
    - "node --test continua verde, com mais testes que os 19 do início da fase"
    - "A documentação do projeto deixa de afirmar que o módulo Máquinas duplica o fluxo de login inline"
  prohibitions:
    - statement: "Nenhum arquivo em refrigeracao/ é lido para modificação, escrito ou referenciado — a única interação permitida é conferência de leitura (PLAT-15)"
      status: unverified
      verification: judgment
    - statement: "Nenhuma correção de código é feita neste plano; se a auditoria encontrar falha, ela é reportada e corrigida no plano de origem, não aqui"
      status: unverified
      verification: judgment
    - statement: "Nenhuma rota é acrescentada ou removida do arquivo de configuração do Vercel"
      status: unverified
      verification: judgment
    - statement: "Nenhum passo introduz build, npm, bundler ou framework no projeto zero-build"
      status: unverified
      verification: judgment
  artifacts:
    - path: "TESTES.md"
      provides: "Roteiro manual da Fase 5 por módulo e conferência de não regressão da refrigeração"
      contains: "Base unificada"
    - path: "CLAUDE.md"
      provides: "Descrição de arquitetura atualizada — shell comum e login compartilhado nos seis módulos"
      contains: "shell.js"
  key_links:
    - from: "TESTES.md"
      to: "os seis módulos"
      via: "seção nova listando por módulo o que abrir e o que confirmar, no formato das seções anteriores do arquivo"
      pattern: "Fase 5"
    - from: "auditoria da fase"
      to: "vercel.json"
      via: "cada destino de rota é conferido como arquivo existente no repositório"
      pattern: "destination"
---

<objective>
Fechar a fase provando os cinco critérios de sucesso, em vez de presumi-los: auditar por comando que
os seis módulos estão de fato na base unificada, que a **refrigeração continua idêntica** e que a
suíte automatizada não regrediu (PLAT-15, PLAT-16); registrar o roteiro manual em `TESTES.md`; e
atualizar a documentação que passou a estar errada.

A pesquisa da fase registra uma armadilha específica aqui: como as sete rotas continuam ativas durante
toda a fase, é fácil deixar a refrigeração aberta numa aba e presumir que "está tudo igual" sem
conferir. PLAT-15 exige confirmação explícita, não presunção — por isso a conferência dela é um passo
isolado, com verificação estática por histórico do repositório e verificação humana com o inspetor do
navegador aberto.

Purpose: sem este plano a fase termina com cinco critérios afirmados e nenhum verificado ponta a ponta.
Output: `TESTES.md` com a seção da fase, documentação corrigida e auditoria dos seis módulos executada.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-base-unificada/05-RESEARCH.md
@.planning/phases/05-base-unificada/05-01-SUMMARY.md
@.planning/phases/05-base-unificada/05-06-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Auditar os seis módulos e registrar o roteiro da fase em TESTES.md</name>

  <read_first>
    - `TESTES.md` linhas 149-190 — a seção do Predial, molde de formato a seguir (título com módulo e data, lista numerada de passos, marcação de estado)
    - `TESTES.md` linhas 258-283 — a seção do Mapa, segundo molde do mesmo formato
    - `vercel.json` — as sete rotas e seus destinos
    - `.planning/phases/05-base-unificada/05-RESEARCH.md` — seção "Verificação de não regressão sem testes de UI" e "Pitfall 4"
    - Os seis arquivos de resumo dos planos 05-01 a 05-06, para saber o que cada módulo entregou e o que precisa constar no roteiro
  </read_first>

  <files>TESTES.md</files>

  <action>
Rodar a auditoria da fase e registrar o roteiro manual — nesta ordem, porque o que for descoberto na
auditoria precisa aparecer no roteiro.

**Auditoria por comando** (é o conteúdo do gate automatizado desta task; rodar antes de escrever):

1. Os seis módulos no escopo carregam a folha comum exatamente uma vez cada.
2. Nenhum dos seis declara token de fundo, superfície, borda, texto ou família de fonte — a única
   declaração de cor permitida em cada um é a da cor de destaque.
3. Nenhum dos seis tem fluxo de login escrito inline: nenhum declara lista própria de cargos nem
   contém endereço do domínio interno de cargo.
4. Os cinco módulos com abas montam a faixa de abas pelo shell, e nenhum deles tem botão de aba
   escrito no HTML.
5. As sete rotas do arquivo de configuração do Vercel apontam para arquivos que existem no
   repositório.
6. A suíte automatizada roda verde e com mais testes que os 19 do início da fase.

**Se qualquer item falhar:** não corrigir aqui. Registrar a falha no resumo do plano, indicando o
plano de origem (05-01 a 05-06), e sinalizar ao coordenador. Este plano audita e documenta; correção
de código pertence ao plano que introduziu o problema.

**Escrever a seção nova em `TESTES.md`**, ao final do arquivo, no formato das seções existentes:
título indicando a fase e a data, uma linha de contexto explicando o que mudou (folha comum, login
compartilhado e shell comum nos seis módulos), e então, por módulo, os passos a executar: abrir a
rota, entrar por cada cargo, conferir que o acesso Livre continua sem senha, conferir que o
observador continua sem botões de escrita, percorrer todas as abas do módulo, e conferir que o rodapé
aparece com nome, versão e link do portal.

Incluir, como item próprio e destacado, a conferência de não regressão da refrigeração descrita na
Task 2 — o roteiro precisa deixar registrado que ela é um passo isolado e não uma presunção.

Incluir também a observação de que a suíte automatizada é a primeira linha de defesa e roda pelo
comando de teste do Node na raiz do repositório, sem argumento.
  </action>

  <acceptance_criteria>
    - Seção da fase presente no arquivo: `grep -q 'Base unificada' TESTES.md`
    - A seção cobre os seis módulos: `for m in maquinas transportes eletrica fonoclama predial mapa; do grep -qi "$m" TESTES.md || exit 1; done`
    - Os seis módulos carregam a folha comum uma vez cada
    - Nenhum dos seis declara token de paleta própria
    - Nenhum dos seis contém endereço do domínio interno de cargo
    - Os cinco módulos com abas não têm botão de aba escrito no HTML
    - As sete rotas do Vercel apontam para arquivos existentes
    - `node --test` verde e com mais de 19 testes
  </acceptance_criteria>

  <verify>
    <automated>bash -c 'set -e; grep -q "Base unificada" TESTES.md; for m in maquinas transportes eletrica fonoclama predial mapa; do grep -qi "$m" TESTES.md || { echo "MODULO AUSENTE NO ROTEIRO: $m"; exit 1; }; done; for m in maquinas transportes eletrica fonoclama predial mapa; do test $(grep -c "shared/pmoc.css" $m/index.html) -eq 1 || { echo "FOLHA COMUM AUSENTE OU DUPLICADA: $m"; exit 1; }; done; for m in maquinas transportes eletrica fonoclama predial mapa; do for t in "--bg:" "--surface:" "--border:" "--text:" "--ff:"; do test $(grep -c -- "$t" $m/index.html) -eq 0 || { echo "TOKEN PROPRIO EM $m: $t"; exit 1; }; done; done; for m in maquinas transportes eletrica fonoclama predial mapa; do test $(grep -rc "@cmasm.local" $m/ | awk -F: "{s+=\$2} END{print s+0}") -eq 0 || { echo "EMAIL DE CARGO EM $m"; exit 1; }; done; for m in maquinas transportes predial; do test $(grep -c "nav-btn" $m/index.html) -eq 0 || { echo "ABA ESCRITA A MAO EM $m"; exit 1; }; done; node -e "const c=require(\"./vercel.json\");const fs=require(\"fs\");let n=0;for(const r of c.rewrites){if(!fs.existsSync(\".\"+r.destination)){console.error(\"ROTA QUEBRADA: \"+r.source+\" -> \"+r.destination);process.exit(1)}n++}if(n!==7){console.error(\"ESPERADAS 7 ROTAS, ACHADAS \"+n);process.exit(1)}console.log(\"rotas ok: \"+n)"; SAIDA=$(node --test 2>&1); echo "$SAIDA" | grep -qE "fail 0"; N=$(echo "$SAIDA" | grep -oE "tests [0-9]+" | head -1 | grep -oE "[0-9]+"); test "$N" -gt 19; echo AUDITORIA_FASE5_OK_TESTES=$N'</automated>
  </verify>

  <done>A auditoria dos seis critérios roda limpa por comando, as sete rotas resolvem para arquivos existentes, a suíte cresceu e está verde, e `TESTES.md` tem a seção da fase cobrindo os seis módulos e a conferência isolada da refrigeração.</done>
</task>

<task type="auto">
  <name>Task 2: Confirmar que a refrigeração continua idêntica e corrigir a documentação</name>

  <read_first>
    - `CLAUDE.md` linha 35 — a afirmação sobre o módulo Máquinas duplicar o fluxo de login, que deixou de ser verdade
    - `CLAUDE.md` seção "Architecture" inteira — para acrescentar o shell comum ao inventário de arquivos compartilhados
    - `.claude/CLAUDE.md` linhas 245-255 — o mesmo ponto repetido na descrição de camadas
    - `README.md` linhas 120-135 — a árvore de arquivos compartilhados, que ainda não lista o shell
    - `.planning/phases/05-base-unificada/05-RESEARCH.md` — "Pitfall 4" (não presumir que a refrigeração está intacta)
  </read_first>

  <files>CLAUDE.md, .claude/CLAUDE.md, README.md</files>

  <action>
**Conferência de não regressão da refrigeração (PLAT-15) — fazer antes de editar qualquer arquivo:**

Conferir por histórico do repositório que nenhum commit desta fase tocou o diretório da refrigeração,
comparando o estado atual com o commit imediatamente anterior ao planejamento da fase, cujo
identificador curto é `b53505c`. O resultado precisa ser lista vazia.

Conferir também, por busca estática, que o arquivo da refrigeração continua sem nenhuma referência a
arquivo compartilhado — nem à folha comum, nem ao shell, nem ao login compartilhado. Essa busca já
retornava vazio antes da fase e precisa continuar retornando vazio.

**Correção da documentação:**

Em `CLAUDE.md`, na descrição do login compartilhado, remover a observação de que o módulo Máquinas
duplica o fluxo inline em vez de importá-lo — ela era verdadeira até o plano 05-06 e deixou de ser.
Em lugar dela, registrar que os seis módulos no escopo importam o login compartilhado e que a
refrigeração permanece fora, congelada por decisão do usuário.

Ainda em `CLAUDE.md`, acrescentar o shell comum ao inventário de arquivos compartilhados, descrevendo
em uma linha que cabeçalho, abas e rodapé dos seis módulos vêm dele e que mudá-lo muda os seis de uma
vez.

Em `.claude/CLAUDE.md`, corrigir a mesma afirmação desatualizada na descrição de camadas, que ainda
cita código de autenticação inline no módulo Máquinas.

Em `README.md`, acrescentar o shell à árvore de arquivos compartilhados, na mesma formatação das
linhas vizinhas, com descrição curta em português.

Restrições inegociáveis: nenhum arquivo da refrigeração é alterado; nenhuma outra afirmação da
documentação é reescrita além das que ficaram factualmente erradas por causa desta fase; o idioma da
documentação de cada arquivo é preservado como está.
  </action>

  <acceptance_criteria>
    - Refrigeração intocada em toda a fase: `test $(git diff --name-only b53505c..HEAD -- refrigeracao/ | wc -l) -eq 0`
    - Refrigeração sem referência a compartilhado: `test $(grep -c 'shared/\|pmoc.css' refrigeracao/index.html) -eq 0`
    - A afirmação desatualizada sobre login duplicado sumiu de `CLAUDE.md`: `test $(grep -ci 'duplicates this auth flow' CLAUDE.md) -eq 0`
    - O shell aparece na documentação principal: `grep -q 'shell.js' CLAUDE.md && grep -q 'shell.js' README.md`
    - A descrição de camadas foi corrigida: `test $(grep -c 'inline auth code in' .claude/CLAUDE.md) -eq 0`
    - Nenhum arquivo fora dos três de documentação foi tocado nesta task
    - `node --test` continua verde
  </acceptance_criteria>

  <verify>
    <automated>bash -c 'set -e; test $(git diff --name-only b53505c..HEAD -- refrigeracao/ | wc -l) -eq 0 || { echo "REFRIGERACAO FOI TOCADA NA FASE — PLAT-15 VIOLADO"; git diff --name-only b53505c..HEAD -- refrigeracao/; exit 1; }; test $(grep -c "shared/\|pmoc.css" refrigeracao/index.html) -eq 0; test $(grep -ci "duplicates this auth flow" CLAUDE.md) -eq 0; grep -q "shell.js" CLAUDE.md; grep -q "shell.js" README.md; test $(grep -c "inline auth code in" .claude/CLAUDE.md) -eq 0; node --test >/dev/null; echo PLAT15_E_DOCS_OK'</automated>
    <human-check>Abrir `/refrigeracao/` no navegador **com o inspetor aberto na aba de rede, depois de toda a fase concluída**, e confirmar os dois pontos que a verificação estática não cobre: (a) **nenhuma requisição** para a folha comum, para o shell ou para qualquer arquivo do diretório compartilhado aparece na lista de rede; (b) o console termina sem nenhum erro. Em seguida, percorrer rapidamente o fluxo principal do módulo (login, inventário, uma ordem de contratação) e confirmar que está igual ao que era antes da fase. Este passo é isolado de propósito: PLAT-15 exige confirmação explícita, não presunção por a rota continuar respondendo.</human-check>
  </verify>

  <done>A refrigeração não aparece em nenhum commit da fase, não referencia nenhum arquivo compartilhado, carrega sem requisição a compartilhados e sem erro de console; e a documentação do projeto deixou de afirmar o que deixou de ser verdade.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Fase de refatoração → módulo congelado em produção | A refrigeração é o app mais valioso em produção e está fora do escopo por decisão do usuário; qualquer vazamento de escopo para dentro dele é a falha mais cara desta fase |
| Documentação → sessões futuras de trabalho | Afirmação desatualizada em arquivo de instruções leva o próximo executor a decidir errado |
| Configuração de rotas → produção | Um destino de rota apontando para arquivo inexistente derruba o módulo no deploy |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-27 | Tampering | Vazamento de escopo para dentro do módulo congelado | high | mitigate | Conferência por histórico do repositório contra o commit anterior à fase, mais busca estática por referência a compartilhados, mais verificação humana com inspetor de rede aberto (Task 2) |
| T-05-28 | Denial of Service | Rota do Vercel apontando para arquivo que deixou de existir após a refatoração | high | mitigate | Gate que lê o arquivo de configuração e confere a existência de cada um dos sete destinos no sistema de arquivos (Task 1) |
| T-05-29 | Repudiation | Fase encerrada com critérios afirmados e não verificados | medium | mitigate | A auditoria dos seis critérios é executada por comando e o resultado fica registrado em `TESTES.md` e no resumo do plano |
| T-05-30 | Information Disclosure | Documentação desatualizada induzindo sessão futura a reintroduzir login inline | low | mitigate | Correção pontual das três afirmações que ficaram falsas, com gate que exige a ausência do texto antigo |
| T-05-SC | Tampering | Cadeia de suprimentos de pacotes | low | accept | Nenhum pacote instalado — projeto zero-build, sem `package.json`; ver `05-RESEARCH.md` §"Package Legitimacy Audit" |

**Bloqueio:** severidade `high` é bloqueante (ASVS L1, `security_block_on: high`). T-05-27 e T-05-28
precisam estar mitigados pelos gates antes de a fase ser dada por concluída.
</threat_model>

<verification>
1. Os seis módulos carregam a folha comum uma vez cada e nenhum declara paleta própria.
2. Nenhum dos seis contém endereço do domínio interno de cargo, e os que navegam por abas não têm botão de aba escrito no HTML.
3. As sete rotas do Vercel resolvem para arquivos existentes.
4. `node --test` está verde e com mais testes que os 19 do início da fase.
5. `git diff --name-only b53505c..HEAD -- refrigeracao/` retorna lista vazia.
6. `refrigeracao/index.html` não referencia nenhum arquivo compartilhado.
7. `TESTES.md` tem a seção da fase e a documentação do projeto está factualmente correta.
</verification>

<success_criteria>
- PLAT-15 verificado explicitamente, por histórico do repositório, por busca estática e por inspeção de rede — não por presunção.
- PLAT-16 verificado: suíte automatizada verde e maior que no início da fase, e roteiro manual registrado por módulo.
- Os cinco critérios de sucesso da Fase 5 no roadmap são demonstráveis por comando.
- A documentação do projeto reflete a base unificada que passou a existir.
</success_criteria>

<output>
Criar `.planning/phases/05-base-unificada/05-07-SUMMARY.md` ao concluir.
</output>
