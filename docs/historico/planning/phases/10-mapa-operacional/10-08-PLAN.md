---
phase: 10-mapa-operacional
plan: 08
type: execute
wave: 5
depends_on: ["10-01", "10-02", "10-03", "10-04", "10-05", "10-06", "10-07"]
files_modified:
  - TESTES.md
  - CLAUDE.md
  - .claude/CLAUDE.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [PLAT-13, PLAT-14, PLAT-17, PLAT-18, PLAT-19, PLAT-20, PLAT-15, PLAT-16]

must_haves:
  truths:
    - "Os dez critérios de sucesso da fase são percorridos um a um, e cada um sai desta auditoria com uma de três marcas: provado por comando, provado por conferência humana pendente de execução, ou impossível de provar neste ambiente — com o motivo escrito"
    - "TESTES.md ganha a seção da Fase 10 no formato das seções anteriores, com preparação, roteiro manual por critério, conferência isolada de não regressão da refrigeração e bloco de testes automatizados"
    - "O roteiro registra que a migração 25 precisa ser executada pelo usuário antes de qualquer conferência, e traz as consultas que confirmam que ela entrou"
    - "O roteiro registra que o teste de abrir sem rede é feito por servidor local, e **não** contra a URL de produção — o projeto não tem service worker, então sem rede o navegador não busca nem a página; escrever isso evita um item de verificação que ninguém consegue fechar"
    - "O roteiro registra que, logo depois da migração, nenhum ativo tem coordenada, e que a primeira tarefa da conferência é posicionar alguns pela lista de não localizados — senão o mapa vazio parece defeito"
    - "O roteiro traz o teste do observador de forma explícita: entrar como Livre, confirmar que nenhum controle de escrita aparece, e tentar gravar pelo console para confirmar que o banco recusa (critério de sucesso 10)"
    - "A não regressão da refrigeração é conferida de forma isolada e por comando, não por presunção (PLAT-15)"
    - "A não regressão funcional é conferida por gate: a suíte cresceu do baseline de 58 sem nenhum teste removido, e as rotas do arquivo de configuração de publicação continuam resolvendo para arquivo existente (PLAT-16)"
    - "A documentação do projeto passa a descrever o módulo mapa como ele ficou: camada de dados única, núcleo puro, editor por cargo, planta vetorial offline e o diretório de tiles com procedimento próprio"
    - "REQUIREMENTS.md registra cada requisito da fase com a evidência por comando que o sustenta, e as pendências que este ambiente não fecha ficam nomeadas em vez de sumirem"
    - "As quatro decisões travadas da fase saem registradas com o gate que as protege, para que a próxima fase encontre a fronteira desenhada"
    - statement: "Nenhuma conferência visual pós-login foi feita: o ambiente autônomo não tem credenciais do Supabase nem navegador controlável. Toda evidência deste plano é estática, por comando, e é assim que as Fases 5 e 6 também fecharam"
      verification: backstop
    - statement: "PLAT-19 fecha parcialmente por construção: a metade que garante o desenho da área sem rede está pronta e conferível localmente; a metade de tiles raster depende de o usuário rodar o procedimento na máquina dele, e isso está em `user_setup` do plano 10-04, não escondido"
      verification: backstop
  prohibitions:
    - statement: "Nenhum arquivo em refrigeracao/ é lido para modificação, escrito ou referenciado; a única interação permitida é conferência de leitura (PLAT-15)"
      status: unverified
      verification: judgment
    - statement: "Nenhuma correção de código é feita neste plano; se a auditoria encontrar defeito, ele volta para o plano de origem e não é remendado aqui"
      status: unverified
      verification: judgment
    - statement: "Nenhum arquivo de código é modificado — este plano só escreve documentação e registro de requisitos"
      status: unverified
      verification: judgment
    - statement: "Nenhum item do roteiro manual é marcado como concluído sem evidência; o que depende de navegador ou de credencial fica explicitamente em aberto"
      status: unverified
      verification: judgment
    - statement: "Nenhuma rota é acrescentada ou removida do arquivo de configuração de publicação"
      status: unverified
      verification: judgment
    - statement: "Nenhum requisito é marcado como completo sem o comando que o sustenta escrito junto"
      status: unverified
      verification: judgment
  artifacts:
    - path: "TESTES.md"
      provides: "Roteiro manual da Fase 10 pelos dez critérios, com preparação, teste do observador, teste sem rede por servidor local e conferência isolada da refrigeração"
      contains: "Fase 10"
    - path: "CLAUDE.md"
      provides: "Arquitetura atualizada — camada de dados única do mapa, núcleo puro, editor por cargo, planta vetorial e diretório de tiles"
      contains: "mapa-dados.js"
    - path: ".claude/CLAUDE.md"
      provides: "Contexto de projeto atualizado com os arquivos novos do módulo mapa e o schema estendido"
      contains: "mapa-geometria.js"
    - path: ".planning/REQUIREMENTS.md"
      provides: "PLAT-13, 14, 17, 18, 20 fechados com evidência; PLAT-19 com a parte pendente nomeada; PLAT-15 e PLAT-16 reverificados para a Fase 10"
      contains: "verificado em 10-08"
  key_links:
    - from: "TESTES.md"
      to: "os dez critérios de sucesso da fase"
      via: "seção nova listando o que abrir e o que confirmar em cada um, no formato das seções anteriores"
      pattern: "Fase 10"
    - from: "auditoria da fase"
      to: "refrigeracao/ e mapa/xmap.css"
      via: "diferença de nomes de arquivo entre o commit de referência da fase e a ponta atual, mais busca negativa dentro dos arquivos"
      pattern: "refrigeracao"
    - from: "REQUIREMENTS.md"
      to: "os comandos que sustentam cada requisito"
      via: "evidência citada junto do requisito, no formato que 05-07 e 06-04 estabeleceram"
      pattern: "verificado em 10-08"
---

<!-- planner-discipline-allow: service worker -->
<!-- planner-discipline-allow: serviceWorker -->
<!-- planner-discipline-allow: observador -->

<objective>
Fechar a fase provando os dez critérios de sucesso em vez de presumi-los, e registrar por escrito o que
este ambiente **não** consegue provar.

É o papel que 05-07 teve na Fase 5 e 06-04 na Fase 6, e vale a mesma disciplina. Só que esta fase tem duas
coisas que aquelas não tinham: uma migração que **o usuário precisa rodar** antes de qualquer conferência,
e um requisito cuja prova só existe com a rede desligada num servidor local. Os dois viram item nomeado no
roteiro, com o motivo — em vez de virarem caixas de verificação que ninguém consegue marcar e que na
próxima fase alguém marca por cansaço.

Nenhuma linha de código é escrita aqui. Se a auditoria encontrar defeito, o defeito volta para o plano de
origem.

Purpose: PLAT-15 e PLAT-16 são gates de não regressão que a REQUIREMENTS.md marca para reverificação em
toda fase de 5 a 10 — esta é a terceira e última dessa série. E PLAT-13, 14, 17, 18, 19 e 20 só fecham com
o roteiro escrito, porque a prova final deles é visual e depende de sessão real.
Output: `TESTES.md` com a seção da Fase 10; `CLAUDE.md` e `.claude/CLAUDE.md` descrevendo o módulo como ele
ficou; `REQUIREMENTS.md` com a evidência de cada requisito.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-mapa-operacional/10-01-SUMMARY.md
@.planning/phases/10-mapa-operacional/10-02-SUMMARY.md
@.planning/phases/10-mapa-operacional/10-03-SUMMARY.md
@.planning/phases/10-mapa-operacional/10-04-SUMMARY.md
@.planning/phases/10-mapa-operacional/10-05-SUMMARY.md
@.planning/phases/10-mapa-operacional/10-06-SUMMARY.md
@.planning/phases/10-mapa-operacional/10-07-SUMMARY.md
@CLAUDE.md
</context>

## Decisões travadas que este plano registra

- **D-01** — a camada `aguada` ficou fora do PLAT-17 e vai para a Phase 11. O roteiro manual registra isso
  como **comportamento esperado**, com a frase explícita de que ver dado de demonstração na aguada **não**
  é defeito a reportar, e aponta o selo de demonstração que ficou só nela como a marca visível da
  fronteira. A auditoria confere que o gate automatizado dessa exclusão existe e passa.
- **D-02** — o satélite permanece apenas online. A auditoria confere por comando que o basemap de satélite
  não referencia tile local, e o roteiro registra que alternar para satélite **com a rede desligada** vai
  mostrar tela vazia, e que isso é a decisão funcionando, não uma falha.
- **D-03** — zonas ficam em `maq_areas`, fora da árvore `cmasm_locais`. Registrado na documentação do
  projeto, para que uma fase futura não as mova para a árvore de locais achando que é organização melhor.
- **D-04** — estimativa de tempo e custo por zona ficou fora. Registrado com a nota de que, quando vier,
  ela sai de execução real e não de constante arbitrária, e com a referência ao gate que a mantém fora.

## Suposições sinalizadas

| Assunto | Suposição assumida neste plano |
|---|---|
| Estado do banco | A auditoria **não** consegue confirmar que a migração 25 foi executada: não há credencial. Toda afirmação sobre o banco é sobre o **arquivo** de migração. O roteiro coloca a execução como primeiro passo da preparação e traz as consultas de conferência |
| PLAT-19 | Fecha com a parte de desenho sem rede pronta e conferível localmente, e com a parte de tiles raster nomeada como passo do usuário. Marcar o requisito como totalmente completo sem que ninguém tenha desligado a rede seria presunção |
| Critérios 1, 5, 7, 8 e 10 | Dependem de sessão real e de dado posicionado. Ficam no roteiro manual, com a marca de pendente, seguindo exatamente o precedente de 05-07 e 06-04 |

<tasks>

<task type="auto">
  <name>Task 1: Escrever a seção da Fase 10 em TESTES.md, pelos dez critérios e com o que este ambiente não prova</name>

  <read_first>
    - `TESTES.md` — as seções "Fase 5 — Base unificada" (linhas 309-369) e "Fase 6 — Tema claro/escuro" (linhas 370-587), que fixam o formato: preparação, roteiro por superfície ou critério, bordas a exercitar, limitações conhecidas, conferência isolada da refrigeração, o que o ambiente autônomo não pode provar, e testes automatizados
    - `TESTES.md` seção "Módulo Mapa (/mapa)" (linhas 258-282) — a seção que já existe para o módulo, escrita quando ele era demonstração; a seção nova da fase acrescenta sem apagar aquela, e registra que a anterior descrevia o estado de demonstração
    - `.planning/ROADMAP.md` seção "Phase 10" — os dez critérios de sucesso, na ordem, que são a espinha do roteiro
    - Os sete resumos de plano da fase — o que cada um entregou e o que cada um deixou pendente
    - `mapa/tiles/GERAR-TILES.md` — o procedimento de tiles, referenciado pelo roteiro em vez de copiado
  </read_first>

  <files>TESTES.md</files>

  <action>
Acrescentar ao fim de `TESTES.md` a seção `## Fase 10 — Mapa operacional — auditoria de fechamento`, com a
data, no formato das seções das Fases 5 e 6. Não apagar nem reescrever nenhuma seção existente; a seção
"Módulo Mapa" anterior ganha uma linha dizendo que ela descreve o estado de demonstração, anterior a esta
fase, e apontando para a nova.

**Preparação, nesta ordem e com os passos explícitos:**

1. **Executar a migração 25 no SQL Editor do Supabase.** É o primeiro passo e é bloqueante: sem ele não
   existem as colunas e nada do resto funciona. Trazer as duas consultas de conferência: a que confirma as
   quatro colunas novas de zona e a que conta ativos com coordenada por tabela.
2. Subir o servidor local a partir da raiz do repositório, com o comando que o próprio `CLAUDE.md`
   documenta, e abrir a rota do módulo.
3. **Aviso destacado:** logo depois da migração, **nenhum ativo tem coordenada**. O mapa abre sem nenhum
   marcador, e isso é o estado inicial correto, não defeito. A primeira tarefa da conferência é posicionar
   alguns ativos pela lista de não localizados. Sem esse aviso, quem abrir o mapa pela primeira vez vai
   concluir que a fase não funcionou.

**Roteiro por critério de sucesso, os dez, na ordem do roadmap.** Cada um com: o que fazer, o que esperar,
e a marca de como ele é provado. Pontos que precisam de texto próprio:

- **Critério 4** (ativo sem posição): conferir a lista de não localizados com a contagem por módulo, e
  conferir o estado vazio depois de posicionar tudo — os dois lados, porque uma lista que some não
  distingue "está tudo certo" de "quebrou".
- **Critério 5 e 10** (observador): entrar como Livre; confirmar que não aparecem botão de editar zona nem
  ação de posicionar; confirmar que as camadas continuam desenhando normalmente; e então, com o console do
  navegador aberto, tentar uma gravação direta em `maq_areas` e em `maq_ativos` e confirmar que o banco
  recusa. Escrever que **essa segunda parte é a que prova o critério**: esconder o botão é interface, a
  restrição é do banco. Anotar que o acesso Livre nunca autentica no Supabase, e que é por isso que ele
  opera como papel anônimo e as políticas escopadas a sessão autenticada já o excluem.
- **Critério 7** (zona): desenhar um polígono de tamanho conhecido — um quarteirão, um campo — e conferir
  que a área calculada é da ordem esperada; escolher os três atributos e ver a lista de compatíveis mudar;
  gravar, recarregar e confirmar que a zona voltou igual. Anotar que, se houver máquina de categoria que a
  regra não classifica, a tela diz quantas e quais, e que isso é o comportamento correto.
- **Critério 9** (sem rede): passo a passo próprio. Servidor local rodando, abrir a rota, **desligar a
  rede**, recarregar, confirmar que a área do CMASM desenha. Escrever, em destaque, que **contra a URL de
  produção esse teste é impossível**, porque o projeto não tem service worker e sem rede o navegador não
  busca nem a página — e que quem tentar e concluir "não funciona" estará testando outra coisa. Anotar
  também que alternar para satélite sem rede mostra tela vazia, e que isso é a decisão D-02 funcionando.
  Referenciar `mapa/tiles/GERAR-TILES.md` para quem quiser o degrau de fidelidade raster, sem copiar o
  procedimento para cá.
- **Critério 3** (link para o módulo): clicar num ativo, seguir o link, confirmar que o módulo de destino
  abre já com a ficha daquele ativo, e repetir num segundo módulo.

**Bordas a exercitar**, no espírito da seção equivalente da Fase 6: arrastar um ativo para fora da região
e confirmar que o sistema recusa com mensagem, em vez de gravar; desenhar polígono de dois vértices e
confirmar que a área não é gravada como valor absurdo; gravar zona sem os três atributos e conferir o que
a tela diz; recarregar a página com o parâmetro de ativo apontando para um identificador inexistente e
confirmar que o módulo abre normal.

**Limitações conhecidas**, cada uma com o motivo: a aguada continua com dado fixo (D-01); o mapa Leaflet
continua escuro nos dois temas (D-01 da Fase 6, herdada); o satélite não funciona sem rede (D-02);
estimativa de tempo e custo por zona não existe (D-04); e a coordenada dos prédios em `cmasm_locais`
continua vazia, o que significa que a posição herdada só passa a valer depois de alguém preenchê-la —
registrar como pendência de dados, com a nota de que posicionar ativo por ativo já resolve o caso de uso.

**Conferência isolada de não regressão da refrigeração**, no formato das Fases 5 e 6, com os comandos.

**O que este ambiente autônomo não pode provar**, listado sem eufemismo: nenhuma conferência visual, porque
não há credencial nem navegador controlável; nenhuma confirmação de que a migração entrou; nenhum teste
com a rede desligada; nenhum tile raster gerado. Cada item com o motivo, no formato que a Fase 6 usou.

**Testes automatizados**: a contagem final da suíte, os arquivos de teste que a fase acrescentou e o que
cada um trava.
  </action>

  <acceptance_criteria>
    - A seção existe: `grep -q "Fase 10" TESTES.md`
    - As seções anteriores continuam presentes: `Fase 5` e `Fase 6` continuam em `TESTES.md`, e o arquivo cresceu
    - Os dez critérios estão percorridos: a seção contém dez itens numerados de critério
    - A preparação começa pela migração: a seção menciona `25_mapa_geometria_posicao.sql`
    - O aviso de banco vazio está escrito: a seção menciona que nenhum ativo tem coordenada logo após a migração
    - O teste sem rede está descrito por servidor local e a impossibilidade contra produção está registrada: a seção menciona `http.server` e `service worker`
    - O teste do observador está descrito com as duas partes: a seção menciona `observador` e a tentativa de gravação pelo console
    - As quatro decisões travadas estão nomeadas: `D-01`, `D-02`, `D-03` e `D-04`
    - A conferência isolada da refrigeração está presente com os comandos
    - Nenhum arquivo de código foi tocado
  </acceptance_criteria>

  <verify>
    <automated>bash -c 'set -e; T=TESTES.md; grep -qF "Fase 10" "$T"; grep -qF "Fase 5" "$T"; grep -qF "Fase 6" "$T"; test $(wc -l < "$T") -gt 587; SEC=$(sed -n "/## Fase 10/,\$p" "$T"); test $(echo "$SEC" | grep -cE "^[0-9]+\.|^- \*\*Crit|Critério [0-9]") -ge 10 || { echo "MENOS DE DEZ CRITERIOS NO ROTEIRO"; exit 1; }; echo "$SEC" | grep -qF "25_mapa_geometria_posicao.sql"; echo "$SEC" | grep -qiE "nenhum ativo (tem|possui) coordenada|todos os ativos (estao|ficam) sem (posicao|coordenada)"; echo "$SEC" | grep -qF "http.server"; echo "$SEC" | grep -qiE "service worker"; echo "$SEC" | grep -qiF "observador"; echo "$SEC" | grep -qiE "console"; for d in D-01 D-02 D-03 D-04; do echo "$SEC" | grep -qF "$d" || { echo "DECISAO AUSENTE NO ROTEIRO: $d"; exit 1; }; done; echo "$SEC" | grep -qF "refrigeracao"; test -z "$(git diff --name-only 511bb9e..HEAD -- mapa/ shared/ maquinas/ tests/ supabase/ | head -1 | grep -v .)" || true; SAIDA=$(node --test 2>&1); echo "$SAIDA" | grep -qE "fail 0"; echo TASK1_ROTEIRO_FASE10_OK'</automated>
  </verify>

  <done>`TESTES.md` tem a seção da Fase 10 no formato das anteriores, percorrendo os dez critérios, com a migração como primeiro passo da preparação, o aviso de que o banco começa sem coordenada, o teste do observador nas duas partes, o teste sem rede por servidor local com a impossibilidade contra produção escrita, as bordas a exercitar, as limitações com o motivo de cada uma, a conferência isolada da refrigeração e o que este ambiente não prova.</done>
</task>

<task type="auto">
  <name>Task 2: Conferir a não regressão por comando e atualizar a documentação do projeto</name>

  <read_first>
    - `CLAUDE.md` — as seções "Architecture", "State pattern", "Auth & security", "Database" e "Commands", que passam a descrever o módulo mapa como ele ficou
    - `.claude/CLAUDE.md` — as seções de arquitetura, camadas e schema, que ganham os arquivos novos e as colunas novas
    - `.planning/phases/10-mapa-operacional/10-04-SUMMARY.md` — o tamanho real e a contagem de feições da planta, para a documentação citar número medido e não estimativa
    - `vercel.json` — as rotas, para conferir que continuam resolvendo para arquivo existente
  </read_first>

  <files>CLAUDE.md, .claude/CLAUDE.md</files>

  <action>
**Parte A — conferência de não regressão, por comando, antes de escrever qualquer linha.**

Rodar e registrar o resultado de cada um no resumo do plano:

1. `refrigeracao/` intocada desde o commit de referência da fase, por diferença de nomes de arquivo; mais
   as buscas negativas dentro do arquivo dela — nenhuma referência a arquivo compartilhado, à chave de
   tema, ao atributo de tema, à camada de dados do mapa e ao núcleo puro do mapa (PLAT-15).
2. `mapa/xmap.css` intocado desde o commit de referência (D-01 da Fase 6, herdada).
3. `node --test` verde, com a contagem final comparada ao baseline de 58 desta fase, e a lista de arquivos
   de teste mostrando que nenhum sumiu (PLAT-16).
4. Todas as rotas do arquivo de configuração de publicação resolvem para arquivo existente, incluindo a do
   módulo mapa.
5. As duas decisões que viraram gate — a exclusão da aguada e a ausência de estimativa — continuam verdes,
   rodando os arquivos de teste que as travam e registrando a saída.
6. O basemap de satélite continua sem referência a tile local (D-02).

**Se algum destes falhar, o plano para e reporta** — a correção é do plano de origem, não daqui.

**Parte B — documentação.**

Em `CLAUDE.md`, atualizar sem inchar:

- Na descrição de arquitetura, o módulo mapa deixa de ser "visualização de dados de demonstração" e passa a
  ser descrito com os arquivos que ele tem: o componente de mapa intocado, a camada de dados única, o
  núcleo puro testável em Node, o editor por cargo, a planta vetorial estática e o diretório de tiles com
  procedimento próprio. Registrar a convenção de que **só a camada de dados fala com o banco** dentro de
  `mapa/`, porque é a regra que os planos seguintes precisam herdar.
- Em segurança, registrar as duas listas de cargo e por que elas são diferentes: cada tela espelha a
  política da tabela que ela grava, e a lista não é uma preferência de interface.
- Em banco de dados, registrar as colunas novas: geometria e os três atributos de terreno em `maq_areas`,
  e as duas coordenadas nas seis tabelas, com as duas travas de integridade e com a arquitetura de duas
  camadas — posição herdada do local e posição própria do ativo, e por que ela existe.
- Em comandos, registrar que a planta vetorial é gerada por um script do repositório, rodado à mão, e que
  os tiles raster têm procedimento próprio e não são passo de publicação.
- Nas decisões travadas, registrar as quatro da fase com o gate que protege cada uma.
- Citar o tamanho real da planta, medido no plano 10-04, e não a estimativa do planejamento.

Em `.claude/CLAUDE.md`, atualizar a lista de arquivos e a de tabelas com o que a fase acrescentou,
mantendo o formato do arquivo.

**Não** reescrever seção que não mudou. **Não** apagar registro de fase anterior.
  </action>

  <acceptance_criteria>
    - A não regressão da refrigeração passa: nenhum arquivo dela mudou desde o commit de referência, e ela não referencia nenhum arquivo novo do mapa
    - `mapa/xmap.css` intocado desde o commit de referência
    - `node --test` verde, com contagem maior que o baseline de 58, e nenhum dos arquivos de teste anteriores removido
    - As rotas do arquivo de configuração de publicação resolvem para arquivo existente
    - Os dois arquivos de documentação mencionam os arquivos novos do módulo: `mapa-dados.js`, `mapa-geometria.js`, `mapa-editor.js`, `planta-cmasm.geojson`
    - A documentação registra a regra de porta única de banco e as duas listas de cargo
    - A documentação registra as colunas novas e as quatro decisões travadas
    - Nenhum arquivo de código foi modificado por este plano
  </acceptance_criteria>

  <verify>
    <automated>bash -c 'set -e; test -z "$(git diff --name-only 511bb9e..HEAD -- refrigeracao/)" || { echo "REFRIGERACAO TOCADA (PLAT-15)"; git diff --name-only 511bb9e..HEAD -- refrigeracao/; exit 1; }; for p in "shared/" "pmoc.css" "pmoc-tema" "data-theme" "mapa-dados" "mapa-geometria"; do test $(grep -c "$p" refrigeracao/index.html) -eq 0 || { echo "REFRIGERACAO REFERENCIA: $p"; exit 1; }; done; test -z "$(git diff --name-only 511bb9e..HEAD -- mapa/xmap.css)" || { echo "xmap.css TOCADO (D-01 da Fase 6)"; exit 1; }; SAT=$(grep -n "_baseLayers.satellite" mapa/xmap.js | head -1 | cut -d: -f1); sed -n "${SAT},$((SAT+6))p" mapa/xmap.js | grep -qF "/mapa/tiles/" && { echo "D-02 VIOLADA"; exit 1; } || true; node -e "const fs=require(\"node:fs\");const v=JSON.parse(fs.readFileSync(\"vercel.json\",\"utf8\"));const r=(v.rewrites||[]);let f=0;for(const x of r){const d=(x.destination||\"\").replace(/^\//,\"\");if(d&&!fs.existsSync(d)){console.error(\"ROTA QUEBRADA:\",x.source,\"->\",x.destination);f++}}if(f)process.exit(1);console.log(\"rotas ok:\",r.length)"; for a in tests/modulos-caminhos.test.js tests/operacoes-maquinas.test.js tests/integracao-operacoes-maquinas.test.js tests/predial-dominio.test.js tests/schema-operacoes-maquinas.test.js tests/shell.test.js tests/tema.test.js tests/tema-superficies.test.js tests/vencimento-modulos.test.js; do test -f "$a" || { echo "TESTE DO BASELINE REMOVIDO: $a"; exit 1; }; done; SAIDA=$(node --test 2>&1); echo "$SAIDA" | tail -8; echo "$SAIDA" | grep -qE "fail 0"; N=$(echo "$SAIDA" | grep -oE "tests [0-9]+" | head -1 | grep -oE "[0-9]+"); test "$N" -gt 58 || { echo "SUITE NAO CRESCEU: $N"; exit 1; }; for f in CLAUDE.md .claude/CLAUDE.md; do for s in mapa-dados.js mapa-geometria.js mapa-editor.js; do grep -qF "$s" "$f" || { echo "DOCUMENTACAO NAO CITA $s EM $f"; exit 1; }; done; done; grep -qF "planta-cmasm.geojson" CLAUDE.md; for d in D-01 D-02 D-03 D-04; do grep -qF "$d" CLAUDE.md || { echo "DECISAO AUSENTE NA DOCUMENTACAO: $d"; exit 1; }; done; test -z "$(git diff --name-only -- mapa/ shared/ maquinas/ supabase/ tests/)" || { echo "PLANO DE AUDITORIA ALTEROU CODIGO"; git diff --name-only -- mapa/ shared/ maquinas/ supabase/ tests/; exit 1; }; echo TASK2_NAO_REGRESSAO_OK_TOTAL=$N'</automated>
  </verify>

  <done>A não regressão está conferida por comando — refrigeração intocada e sem referência a arquivo novo, `mapa/xmap.css` intocado, satélite sem tile local, suíte crescida sobre o baseline de 58 sem teste removido, rotas de publicação resolvendo — e `CLAUDE.md` e `.claude/CLAUDE.md` descrevem o módulo como ele ficou, com a porta única de banco, as duas listas de cargo, as colunas novas e as quatro decisões travadas.</done>
</task>

<task type="auto">
  <name>Task 3: Fechar os requisitos da fase em REQUIREMENTS.md, com evidência e com as pendências nomeadas</name>

  <read_first>
    - `.planning/REQUIREMENTS.md` — as entradas de PLAT-01 a PLAT-05 e PLAT-15/16, que fixam o formato de fechamento com evidência anexada ao texto do requisito e com pendência remanescente escrita por extenso
    - `.planning/REQUIREMENTS.md` §"Traceability" — a tabela que precisa ser atualizada para a Fase 10
    - Os sete resumos de plano da fase, e o resultado dos comandos da Task 2
  </read_first>

  <files>.planning/REQUIREMENTS.md</files>

  <action>
Atualizar `REQUIREMENTS.md` no formato que 05-07 e 06-04 estabeleceram: a evidência vai **junto do texto do
requisito**, com o comando que a sustenta, e a pendência que este ambiente não fecha fica escrita por
extenso em vez de sumir.

- **PLAT-13** — marcar completo, com a evidência: as camadas leem posição das colunas novas, a resolução de
  duas camadas é função pura coberta por teste, e o texto do requisito ganha a **correção de premissa**:
  ele dizia "usando o vínculo já existente", e a pesquisa mostrou que esse vínculo é organizacional e não
  tem coordenada — a fase acrescentou as colunas. Corrigir o texto do requisito é parte do fechamento;
  deixá-lo como está faria a próxima leitura repetir o erro. Pendência nomeada: a conferência visual com
  ativo posicionado depende de sessão real.
- **PLAT-14** — completo, com as duas metades citadas: o link montado por função pura no mapa e a leitura
  do parâmetro nos módulos de destino. Pendência: o percurso de ponta a ponta pela tela.
- **PLAT-17** — completo, com a evidência de que nenhum dado de demonstração continua sendo a origem do
  que grama e elétrica exibem, e com a fronteira de D-01 registrada explicitamente: a aguada fica fora, é
  decisão travada, tem gate, e vai para a Phase 11.
- **PLAT-18** — completo, com a evidência do editor, do cálculo de área conferido numericamente e da
  correspondência entre a lista de cargos da tela e a política da tabela. Pendência: desenhar de fato.
- **PLAT-19** — **parcial, e escrito como parcial.** A parte que garante o desenho da área sem rede está
  pronta e é conferível localmente; a parte de tiles raster depende de o usuário rodar o procedimento na
  máquina dele. Citar `mapa/tiles/GERAR-TILES.md` e o `user_setup` do plano 10-04, e a limitação de o teste
  contra produção ser impossível. Não marcar como completo: seria presunção sobre algo que ninguém
  exercitou.
- **PLAT-20** — completo, com a evidência do caminho de gravação validado e do gate que impede o arraste de
  mover o prédio. Pendência: arrastar de fato.
- **PLAT-15** — reverificar para a Fase 10 **sem apagar** os registros das Fases 5 e 6, no mesmo formato
  acumulativo que 06-04 usou. Anotar que esta fase acrescentou colunas a `equipamentos`, tabela da
  refrigeração, e por que isso **não** viola o requisito: o requisito protege o app, a alteração é aditiva
  no banco compartilhado, e há precedente aceito na migração 19.
- **PLAT-16** — reverificar para a Fase 10, com a contagem da suíte antes e depois e a confirmação de que
  nenhum teste foi removido ou enfraquecido.

Atualizar a tabela de rastreabilidade: as seis entradas da Fase 10 e as duas linhas de não regressão, que
passam a citar as três fases verificadas.

Registrar, no fim da seção de requisitos do mapa, as pendências da fase que não são de requisito e sim de
dados ou de operação: executar a migração 25; preencher a coordenada dos prédios em `cmasm_locais`; e
gerar os tiles raster se o usuário quiser o degrau de fidelidade. Cada uma com onde o procedimento está
escrito.
  </action>

  <acceptance_criteria>
    - Os cinco requisitos completos estão marcados e cada um cita a evidência: `PLAT-13`, `PLAT-14`, `PLAT-17`, `PLAT-18`, `PLAT-20`
    - `PLAT-19` **não** está marcado como completo e traz a explicação da parte pendente
    - `PLAT-15` e `PLAT-16` foram reverificados para a Fase 10 sem apagar os registros anteriores: o arquivo continua citando `05-07` e `06-04` além de `10-08`
    - A tabela de rastreabilidade foi atualizada para a Fase 10
    - As três pendências de dados e operação estão registradas
    - Nenhum arquivo de código foi modificado
    - `node --test` continua verde
  </acceptance_criteria>

  <verify>
    <automated>bash -c 'set -e; R=.planning/REQUIREMENTS.md; for q in PLAT-13 PLAT-14 PLAT-17 PLAT-18 PLAT-20; do grep -qE "^- \[x\] \*\*$q\*\*" "$R" || { echo "REQUISITO NAO FECHADO: $q"; exit 1; }; done; grep -qE "^- \[ \] \*\*PLAT-19\*\*" "$R" || { echo "PLAT-19 MARCADO COMPLETO SEM NINGUEM TER DESLIGADO A REDE"; exit 1; }; grep -qF "10-08" "$R"; grep -qF "05-07" "$R"; grep -qF "06-04" "$R"; grep -qF "25_mapa_geometria_posicao.sql" "$R"; grep -qF "GERAR-TILES.md" "$R"; grep -qF "cmasm_locais" "$R"; test $(sed -n "/^| PLAT-13/,/^| PLAT-20/p" "$R" | grep -c "Phase 10") -ge 5 || { echo "TABELA DE RASTREABILIDADE NAO ATUALIZADA"; exit 1; }; test -z "$(git diff --name-only -- mapa/ shared/ maquinas/ supabase/ tests/)" || { echo "PLANO DE AUDITORIA ALTEROU CODIGO"; exit 1; }; SAIDA=$(node --test 2>&1); echo "$SAIDA" | grep -qE "fail 0"; echo TASK3_REQUISITOS_OK'</automated>
  </verify>

  <done>`REQUIREMENTS.md` fecha PLAT-13, 14, 17, 18 e 20 com a evidência por comando anexada ao texto, corrige a premissa errada do texto de PLAT-13, deixa PLAT-19 explicitamente parcial com a parte pendente nomeada, reverifica PLAT-15 e PLAT-16 para a Fase 10 sem apagar os registros das Fases 5 e 6, atualiza a tabela de rastreabilidade e registra as três pendências de dados e operação.</done>
</task>

</tasks>

<threat_model>
Nível ASVS 1, bloqueio em severidade `high`. Este plano não escreve código e não toca o banco: ele escreve
documentação e registro de requisitos. A ameaça real dele é de **registro**, não de execução.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Auditoria → registro de requisito | O que for marcado como verificado aqui é o que as fases seguintes vão presumir verdadeiro sem reconferir |
| Roteiro manual → conferência humana futura | Um item mal escrito produz uma conferência que passa sem ter olhado a coisa certa |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-40 | Repudiation | Requisito marcado como verificado sem o comando que o sustenta, criando falsa confiança para as fases seguintes | high | mitigate | Formato obrigatório com a evidência anexada ao texto do requisito, no precedente de 05-07 e 06-04; gate confere que PLAT-19 **não** foi marcado completo e que os fechados citam este plano |
| T-10-41 | Repudiation | Pendência que este ambiente não fecha desaparecendo em vez de ficar nomeada | high | mitigate | Seção própria de "o que este ambiente não pode provar" no roteiro e pendência escrita por extenso em cada requisito; gate confere a presença das três pendências de dados e operação |
| T-10-42 | Denial of Service | Item de verificação impossível de fechar (testar sem rede contra a URL de produção) desgastando o roteiro até ninguém o seguir | medium | mitigate | A impossibilidade está escrita junto do item, com o motivo técnico, e o caminho possível está descrito ao lado |
| T-10-43 | Tampering | Documentação descrevendo o módulo de forma que induza uma fase futura a espalhar consultas fora da camada de dados ou a uniformizar as duas listas de cargo | medium | mitigate | As duas regras entram na documentação como regra explícita, com o motivo, e as duas já têm gate no código |
| T-10-SC | Tampering | Cadeia de suprimentos de pacotes | low | accept | Este plano não instala nem carrega pacote nenhum |

As duas ameaças `high` (T-10-40 e T-10-41) têm gate automatizado.
</threat_model>

<verification>
1. `node --test` sai 0, com contagem maior que o baseline de 58 e nenhum teste do baseline removido (PLAT-16).
2. `refrigeracao/` intocada desde `511bb9e` e sem referência a nenhum arquivo novo do mapa (PLAT-15).
3. `mapa/xmap.css` intocado desde `511bb9e` (D-01 da Fase 6, herdada).
4. O basemap de satélite continua sem referência a tile local (D-02).
5. As rotas do arquivo de configuração de publicação resolvem todas para arquivo existente.
6. `TESTES.md` tem a seção da Fase 10 percorrendo os dez critérios, sem apagar as seções anteriores.
7. `REQUIREMENTS.md` fecha cinco requisitos com evidência, deixa PLAT-19 parcial e reverifica os dois de não regressão.
8. Nenhum arquivo de código foi modificado por este plano.
</verification>

<success_criteria>
- Os dez critérios de sucesso da fase estão percorridos, cada um com a marca honesta de como é provado.
- O que este ambiente não consegue provar está escrito com o motivo, e não sumiu.
- A refrigeração e o skin do mapa continuam intocados, conferidos por comando e não por presunção.
- As quatro decisões travadas da fase saem registradas com o gate que protege cada uma, para a Phase 11 encontrar a fronteira desenhada em vez de precisar redescobri-la.
- Os requisitos da fase estão fechados com evidência, e o único que não fecha está escrito como não fechado.
</success_criteria>

<output>
Criar `.planning/phases/10-mapa-operacional/10-08-SUMMARY.md` ao concluir.
</output>
